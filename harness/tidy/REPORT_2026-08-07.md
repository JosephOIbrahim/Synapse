# TIDY — Final Report 2026-08-07

> TIDY run closed 2026-08-07 by the REPORT orchestrator. SWEEP → CLASSIFY →
> DISPATCH → VERIFY → REPORT. Working dir: `C:/Users/User/SYNAPSE`.
> Companion artifacts: `COMMITS.md` (5 staged commit drafts), `DROPS.md`
> (3 drop proposals), `FIXES.md` (6 fix/prune proposals), `GATES.md` (open
> human gates), `GITIGNORE_PROPOSAL.md` (models/ + shot_layers/), and
> `STATE_DIAGNOSIS.md` (legs.json / rope/STATE.json reconciliation).

---

## 1. Executed — what the harness actually did

All safe, reversible actions. No commit, no push, no merge, no delete of real
work, no mutation of another harness's state files.

**PARK — 12 files moved to `harness/notes/scratch/`** (reversible; scratch is
the repo's designated draft/scratch area):

- `harness/notes/MONETA_WATCH.txt`
- `harness/notes/RELEASE_v5.43.0_DRAFT.md` (superseded by the final release note)
- `harness/notes/_diff_runs.py`, `_extract_reasons.py`, `_probe_double_run.py`,
  `_probe_oiio.py`, `_probe_oiio_read.py`, `_probe_to_thread.py`,
  `_route_dispatch.py`, `_why.py` (probe/scratch scripts)
- `harness/notes/watch_moneta.ps1`
- `docs/pkg_info.json` → `harness/notes/scratch/pkg_info.json` (machine-generated diagnostic)

**MOVE — 1 file relocated:**

- `docs/project.md` → `claude/project.md` (the Moneta canonical project-memory
  location; the file is a "Project Memory: SYNAPSE" schema-0.1.0 export, not a
  docs/ document)

**STAGED — 5 logical commits drafted (12 files), `git add` only, no `git commit`:**

| Commit | Items | Staged files |
|---|---|---|
| 1 | TIDY-01 + TIDY-02 | `harness/prompts/ci0.md`, `harness/NEXT_SESSION.md` |
| 2 | TIDY-03 | `harness/notes/RELEASE_v5.43.0.md` |
| 3 | TIDY-04 | `harness/rope/STATE.json` (L5-13 `needs_review` → `blocked`) |
| 4 | TIDY-05 | `harness/tidy/SPEC.md`, `STATE.json`, `runner.py`, `workflow.js` |
| 5 | TIDY-06..08 | 4 autoresearch evidence files (2x `lop_truth_22.0.368.json`, `triage.json`, `triage.md`) |

**CREATED — the tidy harness itself + report artifacts:**

- `harness/tidy/SPEC.md`, `runner.py`, `workflow.js`, `STATE.json` (staged as TIDY-05)
- `harness/tidy/COMMITS.md`, `DROPS.md`, `FIXES.md`, `GATES.md`,
  `GITIGNORE_PROPOSAL.md`, `STATE_DIAGNOSIS.md` (this run's dispatch outputs)
- `harness/tidy/REPORT_2026-08-07.md` (this report)

**Not touched (correctly):** `harness/legs.json` (pre-existing CI0-leg addition
left for the human FIX), `$null`, `harness/rope/OPERATOR_CARD.md.bak`,
`models/`, `shot_layers/` (all gated, see §2).

---

## 2. Gated — prepared, needs human approval

Nothing here executed. Each row is a proposal with its exact ask.

**Commits staged (approve to commit):**
1. Commit 1 — `feat(harness): TIDY-01..02 CI0 leg brief + next-session handoff`
2. Commit 2 — `docs(release): TIDY-03 v5.43.0 release note`
3. Commit 3 — `chore(rope): TIDY-04 L5-13 needs_review -> blocked`
4. Commit 4 — `feat(tidy): TIDY-05 tidy harness`
5. Commit 5 — `feat(autoresearch): TIDY-06..08 completed campaign runs`

**Drops proposed (approve to `rm`):**
6. TIDY-09 — `rm $null` (PowerShell `2>$null` redirect artifact; the ABI-mismatch
   signal inside is preserved in `harness/notes/CTO_RULINGS_01.md` etc.)
7. TIDY-10 — `rm harness/rope/OPERATOR_CARD.md.bak` (superseded backup of a tracked file)

**Fixes proposed (human applies):**
8. TIDY-18 — edit `harness/legs.json`: flip M5 and M5b `state: ready` → `done`,
   add `closed_note` + `worktree_released` (both branches ancestor-merged to master).
   CI0 leg stays `ready` — correct, do not touch.
9. TIDY-19 + TIDY-20 — append `models/` and `shot_layers/` to `.gitignore`
   (exact block in `GITIGNORE_PROPOSAL.md`).

**Prunes proposed (human executes — destructive, irreversible):**
10. TIDY-22 — `git worktree remove .claude/worktrees/m5-reconciler` +
    `git branch -D blocks/m5-reconciler` + `git push origin --delete blocks/m5-reconciler`
11. TIDY-23 — same three commands for `blocks/m5b-rulings`

**Blocked (proposed but NOT recommended):**
12. TIDY-11 — `archive/retina-m2-orphan` branch. Has a remote tracking ref and
    memory records it was deliberately KEPT ("Ref kept + classified, not deleted
    (Law 4)"). Recommendation: keep the ref.

---

## 3. Deferred — surfaced, not acted

Open gates and rulings that TIDY does not decide. Full detail in `GATES.md`.

- **CI0 rulings R-CI0-1..5** — five escalations from the CI0 leg (moneta
  first-deposit force-save, `synapse_evolve_memory` deprecation, three
  zero-elapsed-time Windows tests, two stale evolution sentences in governing
  docs, worktree-aware `statusline.py`). Rule, then merge CI0 (Gate C).
- **R-M5b-1** — warn-not-refuse on the phantom gate; ruled, never committed.
- **M6** — the phrase table (the actual last mile; design decision: exact-match
  only vs aliases).
- **CI0 merge — Gate C** — `git merge ci/ci0-honest-green` with `SYNAPSE_GATE_C=1`.
- **CLEAR L1** — commit-or-drop the 6 latency-relay files.
- **PHANTOM SWEEP** — SPEC ratification, corpus fix branch merge, `phantoms.json` population.
- **RSI** — L3→L4 advances (A3, R, O, E), signal-semantics change (A1), C substrate flip.
- **ROPE blocked_human** — L3-2 (video above the fold), L3-5 (Apprentice verdict + support matrix).
- **MONETA ratification** — P0-1..5 (ratify substrate, pin dependency + CI ref) + `MONETA_DEPLOY_KEY`.
- **TIDY-21** — `mcp_server.py` list_tools CI red: pin `mcp==1.26.0` already on
  master (`9c8fe878`); residual CI green folds into the CI0 leg. No action from TIDY.
- **shot_layers/ root cause** — missing absolute-path guard at
  `solaris_compose_tools.py:133-136`; gitignoring stops the tree-dirtying but the
  write-site fix is a `src/` change, out of scope.

---

## 4. Verification

| Check | Result | Evidence |
|---|---|---|
| tree-delta | **PASS** | `git status` delta is exactly the expected set: 12 files staged, 12 files parked to `harness/notes/scratch/`, `docs/project.md` → `claude/project.md`, tidy harness + report artifacts created. No deletions of real work, no `src/`/`python/`/`panel/`/`shared/`/`tests/` modifications, no new harness-state-file changes beyond the pre-existing uncommitted ones. |
| state-consistency | **PASS** | `harness/tidy/` files present (SPEC, STATE, runner, workflow, REPORT). `harness/legs.json` diff is only the pre-existing CI0-leg addition; `harness/rope/STATE.json` staged flip is the pre-existing L5-13 verdict. `flywheel_queue.json`/`drop.json`/`posture.json` do not exist in the tree — nothing to drift. Scratch dir holds the 12 parked files. |
| test-baseline | **PASS** | `python -m pytest tests/ -q --co` → **5919 tests collected, no collection errors** (8.77s). Baseline 5765/9/147 = 5921; 5919 is within expected range (2 fewer, consistent with CI0-leg marker drift, not a regression). The ABI-mismatch RuntimeWarning surfaced during collection is the same signal captured in the `$null` file — confirming the TIDY-09 DROP rationale. |

---

## 5. Human decisions — the numbered list

1. **Approve the 5 staged commits** (TIDY-01..08) — `git commit` each draft in
   `COMMITS.md`, or edit and commit.
2. **Approve drop `$null`** — `rm $null` (TIDY-09).
3. **Approve drop `harness/rope/OPERATOR_CARD.md.bak`** — `rm` it (TIDY-10).
4. **Decide `archive/retina-m2-orphan`** — keep (recommended, per the recorded
   RETINA V0 verdict) or delete local + remote (TIDY-11).
5. **Apply the `legs.json` FIX** — set M5 and M5b to `done` with `closed_note` +
   `worktree_released` (TIDY-18; exact edits in `FIXES.md` §TIDY-18).
6. **Append `models/` and `shot_layers/` to `.gitignore`** (TIDY-19/20; block in
   `GITIGNORE_PROPOSAL.md`).
7. **Approve the two prunes** — `blocks/m5-reconciler` and `blocks/m5b-rulings`
   worktrees + branches + remote refs (TIDY-22/23; destructive, merged-ancestor-safe).
8. **Rule the five CI0 items (R-CI0-1..5)**, then **merge CI0 (Gate C)**.
9. **Rule R-M5b-1** — commit the warn-not-refuse scout change + decision note.
10. **Decide M6** — phrase table design (exact-match vs aliases), then build.
11. **CLEAR L1** — commit-or-drop the 6 latency-relay files.
12. **PHANTOM SWEEP** — ratify SPEC, merge the corpus fix branch, populate `phantoms.json`.
13. **RSI** — authorize L3→L4 advances / signal-semantics change / C substrate flip.
14. **ROPE** — perform L3-2 (video) and L3-5 (Apprentice verdict + support matrix).
15. **MONETA** — ratify the substrate, pin dependency + CI ref, provision `MONETA_DEPLOY_KEY`.

---

## 6. Skills / protocols consulted

- **TIDY SPEC** (`harness/tidy/SPEC.md`) — the harness's own safety model and
  disposition semantics (COMMIT/DROP/PARK/MOVE/FIX/DEFER).
- **Repo law** (CLAUDE.md) — never auto-commit/push/merge; never mutate another
  harness's state files; provenance-or-it-didn't-happen.
- **PHANTOM SWEEP protocol** (`harness/phantoms/SPEC.md`) — consulted for the
  corpus-fix and phantoms.json gates (deferred, not acted).
- **RSI closure protocol** (`harness/rsi/`) — consulted for the L3→L4 / C-substrate
  gates (deferred, not acted).
- **CLEAR protocol** (`harness/clear/SPEC.md`) — consulted for the L1 commit-or-drop
  gate (deferred, not acted).
- **ROPE protocol** (`harness/rope/`) — consulted for the L5-13 verdict and the
  blocked_human tasks (L5-13 flip staged as TIDY-04; L3-2/L3-5 deferred).
- **Moneta deep-review** (`docs/moneta-deep-review-2026-08-05.md`) — consulted for
  the ratification gates (deferred, not acted).

---

## 7. Summary

TIDY closed 2026-08-07 with all 30 unfinished-work items assigned exactly one
disposition and no orphans. The harness executed only safe, reversible actions:
12 probe/draft files parked to `harness/notes/scratch/`, `docs/project.md` moved
to the Moneta canonical `claude/project.md`, 5 logical commits staged as drafts
(12 files, no commit run), and the tidy harness itself created and staged. All
destructive or state-mutating work is gated: 2 drops, 1 blocked drop, 1 state-file
fix, 2 gitignore entries, and 2 merged-branch prunes are proposed with exact
commands and rationale; the 9 pre-existing open human gates (CI0 rulings, R-M5b-1,
M6, Gate C, CLEAR L1, PHANTOM SWEEP, RSI, ROPE, MONETA) are consolidated in
`GATES.md` as the numbered decision list. Verification passed on all three checks:
the tree delta is exactly the expected set, no other harness state was touched,
and the test suite still collects cleanly (5919 tests, no collection errors).
The working tree is intentionally dirty with documented reasons — staged commits
awaiting approval, gated drops/fixes/prunes, and the pre-existing `legs.json`
CI0-leg addition.
