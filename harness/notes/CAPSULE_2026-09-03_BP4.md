# CAPSULE 2026-09-03 BP4 — wave BP4 shipped (BP3 closing + panel typography + USD composition knowledge)

Boot from this file. Previous capsule: CAPSULE_2026-09-03_EOD.md (BP3, 606d891a). No release cut tonight; v5.61.0 stays Latest. Master = this capsule's commit, pushed on Joe's word (git log is the timestamp).

## What happened, in order
1. 17:36 Joe pasted the EOD capsule; parked. ~17:45 ask: execute the World Labs blueprint docx with a swarm. Read as BP4 = BP3 closing + two new asks (the blueprint had already executed as BP3 today). Joe: yes.
2. Pre-arm (CTO seat, Fable 5.1 / DC): rails referee -> claude-fable-5-1 (runs/2026-09-03/preflight_bp4.json: all three tiers answer at --effort max on Claude Code 2.1.259); harness/readonly-settings.json blanket git add/commit denies removed (deny beats allow; Haiku legs died at that fence in BP3), `git branch --merged` allowed; three skill texts shipped to harness/battleplan/notes/skills/; author_bp4.py -> 8 missions (schema-validated 30/30); make_control effort ultracode -> max; dry run green (ledger orch_20260903-175822, cap `12turns,105000000tokens` parsed). Commit bedb7213.
3. 18:04-18:13 a SECOND CTO seat (Joe's other Claude session) dropped + committed the two .md extractions (28a0e183), ARMED the wave 18:05:29 (orchestrator pid 62864), and fixed status_bp4.py's BP2-era hardcoded leg list (2d26231f - my clone bug, caught by them; I ran the dashboard, not the status script).
4. Six builders ran in parallel 18:05-18:41 (Opus 4.8 max x4, Haiku x2). My R135 in-place INTAKE harvest (2574de6b, receipt 9a90e9af) was redundant - the leg had preserved its product on bp4/intake; resolved at merge (branch canonical; my receipt kept as BP4-INTAKE.harvest-addendum.json, corrected per CRUX I-1). INTAKE and TIDY omitted their S5 release lines (SPATIAL posted its own 6 s after the gate flagged it); the CTO seat posted them.
5. 18:43 BUDGET HALT: INTAKE's settle crossed 105M (measured 105,897,811) -> rails halted dispatch, orchestrator exited. CRUX had launched 18:40:55 (claude-fable-5-1, READ-ONLY profile, six lanes). TIDY not dispatched.
6. 19:28 CRUX landed: 5 SOUND-WITH-NITS (INTAKE, B7FIX, SPATIAL, PANELFONT, USDKNOW), RULINGS BROKEN (BP3-CRUX.json:for_ruling[0] never read; 21 rows vs recount 22); 37 mutations; 6 ruling items; 5 held spawns. Joe read; word `merge 1-6` 19:33. Train harness/notes/h22/bp4_merge_train.ps1: 12bf4600 intake, 97b69c7f b7fix, 6e4eae92 spatial, 50856a4b panelfont, 730e112d usdknow, 85a26c34 crux.
7. Ratchet: 3 reds (test_bp2_meter_tier's fable-5 literal; BOM in the harvest-addendum JSON from the train's Set-Content -Encoding utf8; SYNAPSE_SPATIAL_LANE missing from the DEPLOYMENT env table) -> 60cc1a2b (its message OVERCLAIMED "3 -> 0": the env row had landed outside the parsed section) + 36dc5ab9 correction. Second full pass: 6942 passed, 192 skipped, 0 failed.
8. Joe: `flip AMBER` -> A: TIDY re-armed 19:34 on `2turns,25000000tokens` against master@85a26c34 (ledger orch_20260903-193455); landed green 19:45; merged ff61508e on Joe's word. Both orchestrators stopped; CRUX's claude process (idle 20 min after its receipt, transcript unchanged) killed 19:49.
9. This capsule + final waves/bp4.live.json + both ledgers committed; pushed on Joe's word.

## Truths now on master (anchors: BP4-CRUX_verdicts.md, the leg receipts, docs/reviews/bp4-*)
- B-7 probe fixed (camera bound to the render settings + a light before rop.render; `--only <id>` flag). D2.4 PASS, R-1 CLEAR, reproduced on CRUX's own render. Causal story CORRECTED by CRUX: Karma XPU 22.0.400 substitutes a default distant light when the stage has none; the camera bind silenced husk errors without changing pixels; the composite rewiring is what restores lighting (B7-1: amend review 13.1/13.5). The splat renders lit but UNCOLOURED (SH-DC colour not shaded on the naive USD; shader_calls.surface=0).
- SPATIAL: python/synapse/spatial/{__init__,queries}.py, tests/test_spatial_lane.py (14/14), docs/reviews/bp4-spatial-lane-probes-2026-09-03.md. b6_wl_component.usdc's proxy is a 4-face AUTO-PROXY, not the 46,993-tri collider (the collider lives in the GLB); BP3's S-2/S-3 were degenerate (2 packed prims); authoritative numbers in supplementary.txt CORRECTION RUN v2; frustum 20146/46993. Unregistered behind SYNAPSE_SPATIAL_LANE=1 (documented in docs/studio/DEPLOYMENT.md). Nit S-1: timing t0 starts after the triangle gather (a 6 s stall would pass).
- PANELFONT: designsystem/tokens.py family/scale/weights; FONT_FLOOR_PX=10 - the H22 help cache states no default UI font size, so the floor is UNKNOWN and pinned to the smallest size shipped on master (provenance in the module); python/synapse/panel/scripts/probe_ui_font.py for Joe's GUI paste (gui_required -> UNKNOWN); tests/test_panel_typography.py. Qt's QTextDocument ignores CSS line-height (leading via CHAT_LEADING_PT). CRUX: 166 hardcoded typography values remain outside the builder's scope; `pytest -k panel` deselects test_no_hardcoded_hex_or_px_outside_designsystem - select by path.
- USDKNOW: harness/bench/corpus/usd/usd_composition_worldlabs_22.0.400.json (ratified:false), docs/reviews/bp4-usd-composition-worldlabs.md, harness/probes/bp4_usd_composition_probes.py, harness/battleplan/notes/bp4_usdknow_check.py. Fixture: purpose is authored on the /geo/proxy and /geo/render Scopes, the splat leaf authors none; splatTier / physics variant sets and customData:worldlabs are NOT in the SOP build (PROPOSED). The shipped deep-dive's Specialize line is REFUTED at runtime (a local opinion wins). U-1: the checker matches by substring (three false-assertion mutations survived).
- INTAKE: docs/intake/src/{synapse_worldlabs_blueprint.md, synapse_worldlabs_coffee_shop_talk.md, MANIFEST.md} + blueprint Sources line. Both .docx still missing: dossier_in_repo = partial (the leg's bus 'true' overclaims). Two MANIFEST pointers (:18, :377) off by one.
- CRUX: harness/battleplan/notes/BP4-CRUX_verdicts.md (192 lines), BP4-CRUX_mutations.json, bp4_crux/ evidence (13 logs force-added past .gitignore *.log).
- TIDY: harness/battleplan/notes/BP4_TIDY.md - 27-row worktree census; 18 prune commands PROPOSED (merged AND clean), 0 executed: the house standard is unusable-only and all 18 are usable; receipt-order clean for all 7 BP4 legs; log/scratch census with one Remove-Item list.
- Env: Claude Code 2.1.259; --effort levels low|medium|high|xhigh|max; --max-turns absent; hython puts C:/Users/User/SYNAPSE ahead of PYTHONPATH (CRUX cross-cutting #1); rails refuses to settle into a blocked ledger (CRUX tokens UNKNOWN there; by-hand lead transcript 41.81M in / 675k out, lanes unsummed).

## Open - next session (fresh, reasoning tier), in order
1. RULINGS: fix bp4/rulings per BP4-CRUX_verdicts.md "BP4-RULINGS" (add row 22 CRUX-1 verbatim from harness/notes/receipts/BP3-CRUX.json:for_ruling[0]; count line 22; add the severity column; row-17 id; re-encode the file as UTF-8 (cp1252 bytes at offsets 14 and 74); receipt findings #3/#4; ratification consistency on rows 10/11/12/20) - THEN rule all 22 cold. Ratification (Joe): M-1 schema stays docs/intake, M-2 pin hython 22.0.400, D-DEP-03 hou, D3.1, D3.2. Merge bp4/rulings on his word.
2. Rule the 6 BP4 CRUX items (verdicts "For ruling (compiled)": R-1, I-1, B7-1, S-1, P-1, U-1) + the two-writers rule proposed in BP4-INTAKE.harvest-addendum.json.
3. Joe's GUI hands: paste python/synapse/panel/scripts/probe_ui_font.py in the Houdini 22.0.400 Python shell -> the font floor becomes measured; before/after panel screenshots at 100% and 150% UI scale.
4. Drop the two .docx into docs/intake/src/ (MANIFEST rows flip from `missing`).
5. NITS wave from the 5 held CRUX spawns: SPATIAL timing t0 + registration pin; USDKNOW checker token-boundary + absolute-fixture fallback; PANELFONT -k panel gate; B7FIX causal sentence; INTAKE pointer off-by-ones.
6. Demo predicate is untouched by BP4 (two cross-session takes in 22.0.400 still outstanding).

## Hardening (new items, observed this wave)
- ABSOLUTE-PATH LEAK: INTAKE (18:10 draft) and TIDY (BP4_TIDY.md) each wrote a copy of their product into the MAIN tree from a worktree; identical to the branch products, but the untracked copy blocks the merge ("would be overwritten"). Fence: worktree-relative writes, and a pre-merge untracked-collision check in the train.
- S5 RELEASE LINE omitted by INTAKE and TIDY (SPATIAL posted its own late) - the template instruction is not landing with Haiku; have the receipt step post it, or the orchestrator post it at close.
- Backup-Branches pushed bp4/* to origin every minute with no human word (observed all wave; named item since BP3).
- rails: a blocked ledger refuses settle -> every leg that closes after the halt reads UNKNOWN. Add a closing ledger or `settle --allow-blocked`.
- Token meter: enforced_unit fell back to turns while any dispatched leg was unsettled (tokens UNKNOWN), so the ceiling could only bite at settle time; cache reads are counted (same basis as BP3's 102.8M).
- watch_bp4.ps1 drops the LANDED flag into the main tree and collides with the flag CRUX commits; relocate it.
- Clone scripts carry stale hardcodes across waves (status_bp4 leg list, dashboard words/PAIRS) - parametrize from the manifest.
- CTO seat, own errors: `git commit` chained behind `findstr failed` committed an overclaimed message (60cc1a2b); `powershell -File x.ps1 -Legs a,b,c` flattens the array (call in-process); Set-Content -Encoding utf8 writes a BOM.
- Leg processes idle after their receipt (CRUX 20 min) - the orchestrator reaps only while alive; after a halt nobody reaps.

## Housekeeping done at close
- Orchestrators 62864 (budget halt) and 17932 (idle watch) down; CRUX 45052 killed 19:49; dashboard watcher 72112 and verdict watcher stopped after the push.
- Worktrees: 27 kept (prune none; TIDY's 18 commands stay proposals). Untracked on purpose: harness/notes/h22/{orchestrator-bp4.err,.pid, dashboard-bp4.log/.pid, pytest_bp4_ratchet*.log/.err, watch-bp4.pid}, waves/bp4.dryrun.{err,pid}, harness/notes/h22/bp4_merge_train.ps1 (kept like bp1's train). %TEMP%\orch_BP4-*.ps1 removed after the push.
- Not mine, untouched: the REACH/flow-conductor dirt on master (harness/reach, harness/flow, harness/hardening, .claude/agents, .claude/workflows, docs/REACH_BLUEPRINT.md, modified BP2 prompts, dashboard_bp1.py, rope/STATE.json).

## Held queue
BP2-NITS2, METER-DEDUPE / METERLIVE-SOFTCLOSE, UNKNOWN back-settle (from 09-01); BP3 held spawns (7); BP4 held spawns (5); bp4/rulings BROKEN-carried.

## Metered (real, rails ledgers; cache reads counted)
orch_20260903-180529 (blocked at 105,897,811): B7FIX 14.81M in / 370k out / 27 min; INTAKE 9.55M / 37k / 38 min; PANELFONT 20.03M / 430k / 30 min; RULINGS 9.51M / 55k / 9 min; SPATIAL 31.98M / 482k / 36 min; USDKNOW 18.35M / 297k / 24 min; CRUX UNKNOWN in the ledger, >= 41.81M / 675k by hand (lead transcript only). orch_20260903-193455: TIDY 8.79M / 57k / 12 min. Wave >= 154.8M in / >= 2.4M out; BP3 was 104.3M with a five-lane crucible that did not re-run hython probes. Chat side: ~55 DC calls; the 13 KB .md drop went by Joe's drag, not the bridge.
