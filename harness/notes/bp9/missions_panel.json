[
 {
  "id": "PNL-L1",
  "band": "BUILD",
  "class": "build",
  "tier": "auto",
  "team": "auto",
  "upstream": [],
  "name": "L1 Truth and zero-pixel token cleanups (no ruling)",
  "note": "Spec: docs/design/PANEL_TYPE_AND_COMMANDS_2026-09-21.md sections 2, 3 and 6 (leg L1). Four zero-pixel changes. (1) python/synapse/panel/designsystem/tokens.py: FONT_FLOOR_PROVENANCE currently says 'nothing in the panel scales to the host ... an authored 11px is eleven actual pixels beside host chrome of twenty-seven'. That is false at HEAD: synapse_panel.py:552-554 seeds _chrome_scale = _host_font_scale() (:1622-1638) = host pixel size / SIZE_BODY (27/12 = 2.25 on the measured host) into qss.stylesheet(), every chrome font and the transcript font scale. Rewrite the string to state that, keep its first word 'measured' (a provenance test pins it), keep every value unchanged. (2) tokens.py TRACKING_EM: add LABEL_RHYTHM = 0.08 and TAG_RHYTHM = 0.06; python/synapse/panel/designsystem/rhythm.py _apply_type (~:62-78) currently borrows TRACKING_EM['SEND'] and TRACKING_EM['DATA']*2; point it at the two new names. Zero pixel change. (3) python/synapse/panel/message_formatter.py: _scale (~:94-96) has no floor while chat_display.py:176 floors at FONT_FLOOR_PX; add the same floor for text sizes. Headings h1/h2/h3 computed as _BODY_PX + max(0, 4-n) = 15/14/13 (~:278-279) become SIZE_TITLE weight 700 / SIZE_BODY weight 700 / SIZE_BODY weight 500 through the tokens; the 'signed <model>' note _scale(10, ...) (~:158) becomes SIZE_SMALL. _GROUP_MARGIN_Y and _MSG_MARGIN_Y are LIVE inputs to _ruled_turn (:423, :443) overridden on the top margin by chat_display.py:206; do not delete them, fix any comment that calls them dead. (4) harness/design_review/2026-09-15/READABILITY.md around lines 74-89: add a one-line dated correction that the panel does scale to the host (cite synapse_panel.py:552) without rewriting the section. Pins: if tests/test_panel_typography.py or tests/test_panel_readability_defects.py pin any of these strings or sizes, amend by declaration with the leg id; never delete a test. Verification: stock pytest on the two typography test files; hython offscreen audit (see the common rules) must exit 0.\n\nTHE GATE, AND WHY IT IS A RATCHET. Do not run bare 'hython audit_panel.py --strict' or a bare hytest line and expect exit 0: master itself fails 2 audit checks and 6 tests/panel seat tests, and a worktree lies about both unless HOUDINI_PACKAGE_DIR points at its OWN packages dir (Houdini prepends that tree in-process and it beats PYTHONPATH) and an ANTHROPIC_API_KEY is present (one seat test needs one; it lives in the main tree's gitignored .env). harness/notes/bp9/panel_gate.py handles all three and prints the tree it measured. Run it; read its two ratchets; never widen a baseline to go green.",
  "targets": [
   "python/synapse/panel/designsystem/tokens.py",
   "python/synapse/panel/designsystem/rhythm.py",
   "python/synapse/panel/message_formatter.py",
   "harness/design_review/2026-09-15/READABILITY.md",
   "tests/test_panel_typography.py",
   "tests/test_panel_readability_defects.py"
  ],
  "acceptance": [
   "grep -c 'nothing in the panel scales' python/synapse/panel/designsystem/tokens.py prints 0",
   "python -c \"from synapse.panel.designsystem import tokens as t; assert t.FONT_FLOOR_PROVENANCE.startswith('measured'); assert t.FONT_FLOOR_PX == 11; assert 'LABEL_RHYTHM' in t.TRACKING_EM and 'TAG_RHYTHM' in t.TRACKING_EM\" exits 0",
   "grep -cE 'TRACKING_EM\\[\"(SEND|DATA)\"\\]' python/synapse/panel/designsystem/rhythm.py prints 0",
   "grep -c '_scale(10,' python/synapse/panel/message_formatter.py prints 0",
   "python -m pytest tests/test_panel_typography.py tests/test_panel_readability_defects.py -q -p no:cacheprovider exits 0",
   "python harness/notes/bp9/panel_gate.py exits 0 -- it PROVES which tree hython imported from and refuses if that is not this branch, then ratchets the strict audit against harness/notes/bp9/audit_baseline.json and the whole tests/panel seat suite against harness/notes/bp9/seat_baseline.json; exit 2 means a baseline row now passes and must be deleted from the baseline in this same commit"
  ],
  "crucible_criteria": [
   "every value in tokens.py is unchanged (diff shows string and new tracking names only)",
   "rhythm tracking numbers unchanged (0.08 and 0.06)",
   "the formatter floor applies to text sizes only",
   "no test deleted; pins amended by declaration",
   "panel_gate.py exits 0, it printed THIS branch as the tree under test, and no baseline row was deleted without a real fix"
  ],
  "touches": [
   "panel"
  ],
  "readonly": false,
  "deps": []
 },
 {
  "id": "PNL-L2",
  "band": "BUILD",
  "class": "build",
  "tier": "auto",
  "team": "auto",
  "upstream": [
   "PNL-L1"
  ],
  "name": "L2 SWEEP_A legacy stylesheet block under one scale, by declaration (no ruling)",
  "note": "Spec leg L2. In python/synapse/panel/designsystem/qss.py, below the SWEEP_A marker (~:619-834), about 25 rules use UNSCALED {t.SIZE_LABEL}/{t.SIZE_BODY}/{t.SIZE_UI}/{t.SIZE_SMALL}px, five spell weight 700 as WEIGHT_SEMIBOLD + WEIGHT_MEDIUM - WEIGHT_REGULAR (~:622,:632,:633,:649,:676) and five carry inert QSS letter-spacing (~:622,:626,:649,:673,:676; Qt QSS has no letter-spacing). The builders below the marker are invoked with NO arguments (qss.py:556-557: builder() for builder in _sweep_a_builders), so the scale function s() is not in scope there: thread the scale through every _sweep_a_builders entry (a signature change) and rewrite every size as {s(t.SIZE_*)}, every arithmetic 700 as the bold weight token (read tokens.py for its exact name), and delete the letter-spacing declarations. qss.py is append-only against the git baseline: every change is an exact (old, new) pair in a NEW named tuple in tests/test_panel_sweep_a.py (for example PNL_20260921_QSS_AMENDMENTS), applied through the existing _amend helper (~:305); never append to the 2026-09-15 D3B tuple. No literal font-size, weight or family may appear in qss.py (tests/test_panel_typography.py:45-69). Visible change: none at scale 1.0; at the 2.25x host these rules stop rendering 11-12 px beside 25-27 px chrome. Verification: stock pytest on test_panel_sweep_a.py and test_panel_typography.py; hython offscreen audit --strict; hython tests/panel/test_bc_wave.py (340x760 renders, no clipped button).\n\nYOU OWN A BASELINE AUDIT FAILURE. harness/notes/bp9/audit_baseline.json lists 'no bundled font in QSS' against this leg. The strict audit scans the GENERATED stylesheet for bundled family names and finds 'space mono'; note that qss.py:208 is a COMMENT naming Space Mono and comments ship inside the generated QSS string, so establish with evidence whether the leak is that comment, a real font-family rule, or both, then fix the cause. Clear the row and DELETE it from audit_baseline.json in the same commit (the ratchet exits 2 if a row passes but is still listed). If you cannot clear it, leave the row and say exactly why in the receipt.\n\nTHE GATE, AND WHY IT IS A RATCHET. Do not run bare 'hython audit_panel.py --strict' or a bare hytest line and expect exit 0: master itself fails 2 audit checks and 6 tests/panel seat tests, and a worktree lies about both unless HOUDINI_PACKAGE_DIR points at its OWN packages dir (Houdini prepends that tree in-process and it beats PYTHONPATH) and an ANTHROPIC_API_KEY is present (one seat test needs one; it lives in the main tree's gitignored .env). harness/notes/bp9/panel_gate.py handles all three and prints the tree it measured. Run it; read its two ratchets; never widen a baseline to go green.",
  "targets": [
   "python/synapse/panel/designsystem/qss.py",
   "tests/test_panel_sweep_a.py"
  ],
  "acceptance": [
   "grep -cE '\\{t\\.SIZE_[A-Z_]+\\}px' python/synapse/panel/designsystem/qss.py prints 0",
   "grep -c 'letter-spacing' python/synapse/panel/designsystem/qss.py prints 0",
   "grep -c 'WEIGHT_SEMIBOLD + t.WEIGHT_MEDIUM' python/synapse/panel/designsystem/qss.py prints 0",
   "python -m pytest tests/test_panel_sweep_a.py tests/test_panel_typography.py -q -p no:cacheprovider exits 0",
   "python harness/notes/bp9/panel_gate.py exits 0 -- it PROVES which tree hython imported from and refuses if that is not this branch, then ratchets the strict audit against harness/notes/bp9/audit_baseline.json and the whole tests/panel seat suite against harness/notes/bp9/seat_baseline.json; exit 2 means a baseline row now passes and must be deleted from the baseline in this same commit",
   "git diff --diff-filter=D --name-only master -- tests/ prints nothing",
   "the row this leg owns is gone from harness/notes/bp9/audit_baseline.json and the ratchet exits 0"
  ],
  "crucible_criteria": [
   "scale is threaded, not globally captured",
   "amendment pairs are in a new tuple and each occurs exactly once in qss.py",
   "no literal size/weight/family",
   "340x760 renders clean in every profile under hython",
   "the owned baseline row is genuinely fixed (rerun the audit yourself), not deleted from the baseline while still failing"
  ],
  "touches": [
   "panel"
  ],
  "readonly": false,
  "deps": [
   "PNL-L1"
  ]
 },
 {
  "id": "PNL-L3A",
  "band": "BUILD",
  "class": "build",
  "tier": "auto",
  "team": "auto",
  "upstream": [
   "PNL-L1"
  ],
  "name": "L3a One registry, one name, ranked search (ruling R2-misc)",
  "note": "Spec leg L3a. Three changes to the Commands surface. (1) Rename the overflow action 'Palette   %s' to 'Commands   %s' in python/synapse/panel/synapse_panel.py (~:2770); the live pin is the assertion at tests/panel/test_bc_wave.py:209, amend it by declaration citing R2-misc; _palette_hint (~:1100-1128) stays, it is the action's reader. (2) One registry: python/synapse/panel/tool_palette.py _load_entries (~:141-202) is the only source of rows; python/synapse/panel/command_palette.py _SLASH_COMMANDS (~:87-113) must stop being a second registry the palette merges blindly: keep the data list for now (L3b decides row by row) but route it through one build path with the five panel-answered commands (/render, /events, /saved-recipes, /lookdev-suggestion, /restore-session) marked as such. (3) Ranked search: extract CommandPaletteWidget's ranking (command_palette.py ~:300-369: substring 1.0 / subsequence ratio / all-words 0.8, +0.2 prefix, +0.1 word boundary) into a pure module-level function and use it in ToolPalette's search (~:436-443) after the DO x WHERE chip filter, over title, description and send; ties keep today's order. The unwired CommandPaletteWidget class stays (its deletion is ruling territory). Search placeholder 'Search %d tools, recipes & commands…' (~:242) becomes 'Search commands…'. Popups stay styled at the live font scale (prepare_sweep_b_popup ~:282/:311). Pins: tests/test_panel_finesse.py:41-70 and tests/test_first_session_panel.py:57-64 pin that picks route through _send with the five literals; tests/native_render_workspace.py:475-478 and tests/test_farm_integration.py:383 pin the '/render' send; keep them green. Add a stock test that the pure ranking function ranks a row titled 'Open the render workspace' with send '/render' first for the query '/re' against a recipes row.\n\nYOU OWN A BASELINE AUDIT FAILURE. harness/notes/bp9/audit_baseline.json lists the composer-telling row (the audit reports the placeholder as 'Ask SYNAPSE...' and wants the input itself to carry the folded key hint) against this leg. Read the audit's own check to see exactly what it asserts, then make the composer say what it does, in the spec's voice: words are design material, a control says what happens. Clear the row and DELETE it from audit_baseline.json in the same commit; the ratchet exits 2 if a row passes but is still listed. If you cannot clear it, leave the row and say why in the receipt.\n\nTHE GATE, AND WHY IT IS A RATCHET. Do not run bare 'hython audit_panel.py --strict' or a bare hytest line and expect exit 0: master itself fails 2 audit checks and 6 tests/panel seat tests, and a worktree lies about both unless HOUDINI_PACKAGE_DIR points at its OWN packages dir (Houdini prepends that tree in-process and it beats PYTHONPATH) and an ANTHROPIC_API_KEY is present (one seat test needs one; it lives in the main tree's gitignored .env). harness/notes/bp9/panel_gate.py handles all three and prints the tree it measured. Run it; read its two ratchets; never widen a baseline to go green.",
  "targets": [
   "python/synapse/panel/synapse_panel.py",
   "python/synapse/panel/tool_palette.py",
   "python/synapse/panel/command_palette.py",
   "tests/panel/test_bc_wave.py",
   "tests/test_palette_ranking.py"
  ],
  "acceptance": [
   "grep -c '\"Palette' python/synapse/panel/synapse_panel.py prints 0",
   "grep -c 'Search %d' python/synapse/panel/tool_palette.py prints 0",
   "python -m pytest tests/test_palette_ranking.py tests/test_panel_finesse.py tests/test_first_session_panel.py -q -p no:cacheprovider exits 0",
   "python harness/notes/bp9/panel_gate.py exits 0 -- it PROVES which tree hython imported from and refuses if that is not this branch, then ratchets the strict audit against harness/notes/bp9/audit_baseline.json and the whole tests/panel seat suite against harness/notes/bp9/seat_baseline.json; exit 2 means a baseline row now passes and must be deleted from the baseline in this same commit",
   "git diff --diff-filter=D --name-only master -- tests/ prints nothing",
   "the row this leg owns is gone from harness/notes/bp9/audit_baseline.json and the ratchet exits 0"
  ],
  "crucible_criteria": [
   "the ranking function is pure python and tested",
   "only one code path builds palette rows",
   "the five local intercepts and the /render pins survive",
   "no hidden widget deleted",
   "the owned baseline row is genuinely fixed (rerun the audit yourself), not deleted from the baseline while still failing"
  ],
  "touches": [
   "panel"
  ],
  "readonly": false,
  "deps": [
   "PNL-L1"
  ]
 },
 {
  "id": "PNL-L3B",
  "band": "BUILD",
  "class": "build",
  "tier": "auto",
  "team": "auto",
  "upstream": [
   "PNL-L3A"
  ],
  "name": "L3b The Commands list: order, row drop, ASK prompts, first-click probe (ruling: the Commands list)",
  "note": "Spec leg L3b and section 4. (1) Group order in python/synapse/panel/tool_palette.py: PANEL (the five panel-answered rows, titled by outcome: Open the render workspace, Show updates, Saved networks, Saved lookdev suggestion, Restore last session; sends unchanged), then ASK (Explain / Fix / Optimize), then RECIPES (pinned prefix), then TOOLS by context SOP / LOP / COP / Karma / USD (destructive rows keep their warning prefix), then PRESETS (10 materials, 3 render tiers). Group heads keep SPACE_48 height (BC-3 pin) and take the EYEBROW tracking role. (2) The 20 rows of command_palette._SLASH_COMMANDS that have no panel handler: each either gets a real prompt as its send (a sentence the model can act on) or is dropped; list every decision in the receipt notes; after this leg exactly five palette sends begin with '/'. (3) ASK prompts: _QUICK_ACTIONS in synapse_panel.py (~:178-182) get sends that state the empty-scene fallback, e.g. 'Explain the selected node, or the whole scene if nothing is selected.' (4) New read-only offscreen probe python/synapse/panel/scripts/probe_first_click.py run under hython: builds the panel offscreen on an empty scene, opens Commands via the slash signal on an empty composer, walks the first eight rows and asserts: rows 1-4 open a local view (render workspace, updates, saved networks, saved lookdev), row 5 (restore) emits the 'No parked previous session' system line, rows 6-8 send a prompt string that does not require a selection; exits non-zero on any dead end; prints the eight row titles. Pins: tests/panel/test_bc_wave.py:103-137 (BC-1 palette rows, Build HDA overflow, no divider, nothing clipped at 340) amended by declaration if row titles change; tests/test_panel_finesse.py / test_first_session_panel.py keep passing.\n\nTHE GATE, AND WHY IT IS A RATCHET. Do not run bare 'hython audit_panel.py --strict' or a bare hytest line and expect exit 0: master itself fails 2 audit checks and 6 tests/panel seat tests, and a worktree lies about both unless HOUDINI_PACKAGE_DIR points at its OWN packages dir (Houdini prepends that tree in-process and it beats PYTHONPATH) and an ANTHROPIC_API_KEY is present (one seat test needs one; it lives in the main tree's gitignored .env). harness/notes/bp9/panel_gate.py handles all three and prints the tree it measured. Run it; read its two ratchets; never widen a baseline to go green.",
  "targets": [
   "python/synapse/panel/tool_palette.py",
   "python/synapse/panel/command_palette.py",
   "python/synapse/panel/synapse_panel.py",
   "python/synapse/panel/scripts/probe_first_click.py",
   "tests/panel/test_bc_wave.py",
   "tests/test_palette_ranking.py"
  ],
  "acceptance": [
   "QT_QPA_PLATFORM=offscreen hython python/synapse/panel/scripts/probe_first_click.py exits 0 and prints eight row titles",
   "QT_QPA_PLATFORM=offscreen hython -c \"from synapse.panel.tool_palette import build_palette_entries as b; rows=b(); print(sum(1 for r in rows if str(r.get('send','')).startswith('/')))\" prints 5 (adapt the import to the real entry builder name and record it)",
   "python -m pytest tests/test_panel_finesse.py tests/test_first_session_panel.py tests/test_palette_ranking.py -q -p no:cacheprovider exits 0",
   "python harness/notes/bp9/panel_gate.py exits 0 -- it PROVES which tree hython imported from and refuses if that is not this branch, then ratchets the strict audit against harness/notes/bp9/audit_baseline.json and the whole tests/panel seat suite against harness/notes/bp9/seat_baseline.json; exit 2 means a baseline row now passes and must be deleted from the baseline in this same commit",
   "git diff --diff-filter=D --name-only master -- tests/ prints nothing"
  ],
  "crucible_criteria": [
   "exactly five sends begin with '/'",
   "every dropped or rewritten row is named in the receipt",
   "the probe is read-only and fails on a dead end",
   "group heads still SPACE_48 tall"
  ],
  "touches": [
   "panel"
  ],
  "readonly": false,
  "deps": [
   "PNL-L3A"
  ]
 },
 {
  "id": "PNL-L4",
  "band": "BUILD",
  "class": "build",
  "tier": "auto",
  "team": "auto",
  "upstream": [
   "PNL-L2"
  ],
  "name": "L4 Three sizes, quiet by form (rulings R2-A1, R3-A, R3-B)",
  "note": "Spec leg L4 and section 3. (1) python/synapse/panel/designsystem/tokens.py: SIZE_SMALL = SIZE_BODY (12); SIZE_LABEL follows; the ramp becomes 12 / 15 / 19, three distinct sizes. TYPE_ROLES caption -> sans 12 / 500 / all caps / LABEL_SM 0.12em; status -> mono 12 / 400 / DATA 0.03em. FONT_FLOOR_PX stays 11. The four-size floor at tests/panel/test_type_scale_native.py:27-29 (assert len(set(sizes)) >= 4) moves to >= 3 by declaration citing R3-A; the <=5 ceiling in tests/test_panel_typography.py:118-127 and the SIZE_LABEL < SIZE_TITLE pin in tests/test_panel_readability_defects.py (~:108) stay. (2) Quiet by form: pills and send-adjacent buttons that read as words (CHAT, overflow items, Commands, Send) move from mono SIZE_SMALL to the sans label role at their tracked_font call sites in synapse_panel.py (~:993-2545; census: 10 tracked_font calls, 8 mono); author, meter, khint and the palette hint KEYS stay mono (data). Weight 700 only at SIZE_TITLE and SIZE_HERO. The wordmark (WORDMARK 0.16em at 600, synapse_panel.py ~:994) is the one gesture and is untouched. (3) face_review.py: the 21 px literal (~:227) becomes SIZE_HERO and the 11 px literals (~:326, :370, :380) become tokens; face_work.py literals likewise. (4) No colour token moves; TEXT_SECONDARY / TEXT_TERTIARY untouched; every AA pin re-run. Verification: stock pytest on the typography and readability files; hython tests/panel/test_type_scale_native.py and test_bc_wave.py; hython offscreen audit --strict; the seeded-contrast sweep (tests/panel/test_seeded_contrast.py under hython) green.\n\nTHE GATE, AND WHY IT IS A RATCHET. Do not run bare 'hython audit_panel.py --strict' or a bare hytest line and expect exit 0: master itself fails 2 audit checks and 6 tests/panel seat tests, and a worktree lies about both unless HOUDINI_PACKAGE_DIR points at its OWN packages dir (Houdini prepends that tree in-process and it beats PYTHONPATH) and an ANTHROPIC_API_KEY is present (one seat test needs one; it lives in the main tree's gitignored .env). harness/notes/bp9/panel_gate.py handles all three and prints the tree it measured. Run it; read its two ratchets; never widen a baseline to go green.",
  "targets": [
   "python/synapse/panel/designsystem/tokens.py",
   "python/synapse/panel/synapse_panel.py",
   "python/synapse/panel/face_work.py",
   "python/synapse/panel/face_review.py",
   "tests/panel/test_type_scale_native.py",
   "tests/test_panel_typography.py",
   "tests/test_panel_readability_defects.py",
   "tests/panel/test_bc_wave.py"
  ],
  "acceptance": [
   "python -c \"from synapse.panel.designsystem import tokens as t; print(len({t.SIZE_SMALL,t.SIZE_UI,t.SIZE_BODY,t.SIZE_TITLE,t.SIZE_HERO}))\" prints 3",
   "python -c \"from synapse.panel.designsystem import tokens as t; assert all(getattr(r,'size',r.get('size') if isinstance(r,dict) else 12) >= t.FONT_FLOOR_PX for r in t.TYPE_ROLES.values())\" exits 0 (adapt to the TYPE_ROLES shape and record the real one-liner)",
   "grep -cE '\\b(21|11)\\s*(px|,)' python/synapse/panel/face_review.py prints 0",
   "python -m pytest tests/test_panel_typography.py tests/test_panel_readability_defects.py -q -p no:cacheprovider exits 0",
   "python harness/notes/bp9/panel_gate.py exits 0 -- it PROVES which tree hython imported from and refuses if that is not this branch, then ratchets the strict audit against harness/notes/bp9/audit_baseline.json and the whole tests/panel seat suite against harness/notes/bp9/seat_baseline.json; exit 2 means a baseline row now passes and must be deleted from the baseline in this same commit",
   "git diff master -- python/synapse/panel/designsystem/tokens.py | grep -cE '^[-+].*TEXT_(SECONDARY|TERTIARY)' prints 0"
  ],
  "crucible_criteria": [
   "distinct sizes is 3 and the floor pin moved by declaration naming R3-A",
   "no colour token changed",
   "mono roles never ask for weight 500",
   "the wordmark untouched",
   "seeded contrast green under hython"
  ],
  "touches": [
   "panel"
  ],
  "readonly": false,
  "deps": [
   "PNL-L2"
  ]
 },
 {
  "id": "PNL-L5",
  "band": "BUILD",
  "class": "build",
  "tier": "auto",
  "team": "auto",
  "upstream": [
   "PNL-L4"
  ],
  "name": "L5 Transcript measure and beat (rulings R3-D, R3-E)",
  "note": "Spec leg L5 and section 3 'Transcript'. (1) python/synapse/panel/chat_display.py: _MEASURE_CHARS 90 -> 66 (~:298; keep the 460-1100 px clamp); body stays sans with tracking 0 (confirm no LABEL tracking leaks into body blocks); speaker rows become sans 500, all caps, LABEL_SM 0.12em (the re-font at ~:218-224 currently asks mono for weight 500, which mono cannot draw). (2) python/synapse/panel/message_formatter.py: the speaker row's inline letter-spacing:1.2px (~:391) and its mono family go; the display's tracked font is the one owner. The thinking indicator's inline letter-spacing:1px at chat_display.py ~:733 goes the same way. (3) R3-E: python/synapse/panel/designsystem/rhythm.py ROLE_GAPS gains 'turn' = 24 and 'turn_same' = 8 (the values message_formatter already passes into _ruled_turn), and chat_display.py ~:206 reads them instead of the shared group/row keys; the six other surfaces keep group/row unchanged. (4) New read-only offscreen probe python/synapse/panel/scripts/probe_measure.py under hython: renders a known 400-character paragraph in the transcript at 340 px and 1100 px panel widths at Aa steps 1.0 and 1.6 and prints characters per line for each; exits non-zero if any value is outside 45-75. Line height 18 px on 12 px stays; J3 speaker colours (YOU = SIGNAL, SYNAPSE = CONIFEROUS) untouched. Pins: tests/panel/test_j3_speakers.py and tests/test_j3_panel_formatter.py amended by declaration only if a font pin moves, never a colour.\n\nTHE GATE, AND WHY IT IS A RATCHET. Do not run bare 'hython audit_panel.py --strict' or a bare hytest line and expect exit 0: master itself fails 2 audit checks and 6 tests/panel seat tests, and a worktree lies about both unless HOUDINI_PACKAGE_DIR points at its OWN packages dir (Houdini prepends that tree in-process and it beats PYTHONPATH) and an ANTHROPIC_API_KEY is present (one seat test needs one; it lives in the main tree's gitignored .env). harness/notes/bp9/panel_gate.py handles all three and prints the tree it measured. Run it; read its two ratchets; never widen a baseline to go green.",
  "targets": [
   "python/synapse/panel/chat_display.py",
   "python/synapse/panel/message_formatter.py",
   "python/synapse/panel/designsystem/rhythm.py",
   "python/synapse/panel/scripts/probe_measure.py",
   "tests/panel/test_j3_speakers.py",
   "tests/test_j3_panel_formatter.py"
  ],
  "acceptance": [
   "grep -c '_MEASURE_CHARS = 66' python/synapse/panel/chat_display.py prints 1",
   "QT_QPA_PLATFORM=offscreen hython python/synapse/panel/scripts/probe_measure.py exits 0 and prints four cpl values all within 45-75",
   "grep -c 'letter-spacing' python/synapse/panel/message_formatter.py python/synapse/panel/chat_display.py prints 0 for both files",
   "grep -c \"ROLE_GAPS\\['turn'\\]\\|ROLE_GAPS\\[\\\"turn\\\"\\]\" python/synapse/panel/chat_display.py prints at least 1",
   "python -m pytest tests/test_j3_panel_formatter.py -q -p no:cacheprovider exits 0 and python .synapse/hytest.py tests/panel/test_j3_speakers.py tests/panel/test_bc_wave.py -q exits 0 with a real pass count",
   "python harness/notes/bp9/panel_gate.py exits 0 -- it PROVES which tree hython imported from and refuses if that is not this branch, then ratchets the strict audit against harness/notes/bp9/audit_baseline.json and the whole tests/panel seat suite against harness/notes/bp9/seat_baseline.json; exit 2 means a baseline row now passes and must be deleted from the baseline in this same commit",
   "git diff master -- python/synapse/panel/designsystem/tokens.py | grep -cE '^[-+].*(SIGNAL|CONIFEROUS)' prints 0"
  ],
  "crucible_criteria": [
   "cpl probe measures rendered text, not a formula",
   "speaker block is sans 500 under hython",
   "shared group/row gap values unchanged",
   "no colour moved"
  ],
  "touches": [
   "panel"
  ],
  "readonly": false,
  "deps": [
   "PNL-L4"
  ]
 },
 {
  "id": "PNL-L6",
  "band": "BUILD",
  "class": "build",
  "tier": "auto",
  "team": "auto",
  "upstream": [
   "PNL-L4"
  ],
  "name": "L6 Host-floor assertion (ruling R3-C, needs R3-A)",
  "note": "Spec leg L6 as narrowed by the crucible. The Aa ladder is already host-floored (tokens.next_font_scale ~:601-615 calls host_floored_steps and is invoked at synapse_panel.py ~:2987); do NOT add a redundant call. What is missing is the assertion. (1) python/synapse/panel/scripts/probe_ui_font.py (read-only): print host_px (QFontInfo(app.font()).pixelSize() under hython offscreen) and min_chrome_px (the smallest rendered pixel size over the panel's chrome fonts: khint, rail author and meter, footer links, pills; read them from the built widgets, not from tokens) and exit non-zero when min_chrome_px < host_px. (2) tests/panel/test_font_scale.py: add the same assertion as a test under hython, by declaration; keep FONT_FLOOR_PX == 11 and the provenance string starting with 'measured'; extend the provenance string with one sentence naming the wired assertion (no value change). (3) Record in the receipt the measured host_px and min_chrome_px on this machine.\n\nTHE GATE, AND WHY IT IS A RATCHET. Do not run bare 'hython audit_panel.py --strict' or a bare hytest line and expect exit 0: master itself fails 2 audit checks and 6 tests/panel seat tests, and a worktree lies about both unless HOUDINI_PACKAGE_DIR points at its OWN packages dir (Houdini prepends that tree in-process and it beats PYTHONPATH) and an ANTHROPIC_API_KEY is present (one seat test needs one; it lives in the main tree's gitignored .env). harness/notes/bp9/panel_gate.py handles all three and prints the tree it measured. Run it; read its two ratchets; never widen a baseline to go green.",
  "targets": [
   "python/synapse/panel/scripts/probe_ui_font.py",
   "python/synapse/panel/designsystem/tokens.py",
   "tests/panel/test_font_scale.py"
  ],
  "acceptance": [
   "QT_QPA_PLATFORM=offscreen hython python/synapse/panel/scripts/probe_ui_font.py exits 0 and prints host_px and min_chrome_px with min_chrome_px >= host_px",
   "python -c \"from synapse.panel.designsystem import tokens as t; assert t.FONT_FLOOR_PX == 11 and t.FONT_FLOOR_PROVENANCE.startswith('measured')\" exits 0",
   "grep -c 'host_floored_steps' python/synapse/panel/synapse_panel.py prints the same number as on master (no redundant wiring added)",
   "git diff --diff-filter=D --name-only master -- tests/ prints nothing",
   "python harness/notes/bp9/panel_gate.py exits 0 -- it PROVES which tree hython imported from and refuses if that is not this branch, then ratchets the strict audit against harness/notes/bp9/audit_baseline.json and the whole tests/panel seat suite against harness/notes/bp9/seat_baseline.json; exit 2 means a baseline row now passes and must be deleted from the baseline in this same commit"
  ],
  "crucible_criteria": [
   "the probe reads rendered widget fonts, not token values",
   "no redundant wiring",
   "floor value and provenance first word unchanged"
  ],
  "touches": [
   "panel"
  ],
  "readonly": false,
  "deps": [
   "PNL-L4"
  ]
 },
 {
  "id": "PNL-L7",
  "band": "BUILD",
  "class": "build",
  "tier": "auto",
  "team": "auto",
  "upstream": [
   "PNL-L4"
  ],
  "name": "L7 Doctor takes the shipped action family (ruling R2-B1)",
  "note": "Spec section 5, R2-B1: drop the Doctor button's yellow; Doctor takes the shipped SIGNAL action family; no new tint and NO unification of existing accent tints (the cyan/blue three-source token trap). Find where the Doctor button's tone or colour is set (grep -rn 'Doctor' python/synapse/panel/designsystem/qss.py python/synapse/panel/synapse_panel.py python/synapse/panel/designsystem/tokens.py; look for a WARM/HOT/yellow tone or a dedicated token) and route it to the same family the other action buttons use. The hue-bucket guard at tests/panel/test_bc_wave.py:329 (at most three hue buckets at rest) is RED at HEAD with four buckets ('curious', [0, 2, 13, 14]); after this leg it must be green without touching the guard's threshold. If a qss rule changes, it goes through an exact (old, new) pair in the same NEW amendment tuple L2 created in tests/test_panel_sweep_a.py. Do not remove any yellow that is a status colour (warning) elsewhere; only the Doctor action. Verification: hython tests/panel/test_bc_wave.py (the hue-bucket test must pass, not be skipped), tests/panel/test_seeded_contrast.py, offscreen audit --strict.\n\nTHE GATE, AND WHY IT IS A RATCHET. Do not run bare 'hython audit_panel.py --strict' or a bare hytest line and expect exit 0: master itself fails 2 audit checks and 6 tests/panel seat tests, and a worktree lies about both unless HOUDINI_PACKAGE_DIR points at its OWN packages dir (Houdini prepends that tree in-process and it beats PYTHONPATH) and an ANTHROPIC_API_KEY is present (one seat test needs one; it lives in the main tree's gitignored .env). harness/notes/bp9/panel_gate.py handles all three and prints the tree it measured. Run it; read its two ratchets; never widen a baseline to go green.\n\nYOU ALSO OWN A SEAT BASELINE ROW. harness/notes/bp9/seat_baseline.json lists tests/panel/test_bc_wave.py::test_chat_face_monochrome_one_accent_plus_state_marks against this leg: it is the 4-hue-bucket guard that dropping the Doctor yellow is meant to turn green. Clear it and delete the row in the same commit; the gate exits 2 if it passes and is still listed.",
  "targets": [
   "python/synapse/panel/designsystem/qss.py",
   "python/synapse/panel/designsystem/tokens.py",
   "python/synapse/panel/synapse_panel.py",
   "tests/test_panel_sweep_a.py"
  ],
  "acceptance": [
   "python harness/notes/bp9/panel_gate.py exits 0 -- it PROVES which tree hython imported from and refuses if that is not this branch, then ratchets the strict audit against harness/notes/bp9/audit_baseline.json and the whole tests/panel seat suite against harness/notes/bp9/seat_baseline.json; exit 2 means a baseline row now passes and must be deleted from the baseline in this same commit",
   "git diff master -- python/synapse/panel/designsystem/tokens.py | grep -cE '^[-+].*(SIGNAL|SIGNAL_DEEP|CONIFEROUS)\\s*=' prints 0 (no accent tint value changed)",
   "git diff --diff-filter=D --name-only master -- tests/ prints nothing",
   "the seat row this leg owns is gone from harness/notes/bp9/seat_baseline.json and the gate exits 0"
  ],
  "crucible_criteria": [
   "the hue-bucket guard passes at its existing threshold",
   "no accent tint value changed",
   "status yellows elsewhere untouched"
  ],
  "touches": [
   "panel"
  ],
  "readonly": false,
  "deps": [
   "PNL-L4"
  ]
 }
]