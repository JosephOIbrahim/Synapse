{
 "_why": "Panel seat-suite tests failing at this commit under hython offscreen, measured on THIS tree with a key present. Refuse anything new; delete a row in the commit that fixes it. PNL-L4 (2026-09-21) deleted tests/panel/test_bc_wave.py::test_chat_face_monochrome_one_accent_plus_state_marks: it now passes. It was expected to fall to PNL-L7 and fell earlier, to this leg. Reproduced both ways, that test alone under hython offscreen -- upstream (worktree-wf_27091aaa-db8-1) RED with ('curious', [0, 2, 13, 14]) against a limit of 3 buckets, this branch GREEN. The guard grabs the composed face and buckets every chromatic pixel, so it reads ANTI-ALIASED GLYPH pixels too; bucket 2 was fringe off the curious profile's chrome. Moving the pills, verbs and header controls off mono onto the sans label role changed those sub-pixel fringes and the extra bucket went with them. Honest reading: the row is green, and the hue it was really guarding (the Doctor yellow, R2-B1) is still PNL-L7's to remove.",
 "failures": [
  {
   "check": "tests/panel/test_bc_wave.py::test_profile_row_retired",
   "owner": "unassigned"
  },
  {
   "check": "tests/panel/test_doctor_button.py::test_transport_discovers_only_the_running_owner_and_tracks_reconnection",
   "owner": "unassigned"
  },
  {
   "check": "tests/panel/test_failure_trail.py::test_dead_verb_hidden",
   "owner": "unassigned"
  },
  {
   "check": "tests/panel/test_j1_token_liveness.py::test_connected_keyed_engine_is_live_then_working_then_live",
   "owner": "unassigned"
  },
  {
   "check": "tests/panel/test_j2_token_face.py::test_face_counts_an_ollama_task",
   "owner": "unassigned"
  },
  {
   "check": "tests/panel/test_ollama_discovery.py::test_closing_parent_during_discovery_never_calls_deleted_qt",
   "owner": "pre-existing on master (baseline correction)",
   "flaky": true,
   "why": "PRE-EXISTING on master, not introduced by any panel leg. Measured 2026-09-21 at 357bf1e7: the full seat suite ON THE MAIN TREE fails 7, including this row; the integration fails 6, the same set minus the one PNL-L4 fixed. It passes in isolation (2 runs out of 2) and fails under full-suite load -- 'Discovery did not settle' at test_ollama_discovery.py:37 -- so the original six-row baseline caught a lucky run. ROOT CAUSE, found independently by PNL-L5 with a control run (same tree, same env, its own three source files reverted to the merge-base -- still fails, so no leg causes it): the test's settle() helper allows 3s but the worker's release.wait(2) is the real ceiling, and the main thread misses it under full-suite load. NOT fixed by widening that budget -- the house rule is to anchor on real sync state, never to widen a sleep -- so it stays quarantined with its cause written down and deserves a proper fix on its own."
  }
 ]
}
