**Every spec leg landed.** All seven are in, including the two that came back with an open question rather than a finished instruction. Those questions are above, and neither is a leg falling short: in both cases a leg measured the instruction against the code, found that applying it literally would do harm, shipped the honest half, and said so in the code.

**The quarantined flake is not fixed, only named.** A discovery test in the seat suite passes alone and fails under full-suite load, on master as well as here. Its cause is known and written down: the test allows three seconds to settle while the worker's own wait is two, and the main thread misses it under load. The house rule is to anchor on real sync state rather than widen a sleep, so it stays quarantined with its evidence rather than getting a bigger timeout. It carries no signal in either direction and the gate counts it neither way.
