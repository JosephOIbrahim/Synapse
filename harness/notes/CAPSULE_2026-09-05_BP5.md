+== PROJECT CAPSULE: BP5 swarms (Solaris v3 + Panel PD) ==========+
| WHERE WE ARE:  v5.63.0 published (e422efb6). Solaris v3 wave merged, dormant. |
|                Panel PD: CENSUS merged; four legs held on pd/panel-integrate    |
|                (aca05ccb) under CRUX round-2 BROKEN.                             |
| MILE MARKER:   Solaris 4 of ~7 (seam+streams+integrate done; golden HIP,        |
|                hookups, undo binding, T-row bindings ahead). Panel 5 of ~8      |
|                (CRUX rulings + fix leg + round 3 + GUI sign-off ahead).         |
| WHAT I WAS THINKING: BROKEN does not ride. Merge what the referee cleared,      |
|                carry the rest with the verdict attached, say so in the notes.   |
| NEXT ACTION:   Joe rules the two CRUX for_ruling items (residual target,        |
|                docking + HDA parity), then one fix leg on pd/panel-integrate    |
|                for R2-01..06, then CRUX round 3.                                |
| BLOCKERS:      golden sphere/ground HIP (Joe, by hand, H22.0.400); GUI sign-off |
|                on the five camera regions; hookup review for run_recipe.        |
| ENERGY REQUIRED: rulings = 10 min of Joe; fix leg = one Codex worker + Qt tier. |
| IDEAS PARKED:  full clone per worker instead of linked worktree (sandbox can    |
|                commit); bind T4-T12 rows in solaris_v3_accept.py; regen h22    |
|                symbol table so scout stops flagging hou.undos.                 |
+==================================================================================+

## What the 09-04 plan defined as closed, and its status

`CAPSULE_2026-09-04_BP5_PLAN.md` §1 defined "closed" as a predicate per item. v5.63.0
shipped none of the three; the plan's legs (BP5-SPATIAL-CLOSE, BP5-PANEL-LITERALS,
BP5-SPLAT-COLOUR) were never armed — the day went to the Solaris v3 + Panel PD swarms
instead. Status on master `a4d86ce4`, re-grounded 2026-09-05 (CTO B8):

| Item | Closed means (09-04 predicate) | Status | Evidence on master |
|---|---|---|---|
| SPATIAL lane | three queries registered by default; lane `ratified:true` with its diff applied; tests green in a FRESH clone; timing measured from before the triangle gather | **OPEN — carried** | `python/synapse/spatial/__init__.py:21` still gates on `SYNAPSE_SPATIAL_LANE == "1"` (opt-in, not default-on); `docs/studio/DEPLOYMENT.md:158` still says `ratified:false`, default off; `tests/test_spatial_lane.py:262-281` pins the flag as read NOWHERE outside the package; no `harness/probes/fetch_worldlabs_fixture.py` exists, so no fresh-clone proof. The D3.1/D3.2 ratification words were Mile 0 step 3 of the plan (`CAPSULE_2026-09-04_BP5_PLAN.md:43`); `CTO_RULINGS_BP3.md` is not tracked on master, so from the tree it is UNKNOWN whether they were given — and the flip they unlock was not applied either way. |
| PANEL typography | `FONT_FLOOR_PX` provenance = `measured (probe_ui_font.py, 22.0.400 GUI, <date>)`; zero typography literals outside `designsystem/tokens.py`; stylesheet scale-invariant | **OPEN — carried** | `python/synapse/panel/designsystem/tokens.py:339-346` — `FONT_FLOOR_PX = 10` with `FONT_FLOOR_PROVENANCE` beginning `"UNKNOWN — … awaiting the scripts/probe_ui_font.py GUI paste"`. No GUI paste landed. The literal sweep is what SWEEP_A/SWEEP_B attempted on `pd/panel-integrate` (aca05ccb) and CRUX ruled BROKEN; on master the CENSUS still counts 135 raw hex sites (`docs/PANEL_REGION_MAP.md`). |
| SPLAT colour | the World Labs splat renders IN COLOUR through Karma XPU 22.0.400 (per-channel EXR means differ); review §13.1/§13.5 amended, §14 appended | **OPEN — carried** | No B-8 render probe exists (`harness/probes/synapse_blueprint_probes.py:410` "B-8" is the unrelated Chisel round-trip checklist); no `harness/notes/h22wl/bp5_splat_colour/`; `docs/reviews/bp3-h22-worldlabs-probes-2026-09-03.md` ends at §13 (no §14). Last evidence is BP4's grey render (`RGB max/avg/stddev identical to 6 d.p.`). Verdict per plan §4: UNKNOWN, not "black by design". |

The 09-04 plan's §5 one-liner ("Then v5.63.0 says exactly that") was not honoured by the
v5.63.0 notes at publish time; `RELEASE_v5.63.0.md` → "Not in this release" now carries
the pointer. Nothing here closes; this block exists so the next capsule cannot lose them.

Receipts: docs/solaris_v3/INTEGRATION.md · docs/panel_pd/INTEGRATION.md (held branch) ·
docs/panel_pd/CRUX_VERDICT.md + crux.json (held branch) · harness/notes/RELEASE_v5.63.0.md ·
harness/notes/h22/pytest_v5630_master.txt · memory: codex-plugin-swarm-traps.
Live artifacts this session: swarm dashboard https://claude.ai/code/artifact/b3325efa-3b7d-4820-ab20-93d8bfa89eb3
