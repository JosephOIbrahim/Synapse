# Wave 4 capsule — 2026-08-15

## 15-second version

**Wave 4 shipped.** SYNAPSE now answers node questions honestly instead of
confidently wrong. Master pushed, CI green, all receipts in-tree.

**Next action:** rule Gate P — read `harness/notes/h22/crux-ruling.md`,
section A5. Five minutes.

## You are here

- master `30248d72` (this capsule) · origin 1 behind (just the capsule)
- shipped: `e9660bf1` pushed · CI green (run 31898569400, 3m08s)
- wave 4: **5/5 legs merged** · 80/80 integration tests
- orchestrator still idling — safe to leave, or kill PID in `orchestrator-w4.pid`

## Tomorrow-you — in order, one at a time

1. **Rule Gate P.** Read crux-ruling.md A5. Advisory on the table: ship the
   4 parser patches now; bookish adapter = opt-in later, behind guards.
2. **Author wave 5.** Two seeds, missions not yet written:
   **S1** (embed node entries in the dense index → lifts P@1 from 0.755 to bar)
   and **ING-DELTA** (.400 re-ingest → turns the freshness gate green).
3. **Two docket words still open:** non-node surfaces (VEX/hscript/HOM) in or
   out of scope · wave-2 crucible debt accept.
4. **suite_baseline is RED on master.** Pre-existing (R31 flat-baseline), not
   wave 4's doing. Deserves its own leg — don't fold it into wave 5.

<details><summary><b>What shipped</b> (the substance, 30 seconds)</summary>

- **Retrieval repair** — scout sees the node corpus (was 0 rows) · keys carry
  `(context, type)` with a disambiguation list · sentence questions route to
  the node path or honest not-found (0/25 confident-wrong on fresh probes) ·
  datasheets return **all** internal parm names + channels (12-label cap dead) ·
  similarity floor · build-stamp guard at load. `knowledge.py` +459.
- **helpdoc parameterized** — caller picks `.368`/`.400`, loud on a missing archive.
- **Freshness gate + ingest ledger** — gate is *honestly RED* today
  (corpus `.368` ≠ ratified `.400`) until ING-DELTA; ledger is single-writer.
- **Gate P packet** — bookish ruling adversarially re-executed; regen reproduced
  to the integer; staged-adoption advisory written.
- **CRX0 fixed forward** — `_template.md` now mandates commit-before-receipt.

</details>

<details><summary><b>Where everything lives</b></summary>

- Verdicts: `harness/notes/receipts/W4-*.json` — **W4-CRUX.json is the wave's
  truth document**, read it before trusting anything else.
- Blueprint + ruling + probes: `harness/notes/h22/`
- Reusable ops scripts (rename wave4→wave5 and they're next wave's kit):
  `arm_w4.ps1` · `poll_w4.ps1` · `secure_w4.ps1` · `close_w4.ps1`
- Debris, untracked on purpose: `orchestrator-w4.{log,err,pid}`

</details>

<details><summary><b>Held evidence for Gate P</b></summary>

- RULING spawn 1: recount raw `#id:` baseline (settles the AST +43% claim)
- RULING spawn 2: measure ID-join **resolution** rate per context (coverage ≠
  resolution; LABEL resolves ~89% today, `#id` ~43–53%)
- Serve envelope widened by design: median 4.1KB · p95 20.8KB · max 92.8KB
  (`lop/karma`, 233 params) — the price of returning real parm names.

</details>

## Wave 5 handoff (appended post-v5.50.0)
- Wave 5 ESTABLISHED, not armed: 5 missions valid, manifest wave5.live.json (opus-4-8 draft pin), base master.
- Arm button: powershell -File harness/notes/h22/arm_w5.ps1 (retires w4 idle orchestrator first).
- Watch: rename poll_w4.ps1 pattern to w5. Seam warning: W5-UNDO shares handlers_node.py with Joe's parallel session - land/stash before arming.
- Establishment commit 6b76515d local; push rides with next word.
