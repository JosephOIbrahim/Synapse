# SYNAPSE SESSION CAPSULE — 2026-08-15 (wave 5 CLOSED — paste to boot)

## STATE
- Repo C:\Users\User\SYNAPSE, master `76ca94a0` == origin (PUSHED 17:17). This capsule commit rides.
- **Wave 5 "trust the gauges" SHIPPED**: 4 legs + crux merged --no-ff (f9214c7d UNDO, b5a7d314 BASE,
  f2bc911a DELTA-coupled, 3405ca10 DENSE, ad536d2a CRUX, 76ca94a0 receipt-in-tree).
- CI run 31909036581 in_progress at close — verify green: `gh run list -L 1`.
- **Commit-before-receipt mandate: 4/4 PASS on first live enforcement** (mandate_table in W5-CRUX.json).

## THE GAUGES (all observed, CRUX re-ran everything itself)
- Hybrid P@1 **1.0** (603/603, was 0.7529, bar ≥0.98) · floor 1.0 · disamb 1.0 · served_phantom 0.00
- suite_baseline **GREEN**: ratchet 6424/0 vs base 6389/0 (R31 tuple promoted 3db5b244, Joe's (a))
- Freshness **GREEN**: corpus .400 + ingest_ledger rev 2 flipped COUPLED (R1, single-writer
  seed_from_repo) + freshness pin now test_live_worktree_is_honestly_green. F-CRUX-3 honored.
- guardrail_violations [] on merged tree · release-half of R.R stays RC-red (ritual gates, due at next tag).

## JOE'S WORDS DUE (next session)
1. **W5-UNDO-GUI**: live Ctrl+Z receipt — in Houdini via /synapse: create+wire+parm, ONE Ctrl+Z,
   whole build reverses. UNKNOWN until your receipt, never simulated.
2. **Spawn W5-UNDO-B** authorize: wrap set_parm (handlers.py:1091) + set_keyframe
   (handlers_render.py:1138) + sync integrity_envelope.py:19-28 docstring.
3. **Spawn W5-CRUX-S1** authorize: anatomy doc into rag/corpus (F-CRUX-1: CTO-bound doc unreachable).
4. **CRUX-R2**: fix series work-order phrasing (set_parm mis-location) before wave 6 authoring.
5. Still open from w4: Gate P A5 · non-node surfaces in/out · wave-2 debt accept.
6. Statusline worktree-gitdir spawn (W5-STATUSLINE-WT, two legs confirmed it independently).

## HOUSE NOTES (small, real)
- Orchestrator pid 14424 idling — safe to leave, or kill via orchestrator-w5.pid.
- Observability nit: orchestrator last-write tracker is blind to `subagents/workflows/` —
  crux looked dead 28m while live-probing. Teach the tracker the glob before wave 6.
- Template nudge: CRUX wrote its receipt but never made the closing commit (operator rescued it
  in 76ca94a0, sha-verified). Receipt-commit should be the leg's own final act — add to _template.md.
- arm_w5.ps1 still untracked (w4 kit was tracked) — track with wave-6 kit or drop.
- Debris untracked on purpose: orchestrator-w5.{log,err,pid}, suite_baseline.flat.pre-w5base.backup.json.

## NEXT WAVES
- **6 = SQLite+FTS5 storage BEFORE mass** · 7–9 = ingest ladder CHOP→TOP→DOP→OBJ/ROP→VOP→SOP ·
  hardening leg rides every wave (next: provenance guardrail — still warn-only/unwired).

## SESSION DISCIPLINE (carried)
- Token-saver + budget-advisor were stacked; Chunk B (fresh session) = teach-down + Operator's Card
  update + the cold rulings (item 5 above). Teach-down is QUEUED, not skipped.
- First actions for the new driver: read this capsule → W5-CRUX.json (truth doc, in-tree) →
  BLUEPRINT.md Series plan → `gh run list -L 1` for CI state.

HOUSE RULES IN FORCE: UNKNOWN never zero/estimate · one writer per surface · per-act words for
merge/push/flips · conflicts stop the train · CRUX before merge · commit-before-receipt.
