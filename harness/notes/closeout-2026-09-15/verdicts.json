[
 {
  "branch": "design/rulings-2026-09-14",
  "team": "pr82-rulings",
  "tier": "HOT",
  "verdict": "MERGE_NOW",
  "original": "MERGE_NOW",
  "survived": true,
  "confidence": 0.95,
  "artist_value": "indirect",
  "risk": "low",
  "one_line": "Merge it today \u2014 one new .md, built on the exact master HEAD it cites, every spot-checked figure still true at the line number it names; then fix two legibility defects before you rule: the headline oversells A1's reach in a way A1's own body already retracts, and C2 hides two separate rulings under one id where the ambiguous answer is the 73-87px expensive one.",
  "action_steps": [
   "cd C:/Users/User/SYNAPSE && gh pr view 82 --json mergeable,mergeStateStatus,statusCheckRollup    # confirm still CLEAN and see whether the phantom lint ran",
   "gh pr merge 82 --squash --subject 'docs(design): put the 16 open design calls on one decision surface'    # merging the roster is NOT ruling on the roster",
   "# BEFORE ruling, make two edits so the surface can be answered in one word each:",
   "#   (1) Split C2 into C2a (ship / don't ship the receipt line) and C2b (scroll content / widget). As written, the reply 'C2 ship' does not say which, and the ambiguous reading is the one the doc itself prices at -73 to -87px.",
   "#   (2) Correct the 'If you only do one thing' sentence to match A1's own body: A1 gates B2, B2 gates A2, A3 is a conditional guard on A1. It does not collapse three items into bookkeeping.",
   "# THEN rule, in this order, replying on the PR:",
   "#   A1 first (it is genuinely the head of the chain). Then B2 (still a real 3-leg fork after A1). Then A2 falls out with a number and an owner.",
   "#   A3, A4, C5, C6 are one word each and independent \u2014 clear them in the same reply, they cost nothing.",
   "#   Defer C1 and C3: C1's second leg is not a ruling (see below) and C3 explicitly depends on it."
  ],
  "refuter_reason": "Attacked on all five axes; nothing broke, and the verdict's own admitted blind spot closed green.\n\nAXIS 1 \u2014 evidence real? Every spot-check is exact, not approximate. `git merge-base master design/rulings-2026-09-14` == `git rev-parse master` == 67625294161173b715b23f41c3f9aa8fc083f916 (zero drift, no rebase surface). Diffstat is literally `harness/design_review/2026-09-14/RULINGS-OPEN.md | 582 +++++, 1 file changed, 582 insertions(+)`. `git cherry master <branch> | grep -c '^+'` == 1. Three cited code lines are verbatim at the line numbers named: tokens.py:139 is `\"surface\": step(12), \"raised\": step(8), \"border\": step(12),`; synapse_panel.py:732 is `profile = DEFAULT_PROFILE` preceded at :730-731 by exactly the quoted comment; `grep -rn '_select_profile' python/` returns exactly two .py hits (def at :757, comment at :754) \u2014 zero call sites, as claimed. No fabricated citation found.\n\nThe load-bearing \"16 calls\" count is real and the classification split is exact. Heading census from `git show <branch>:<file> | grep -nE '^#{1,6} '`: RATIFY = A1(:72) A3(:129) A4(:157) C5(:454) C6(:476) = 5; FORK = B1(:174) B2(:220) C1(:297) C2(:317) C3(:382) C4(:405) = 6; ASSIGN = A2(:108) D1(:494) D2(:505) D3(:518) D4(:529) = 5. Total 16, matching the verdict item for item. The A1 headline the verdict partially refuted is verbatim at :27-28: \"**Rule A1.** It is the only item other items are waiting on. Answering it collapses A2, A3 and B2 from open questions into bookkeeping with a number attached.\" The self-refutation the verdict found is genuine and correctly quoted. C2's heading at :317 does carry the double-ruling in its own subtitle (\"**[FORK]** \u00b7 *and one placement ruling that must be explicit*\") \u2014 the ambiguity the verdict flags is on the face of the heading.\n\nAXIS 2 \u2014 neighbour contradiction? None available. The work graph gives this branch no file-sharing neighbours, and I confirmed why: `git ls-tree --name-only master -- harness/design_review/2026-09-14/RULINGS-OPEN.md` returns empty, so the branch's single file occupies a path no tracked file holds. It cannot collide with the fix/cto-render-20260908 <-> feature/tops-renderfarm-20260907 42-file duplicate problem; that contradiction belongs to those two and is not this branch's to own.\n\nAXIS 3 \u2014 N/A, verdict is not ARCHIVE_DELETE.\n\nAXIS 4 \u2014 \"commits behind, name a moved interface\": structurally unavailable. The branch is ZERO commits behind; merge-base IS master HEAD. There is no window in which an interface could have moved under it. This is the strongest single fact in the verdict and it holds.\n\nAXIS 5 \u2014 action steps runnable? I executed step 1 as written (`gh pr view 82 --json ...`) and it returned. `gh pr merge 82 --squash --subject '...'` uses single quotes, valid in both PowerShell 7 and the Bash tool here. No here-string, no `cd && git commit` (the known-denied form in this environment).\n\nCI \u2014 the surface the verdict explicitly marked unchecked, now checked, and it REFUTES the verdict's own stated worry rather than the verdict: statusCheckRollup on headRefOid 5c1e7acb (the branch tip) is 5/5 SUCCESS \u2014 test (ubuntu 3.11), test (ubuntu 3.14), test (macos 3.11), test (macos 3.14), all conclusion SUCCESS at 2026-09-14T21:05Z, plus CodeRabbit SUCCESS. mergeable MERGEABLE, mergeStateStatus CLEAN. The phantom-lint fear (doc quotes `hou.undos.group` and `houdini_execute_python` in prose) is empirically dead \u2014 the lint ran on this head and passed. That removes the verdict's only self-declared unknown and is why I raise confidence 0.90 -> 0.95 rather than leaving it flat.\n\nThe verdict's two legibility criticisms of the doc (A1 headline oversell, C2 double-ruling) are themselves verified and should stay attached to the ruling. They are reasons to edit the doc, not reasons to withhold the merge \u2014 the file changes nothing executable, lands at an unoccupied path, and is built on the exact sha it cites."
 },
 {
  "branch": "feature/windows-installer-20260909",
  "team": "pr80-installer",
  "tier": "HOT",
  "verdict": "SUPERSEDED",
  "original": "SUPERSEDED",
  "survived": true,
  "confidence": 0.97,
  "artist_value": "none",
  "risk": "low",
  "one_line": "Master already shipped this exact installer (21 of its 25 files are byte-identical on master; the other 4 are the branch's OLDER 5.67.4-preview text), so merging PR #80 would roll the public download link back from v5.70.0 to a dead preview tag \u2014 close it and delete the branch.",
  "action_steps": [
   "git -C C:/Users/User/SYNAPSE log --format='%h|%cs|%s' master -- installer/    # confirm 8c23fccc shipped it",
   "git -C C:/Users/User/SYNAPSE diff --stat master..feature/windows-installer-20260909 -- README.md docs/getting-started installer install.py scripts/install_synapse_package.py    # confirm only 4 files differ, all older",
   "gh pr close 80 --comment \"Superseded: this installer shipped on master in 8c23fccc (v5.68.0) and is now published as SYNAPSE-5.70.0-Setup.exe. The 4 files still differing here are the older 5.67.4-preview text; merging would regress the download link.\"",
   "git -C C:/Users/User/SYNAPSE branch -D feature/windows-installer-20260909",
   "git -C C:/Users/User/SYNAPSE worktree prune",
   "# UNRELATED follow-up, do NOT take it from this branch: master's docs/getting-started/installation.md line 10 still advertises SYNAPSE-5.68.0-Setup.exe while README.md line 13 advertises 5.70.0 \u2014 a one-line master-side doc fix, not a reason to keep PR #80."
  ],
  "refuter_reason": "Every cited fact verified live. `git log master -- installer/` returns the single commit 8c23fccc (2026-09-13, \"Release v5.68.0 with proposal controls and Windows Setup\") \u2014 the installer tree is on master. `git diff --stat master..feature/windows-installer-20260909` restricted to the branch's 25 paths shows exactly 4 files differing (README.md, docs/getting-started/installation.md, installer/README.md, installer/test_executable.py); the other 21 including all of installer/synapse_setup/, Synapse.iss, both test files and toolchain.lock.json are byte-identical. All 4 differences point backwards: master README:13 advertises v5.70.0/SYNAPSE-5.70.0-Setup.exe while branch installation.md:9 advertises windows-installer-5.67.4-preview.1; installer/README.md differs only in 5.68.0->5.67.4 version strings; installer/test_executable.py on the branch REMOVES master's --expected-version argparse flag and reverts the upgrade assertion to a hardcoded \"5.67.4\". I hunted for unreplicated value per axis 3 by diffing the branch against its own merge-base d7c90a15 to isolate its own additions: they are the 5.67.4-preview download block plus the wizard walkthrough, and master's installation.md already carries that same walkthrough at 5.68.0 AND master additionally has docs/getting-started/windows-installer-verification.md and windows-installer-demo.md, which the branch's README only links to. Zero unique hunks. No neighbour shares files with this installer tree, so no cross-team contradiction to own. Axis 4 is n/a (not a merge verdict). NOT CHECKED: whether the v5.70.0 release asset actually downloads (no network call was made), and the installer test suite was not executed."
 },
 {
  "branch": "feature/tops-renderfarm-20260907",
  "team": "render-vs-tops",
  "tier": "HOT",
  "verdict": "SUPERSEDED",
  "original": "SUPERSEDED",
  "survived": true,
  "confidence": 0.97,
  "artist_value": "direct",
  "risk": "low",
  "one_line": "This branch is a strict ancestor of fix/cto-render-20260908 \u2014 same seven commit shas, one fix commit behind \u2014 so it carries nothing unique and can be archived the moment render lands.",
  "action_steps": [
   "# 1. Re-confirm the ancestry immediately before deleting (it is the whole basis of this verdict):",
   "git merge-base --is-ancestor feature/tops-renderfarm-20260907 fix/cto-render-20260908 && echo SAFE-TO-ARCHIVE",
   "# 2. Do NOT delete yet. Wait until fix/cto-render-20260908 is merged to master.",
   "# 3. After that merge, confirm the shas landed:",
   "git cherry master feature/tops-renderfarm-20260907 | grep -c '^+'   # expect 0",
   "# 4. Then archive locally and on the public remote:",
   "git branch -D feature/tops-renderfarm-20260907",
   "git push origin --delete feature/tops-renderfarm-20260907",
   "git worktree prune"
  ],
  "refuter_reason": "Attacked on all five axes and could not break it. (1) Evidence is real, not fabricated \u2014 every citation reproduces: `git merge-base --is-ancestor feature/tops-renderfarm-20260907 fix/cto-render-20260908` exits 0, the reverse exits non-zero; the seven shas be7cb4fe/c867f61f/89686a3e/52a79c53/0c832cd5/d92b6487/5b7f3964 appear identically in both `git log` outputs; `git cherry master B | grep -c '^+'` gives tops 7, render 8 with b07eaa40 as the sole delta; merge-base is 87ce354a and both are exactly 81 behind master. (2) The strongest possible attack \u2014 find one hunk on tops that is absent from render \u2014 is closed by construction: `git diff fix/cto-render-20260908..feature/tops-renderfarm-20260907 --stat` is the exact byte-inverse of the forward diff (10 files, +27/-788, same file list). There is no unreplicated value to salvage; render's tree is a strict superset. (3) No neighbour contradiction exists to adjudicate \u2014 none of the listed neighbours (demo-repairs, cold-cache-rope, cold-repairs, cold-panel-scene) touches the farm file set, and fix/cto-render-20260908, the one branch that shares 42 files, was not given a verdict in this roster, so nothing here competes for the same work. (4) Axis 4 does not apply: this is not a merge verdict, and being 81 behind is irrelevant to an archive that ships zero bytes. What I did NOT check: whether the seven farm commits actually pass tests against current master, whether python/synapse/farm/ still imports cleanly after 81 commits of drift, and whether fix/cto-render-20260908 itself deserves to land \u2014 that branch's own disposition is the real open question and this verdict is entirely parasitic on it. What I DID find that the verdict missed cuts in its favour, not against it: feature/tops-renderfarm-20260907 has no ref on origin at all (`git ls-remote origin` returns only refs/heads/fix/cto-render-20260908), so the shared history is already preserved on a pushed remote branch and deleting the local ref strands nothing even if render never merges. The action_steps, however, are not runnable as written: `git branch -D feature/tops-renderfarm-20260907` will hard-fail because that branch is checked out in a live worktree at C:/Users/User/OneDrive/Documents/ChatGPT/SYNAPSE_Refactor/worktrees/tops-renderfarm, and `git worktree prune` is both listed last and cannot remove a worktree whose directory still exists; and `git push origin --delete feature/tops-renderfarm-20260907` will error with \"remote ref does not exist\" since there is no remote ref. Correct sequence: `git worktree remove <that path>`, then `git branch -D`, and drop the push step entirely. `grep` is fine under PowerShell here (C:\\Program Files\\Git\\usr\\bin\\grep.exe is on PATH), so the cherry-count step runs as written."
 },
 {
  "branch": "fix/cto-render-20260908",
  "team": "render-vs-tops",
  "tier": "HOT",
  "verdict": "REBASE_THEN_MERGE",
  "original": "REBASE_THEN_MERGE",
  "survived": true,
  "confidence": 0.78,
  "artist_value": "direct",
  "risk": "high",
  "one_line": "The real work and the one branch that should survive \u2014 a durable out-of-process render farm an artist can actually feel \u2014 but master moved 81 commits under it, five files conflict, and two live landmines (a hard 22.0.400 build pin and 541 lines of tests pytest never collects) must be fixed during the rebase, not after.",
  "action_steps": [
   "# 1. Create a rebase worktree \u2014 do NOT rebase in the main tree (absolute-path hazard):",
   "git worktree add ../SYN-render-rebase fix/cto-render-20260908",
   "# 2. Rebase onto master. Exactly five files conflict (verified via merge-tree):",
   "git -C ../SYN-render-rebase rebase master",
   "#    CONFLICTS, in order of danger:",
   "#      python/synapse/panel/tool_executor.py      <- REAL CODE, master's Panel PD wave rewrote this",
   "#      python/synapse/panel/synapse_panel.py      <- REAL CODE, same wave",
   "#      python/synapse/server/hwebserver_adapter.py<- REAL CODE",
   "#      CLAUDE.md                                  <- GOVERNING DOC, Joe resolves this one, not a tool",
   "#      README.md                                  <- ADHD-friendly convention applies",
   "#    handlers.py, shared/bridge.py, mcp/server.py, bridge_adapter.py, qss.py all auto-merge clean.",
   "# 3. BLOCKER \u2014 unpin the build before merging. Either widen the check or make it a warning:",
   "grep -n 'QUALIFIED_BUILD' ../SYN-render-rebase/python/synapse/farm/package.py ../SYN-render-rebase/python/synapse/farm/native_driver.py",
   "#    Confirm behaviour on a 22.0.429 seat, not just 22.0.400.",
   "# 4. BLOCKER \u2014 make the 541-line native harness either collected or honestly labelled:",
   "git -C ../SYN-render-rebase mv tests/native_render_workspace.py tests/test_native_render_workspace.py   # if it is meant to run",
   "#    ...or move it out of tests/ entirely so it stops reading as coverage it is not.",
   "# 5. Run the real gate (full suite, not hython-only). Floor is 8869 passed / 412 skipped / 0 failed:",
   "pytest tests/",
   "# 6. Open a PR so this gets CI at all \u2014 it currently has none (only #82 and #80 are open).",
   "# 7. Ask Joe the architecture question in risk_why(1) before or at merge: does houdini_render stay as a second road?"
  ],
  "refuter_reason": "Verdict survives; two defects found, neither flips it. (1) EVIDENCE SPOT-CHECKS HELD: package.py:12 `QUALIFIED_BUILD = \"22.0.400\"` exact; native_driver.py:597 `if hou.isUIAvailable() or hou.applicationVersionString() != pkg.QUALIFIED_BUILD: raise ValueError` exact (and correctly read as the spawned worker's gate); master:pyproject.toml:110 `python_files = [\"test_*.py\"]` exact and `git grep native_render_workspace fix/cto-render-20260908` returns ZERO references, so the 541-line harness is genuinely uncollected; handlers.py:523 and :609 carve-outs verbatim; `git ls-tree master --name-only python/synapse/farm/` empty, so the work is not absorbed. (2) THE 42-OF-45 OVERLAP IS NOT A CONTRADICTION: feature/tops-renderfarm-20260907 is a STRICT ANCESTOR. `git rev-list --count fix/cto-render-20260908..feature/tops-renderfarm-20260907` = 0; reverse = 1 (b07eaa40). Tip-to-tip A->B is pure subtraction, -788 lines incl. tests/test_farm_recovery.py (412), test_farm_usd_settings.py, test_render_workspace.py, docs/tops/RENDER_RECOVERY.md. The other branch can hold no unique hunk by construction and should be ruled SUPERSEDED. The verdict asserted survivorship without proving containment; the proof exists and backs it. (3) ONE CITATION PATH IS WRONG: \"mcp_tools_render.py\" does not exist under python/synapse/mcp/ on this branch -- it is at the REPOSITORY ROOT (fix/cto-render-20260908:mcp_tools_render.py:24-25,47-53 registers the seven synapse_farm_* tools; python/synapse/mcp/_tool_registry.py:1276+ registers them too). Claim true, path unresolvable as written, and a root-level MCP tool module is an unruled layout smell. (4) MISSED BLOCKER, axis 4 -- an interface moved under the branch that merge-tree cannot flag: master shipped b475ee22 on 2026-09-14 (one day before this review) adding the artist-facing tool-label authority python/synapse/panel/activity.py (86 lines) + its pin tests/test_activity_labels.py. `git grep -n farm master -- python/synapse/panel/activity.py` -> no hits, and the branch's panel touches (bridge_adapter, command_palette, designsystem/qss, render_presenter, render_workspace, synapse_panel, tool_executor) do NOT include activity.py. Seven new synapse_farm_* tools and a Render workspace therefore land with zero entries in the label map that commit exists to feed, falling through the exact fallback it repaired. No textual overlap => not in the 5-file conflict list => the action_steps as written will not surface it. Add it as blocker #6. (5) Steps otherwise runnable; step 3's bare `grep -n` is Git-Bash-only (PowerShell needs Select-String), and step 5's `pytest tests/` must not run concurrently with the hython seat suite. Attacks that FAILED to break it: no tool-count or len(TOOL_NAMES) pin on master that +7 tools would red; tests/test_knowledge.py's DISPATCH_KEYS-covers-TOOL_NAMES conformance predates the merge-base (`git log 87ce354a..master -- tests/test_knowledge.py` empty), so it is not newly-moved ground. NOT CHECKED: whether the rebase completes, whether the suite is green after it, the handlers_render.py-untouched claim (taken on the verdict's trust), exact RBAC levels, real 22.0.429 seat behaviour, and whether root-level mcp_tools_render.py pre-exists on master."
 },
 {
  "branch": "fix/demo-repairs-20260909",
  "team": "demo-repairs",
  "tier": "HOT",
  "verdict": "REBASE_THEN_MERGE",
  "original": "REBASE_THEN_MERGE",
  "survived": true,
  "confidence": 0.88,
  "artist_value": "direct",
  "risk": "med",
  "one_line": "One unapplied commit that adds a brand-new artist-facing tool (houdini_layout_network) plus a scoped-memory fix from the 9-Sep demo post-mortem; nothing on master or any other branch duplicates it, and a test-merge conflicts in exactly two lines (the '128 tools' count in CLAUDE.md and README.md).",
  "action_steps": [
   "git fetch --all && git log --oneline -1 master   # confirm still 67625294 before starting",
   "git checkout -b rebase/demo-repairs fix/demo-repairs-20260909",
   "git rebase master   # expect exactly 2 conflicts, both one-line",
   "# CLAUDE.md line 3: keep MASTER's line (SYNAPSE v5.70.0) and change '128 MCP tools registered' -> '129 MCP tools registered'",
   "# README.md line 121: keep MASTER's line ('**128 tools, two paths.** The external MCP bridge and live') and change 128 -> 129",
   "git rebase --continue",
   "python -m pytest tests/test_network_layout.py tests/test_demo_memory_scope.py tests/test_demo_memory_recall.py tests/test_memory_lifecycle.py tests/test_memory_owner_dispatch.py tests/test_moneta_search_serialization.py tests/test_session_journal_main_thread.py -q   # the 7 new suites first",
   "python -m pytest tests/ -q   # FULL suite - the floor is 8869 passed / 0 failed; half B rewrote store.py, so treat any new red here as blocking",
   "grep -rn \"128\" tests/ --include=*.py | grep -i tool   # a tool-count-pinning conformance test may also need the 129 bump",
   "hython scripts/probe_network_layout.py   # live H22.0.400 probe - never run in this triage, and the layout contract (undo grouping, artist-box refusal) is hou-side and unproven",
   "# OPTIONAL but recommended before merge: git reset --soft master && split into two commits - (A) layout tool: protocol.py, network_layout.py, handlers_network_layout.py, _tool_registry.py layout hunk, mcp_tools_scene.py, rbac.py, system_prompt.py layout paragraph, scripts/probe_network_layout.py, tests/test_network_layout.py, CLAUDE.md, README.md ; (B) memory scope: everything else. A can merge on green units alone; B should wait on the full suite.",
   "# ORDERING: fix/cto-render-20260908 and feature/tops-renderfarm-20260907 also edit CLAUDE.md, README.md, protocol.py and _tool_registry.py - whichever merges second re-resolves the same tool-count line. Merge this one first; it is the smaller resolution."
  ],
  "refuter_reason": "Attacked on all five axes and it held; my checks raise confidence rather than lower it. (1) Evidence real: `git cherry master fix/demo-repairs-20260909` returns exactly `+ 72de0881759a04fcc6f3ca56eb6c78ce37351623`, merge-base is d7c90a1522bb66b9a4cbfdfe0058df48f2d787d4, and `git grep -c layout_network master` returns nothing - no fabricated citation found. (2) Axis 4 refutation attempt FAILED and inverts: of the branch's 32 touched files, master has touched only TWO since the merge-base, both in disjoint functions - `panel/session_journal.py` (master 0c98c6a0 rewrote `_timestamp()`; branch edits `_resolve_log_dir()` to route `hou.hipFile.path` through `run_on_main`) and `panel/bridge_adapter.py` (branch hunks are an additive `_TOOL_TO_OPERATION` entry + a `summary` branch). `git log MB..master --` on `_tool_registry.py`, `handlers_memory.py`, `core/protocol.py`, `rbac.py`, `memory/store.py`, `moneta_store.py`, `scene_memory.py`, `session/tracker.py`, `host/memory_loop.py`, `server/handlers.py`, `panel/system_prompt.py`, `mcp_tools_scene.py` returns 0 commits each - so the TOOL_DEFS tuple arity cannot have drifted under the new registry entry, which was my best shot. 51-behind is a commit count, not an interface count, on this branch. (3) The tool-count step is confirmed right and needs no extra work: `tests/test_phase0c_doc1_toolcount.py` binds the CLAUDE.md banner dynamically to `len(TOOL_DEFS)` (it imports the registry, no hardcoded number) and a sibling test does the same for README's \"N tools, two paths\" - so 128->129 in both files IS the whole fix; the only literal 128 under tests/ is a comment at :130. (4) No neighbour contradiction: the render/tops pair shares only CLAUDE.md/README/protocol.py/_tool_registry.py and this verdict already claims merge-first ordering on the smaller resolution, which is correct. Residual risk is exactly what the original verdict named and no more: store.py's 345 reworked lines are unread and untested here, and the hou-side layout contract (undo grouping, artist-box refusal) is unproven - I ran no tests, no rebase, and no live probe."
 },
 {
  "branch": "bp2/nits",
  "team": "bp2-nits",
  "tier": "WARM",
  "verdict": "SPLIT_SALVAGE",
  "original": "SPLIT_SALVAGE",
  "survived": true,
  "confidence": 0.88,
  "artist_value": "indirect",
  "risk": "med",
  "one_line": "Three of the four nits still describe real defects on master today and merge clean, but the fourth (dc9e803e, dashboard pill 'open' -> green) is a legibility regression on the human-gate table, and two evidence paths the branch adds are wrong and need a one-word fix before landing.",
  "action_steps": [
   "# 1. Confirm the merge is still clean (it was at master 67625294):",
   "git -C C:/Users/User/SYNAPSE merge-tree --write-tree --name-only master bp2/nits >/dev/null && echo CLEAN",
   "# 2. Take the three good commits onto a landing branch, leaving T3 and the receipt behind:",
   "git -C C:/Users/User/SYNAPSE checkout -b bp2/nits-landed master",
   "git -C C:/Users/User/SYNAPSE cherry-pick 19a6697c 361cc6e7 dab6c634",
   "# 3. Fix the two broken citations the branch introduces, then amend:",
   "#    docs/BATTLEPLAN.md row 6: memory_latency_hython_provisioned.json -> .jsonl (the file on disk is .jsonl)",
   "#    docs/MONETA_FOLLOWUPS.md FU-1: test_moneta_crucible.py::test_duplicate_content_gets_distinct_ids",
   "#      -> test_moneta_crucible.py::test_repeat_deposits_distinct_ids_count_equals_all_divergence_gone",
   "git -C C:/Users/User/SYNAPSE commit -a --amend --no-edit   # or a separate fixup commit",
   "# 4. Verify the corrected pin actually collects before merging:",
   "C:/Users/User/SYNAPSE> python -m pytest tests/test_moneta_crucible.py::test_repeat_deposits_distinct_ids_count_equals_all_divergence_gone -q",
   "# 5. Merge bp2/nits-landed. Then either drop the source branch or keep it for the T3 question:",
   "git -C C:/Users/User/SYNAPSE branch -D bp2/nits   # only after the landing branch is merged",
   "git -C C:/Users/User/SYNAPSE worktree remove C:/Users/User/SYNAPSE/.claude/worktrees/bp2-nits",
   "git -C C:/Users/User/SYNAPSE push origin --delete bp2/nits   # branch is already public and in sync with origin"
  ],
  "refuter_reason": "Four independent spot-checks, no fabricated citation found. (1) The T3 risk \u2014 the load-bearing claim \u2014 is exactly as stated: `git show master:harness/battleplan/dashboard_bp2.py` pill() has ok-list (\"GREEN\",\"SOUND\",\"receipted\",\"MEASURED\",\"ALIVE\",\"done\",\"RECEIPTED\") with no 'open'; the branch version adds \"open\" to that same elif; and line 258 does read `h.append(f\"<tr>...<td>{pill('open' if open_ else 'done')}</td></tr>\")` inside `for w, obs, open_ in wds` under the \"Words \u00b7 yours, per act\" header. Matching is `any(k in t for k in ...)` over `t = str(text)` with no .lower(), so the substring-flip concern is real. T3 is not inert and does turn an outstanding human word green. (2) `git cherry master bp2/nits` = 5 '+' lines; `merge-tree --write-tree --name-only master bp2/nits` exits 0 => CLEAN. Both confirmed. (3) Both \"broken citation\" claims are real: `git ls-tree -r --name-only master` shows only `memory_latency_hython_provisioned.jsonl` while the branch's BATTLEPLAN row 6 cites `...provisioned.json`; and `test_duplicate_content_gets_distinct_ids` appears on master only as prose at tests/test_moneta_crucible.py:213, with the live test at :244 named `test_repeat_deposits_distinct_ids_count_equals_all_divergence_gone` \u2014 the branch pins the dead name twice (MONETA_FOLLOWUPS prose + the FU-1 table row). Corrections the verdict owes: T1 is presented as a defect fix; it is a defect swap. `$editedOrch` is the worktree file (prove_bp2_meter_dryrun.ps1:18 `$editedOrch = Join-Path $repoRoot 'harness\\orchestrate.ps1'`), so master's `HEAD:` form is a tautology only while the tree is clean \u2014 the verdict's \"comparing HEAD to itself\" is loose but the defect is genuine. The branch's `HEAD~1:` is equally unpinned: master HEAD is 67625294 and HEAD~1 is 2c30f890 (a release-docs commit), so running the \"fixed\" script today captures a baseline that has nothing to do with the pre-METER parent. The correct fix pins the actual sha. This does not move the disposition \u2014 the script lives under runs/2026-09-01/, has zero runtime reach, and lands clean either way \u2014 but T1 should not be described as fixed. Did NOT check: whether the three salvaged commits cherry-pick without textual conflict individually (only the whole-branch merge-tree was verified), and did not run any test."
 },
 {
  "branch": "rope/beacon",
  "team": "rope-beacon",
  "tier": "WARM",
  "verdict": "ARCHIVE_DELETE",
  "original": "ARCHIVE_DELETE",
  "survived": true,
  "confidence": 0.95,
  "artist_value": "none",
  "risk": "low",
  "one_line": "A stopped 5-minute cron heartbeat that rewrote one root-level STATUS.md 20 times on 2026-08-03; every verdict it reports is stale against master's harness/rope/STATE.json, so extract the 10 telemetry rows if wanted, then delete local + origin.",
  "action_steps": [
   "cd C:/Users/User/SYNAPSE",
   "git show rope/beacon:STATUS.md > C:/Users/User/SYNAPSE/harness/rope/beacon_ledger_2026-08-03.md   # OPTIONAL: keep the 10 telemetry rows; skip if runner telemetry from one afternoon is not wanted",
   "git worktree remove C:/Users/User/rope-beacon-wt   # worktree is clean; add --force only if it refuses",
   "git branch -D rope/beacon",
   "git push origin --delete rope/beacon   # REQUIRED: the branch is live on the PUBLIC origin at the same sha, so the local delete alone leaves it published",
   "git worktree prune"
  ],
  "refuter_reason": "Every load-bearing citation reproduced. `git cherry master rope/beacon | grep -c '^+'` -> 20; `git diff --stat` -> ` STATUS.md | 25 +++++ / 1 file changed, 25 insertions(+)`; first/last commits eda15a55 2026-08-03T14:14:13-04:00 and 6854c72c 2026-08-03T15:44:46-04:00 at 5-min spacing; `git cat-file -e master:STATUS.md` -> fatal, ABSENT; `git ls-remote --heads origin` -> 6854c72c9a880224c7c278fad531ab409a089a47 refs/heads/rope/beacon (same sha, live on the PUBLIC origin); worktree C:/Users/User/rope-beacon-wt 6854c72c [rope/beacon] exists. STATUS.md line 2 reads `runner: stopped | sentinel relaunches used: 0/6` and the footer is verbatim as quoted. The \"master is strictly ahead\" claim survives the hardest check: counting task statuses in master:harness/rope/STATE.json yields exactly {verified 22, blocked_seat 1, blocked_human 2, blocked 1, needs_review 10}, and L5-4/L5-10/L5-11 are all `verified` on master while the beacon still prints them as needs_review with \"manual pending\"/\"DESIGN REVIEW BY JOE\". So the branch's only content is a stale read-out. I found no unreplicated hunk of value beyond the 10 telemetry rows the verdict already preserves. The one thing I found that the verdict missed cuts the same way, not against it: master:harness/rope/rc.ps1 is a tracked producer that does `git worktree add -B rope/beacon $bw` (:7) and `git push -f origin rope/beacon` (:50), which makes this branch machine-regenerated output rather than authored work \u2014 deleting it is even safer than stated, but the delete is also not durable."
 },
 {
  "branch": "mem/m2-pgdrm",
  "team": "mem-pgdrm",
  "tier": "WARM",
  "verdict": "REBASE_THEN_MERGE",
  "original": "REBASE_THEN_MERGE",
  "survived": true,
  "confidence": 0.85,
  "artist_value": "none",
  "risk": "low",
  "one_line": "Master has no pgdrm.py at all and the MEMORY board still marks M2 open, so this is live unfinished work - but the branch HEAD still carries the crucible-found one-shot-iterable defect, and its repair is sitting UNCOMMITTED in the worktree, so commit that first, rebase (zero conflicts - both files are new), re-measure, then merge.",
  "action_steps": [
   "git -C C:/Users/User/synapse-m2-pgdrm-wt status --porcelain   # expect: M python/synapse/loop/pgdrm.py, M tests/test_pgdrm_kernel.py",
   "git -C C:/Users/User/synapse-m2-pgdrm-wt diff   # review the +326/-1 fix-leg repair before trusting it",
   "git -C C:/Users/User/synapse-m2-pgdrm-wt add python/synapse/loop/pgdrm.py tests/test_pgdrm_kernel.py && git -C C:/Users/User/synapse-m2-pgdrm-wt commit -F <msg-file>   # msg names defect 1 (one-shot iterables refused) + defect 2 (lines 201/231/263 covered); use -F, never a here-string",
   "git -C C:/Users/User/synapse-m2-pgdrm-wt fetch origin && git -C C:/Users/User/synapse-m2-pgdrm-wt rebase master   # 403 commits behind; expect zero conflicts (two new files)",
   "cd C:/Users/User/synapse-m2-pgdrm-wt && python -m pytest tests/test_pgdrm_kernel.py tests/test_loop_contracts.py -q --no-header -p no:cacheprovider",
   "cd C:/Users/User/synapse-m2-pgdrm-wt && python -m pytest tests/ -q --no-header -p no:cacheprovider   # compare against a FRESH floor at merge-base(master,HEAD); the 2026-08-22 numbers (6856 passed) are ~2000 tests stale vs the current 8869 floor",
   "python C:/Users/User/SYNAPSE/harness/memory/runs/2026-08-22/m2fix_mutate_harness.py   # re-confirm 28 RED / 0 survivors on the rebased tree",
   "# after merge: flip harness/memory/STATE.json rungs.m2 status off 'FIX LEG RUNNING' and set merged/pushed in harness/memory/bus/memory_m2_pgdrm_kernel.json (both are stale)",
   "git -C C:/Users/User/SYNAPSE worktree remove C:/Users/User/synapse-m2-pgdrm-wt   # only AFTER the fix is committed - removing it now destroys the repair"
  ],
  "refuter_reason": "Attacked on all five axes; it survived. (1) EVIDENCE IS REAL \u2014 spot-checked more than one citation: `git log $(merge-base)..mem/m2-pgdrm` = the single commit e4730869|2026-08-21 exactly as quoted; `git diff --stat` = pgdrm.py +348, test_pgdrm_kernel.py +429, 777 insertions; `git cherry master mem/m2-pgdrm` = `+ e47308690b5ec0e5fbfdb97425eafa39e5ff3da2` (unapplied); `git rev-list --left-right --count master...mem/m2-pgdrm` = 403/1; `git ls-tree -r master -- python/synapse/loop` lists 9 files and NO pgdrm.py; `git -C C:/Users/User/synapse-m2-pgdrm-wt status --porcelain -b` still shows ' M' on both files today (2026-09-15) with diffstat +326/-1 \u2014 the uncommitted repair is genuinely still sitting there and is genuinely destroyed by a `worktree remove`; harness/memory/runs/2026-08-22/m2fix_mutate_harness.py and m2fix_mutation_ledger.json both exist, ledger reads {\"total\":28,\"red\":28,\"survivors\":[]}; STATE.json:107 carries the quoted status string verbatim; `git ls-remote --heads origin mem/m2-pgdrm` returns e4730869, so the verdict is right that bus `pushed:false` is stale AND right that the work is already on the public remote. No fabricated citation found. (2) NO NEIGHBOUR CONTRADICTION \u2014 repair/ledger-moneta-seam, repair/h1-schemas-b and stray/audit-log-threading touch none of python/synapse/loop/pgdrm.py or tests/test_pgdrm_kernel.py; nothing else on master or in the graph claims this file, and merge-tree 'changed in both' = 0. Nobody has to own a conflict here. (3) N/A \u2014 not ARCHIVE_DELETE. (4) THE 403-COMMITS-BEHIND ATTACK FAILED, and I pushed it hard because that is the axis that should kill a stale rebase verdict. pgdrm.py itself imports nothing from the repo, but the TEST file does: `from synapse.loop import ports`, and tests/test_pgdrm_kernel.py:94-96 asserts both `\"pgdrm\" not in ports.py` source AND `ports.MemoryPort().query_and_filter([\"r\"],[\"t\"]).status == \"UNAVAILABLE\"`. That is a live coupling to a surface that had 403 commits to move under it. I executed it against master HEAD (67625294): status = UNAVAILABLE. `query_and_filter` still exists at python/synapse/loop/ports.py:456 and `MemoryPort.__init__(storage_uri: Optional[str] = None, ...)` still takes zero required args, so `ports.MemoryPort()` still constructs. No moved interface. I also found no test on master that walks the python/synapse/loop package directory or enumerates its modules, so adding a 10th file trips no census gate. (5) STEPS ARE RUNNABLE \u2014 they use `git -C <abs>` (correct: this host denies `cd && git add/commit`) and `git commit -F <msg-file>` (correct: the here-string trap). Two nits below, neither fatal. WHAT I DID NOT CHECK: I did not run the rebase, did not run tests/test_pgdrm_kernel.py or the full suite, did not re-run the mutation harness, did not verify the 66-test count or the hand-computed decay table, did not read m2fix_suite_branch.txt's contents, and did not check CI. A green isolated import probe is not a composed-suite green."
 },
 {
  "branch": "feat/scene-model",
  "team": "cold-panel-scene",
  "tier": "COLD",
  "verdict": "SPLIT_SALVAGE",
  "original": "SPLIT_SALVAGE",
  "survived": false,
  "confidence": 0.86,
  "artist_value": "indirect",
  "risk": "high",
  "one_line": "Kill the 1,900-line turn-measurement instrument, but re-apply two small unreplicated fixes by hand: the audit session-id collision (master:python/synapse/core/audit.py:186) and the render_farm_status read-only classification.",
  "action_steps": [
   "git -C C:/Users/User/SYNAPSE tag archive/feat-scene-model 15843434   # keep the history before deleting the branch",
   "git -C C:/Users/User/SYNAPSE show 15843434 -- python/synapse/core/audit.py   # re-type this 1-line seed fix + current_session_id() onto a fresh branch off master; do NOT cherry-pick the commit",
   "python -m pytest tests/test_stress.py tests/test_studio_integration.py tests/ -k audit   # the two master tests that read _current_session",
   "git -C C:/Users/User/SYNAPSE show 15843434 -- python/synapse/server/handlers.py   # decide separately on adding \"render_farm_status\" to master's _READ_ONLY_COMMANDS frozenset",
   "git -C C:/Users/User/SYNAPSE worktree list | grep -E 'scene-model' ; git -C C:/Users/User/SYNAPSE branch -D feat/scene-model",
   "git -C C:/Users/User/SYNAPSE push origin --delete feat/scene-model   # branch is already public on origin; optional, the archive tag holds the history"
  ],
  "refuter_reason": "Disposition survives, but one citation is fabricated, so refuted=true under the spot-check rule. VERIFIED EXACT: master:python/synapse/core/audit.py:186 is verbatim the cited deterministic_uuid session line; master:python/synapse/server/handlers.py:233 has \"render_farm_cancel\" and the frozenset omits render_farm_status, which is only registered at :763; merge-base 0f14c9500b, 1337 behind, 2 unapplied (d02b0ed0, 15843434), 11 files / 1967 insertions of which 920 are the two new test files; all four new modules ABSENT on master. FALSE: the risk field claims master:tests/test_stress.py and master:tests/test_studio_integration.py both read _current_session -- `git grep -n \"_current_session|current_session_id\" master -- python tests scripts` yields exactly three hits, ALL in python/synapse/core/audit.py (186, 262, 433), and ZERO under tests/. The named verification gate does not exist; a human running it would get a vacuous green. Corrected gate: the real consumers are audit.py:262 (session_id stamped on each AuditEntry) and audit.py:433 (target_session in the verify/filter path), so gate on the audit hash-chain / session-filter tests, not those two files. STRENGTHENED, not broken: master's poll exemption at handlers.py:509-520 is scoped to cmd_type==\"render\" with a poll payload only, and handlers_render.py:1678 spells out master's own rationale (\"a running render holds the C5 mutation lock for its duration\") for classifying render_farm_cancel/render_processes/render_stop read-only -- render_farm_status was skipped, so the frozen-farm-status-readout premise holds on today's master and the salvage is worth more, not less. NOT CHECKED: whether the branch's 15-line audit.py hunk is only the seed fix + current_session_id(); whether any master audit test actually fails under a time_ns seed; no test-merge was run, so \"claude_worker.py conflicts wholesale\" is asserted, not demonstrated."
 },
 {
  "branch": "feat/panel-ux-pass",
  "team": "cold-panel-scene",
  "tier": "COLD",
  "verdict": "SUPERSEDED",
  "original": "SUPERSEDED",
  "survived": true,
  "confidence": 0.88,
  "artist_value": "direct",
  "risk": "low",
  "one_line": "The shipped Pentagram panel already does connection-state-at-a-glance on these exact two buttons, and this branch's raw QPushButton would regress the design-system token authority -- archive it.",
  "action_steps": [
   "git -C C:/Users/User/SYNAPSE tag archive/feat-panel-ux-pass 0d27a17f",
   "git -C C:/Users/User/SYNAPSE worktree list | grep -E 'panel-ux' ; git -C C:/Users/User/SYNAPSE branch -D feat/panel-ux-pass",
   "git -C C:/Users/User/SYNAPSE push origin --delete feat/panel-ux-pass   # already public on origin; the archive tag holds the history",
   "OPTIONAL, separate ticket: master's _on_corpus announces 'Corpus loaded (n)' but never names the directory -- a one-line addition of str(_scout.RAG_ROOT) to that message would carry the only idea worth keeping, without the modal"
  ],
  "refuter_reason": "Axis 1 (evidence real): every spot-check landed. `git show --stat 0d27a17f` = \"feat(panel): Bridge/Corpus connection pills + RAG-dir confirm on connect\", 2026-06-26, 3 files, +75/-3 (qss.py +15, tokens.py +3, synapse_panel.py +60/-3) \u2014 exactly as claimed. merge-base = 808775c19c; `git rev-list --count feat/panel-ux-pass..master` = 1598; `git cherry` = 1 unapplied (+0d27a17f). Branch diff confirms `- self._connect_btn = c.Button(\"Connect\", variant=\"ghost\")` / `+ QtWidgets.QPushButton(\"Connect\")` + `setObjectName(\"DsConnPill\")`, same for Corpus, plus `_confirm_rag_dir()` ending in `box.exec() == QMessageBox.StandardButton.Ok` on the connect path. Master side: synapse_panel.py:1091 is verbatim `c.Button(\"Corpus\", variant=\"primary\", parent=w)`; `_refresh_corpus_state` at 1209-1223 sets `\"Corpus \u2713\" if loaded else \"Corpus\"`; tokens.py SIGNAL_DEEP line is verbatim; compositor.py:66 comment is verbatim; tests/panel/test_token_authority.py, test_theme_source.py and test_modal_guard.py all exist, and the modal-guard docstring literally reads \"a modal exec() raises instead of blocking\". No fabrication anywhere.\n\nAxis 2 (neighbour contradiction): none. All four neighbour teams (render/tops, demo-repairs, cold-repairs, cache-rope) are server/harness/docs branches; none touches python/synapse/panel/*. No competing claim to own.\n\nAxis 3 (hunt for unreplicated value): the RAG-directory idea is the only candidate and the verdict already carves it out. I confirmed it is genuinely unreplicated AND correctly scoped: master's `_on_corpus` (synapse_panel.py:1195-1205) announces \"Corpus active (%d entries, probe hit %d docs incl. %s)\" \u2014 entries and a source doc name, never the corpus root path. So the optional follow-up ticket is real, and it is correctly kept out of the merge (the value is one string, not a modal). Everything else on the branch is replicated: master answers \"is the bridge up\" with a live connection dot (chat_panel.py:613-616, `_conn_dot` QLabel objectName `status_dot`, recoloured to _SIGNAL on connect at :663) and \"is the corpus grounded\" with the Corpus \u2713 flip \u2014 both built out of the design system.\n\nAxis 4: n/a (not a merge verdict).\n\nAxis 5: the steps are mostly runnable but two defects \u2014 see missed[2] and missed[3].\n\nWhat I did NOT check: I never test-merged the branch, so the qss.py +15 DsConnPill block was not checked for collision with master's current qss/sweep_a_style machinery, and no conflict count exists for this branch. I also did not run tests/panel/ against a merged tree \u2014 the merge-blocker claims are read from test docstrings and source, not from a red run."
 },
 {
  "branch": "wip/panel-goalposts",
  "team": "cold-panel-scene",
  "tier": "COLD",
  "verdict": "SPLIT_SALVAGE",
  "original": "ARCHIVE_DELETE",
  "survived": false,
  "confidence": 0.85,
  "artist_value": "none",
  "risk": "low",
  "one_line": "June WIP goalpost tests for a model picker, an Ollama provider and a hython matrix -- master shipped all three with different APIs, so these tests now pin a June surface that no longer exists.",
  "action_steps": [
   "git -C C:/Users/User/SYNAPSE tag archive/wip-panel-goalposts c3a60324",
   "git -C C:/Users/User/SYNAPSE worktree list | grep -E 'goalposts' ; git -C C:/Users/User/SYNAPSE branch -D wip/panel-goalposts",
   "git -C C:/Users/User/SYNAPSE push origin --delete wip/panel-goalposts   # already public on origin; the archive tag holds the history"
  ],
  "refuter_reason": "The verdict's load-bearing claim is \"master shipped all three with different APIs, so these tests pin a June surface that no longer exists.\" That is false for one of the three. master's .synapse/hytest.py carries only the singular pin (master:.synapse/hytest.py:100 os.environ.get(\"SYNAPSE_HYTHON\")) and a single-best-build resolver (_candidates / _usable / find_hython / main) \u2014 there is no SYNAPSE_HYTHONS, no per-build aggregation, and `git grep -iE 'SYNAPSE_HYTHONS|build matrix' master` over the entire tree returns zero hits, so the idea is not superseded and not even recorded elsewhere. tests/panel/test_hython_matrix.py (182 lines) is therefore not a stale pin; it is a live unimplemented spec, written pure-Python with monkeypatched hython resolution so it runs under stock `pytest -q` with no Houdini, and its subject got MORE relevant since June: this host now has both Houdini 22.0.400 and 22.0.429 and the shim picks newest. That is the unreplicated hunk ARCHIVE_DELETE would destroy. A second, smaller break: the cited grep `git grep -li 'model_picker|ModelPicker' master -- python/synapse/panel => providers/registry.py and synapse_panel.py` returns NOTHING (re-run with `-e model_picker -e ModelPicker` as well). What master actually has is an engine/provider selector (python/synapse/panel/synapse_panel.py:1225 `_refresh_engine_selector`), which is not the same surface as the branch's goalpost (registry.MODELS with >=2 Claude entries incl. opus+haiku, build_provider taking a model key). The rest of the evidence IS real and I re-ran it: `git cherry master wip/panel-goalposts` = 2 unapplied (91fe5b1e, c3a60324), diffstat exactly 8 files/+1130/-0, the four test files and harness/rescope_settings.py absent from master, and test_type_scale_native.py diverging by exactly +121/-29. So the disposition is right for 6 of 8 files and wrong for at least one: salvage test_hython_matrix.py (or record the H-MATRIX contract in the backlog) BEFORE archiving, then delete the rest. Operational gap in the steps regardless of verdict: they push-delete the public branch but never push the archive tag, leaving a local-only tag as the sole copy of history destroyed on origin."
 },
 {
  "branch": "repair/ledger-moneta-seam",
  "team": "cold-repairs",
  "tier": "COLD",
  "verdict": "SUPERSEDED",
  "original": "SUPERSEDED",
  "survived": true,
  "confidence": 0.95,
  "artist_value": "indirect",
  "risk": "low",
  "one_line": "Every code and test file on this branch is byte-identical to master already, and moneta_runtime.py went further on master a day later \u2014 patch-id misses it only because the content landed under a different commit.",
  "action_steps": [
   "git -C C:/Users/User/SYNAPSE tag archive/repair-ledger-moneta-seam repair/ledger-moneta-seam",
   "git -C C:/Users/User/SYNAPSE branch -D repair/ledger-moneta-seam",
   "git -C C:/Users/User/SYNAPSE worktree prune"
  ],
  "refuter_reason": "Conclusion holds, stated reason was wrong. I verified the cited sha exists verbatim (eb25abed|2026-07-26|leg(LEDGER): moneta seam - two defects closed, bounded revision walk, 8 files) and independently confirmed byte-identity via empty `git diff --shortstat master:<f> repair/ledger-moneta-seam:<f>` for python/synapse/memory/ledger.py, python/synapse/science/deposit.py, tests/test_ledger_moneta_seam.py and harness/notes/receipts/LEDGER.json. The verdict's risk line (\"the only branch-only bytes are an older, shorter moneta_provenance() docstring\") is false by 72 lines: four files differ, not one -- docs/studio/DEPLOYMENT.md (+35/-75), harness/notes/mutation_ledger_moneta.json (+33/-149), harness/verify/mutate_ledger_moneta_seam.py (+4/-153), python/synapse/memory/moneta_runtime.py (+22/-611). I read all 94 branch-only lines. Every hunk is a shorter predecessor of what master carries: the harness's 4 lines are a hardcoded single-target TESTS constant master generalized; the receipt's 33 are stale pytest timings from a 25-mutation run master superseded; moneta_runtime's 22 are the pre-U1 single-function moneta_provenance(). The one genuine candidate for unreplicated value -- the commit's single DEPLOYMENT.md line teaching that SYNAPSE_MEMORY_BACKEND gates both the memory store and the Ledger->Moneta enrichment seam -- is on master verbatim at docs/studio/DEPLOYMENT.md:130 (it showed as a diff `+` only as a whitespace/context artifact). No unreplicated value anywhere. No neighbour contradiction: render-vs-tops and cold-panel-scene claim render-farm and panel/audit files; nobody else touches python/synapse/memory/. Action steps are runnable: `git worktree list | grep -i ledger` is empty and `git branch --format='%(refname:short) %(worktreepath)'` shows an empty worktree path, so `git branch -D` will not be refused; HEAD is on master; the trailing `git worktree prune` is a harmless no-op. The steps tag before deleting, so a wrong call here is recoverable, which is why confidence rises rather than falls despite the corrected risk line."
 },
 {
  "branch": "repair/h1-schemas-b",
  "team": "cold-repairs",
  "tier": "COLD",
  "verdict": "SUPERSEDED",
  "original": "SUPERSEDED",
  "survived": true,
  "confidence": 0.9,
  "artist_value": "indirect",
  "risk": "low",
  "one_line": "Master already carries both reconciled TOOL_RETURN enums AND the derived AST pin that guards them \u2014 it just lives inside tests/test_solaris_tool_registration.py instead of the branch's separate file.",
  "action_steps": [
   "git -C C:/Users/User/SYNAPSE tag archive/repair-h1-schemas-b repair/h1-schemas-b",
   "git -C C:/Users/User/SYNAPSE branch -D repair/h1-schemas-b",
   "git -C C:/Users/User/SYNAPSE worktree prune"
  ],
  "refuter_reason": "Verdict survives a four-axis attack; the core claim is verified byte-level, not inferred. (1) Evidence is real: sha 0929d568 has the exact cited subject and 4-file shape; `git cherry master B | grep -c '^+'` = 1; branch is 1227 behind / 1 ahead. The decisive check reproduces: `git diff master:python/synapse/mcp/tool_impls/solaris/schema_set_purpose.py repair/h1-schemas-b:...` shows `\"enum\": [\"set\", \"updated\", \"unchanged\", \"noop\", \"not_found\"]` as CONTEXT (no +/- prefix), and the create_variants diff shows `\"enum\": [\"created\", \"already_exists\"]` as CONTEXT \u2014 master already carries both reconciled enums, only prose differs. (2) master:tests/test_solaris_tool_registration.py carries test_schema_return_status_enum_matches_implementation (:414), test_schema_return_keys_are_producible (:442), test_every_schema_return_contract_is_pinned (:460), test_tool_audit_schema_declares_no_return_contract (:481) AND four reader tests the branch never had (:523/:532/:536/:544) \u2014 master's pin is a strict superset of the branch's 364-line file. harness/notes/receipts/H1.json present on master (blob 976d21f6); tests/test_solaris_schema_return_contract.py absent. (3) No neighbour contradiction \u2014 render-vs-tops and cold-panel-scene share zero files with these five Solaris schemas. (4) Unreplicated-value hunt found one genuine but sub-threshold hunk (see `missed`), which the verdict itself disclosed as \"reword two schema description strings\" and which the archive tag preserves. (5) Steps run: repair/h1-schemas-b is NOT in `git worktree list` (55 worktrees enumerated, none on it), so `git branch -D` will not hit the used-by-worktree refusal. Correction to the steps, not the verdict: `origin/repair/h1-schemas-b` EXISTS, so a local delete plus a local-only tag leaves the branch alive on the public remote and it returns on any fresh clone \u2014 the steps are incomplete. WHAT I DID NOT CHECK: I never ran the test suite, so \"master's pin is green\" is UNVERIFIED \u2014 I verified the pin exists and what it reads, not that it passes; and I did not read set_purpose.py/create_variants.py execute() to independently confirm which statuses they actually emit, so the enum reconciliation is verified as \"master == branch\", not as \"master == implementation\"."
 },
 {
  "branch": "stray/audit-log-threading",
  "team": "cold-repairs",
  "tier": "COLD",
  "verdict": "ARCHIVE_DELETE",
  "original": "ARCHIVE_DELETE",
  "survived": true,
  "confidence": 0.85,
  "artist_value": "none",
  "risk": "med",
  "one_line": "Genuinely unapplied \u2014 826 lines master does not have \u2014 but it has zero consumers on either side and its written revival condition (the R302 instrument lane wanting these fields) came and went without reviving it.",
  "action_steps": [
   "git -C C:/Users/User/SYNAPSE tag archive/stray-audit-log-threading stray/audit-log-threading",
   "git -C C:/Users/User/SYNAPSE branch -D stray/audit-log-threading",
   "git -C C:/Users/User/SYNAPSE worktree prune"
  ],
  "refuter_reason": "Attacked on all five axes; the disposition survives and got stronger, but two factual corrections are owed. AXIS 1 (evidence real): five spot-checks, all verbatim. `git log -1 03f279fc` -> `03f279fc|2026-08-02|stray(audit): preserve the abandoned audit-state-capture lane (R308)`. `git diff --stat` -> exactly 6 files, +826/-0, the five named siblings plus the 178-line scope doc. `git cherry master` -> 1 unapplied. master:python/synapse/core/audit.py `log()` ends at `sequence_id: str = \"\"` while the branch adds `duration_ms`/`before_state_hash`/`after_state_hash` \u2014 threading genuinely absent from master. R308 in master:harness/notes/CTO_RULINGS_03.md:79-86 reads verbatim \"it revives the branch; otherwise it dies of natural causes in the branch list.\" PROPOSED_perf_baseline.json:58 cites R302 lane I as landed. Nothing fabricated. AXIS 3 (hunt for unreplicated value): the strongest candidate was python/synapse/core/state_digest.py (197 lines, a thought-through module \u2014 scope gate, playbar-varying parms excluded, null-rather-than-fake for unbounded blast radius). It is NOT an unreplicated capability: `git grep -l 'scene_hash_before|def _topo_hash' master` returns four live implementations of hash-scene-state-around-an-op \u2014 shared/bridge.py, python/synapse/server/integrity_envelope.py, python/synapse/panel/session_integrity.py, python/synapse/memory/agent_state.py. The branch is a fifth copy at a dead layer. Dead-reader claim confirmed: `replay_info` appears only at its own definition (audit.py:440) and the fields only at the dataclass (88/132/156) \u2014 producer-less and consumer-less on both sides. The one forward hook anyone could cite for keeping it: master:python/synapse/loop/ports.py:90 declares `SafetyPort.evaluate_path(..., scene_state_digest)` and NOTHING on master produces that value (grep over python/tests/panel/scripts hits only the contract stub and tests/test_loop_contracts.py:60) \u2014 a signature with no filler, not a live demand. AXIS 4: n/a (not a merge verdict). AXIS 2: no neighbour contradiction \u2014 no other branch contains 03f279fc (`git branch -a --contains` -> only itself + its origin ref), and feat/scene-model's audit.py claim is a different hunk (session-id collision at :186). AXIS 5 is where the verdict is WRONG, twice, and both cut toward less risk, not more: (1) \"This is the only copy of 826 lines\" is false \u2014 `git for-each-ref` shows `refs/remotes/origin/stray/audit-log-threading 03f279fc`, i.e. the commit is already on the PUBLIC origin. `git branch -D` deletes the local ref only; the branch stays on GitHub, stays in `git branch -a`, and a future work-graph run rediscovers it. So the steps do not achieve their stated effect, and the risk is low, not med \u2014 archiving loses nothing. Actually removing it from the list would need `git push origin --delete`, a public-repo mutation only Joe can authorize; leaving the origin ref alone is also exactly what R308's \"dies of natural causes in the branch list\" describes. (2) `git worktree prune` is a no-op: `git worktree list` (54 worktrees) has none on this branch. The local `git tag archive/...` is also local-only and unpushed \u2014 fine, since origin holds the copy. NOT CHECKED: whether the 6-line hunk still applies cleanly to master's audit.py (919 commits behind), whether tests/test_state_digest.py passes against master's tree, and the 178-line AUDIT_STATE_CAPTURE_SCOPE.md was not read for decision content absent from CTO_RULINGS_03.md."
 },
 {
  "branch": "feat/cache-insert",
  "team": "cold-cache-rope",
  "tier": "COLD",
  "verdict": "REBASE_THEN_MERGE",
  "original": "REBASE_THEN_MERGE",
  "survived": true,
  "confidence": 0.86,
  "artist_value": "direct",
  "risk": "med",
  "one_line": "The brief's hypothesis is refuted - prompt caching already shipped on master - but this branch is something better: the missing action half of the cache advisor (synapse_insert_cache), which master's own code still documents as absent, and it conflicts in exactly two files.",
  "action_steps": [
   "git worktree add ../syn-cacheinsert feat/cache-insert",
   "git -C ../syn-cacheinsert rebase master",
   "Resolve conflict 1 - CLAUDE.md: branch bumps the header tool count 124 -> 125; master's header now reads 128, so write 129.",
   "Resolve conflict 2 - python/synapse/server/handlers_cache.py: master's only drift is the 4-line docstring at :11 asserting \"there is no ``synapse_insert_cache``\". Delete that sentence, keep the branch body.",
   "Check the new registry entry against the post-v5.70.0 artist-readable tool naming/description convention (b475ee22) before it reaches a panel - the branch predates that rename.",
   "pytest tests/test_cache_insert_gates.py, then the full pytest tests/ against the floor 8869 passed / 412 skipped / 0 failed.",
   "Live-verify on 22.0.400 through the bridge: run tests/assay_h22_cache_insert.py, insert on a real SOP chain, confirm ONE Ctrl+Z reverses node + wires, and confirm no file hit disk.",
   "Decide the flag: merge dark (SYNAPSE_CACHE_ADVISOR_ENABLED unset) or flip it on - that is a separate call from the merge.",
   "Human merges. No push/tag from this triage."
  ],
  "refuter_reason": "I attacked five surfaces and the verdict survived all five, including the one claim it openly marked unverified.\n\n(1) EVIDENCE IS REAL. master:python/synapse/server/handlers_cache.py line 11 literally reads \"creates a node, writes a file, or mutates the scene -- there is no ``synapse_insert_cache``\" (verbatim, `git show master:...| sed -n '1,18p'`). `git cherry master feat/cache-insert` returns exactly one line: `+ 206947a673ef30f3633879910917817eeeffa762`. No fabricated citation.\n\n(2) THE VERDICT'S OWN ADMITTED GAP CLOSES IN ITS FAVOUR. It wrote \"shared/bridge.py:767 per the comment - I did not open bridge.py to confirm that line still says REVIEW\". I opened it: master's `Operation.gate_level` still reads `base = OPERATION_GATES.get(self.operation_type, GateLevel.REVIEW)` with the R4 disk-write elevation intact. The deliberate OPERATION_GATES omission still resolves to REVIEW on master. Claim holds.\n\n(3) CONFLICT SET IS EXACT. `git merge-tree --write-tree master feat/cache-insert` emits exactly two CONFLICT lines: CLAUDE.md and python/synapse/server/handlers_cache.py. The \"2 conflicts, 8 files auto-merge\" claim is verified, not estimated.\n\n(4) AXIS 4 FAILED \u2014 I could not find an interface that moved under it. The two files the verdict flagged as the likeliest silent break both held. handlers.py: master still registers at line 785 as `reg.register(\"assess_cache\", self._handle_assess_cache)` \u2014 byte-identical call form to the branch's line 785/791 pair, so the 263 lines of drift did not touch the registration API. _tool_registry.py: master's assess_cache entry (:1051) and the branch's insert_cache entry (:1017) are the same 8-field tuple shape (name, wire-op, payload_fn, description, schema, readOnlyHint, destructiveHint, idempotentHint) \u2014 the 125 lines of drift did not change the tuple contract. The near-duplicate-branch problem that owns the render pair does not exist here: this branch shares no files with any neighbour except the CLAUDE.md counter line.\n\n(5) STEP 3 IS ALREADY DISCHARGED \u2014 and this is the one place I can improve the verdict rather than break it. The v5.70.0 rename (b475ee22) touched only python/synapse/panel/activity.py (+76) and tests/test_activity_labels.py (+156). That test scrapes column 1 of _tool_registry.py and at :48 asserts one-directionally (`dead = k for k in _TOOLS if k not in live`) \u2014 a hand-map key that no longer exists fails; a registry tool with no map entry does not. At :92 it then sweeps `for name in _live_tool_names()` through `tool_label()` and checks sentence case on words[1:]. \"synapse_insert_cache\" auto-humanizes to \"Insert cache\", which passes. So the rebase does NOT land a red label test and no hand-list entry is required.\n\nNEIGHBOUR CHECK: no contradiction. fix/demo-repairs-20260909 (also REBASE_THEN_MERGE) touches the same CLAUDE.md \"128 tools\" counter but no other file in common \u2014 that is a merge-order dependency, not two branches claiming the same work."
 },
 {
  "branch": "rope/gate-a",
  "team": "cold-cache-rope",
  "tier": "COLD",
  "verdict": "SPLIT_SALVAGE",
  "original": "SPLIT_SALVAGE",
  "survived": true,
  "confidence": 0.9,
  "artist_value": "indirect",
  "risk": "low",
  "one_line": "The engineering commit already landed on master and the harness half is already there too - what remains is documentation that every file conflicts on and that is now factually false, but it names a real still-open gap: BLOCKS shipped and the CHANGELOG never mentioned it.",
  "action_steps": [
   "Do NOT merge this branch. Every one of its four files conflicts and its headline claim is now false.",
   "Salvage 1 (the real gap): master's CHANGELOG has zero mention of BLOCKS or apply_fixture while the code ships. Use `git show rope/gate-a:CHANGELOG.md | sed -n '1,30p'` as SOURCE MATERIAL for a fresh entry - keep the four-silent-failures paragraph and the F-1..F-5 invariant list, re-verify every number, delete \"not merged\" and the stale suite counts, and write it against the real shipped state.",
   "Salvage 2 (optional, artist-facing): rewrite the BLOCKS section for today's 148-line README. Do not port the hunks - the README it targeted no longer exists. Drop the \"*Not on master yet*\" line.",
   "Salvage 3 (tiny, harness doctrine): the 2-line delta in m5b.md - wrong base means HALT, not self-repair. m5b's own leg is finished, so carry the doctrine into harness/prompts/base.md if it is worth keeping, and otherwise let it go.",
   "Drop the mermaid theme hunks entirely - master's README has no mermaid diagrams left.",
   "After salvaging: git branch -D rope/gate-a && git worktree prune (human runs it, and only after the salvage text is captured somewhere durable)."
  ],
  "refuter_reason": "Attacked on all five axes; nothing broke. Every citation verified live and none was fabricated: git cherry returns exactly `- e69703a8 / + de254d09 / + 3806af20`; master carries all six python/synapse/blocks/*.py plus cognitive/tools/apply_fixture.py, so the branch's own text \"*Branch blocks/m5-reconciler (87927076), not merged*\" (rope/gate-a:CHANGELOG.md:7) and the README hunk \"*On blocks/m5-reconciler. Not on master yet.*\" are both false today; master:harness/legs.json:497-504 is exactly the M5b leg block cited, line numbers correct; master:harness/prompts/m5b.md blob 31098651 vs branch cfdeb570 diffs by precisely 1 file / 2 insertions / 2 deletions, the halt-vs-rebase wording at :12; master:README.md is 148 lines and a case-insensitive grep for mermaid|fixture|BLOCKS|Deterministic returns zero, so the +63 lines target context that no longer exists; a CASE-SENSITIVE grep for BLOCKS|apply_fixture on master:CHANGELOG.md returns zero, confirming the still-open gap the salvage names (a case-insensitive grep hits only generic English \"solver blocks\"/\"thinking blocks\" \u2014 not the subsystem, so the gap claim holds); git merge-tree --write-tree master rope/gate-a yields exactly four conflicts on the four touched files (CHANGELOG.md, README.md, harness/legs.json content, harness/prompts/m5b.md add/add); 794 behind / 3 ahead off merge-base be71cce6 dated 2026-08-05. Axis 2: no neighbour contradiction \u2014 fix/demo-repairs-20260909 also touches README.md but its conflict is the \"128 tools\" count line, a different region, and rope/gate-a is not proposed for merge either way. Axis 4 is the verdict's own finding, not a counter. Axis 5: rope/gate-a is NOT checked out in any of the 50 live worktrees (only rope/beacon is, at C:/Users/User/rope-beacon-wt), so `git branch -D rope/gate-a` will succeed rather than fail with \"used by worktree\", and `git worktree prune` is a harmless no-op; PS7 supports `&&` and the `git show | sed -n` steps run under the Bash tool. The risk framing is also correct and load-bearing: origin is public, and merging verbatim would publish \"not merged\" plus a stale \"5,745 passing, 9 failing\" against today's 8869/412/0 floor. Verdict survives intact; confidence raised from 0.85 to 0.90 because every spot-check landed. NOT CHECKED: whether refs blocks/m5-reconciler or blocks/m5b-rulings still exist, whether an M5b.json receipt exists, and the substance of the +63 README lines beyond the grep anchors."
 }
]