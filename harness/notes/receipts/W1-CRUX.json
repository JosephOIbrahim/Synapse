{
  "leg": "W1-CRUX",
  "name": "wave-1 crucible",
  "schema": "receipt/v1",
  "band": "TRUST",
  "status": "blocked",
  "readonly": true,
  "status_reason": "Whole-wave adversarial review of W1-HSTRIP / W1-MTFIX / W1-KPRE against each leg's own crucible_criteria and acceptance, verified at HEAD 816cd36c (the shipping state; wave1/crux is an ancestor of master@v5.79.1). The wave is NOT uniformly green: W1-HSTRIP carries a live BLOCK (CRUX-W1-B1) because its central guarantee - surface silent memory-backend degradation LOUDLY, no click - fails at HEAD. A later panel-redesign commit (5438ddc6, BC-2) deleted the always-visible rail strip the leg delivered; a moneta->jsonl fallback now surfaces only as a disabled grey QAction with the 384/256 reason in a tooltip inside an overflow 'Health' submenu. The block is a downstream regression, NOT a defect in the leg's delivery (HSTRIP delivered its guarantee correctly at a5603b60) and NOT a receipt fabrication - and remedy is a human scope ruling, not a rebuild. W1-MTFIX and W1-KPRE are green_with_findings with all gui_required/probe predicates honestly UNKNOWN and no faked green; two acceptance verdicts must be recorded softer than the leg receipts self-report (MTFIX P4 is an orphaned/unwired gate, KPRE #2's ephemeral probe artifacts). A green whole-wave receipt would inherit a stale green, so status=blocked with CRUX-W1-B1 in for_ruling. Builds nothing, fixes nothing - findings only.",

  "review_head": "816cd36c (branch wave1/crux; git merge-base --is-ancestor 816cd36c master = true; master@82166574 == annotated tag v5.79.1, git rev-list v5.79.1..master = 0)",
  "leg_commits": {
    "W1-HSTRIP": "a5603b60 (parent ecdf4296) - health_strip landing; receipt commit_at_run 'ecdf4296' is the pre-strip base (health_strip.py ABSENT there)",
    "W1-MTFIX": "8c1003aa (parent ecdf4296) - result-path off-main-safe + leg tests; doctor.py/mcp server.py reverted byte-clean, absent from the diff",
    "W1-KPRE": "0124f5c4 (parent ecdf4296) - render presets; parm surface byte-unchanged since (git log 0124f5c4..HEAD -- render_presets.py empty)"
  },

  "method": {
    "crux_own_reruns_stock_py314": {
      "tests/panel/test_health_strip.py": "15 passed, 2 skipped, 0 failed (2 skips = Qt-widget pins, no PySide on 3.14)",
      "tests/test_karma_progressive_presets.py": "25 passed, 0 failed",
      "tests/test_w1_mtfix.py": "6 passed, 2 skipped, 0 failed (2 skips = live-GUI-gated)"
    },
    "crux_live_reprobe_22_0_400": "read-only node-type introspection on the connected bridge (synapse_ping pong; hou.applicationVersionString()=='22.0.400'), no node creation, no scene mutation. All 11 KPRE preset-override parm names present on karmarendersettings/karma; all 3 doc-phantoms (reflectlimit/refractlimit/denoisemode) absent.",
    "adversarial_pass": "7-agent read-only crucible workflow (3 review -> 3 refute -> 1 completeness critic), all agentType=crucible (Read/Grep/Glob/Bash only, structurally fenced from Write/Edit). 794070 subagent tokens, 132 tool calls, 0 errors. Independent 3-refuter + critic pass overturned exactly ONE verdict (MTFIX P4) and confirmed the HSTRIP BLOCK.",
    "line_anchor_note": "Every receipt anchor cited by the three legs is precise at its LEG commit but stale at HEAD (files grew via later commits). Every underlying claim re-verifies at the new HEAD line; anchors below are the HEAD lines unless suffixed @<commit>."
  },

  "legs": {
    "W1-HSTRIP": {
      "overall": "blocked",
      "crucible_criteria": [
        {"criterion": "strip states are FACT-sourced (a producer path per cell); a green cell without a producer is a BLOCK", "verdict": "UPHELD", "anchor": "health_strip.py build_cells producers -> store.py:1051 backend_fallback + store.py:1994 _global_synapse peek + render_session.py:95 active_session; silent-green traps deliberately UNWIRED: synapse_health 'healthy':True @ handlers.py:931, render_farm_status {'running':False} @ handlers_render.py:1615; pin test_no_cell_is_green_without_a_producing_fact", "note": "confirmed by CRUX grep + reviewer + refuter"},
        {"criterion": "degraded is loud: amber/red + reason, no click required to notice", "verdict": "VIOLATED", "anchor": "synapse_panel.py:2843-2857 (overflow 'Health' submenu: health.addAction(label+value) + fact.setEnabled(False) + fact.setToolTip(cell.reason)) and :3948-3959 (_update_health_strip caches _health_facts only) vs a5603b60 persistent rail strip; demotion commit 5438ddc6 (BC-2); removal CI-pinned by test_bc_wave.py:224 (assert not hasattr(p,_health_strip))", "note": "VIOLATED AT HEAD. Delivered correctly at the leg commit; regressed by BC-2. See CRUX-W1-B1. Confirmed independently by CRUX (grep: zero build_health_strip_widget/HealthStrip( calls in synapse_panel.py) + reviewer + refuter."},
        {"criterion": "no main-thread I/O added by the strip itself (must not become the next 648ms hold)", "verdict": "UPHELD", "anchor": "health_strip.py gather_snapshot O(1) process-global reads; B6 (ae34ed96) added store.health() which is lock-light by construction (records=len(self._memories) read WITHOUT the read lock, store.py:601-604); NO synapse_doctor/get_health round-trip; menu builds this O(1) gather only at menu-open", "note": "receipt wording 'never calls get_health' is literally stale (B6 added store.health()), but substance holds - no 648ms path"},
        {"criterion": "H4 receipt precedes this leg; build on H4 landed token state, no re-litigating", "verdict": "UPHELD", "anchor": "git merge-base --is-ancestor 92a48eb HEAD = ancestor; health_strip.py:61 imports designsystem.tokens, declares zero colour; only '#' is the &#9679; status-dot HTML entity, not a hex colour"}
      ],
      "acceptance": [
        {"predicate": "strip renders connection/memory/project/job cells from live health facts", "evidence": "test", "verdict": "pass", "anchor": "test_health_strip.py test_build_cells_returns_four_keyed_cells + test_cells_reflect_supplied_live_facts; CRUX rerun 15p/2s/0f; shipping menu uses the same build_cells producer"},
        {"predicate": "backend fallback (moneta->jsonl) renders AMBER/RED with the reason; the 384/256 case must display, not hide", "evidence": "test", "verdict": "fail", "leg_self_verdict": "pass", "anchor": "data core PASS (cell_memory RED, color==tokens.ERROR, reason carries 384/256) BUT cell_html/HealthStrip is DEAD CODE at HEAD; shipping surface hides the reason in a tooltip on a colourless disabled action (synapse_panel.py:2852-2855). Green-in-test, broken-on-surface. Would be pass at a5603b60.", "note": "FAIL AT HEAD - the pinning test exercises the un-mounted path"},
        {"predicate": "unmeasured/unreachable health facts render UNKNOWN, never green and never 0", "evidence": "test", "verdict": "pass", "anchor": "UNMEASURED sentinel -> UNKNOWN/SLATE; B6 (ae34ed96) STRENGTHENED this (live backend without a write_health reading renders UNKNOWN not OK, closing the 2026-09-15 green-while-degraded incident); non-vacuous"},
        {"predicate": "strip visible in a live GUI session without stealing focus or stalling paint", "evidence": "gui_probe", "gui_required": true, "verdict": "UNKNOWN", "anchor": "receipt records UNKNOWN correctly (constitution: unobtainable -> UNKNOWN); a live interactive Houdini GUI cannot be launched headless. Two caveats: the offscreen 'measurable half' builds HealthStrip which is no longer mounted at HEAD, and the 'strip visible' premise itself no longer holds (menu-gated readout, not a persistent strip)."}
      ]
    },

    "W1-MTFIX": {
      "overall": "green_with_findings",
      "crucible_criteria": [
        {"criterion": "off-thread work never touches hou.* off the main thread; marshal results back", "verdict": "UPHELD", "anchor": "leg built doctor-off-main, found F1 (store._resolve_project_path calls hou.hipFile.path()/hou.text.expandString() unmarshalled under HOU_AVAILABLE @ store.py:975,984 @8c1003aa), and REVERTED rather than trade a stall for an uncatchable off-main crash; revert byte-clean (git show --stat 8c1003aa = 4 files, doctor.py + mcp/server.py ABSENT); result_telemetry.py pulls no hou at import (lazy)"},
        {"criterion": "before/after numbers carry probe receipts from the SAME session type; a headless 0.0 claimed as improvement is the documented bug class", "verdict": "UPHELD", "anchor": "no before/after improvement claimed anywhere; P2/P3 recorded UNKNOWN; live baseline stamped MAIN-TREE before-baseline, never an after-fix number; gui_metric_verdict returns UNKNOWN off-GUI (incl a genuine 0.0), non-vacuous negative control at test_w1_mtfix.py"},
        {"criterion": "the working indicator is driven by real state, not a timer", "verdict": "UPHELD", "anchor": "no working indicator shipped (spawned S3, blocked on synapse_panel.py held by HSTRIP); the two changed files (result_telemetry.py sink, chat_display.py set_max_result_blocks) contain no timer"},
        {"criterion": "if FRZ attributes the ~6s freeze Qt-side, that stays OUT of scope here - spawn only", "verdict": "UPHELD", "anchor": "agent_health.py gc-walk candidate spawned as S4 (class=probe), no Qt-side freeze fix in the 4-file diff"}
      ],
      "acceptance": [
        {"predicate": "FRZ receipt consumed first; attribution is the map, do not re-derive", "evidence": "receipt", "verdict": "pass", "anchor": "FRZ.json not_observed[] (freeze never reproduced, rung-5 numbers UNMEASURED) faithfully carried into W1-MTFIX.json acceptance-1 + F2"},
        {"predicate": "GUI: main_thread_hold_slowest_ms{synapse_doctor} < 100ms after fix", "evidence": "gui_probe", "gui_required": true, "verdict": "UNKNOWN", "anchor": "fix reverted (F1), no after-fix to measure; requires a human to deploy branch + reload bridge; recorded UNKNOWN, never green"},
        {"predicate": "GUI: panel_result append/finalize under slow threshold on the 648/780 volume", "evidence": "gui_probe", "gui_required": true, "verdict": "UNKNOWN", "anchor": "off-main formatting fix spawned S2 (blocked on synapse_panel.py); only shipped lever set_max_result_blocks is opt-in/default-unlimited; recorded UNKNOWN, never a headless 0.0"},
        {"predicate": "headless run reports these three as UNKNOWN (headless timing is not evidence)", "evidence": "check", "verdict": "fail_or_unknown", "leg_self_verdict": "pass", "anchor": "ORPHANED GATE: gui_metric_verdict/result_evidence_verdict/gui_timing_is_evidence have ZERO production consumers (only result_telemetry.py + tests). The real reporting surfaces export RAW zeros: metrics.py:272-282 ('Exporting the zero is the point'), handlers.py:1687, telemetry_dump.py:150,166. Receipt claim 'the main_thread_hold reader uses the same gui_metric_verdict primitive' (W1-MTFIX.json:47) is FALSE - no such reader. A strict check-class reading is FAIL (a headless run demonstrably exports the raw zero); at minimum UNKNOWN. MUST NOT be recorded pass.", "note": "refuter overturned pass->UNKNOWN; critic concurs. BOUNDED WEAKNESS - the leg ships no false improvement (P2/P3 honestly UNKNOWN)."},
        {"predicate": "suite ratchet floor holds; no behavioural regression in panel result content", "evidence": "test", "verdict": "PLAUSIBLE", "leg_self_verdict": "pass", "anchor": "no-regression half supported (additive-only chat_display set_max_result_blocks, default unlimited, not in __init__); BUT the floor-holds half is receipt-attested only and self-stamped 'PENDING_FINAL_SUITE' (W1-MTFIX.json:53); CRUX scoped out of the full suite -> under 'no anchor no verdict' the floor half is UNKNOWN, not a clean pass"}
      ]
    },

    "W1-KPRE": {
      "overall": "green_with_findings",
      "crucible_criteria": [
        {"criterion": "presets are additive - no existing render path changes behaviour unless a preset is invoked", "verdict": "UPHELD", "anchor": "handlers_render.py: default test/preview/production ladder is unchanged context; preset chain gated on use_presets; preset_overrides merged only when the key is truthy; extra return keys (preset_chain/xpu_flush_delay_note) only under use_presets; _tool_registry.py keeps required:[]; git log 0124f5c4..HEAD -- render_presets.py empty (parm surface byte-unchanged)"},
        {"criterion": "camera stays a USD prim path on usdrender; a preset must not smuggle a node path in", "verdict": "UPHELD", "anchor": "render_presets.py preset_parm_overrides emits no camera/override_camera key and no /-prefixed value; test_no_camera_key_smuggled + test_no_override_value_looks_like_a_node_path; cheatsheet documents camera as artist-owned USD prim path"},
        {"criterion": "no parm name ships un-probed; a name asserted from docs alone is a BLOCK", "verdict": "UPHELD", "anchor": "render_presets.py:66-78 PARM_NAMES ships reflectionlimit/refractionlimit/denoiser; the addendum prose uses the phantoms reflectlimit/refractlimit/denoisemode - shipped names DIVERGE from the docs (affirmative not-copied); phantom strings appear ONLY in correction comments (:33-35,71-72); presets apply concrete names via _handle_render_settings direct node.parm().set(), bypassing the phantom _KARMA_PARM_MAP. CRUX LIVE RE-PROBE (22.0.400): all 11 preset-override names present, 3 phantoms absent - closes the probe-provenance question the subagents left UNKNOWN (they lacked bridge access)."}
      ],
      "acceptance": [
        {"predicate": "presets exist and set pathtracedsamples/resolution/denoise/engine per the addendum table", "evidence": "test", "verdict": "pass", "anchor": "render_presets.py:133-172 PROGRESSIVE_PRESETS (layout 320x240/4/off/xpu, lighting 960x540/24/oidn/xpu, quality 1080p/64/oidn/xpu, final 1080p/256/off/cpu); TestPresetTable 8/8; CRUX rerun 25 passed"},
        {"predicate": "parm NAMES probe-verified against the live 22.0.400 build", "evidence": "probe", "verdict": "pass", "leg_self_verdict": "pass", "subagent_verdict": "UNKNOWN (probe artifacts absent)", "anchor": "CRUX LIVE RE-PROBE on the connected 22.0.400 bridge (read-only nodeType.parmTemplateGroup introspection, no node create): pathtracedsamples/engine/denoiser/diffuselimit/reflectionlimit/refractionlimit/volumelimit/ssslimit/xformsamples/geosamples/soho_foreground ALL PRESENT; reflectlimit/refractlimit/denoisemode ALL ABSENT. SCOPE: the 5 resolution component names (resolutionx/y, res_user1/2, override_res) are tuple-component names not in the preset-override dict (applied by the existing _handle_render path) and were not separately re-probed. The receipt's cited scratchpad/probe1-3.json are session-ephemeral and absent (git log --all clean) - see finding KPRE.F1.", "note": "CRUX supersedes the subagents' UNKNOWN with live evidence they lacked; durable in-repo evidence is test_phantom_names_never_appear_anywhere"},
        {"predicate": "quality and final presets set background mode; layout and lighting stay foreground", "evidence": "test", "verdict": "pass", "anchor": "render_presets.py background flags layout/lighting False, quality/final True; soho_foreground = 0 if background else 1; TestBackgroundPolicy 3/3"},
        {"predicate": "XPU flush-delay note travels with the preset surface", "evidence": "check", "verdict": "pass", "anchor": "XPU_FLUSH_DELAY_NOTE defined; surfaced in handler return key + _tool_registry description + docs/karma_render_cheatsheet.md; TestXpuFlushNote asserts '10-15'/'flush'/'not a hang'"}
      ]
    }
  },

  "acceptance": [
    {"predicate": "every wave leg reviewed against its own crucible_criteria; verdict per criterion", "verdict": "pass", "evidence": "receipt - legs.W1-HSTRIP/W1-MTFIX/W1-KPRE above carry a verdict + anchor for all 4+4+3=11 crucible_criteria; independently re-verified by CRUX and by a 3-review/3-refute/1-critic adversarial pass (0->1 verdicts overturned: MTFIX P4)"},
    {"predicate": "every BLOCK posted to the bus AND recorded in the receipt for_ruling[]", "verdict": "pass", "evidence": "receipt - CRUX-W1-B1 posted to wave1 bus (type=block) and recorded in for_ruling[] below"},
    {"predicate": "gui_required acceptance not yet measured is recorded UNKNOWN in the verdict, never assumed", "verdict": "pass", "evidence": "check - HSTRIP pred4, MTFIX P2, MTFIX P3 are all gui_required and all recorded UNKNOWN (never green, never a headless 0.0). CRUX did NOT attempt a live GUI measurement (unobtainable headless; constructing panel state is a known session-destroying hazard)."},
    {"predicate": "builds nothing, fixes nothing - findings only", "verdict": "pass", "evidence": "check - CRUX authored no code fix and wrote exactly one file, this receipt (harness/notes/receipts/W1-CRUX.json). The 7 subagents are agentType=crucible (no Write/Edit tools). Live probes were read-only introspection. A fix authored here would itself be a BLOCK per crucible_criteria."}
  ],

  "findings": [
    {"id": "CRUX-W1-B1", "severity": "BLOCK", "leg": "W1-HSTRIP", "claim": "The W1-HSTRIP central guarantee - surface silent memory-backend degradation LOUDLY with no click (P0.3, docs S2.1/S4.1) - does NOT hold at HEAD. Later panel-redesign commit 5438ddc6 (BC-2) deleted the always-visible rail strip the leg mounted at a5603b60; a moneta->jsonl fallback (the 384/256 embedding-dim mismatch case) now surfaces ONLY as a disabled grey QAction with the one-line reason in a tooltip, inside an overflow 'Health' submenu. Noticing it requires open-overflow + open-Health + hover; no amber/red verdict colour survives a disabled action. This is the exact silent degradation the leg existed to kill. It is a downstream regression, not a receipt lie (the leg delivered correctly at a5603b60) - and the leg's own receipt is now a false green at HEAD (status green_with_findings, pred2 pass, resume_token asserts the strip is mounted).", "anchor": "python/synapse/panel/synapse_panel.py:2843-2857 & :3948-3959; demotion commit 5438ddc6; CI-pin tests/panel/test_bc_wave.py:224", "truth": "VERIFIED-STATIC (CRUX grep + reviewer + refuter + critic, all CONFIRMED)"},
    {"id": "CRUX-W1-F2", "severity": "major", "leg": "W1-MTFIX", "claim": "Orphaned UNKNOWN-gate. gui_metric_verdict/result_evidence_verdict/gui_timing_is_evidence have zero production consumers; the real telemetry surfaces (handlers.py:1687, telemetry_dump.py:150,166, metrics.py:280-282) export raw zeros by explicit design (metrics.py:272-273 'Exporting the zero is the point'). So a headless run does NOT report UNKNOWN - the check-class predicate P4 is unwired. The receipt's producer claim (W1-MTFIX.json:47) is false. Bounded: the leg ships no false improvement (P2/P3 honestly UNKNOWN).", "anchor": "python/synapse/panel/result_telemetry.py:286-343; python/synapse/server/metrics.py:272-282; python/synapse/server/handlers.py:1687; python/synapse/server/telemetry_dump.py:150,166", "truth": "VERIFIED-STATIC (refuter overturn, critic concurs)"},
    {"id": "CRUX-W1-F3", "severity": "info", "leg": "W1-MTFIX", "claim": "MOVING-PICTURE (strengthening): F1's named crash path is REMEDIATED at HEAD. store._resolve_project_path now routes its hou reads through _read_on_main (store.py:1493-1495), and scene_memory.py:146 wraps the embedder token. This validates F1 as a real defect and partly satisfies the prerequisite of spawn S1 (marshal store._resolve_project_path). CRUX confirmed independently.", "anchor": "python/synapse/memory/store.py:1493-1495 vs store.py:975,984 @8c1003aa; scene_memory.py:146", "truth": "VERIFIED-STATIC"},
    {"id": "CRUX-W1-F4", "severity": "minor", "leg": "W1-MTFIX", "claim": "Receipt R1's assertion that the source doc docs/SYNAPSE_latency_and_karma_rendersettings_2026.md 'does not exist and never did' is a factual overclaim. The doc was committed on sibling 00c5afa2 (2026-08-12 20:48, ~14h BEFORE the leg commit 8c1003aa) and is present at HEAD carrying the exact magnitudes (doc lines 24/30-31/37-38 = 306ms/648ms{synapse_doctor}/648.6ms/780.2ms). It was merely absent from the leg's own tree at 8c1003aa (00c5afa2 is a sibling, not an ancestor). Bounded: R1 still correctly escalates the authority/gating question and F2's operative conclusion (magnitudes UNMEASURED per FRZ; refuse before/after deltas) holds. The receipt corrections[] block fixes a different false claim (CRUX-B2) and leaves this one uncorrected.", "anchor": "W1-MTFIX.json:110 vs git 00c5afa2:docs/SYNAPSE_latency_and_karma_rendersettings_2026.md (present) and HEAD doc:24,30-31,37-38", "truth": "VERIFIED (CRUX topology + refuter)"},
    {"id": "CRUX-W1-F5", "severity": "debt", "leg": "W1-MTFIX", "claim": "gui_timing_is_evidence() calls hou.isUIAvailable() lazily (preserving zero-hou-at-import) but WITHOUT marshalling. No off-main caller is wired by this leg, so no shipped violation - but it is the same hou-off-main hazard class F1 reverted over. If a future metrics endpoint invokes the verdict helpers off a worker thread, it would hit F1's failure mode.", "anchor": "python/synapse/panel/result_telemetry.py:299-306", "truth": "VERIFIED-STATIC (latent)"},
    {"id": "KPRE.F1", "severity": "minor", "leg": "W1-KPRE", "claim": "Probe-class acceptance evidence is session-ephemeral. The receipt cites scratchpad/probe1-3.json for the live 22.0.400 name verification; those artifacts are absent from the worktree AND from git log --all, so a later crucible cannot re-verify from disk. CRUX closed this for THIS review by a fresh live re-probe (see acceptance KPRE #2), but the leg should point its probe-class evidence at a durable committed artifact (committed probe log / receipt-embedded read-back).", "anchor": "harness/notes/receipts/W1-KPRE.json:23; ls scratchpad/probe*.json -> absent", "truth": "VERIFIED"},
    {"id": "KPRE.F2", "severity": "minor", "leg": "W1-KPRE", "claim": "Latent guard hole (no phantom shipped; document-and-keep). test_overrides_only_use_probe_verified_names is near-vacuous: PROBE_VERIFIED_PARM_NAMES is self-derived from PARM_NAMES.values()+RESOLUTION_PARMS, so a NEW un-probed name added to PARM_NAMES auto-verifies and slips the suite. Only the 3 hard-coded phantoms (test:124-130) are durably blocked. Moving-picture clean (git log 0124f5c4..HEAD -- render_presets.py empty), so the hole is latent, not exploited.", "anchor": "python/synapse/core/render_presets.py:94-99; tests/test_karma_progressive_presets.py:124-130,132-136", "truth": "VERIFIED-STATIC (latent)"},
    {"id": "CRUX-W1-F8", "severity": "info", "leg": "wave1", "claim": "Systemic: all three leg receipts self-report verdicts softer than HEAD-reality (HSTRIP status green + pred2 pass; MTFIX P4 pass; KPRE #2 pass) - but NONE originated a NEW block the crucible glossed. Every softening was independently caught (HSTRIP demotion, MTFIX orphaned gate, KPRE ephemeral probe) and re-confirmed against real file:line. No gui_required predicate was faked green; no hou.* off-main path ships; no un-probed parm proven phantom. The crucible's work is sound; the whole-wave status must be pinned to HEAD, not inherited from the leg self-verdicts.", "anchor": "W1-HSTRIP.json:6,43; W1-MTFIX.json:45,47; W1-KPRE.json:20-22", "truth": "VERIFIED"}
  ],

  "for_ruling": [
    {"id": "CRUX-W1-B1", "question": "W1-HSTRIP's shipped 'degraded is loud, no click' guarantee (P0.3) is regressed at HEAD by commit 5438ddc6 (BC-2), which deleted the persistent rail strip; the moneta->jsonl fallback now surfaces only as a disabled grey menu action + tooltip. A naive re-mount is blocked by test_bc_wave.py:224 (which pins the strip's removal). How should this be closed?", "options": ["Add loudness to the surviving overflow surface: render the Health cells with the verdict colour (amber/red, not disabled-grey) and the reason inline (via cell_html), so a fallback is visible without a hover - keeps BC-2's fold-into-overflow decision", "Restore a minimal always-visible degraded indicator (a single amber/red pip in the rail that only appears when a cell is not-OK), reconciling with the 380px docking constraint that motivated BC-2, and update test_bc_wave.py", "Formally re-scope the leg's 'degraded is loud, no click' criterion under BC-2's decision - accept menu-gated degradation as sufficient and mark P0.3 as consciously de-scoped"], "recommendation": "Option 1 (menu-loudness) - smallest change that restores the guarantee within the shipped overflow model without fighting the test that pins BC-2. This is a decision + a targeted subtree loop, NOT a rebuild. The crucible authored no fix (that would itself be a BLOCK).", "unblocks": "the P0.3 silent-degradation guarantee on the shipping panel"},
    {"id": "CRUX-W1-R2", "question": "MTFIX P4 (headless reports these three as UNKNOWN) is recorded pass in the leg receipt but the gate is unwired (zero production consumers; the real telemetry surfaces export raw zeros). Wire the gate into the reporting surfaces (route main_thread_hold/append/finalize through gui_metric_verdict), or drop the gate and record P4 as not-met?", "recommendation": "Wire it: the check-class predicate is only satisfied if a headless synapse_metrics/telemetry_dump run actually returns UNKNOWN for those three; today it returns raw zeros (metrics.py:272 'Exporting the zero is the point'). Until wired, P4 is not pass.", "unblocks": "MTFIX P4 acceptance"},
    {"id": "CRUX-W1-R3", "question": "KPRE probe-class evidence lives only in ephemeral scratchpad artifacts (now gone), and the anti-phantom membership test is self-derived (a new un-probed name auto-verifies). Require probe-class receipts to cite a durable committed artifact, and harden the test to pin the concrete expected name set rather than a self-derived one?", "recommendation": "Yes to both (small). CRUX re-verified the current names live on 22.0.400, so nothing phantom ships today; these harden future changes.", "unblocks": "durable KPRE probe evidence + future-proofing"}
  ],

  "spawn": [
    {"id": "W1-HSTRIP-LOUD-RESTORE", "name": "restore loud memory-degradation surfacing on the shipped panel", "band": "BUILD", "class": "feature", "source": {"doc": "harness/notes/receipts/W1-CRUX.json", "anchor": "CRUX-W1-B1"}, "note": "Held for Joe (class 'feature' is outside W1-CRUX spawn_classes=[]). Per CRUX-W1-B1 option 1: render the overflow Health cells with verdict colour + inline reason (cell_html) instead of disabled-grey + tooltip, OR a minimal always-visible amber/red pip. Reconcile with test_bc_wave.py:224.", "touches": ["python/synapse/panel/synapse_panel.py", "python/synapse/panel/health_strip.py", "tests/panel/"]},
    {"id": "W1-MTFIX-GATE-WIRE", "name": "wire the headless-evidence gate into the metrics/telemetry surfaces (or drop it)", "band": "BUILD", "class": "mtfix-followup", "source": {"doc": "harness/notes/receipts/W1-CRUX.json", "anchor": "CRUX-W1-F2 / CRUX-W1-R2"}, "note": "Held for Joe. Route the three gui_required numbers through gui_metric_verdict at the reporting surfaces (handlers.py:1687, telemetry_dump.py, metrics.py) so a headless run reports UNKNOWN, honouring P4 - or delete the orphaned primitive and record P4 as not-met.", "touches": ["python/synapse/server/metrics.py", "python/synapse/server/handlers.py", "python/synapse/server/telemetry_dump.py", "tests/"]},
    {"id": "W1-KPRE-DURABLE-PROBE", "name": "durable probe artifact + concrete-set anti-phantom test for render presets", "band": "BUILD", "class": "probe", "source": {"doc": "harness/notes/receipts/W1-CRUX.json", "anchor": "KPRE.F1 / KPRE.F2 / CRUX-W1-R3"}, "note": "Held for Joe. Commit a durable probe read-back (not scratchpad) and pin the test to the concrete expected parm-name set (not the self-derived PROBE_VERIFIED_PARM_NAMES) so a new un-probed name fails the suite. class=probe is inside W1-KPRE's spawn_classes but outside W1-CRUX's (=[]), so it lands held.", "touches": ["python/synapse/core/render_presets.py", "tests/"]}
  ],

  "prior_crux": {
    "note": "A prior W1-CRUX run (bus 2026-08-13T10:16) posted CRUX-B1 (HSTRIP+MTFIX deliverables uncommitted; HEAD==BASE ecdf4296) and CRUX-B2 (MTFIX resume_token falsely asserted committed). BOTH RESOLVED: HSTRIP a5603b60 + MTFIX 8c1003aa are now committed and merged to master@v5.79.1; the MTFIX receipt carries a corrections[] block acknowledging CRUX-B2. The prior run verdict was green_with_findings / 0 overturned - correct FOR ITS TIME, but the picture moved: commit 5438ddc6 (BC-2, later) demoted the HSTRIP strip, so this re-review at HEAD finds a live BLOCK the earlier run could not have seen."
  },

  "resume_token": "W1-CRUX complete, status=blocked. Whole wave reviewed at HEAD 816cd36c (== ancestor of master@v5.79.1). HSTRIP=blocked (CRUX-W1-B1: BC-2/5438ddc6 demoted the loud rail strip to a disabled-grey menu action + tooltip; P0.3 silent-degradation gap reopened; remedy = scope ruling, re-mount is CI-pinned against by test_bc_wave.py:224). MTFIX=green_with_findings (P4 orphaned gate -> not pass; P5 PENDING_FINAL_SUITE -> plausible; F1 real + remediated at HEAD; R1 'never did' overclaim). KPRE=green_with_findings (P2 re-verified LIVE by CRUX on 22.0.400 - all 11 preset names present, 3 phantoms absent, superseding the subagents' artifact-absent UNKNOWN; ephemeral-probe + near-vacuous-test debt). No fix authored, no push, no release. 3 gui_required predicates UNKNOWN. Adversarial 3-refute + critic pass overturned 1 verdict (MTFIX P4). Merge/remedy is Joe's word."
}
