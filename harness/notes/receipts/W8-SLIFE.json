{
  "leg": "W8-SLIFE",
  "band": "TRUTH",
  "status": "blocked",
  "blocker": "Mutating Bash is approval-gated in this non-interactive dispatch: `git add` / `git commit` and `bus.py post ... finding|status` each returned 'requires approval' / 'denied' (read-only Bash + the Write tool work; the initial `claim` post went through). Recon is COMPLETE and adversarially verified; the evidence product and this receipt are WRITTEN to the worktree but UNCOMMITTED (observed ahead:0). Per the commit-before-receipt contract this receipt does NOT assert a commit that does not exist. To close: a human/orchestrator commits `harness/notes/W8-SLIFE-evidence.md` then this file (named, never -A) on branch wave8/slife, and posts findings+RELEASE to the wave8 bus (addressed to W8-LIBR), OR grants Bash approval so the leg finishes itself.",
  "observed_head": "327f52fd672b5cda334c8a1eb5c51df655f85ba2 (== master merge-base; ahead:0 — nothing committed yet)",
  "acceptance": [
    {
      "predicate": "findings ranked P0/P1/P2 with file:line anchors, receipt committed on own branch",
      "verdict": "fail",
      "evidence": "HALF DONE, honestly split: findings ARE ranked P0(none confirmed)/P1/P2 with first-hand file:line anchors (see findings[] + harness/notes/W8-SLIFE-evidence.md). Receipt committed on own branch = NOT done — git commit approval-gated (3 denials), ahead:0. No fabricated commit-state per CRX0/W5H."
    },
    {
      "predicate": "every claim traced to first-hand observation or named UNKNOWN",
      "verdict": "pass",
      "evidence": "Every finding is first-hand: 4-target Explore scout fan-out + per-target adversarial Explore verify (8 agents, 0 errors), then a personal re-read of EVERY P1 anchor before it landed. Verify refuted 1 finding (freeze_dump 'most durable' -> actually least atomic, telemetry_dump.py:312) and corrected several off-by-one anchors — all reflected. Unobtainable items (live one-Ctrl+Z, half-built-network survival, api_adapter live status, orphan-timer GC) are recorded UNKNOWN, never zero, never estimate."
    }
  ],
  "summary": "Lifecycle is fundamentally SOUND: R.2/g5/W7-SESSCOPE (2026-08-16) fixed the headline crashes (panel-parented heartbeat false-freeze; chat lost on reopen). NO confirmed P0. Two P0 CANDIDATES gated on UNKNOWNs: (a) api_adapter.py parallel /api transport mutates with zero undo grouping — P0 IF routed live, live-status UNKNOWN; (b) runtime_beat double-arms across the pypanel module flush on reopen — orphan-timer firing GC-nondeterministic. Undo contract is broadly covered (~53/59 mutating paths wrap; grouping-not-rollback). Marshaling core is sound; no accidental off-main mutating hou found. SESSCOPE persists ONLY the chat transcript (boot-scoped, per-turn+close save); the installed panel's closeEvent stops nothing else.",
  "findings": [
    {"id": "L1", "target": "ui-lifetime", "sev": "P1", "verdict": "uncovered", "anchor": "python/synapse/server/runtime_beat.py:139 + houdini/python_panels/synapse_panel.pypanel:36", "claim": "Freeze heartbeat double-arms on panel reopen: the pypanel flush deletes synapse.server.runtime_beat so _timer resets to None; ensure_beat_started's `if _timer is not None: return True` guard misses and arms a SECOND parentless QTimer (152). The false-freeze class R.2 fixed, reintroduced via the module flush. Whether the orphaned first timer keeps firing (double-beat) or is GC-stopped (gap) is UNKNOWN (PyQt module-global GC nondeterministic)."},
    {"id": "L2", "target": "ui-lifetime", "sev": "P1", "verdict": "uncovered", "anchor": "python/synapse/panel/synapse_panel.py:2620", "claim": "Installed SynapsePanel.closeEvent is the SOLE teardown hook (no onDestroyInterface/onDeactivateInterface) and stops NOTHING beyond removeSelectionCallback + detach beat + save_conversation: no timer.stop, no QThread quit/wait, no signal disconnect, no gate unregister. All else delegated to implicit Qt child destruction. chat_panel.py's clean onDestroyInterface teardown belongs to SynapseChatPanel, which is NOT the installed panel (dead code)."},
    {"id": "L3", "target": "ui-lifetime", "sev": "P1", "verdict": "uncovered", "anchor": "python/synapse/panel/synapse_panel.py:2287", "claim": "ClaudeWorker QThread is parent=self and is never quit()/wait()ed on close (abort exists only on the Stop path, 2413). Panel closing mid-stream leaves a running QThread to Qt child destruction -> 'QThread: Destroyed while thread is still running' crash/hang risk."},
    {"id": "L4", "target": "ui-lifetime", "sev": "P1", "verdict": "uncovered", "anchor": "python/synapse/panel/gate_widget.py:549 + python/synapse/core/gates.py:262", "claim": "GateWidget registers bound methods into the process HumanGate singleton (plain strong-ref list append, no weakref/removal) and never unregisters; closeEvent has no gate teardown -> the callback fires self._proposal_received.emit into a deleted C++ widget ('Internal C++ object already deleted'), and the list pins the widget from GC within a boot. removeSelectionCallback (2623) is the model this should follow but does not."},
    {"id": "L5", "target": "undo-contract", "sev": "P1", "verdict": "uncovered", "anchor": "python/synapse/server/handlers.py:1411", "claim": "execute_vex creates a geo container + attribwrangle under cook_sandwich (update-mode toggle only, NOT an undo group) with NO hou.undos.group; the code comment at 1396 admits 'execute_vex has no undo group of its own'. Multiple createNode = multiple undo entries; one Ctrl+Z will not reverse the VEX build."},
    {"id": "L6", "target": "undo-contract", "sev": "P1", "verdict": "uncovered", "anchor": "python/synapse/server/handlers_tops/render_sequence.py:402", "claim": "tops multi_shot creates topnet/genericgenerator/ropfetch/partition/encode with NO hou.undos.group (the only wrap in the entire handlers_tops package is render_sequence:182, a different handler)."},
    {"id": "L7", "target": "undo-contract", "sev": "P1", "verdict": "uncovered", "anchor": "python/synapse/server/handlers_tops/wedge.py:121", "claim": "tops setup_wedge creates a wedge node + multiparm with NO hou.undos.group."},
    {"id": "L8", "target": "undo-contract", "sev": "P1", "verdict": "uncovered", "anchor": "python/synapse/server/handlers_render.py:1228", "claim": "render_settings writes ROP/Karma parms via p.set(v) (and _apply_karma_advanced_settings) with NO hou.undos.group — the exact mutation class W5-UNDOB explicitly wrapped for set_parm, left unwrapped here."},
    {"id": "L9", "target": "undo-contract", "sev": "P1", "verdict": "uncovered+UNKNOWN", "anchor": "python/synapse/server/api_adapter.py:215/248/310/358", "claim": "An entire parallel hwebserver @apiFunction('synapse') /api transport mutates the scene (create_node createNode 224/226, delete_node n.destroy() 248, set_parm p.set 310, execute_python 358) with ZERO hou.undos.group anywhere in the file. Total undo-contract bypass. P0 IF routed live; live/dead status is UNKNOWN (CLAUDE.md:34 says the live path calls handlers.py directly, which DO wrap) — needs adjudication."},
    {"id": "L10", "target": "thread-discipline", "sev": "P1", "verdict": "observed", "anchor": "python/synapse/server/emergency_live.py:126", "claim": "Deliberate off-main hou WITHOUT marshal: the WS-path emergency halt drives hou.node('/obj').allSubChildren() + ctx.cancelCook() from the freeze-chain escalation-timer thread (the main thread it would marshal onto is the frozen one it is breaking). Residual hou-thread-safety risk (graph traversal off-main under active mutation). Accept-with-residual, on record — not an accidental defect."},
    {"id": "L11", "target": "crash-recovery", "sev": "P1", "verdict": "observed", "anchor": "python/synapse/host/graph_synth_runtime.py:121 + python/synapse/memory/agent_state.py:594", "claim": "agent.usd is NOT wholly dormant: 2 of 5 writers are live during a build (log_decision build receipts; create_task/update_task_status for autonomous_render), contradicting a blanket 'provenance writers dormant' reading. Each writer ends in in-place stage.GetRootLayer().Save() — NOT the repo's atomic .tmp+os.replace idiom — so a hard kill mid-Save risks a corrupt agent.usd (USD-writer atomicity is version-dependent, UNKNOWN headless)."},
    {"id": "L12", "target": "crash-recovery", "sev": "P1", "verdict": "observed", "anchor": "python/synapse/memory/store.py:982 + python/synapse/memory/moneta_store.py:170", "claim": "Memory hard-kill loss window: default backend is jsonl (buffered writes + atexit shutdown-flush; a hard kill bypasses atexit -> unflushed _write_buffer lost, window UNKNOWN). Under Moneta, deposits are in-memory with a periodic 30s atomic snapshot; atexit covers clean exit only; the WAL is inert (signal_attention never called) -> hard kill loses <=30s of deposits."},
    {"id": "L13", "target": "ui-lifetime", "sev": "P2", "verdict": "uncovered", "anchor": "python/synapse/panel/synapse_panel.py:433 + :441", "claim": "_ctx_timer (QTimer(self), 2s) and _health_timer (QTimer(self), 4s) are never stopped in closeEvent. They die with the widget via Qt parent-child and re-arm cleanly on reopen, but an unstopped tick can fire mid-teardown; _ctx_timer's tick spawns an off-main daemon (gather_context_off_main) that marshals back into a possibly-deleted widget."},
    {"id": "L14", "target": "ui-lifetime", "sev": "P2", "verdict": "uncovered", "anchor": "python/synapse/panel/synapse_panel.py:456 + python/synapse/server/telemetry_dump.py", "claim": "telemetry_dump.start_periodic_flush daemon thread is started in __init__ and never stopped in closeEvent; as a synapse.* module-singleton it resets on the pypanel flush -> SAME double-start hazard as runtime_beat (a second flush thread per reopen). stop_periodic_flush exists but the panel never calls it. (Surfaced by the adversarial verify pass.)"},
    {"id": "L15", "target": "ui-lifetime", "sev": "P2", "verdict": "uncovered", "anchor": "python/synapse/panel/synapse_panel.py:2008", "claim": "DirectToolCall QThread (parent=self) is kept off the GC (363) but never wait()ed on close; same running-QThread-destroyed hazard as ClaudeWorker, though direct calls are short."},
    {"id": "L16", "target": "ui-lifetime", "sev": "P2", "verdict": "uncovered", "anchor": "python/synapse/panel/session_integrity.py:236", "claim": "session_integrity._tracker is an in-memory module singleton (blocks/violations); the pypanel flush deletes it on reopen -> integrity history resets to zero and the integrity strip is restored BLANK while SESSCOPE restores the chat FULL. A coverage asymmetry SESSCOPE does not close."},
    {"id": "L17", "target": "undo-contract", "sev": "P2", "verdict": "uncovered", "anchor": "python/synapse/server/handlers_tops/cook.py:192 + python/synapse/server/handlers_hda.py:122/246", "claim": "tops configure_scheduler sets scheduler parms via parm.set with no undo group (low artist-visibility). hda_promote_parm (setParmTemplateGroup) and hda_set_help edit the HDA DEFINITION with no undo group — generally not captured by hou.undos, so possibly a no-op, but the asymmetry vs the wrapped hda_create/hda_package should be adjudicated (needs a live probe)."},
    {"id": "L18", "target": "thread-discipline", "sev": "P2", "verdict": "partial", "anchor": "python/synapse/server/handlers_tops/_common.py:82 + python/synapse/server/marshal_guard.py:16 + python/synapse/panel/claude_worker.py:406", "claim": "Doc-debt (behaviorally safe): _common.py:82 docstring claims it wraps hdefereval.executeInMainThreadWithResult (the banned blocking primitive) but the code routes run_on_main — could mislead a maintainer into 'restoring' the blocking call. Plus stale 'main_thread.py:240' fast-path-2 pointers in marshal_guard.py:16 and claude_worker.py:406 (actual line is 399)."},
    {"id": "L19", "target": "thread-discipline", "sev": "P2", "verdict": "observed", "anchor": "python/synapse/server/websocket.py:605", "claim": "The SynapseServer client-disconnect hook calls hou.hipFile.path() + hou.getenv() directly on the 'Synapse-Server' daemon thread with no run_on_main — read-only, try-guarded, fallback transport, so low blast radius, but inconsistent with the marshaling discipline enforced everywhere else."},
    {"id": "L20", "target": "crash-recovery", "sev": "P2", "verdict": "observed", "anchor": "python/synapse/panel/session_journal.py:145 + python/synapse/server/emergency_live.py:171 + python/synapse/server/telemetry_dump.py:312", "claim": "Durability nuances: session_journal appends per-event but omits os.fsync; emergency_halt_<UTC>.json is atomic-rename but no fsync; freeze_dump_*.json is a PLAIN non-atomic open('wb')+write (no tmp/flush/fsync/replace) — the scout first called it the MOST crash-durable artifact; the adversarial verify REFUTED that (the fsync path is the 'periodic' telemetry branch) — it is the LEAST atomic write of the set. Power-loss tail not guaranteed for any of the three."},
    {"id": "L21", "target": "ui-lifetime", "sev": "info", "verdict": "observed", "anchor": "python/synapse/server/session_store.py:233 + python/synapse/panel/synapse_panel.py:2354", "claim": "SESSCOPE covers EXACTLY the conversation transcript (boot-scoped, HIP-keyed, atomic disk). Saved on every completed turn (_on_done 2354) AND on close (2650) -> restore is robust even if Houdini skips closeEvent. On a new boot the token differs -> transcript is PARKED to conversation.previous.json, not auto-reattached; recovery is restore_previous_conversation()."},
    {"id": "L22", "target": "undo-contract", "sev": "info", "verdict": "observed", "anchor": "CLAUDE.md:44 + shared/bridge.py:1818", "claim": "Undo contract is broadly covered: ~53/59 mutating paths wrap in hou.undos.group (node/usd/cops/material/hda/render/solaris/tops-render_sequence/graph_builder + the /mcp bridge sync 1818 & async 1954). Grouping ONLY, not rollback — a partial network survives the exception path — EXCEPT the Solaris build paths, which performUndo-rollback (the build_graph idiom)."},
    {"id": "L23", "target": "thread-discipline", "sev": "info", "verdict": "observed", "anchor": "python/synapse/server/main_thread.py:399 + :514", "claim": "Marshaling core sound: run_on_main has fast-path-1 (reentrant), fast-path-2 (caller-on-main inline fn(), unbounded — the accepted, fully-instrumented residual GUI-freeze surface), and the off-main worker arm (executeDeferred + Event.wait timeout + C4 zombie-kill + F3/F4 registries). The /mcp bridge is a defense-in-depth detector: records main_thread_executed and hard-rejects a mutation that ran off-main. No accidental off-main MUTATING hou found."},
    {"id": "L24", "target": "crash-recovery", "sev": "info", "verdict": "UNKNOWN", "anchor": "python/synapse/server/solaris_compose_tools.py:99", "claim": "No auto HIP-save exists in the mutation path (first-hand grep across python/synapse: the only hipFile.save hit is a $HIP-resolution comment). A half-built network survives a hard kill ONLY if the artist manually saved -> most likely LOST; true survival of an unsaved build is process/GUI-level and UNKNOWN headless."},
    {"id": "L25", "target": "undo-contract", "sev": "info", "verdict": "UNKNOWN", "anchor": "harness/notes/receipts/W5-UNDOB.json:51", "claim": "Live artist-visible one-Ctrl+Z reversal is UNKNOWN (gui_required) for every path, covered or not: both W5 receipts record this UNKNOWN; static audit + recorder tests prove only that the group is ENTERED around the mutation. House rule: unobtainable-headless renders UNKNOWN."},
    {"id": "L26", "target": "ui-lifetime", "sev": "info", "verdict": "UNKNOWN", "anchor": "houdini/python_panels/synapse_panel.pypanel:41", "claim": "Whether Houdini invokes QWidget.closeEvent on the embedded SynapsePanel when a pane tab closes (vs silently deleting the widget) is UNKNOWN headless. If skipped, none of the (already partial) teardown runs — while the transcript still survives via the per-turn save."}
  ],
  "for_ruling": [
    "api_adapter.py (/api hwebserver transport) mutates with ZERO undo grouping. Is it live-routed on the H22 stack, or dead/experimental? If live -> P0 undo bypass; if dead -> quarantine/annotate. Needs a live routing probe.",
    "CLAUDE.md's 'provenance writers dormant' (agent.usd) is only ~60% true: log_decision + create_task/update_task_status ARE live during a build and each does a non-atomic USD Save(). Doc-accuracy fix + adjudicate the non-atomic Save durability (adopt .tmp+os.replace?).",
    "Close-out mechanics BLOCKED: mutating Bash (git commit, bus.py post finding/status) is approval-gated in this non-interactive dispatch. Product + receipt are written to the worktree but uncommitted (ahead:0). Human/orchestrator must commit both named files on wave8/slife and post findings+RELEASE to the wave8 bus (to W8-LIBR), or grant Bash approval."
  ],
  "spawn": [
    {"id": "W8-SLIFE-P1", "class": "probe", "band": "TRUTH", "name": "GUI one-Ctrl+Z reversal probe (interactive Houdini): confirm a single artist Ctrl+Z reverses each covered build op AND the 6 uncovered holes (execute_vex, tops multi_shot/setup_wedge, render_settings) leave orphan entries. gui_required — cannot be closed headless.", "readonly": true},
    {"id": "W8-SLIFE-P2", "class": "probe", "band": "TRUTH", "name": "api_adapter live-routing probe: determine whether the hwebserver @apiFunction('synapse') /api transport is reachable/routed on the live H22 stack. If live, its zero-undo-grouping mutations are a P0 undo bypass.", "readonly": true}
  ],
  "provenance": "4-target Explore scout fan-out + per-target adversarial Explore verify (8 agents, 0 errors, ~680k subagent tokens); every P1 anchor personally re-read by the leg before landing. Verify refuted 1 finding and corrected several off-by-one anchors — all reflected. Evidence map: harness/notes/W8-SLIFE-evidence.md."
}
