# CAPSULE — 2026-08-13 · Session: Memory Wave end-to-end · v5.47.0 shipped

**Position at close.** `master b7a21a59` == `origin/master`; tag `v5.47.0` on origin;
GitHub release published (https://github.com/JosephOIbrahim/Synapse/releases/tag/v5.47.0).
Both orchestrators stood down. CI state: 138 wave-3 tests + 40 W1/S1 regression tests
green on merged master; release gate pytest pre/post PASS (conformance suite 11/11).

**Merged this session, one lineage:** W1 store recovery (fix/memory-store-recovery,
+5,124 lines), wave-2 latency (S1..S5 + crux, incl. the two receipt-only legs),
wave-3 memory (dim, store, kind, vec, evolve, migrate, harden, crux, paper — 9/9).

**Seam resolutions, all receipted in-tree (bus/_runs, force-added):**
- autoresearch probe-kind registry union (usd_schema_probe + store_census) — resolve_union_merge.py
- store.py W1 x W2-S1 composition (W1 _safe_unsaved_base semantics under S1 _read_on_main) — resolve_s1_seam.py, 40 tests green
- SUPPORT_MATRIX union (W2 registration + wave-3 contracts, dated post-merge note) — resolve_matrix_union.py

**Re-pins (deliberate changes; detectors re-pinned, never reversed):**
- tests/test_w3_harden_evolve_dryrun.py::test_dim_mismatch... → asserts FIXED contract (init succeeds via loud derived-data rebuild)
- scripts/sync_version.py + tests/test_phase0c_doc1_version_conformance.py → README banner locator tracks the redesigned <sub> form

**CTO calls made under standing pre-approval, stated in the record:**
- Wave-3 base = fix/memory-store-recovery (rationale in wave3.live.json _comment)
- Proceeded on CRUX BLOCKED-solely-on-durability per the wave-2 precedent; remedy =
  5 named-file commits (store e8b691d, kind a82492a, evolve c2bedf3, migrate 0c87a83,
  harden 1b8d1e8); substance was crucible-verified sound (re-executed tests).

**Held spawns awaiting Joe's word:** W3-LOCK (cross-process owner lock — closes the
observed last-writer-wins snapshot.json clobber across two OS processes) and
MONETA-DIM-HYDRATE (vendor-side typed EmbeddingDimMismatchError / re-derive).

**Carried risks (stated, not hidden):** URI lock is process-local (single-user envelope);
kill-durable not power-loss-durable (no dir fsync); live seat schema_registered=False
until relaunch; usd_root under a Temp dir (W3-STORE receipt finding).

**L-tasks for next session, in order:**
1. Relaunch Houdini + reinstall (RELEASE_CARD step 8) → read the panel doctor line.
   That observation completes "the doctor SHOWS it": memory section honest-green on
   the live seat for the first time. Until then moneta_substrate=fail is EXPECTED.
2. Skim harness/notes/W3_RECEIPTS_INDEX.md + per-leg receipts (~15 ruling items).
3. Word (or decline) the two held spawns.
4. Optional housekeeping: prune merged worktrees/branches (18 worktrees live).

**Paper trail:** receipts under harness/notes/receipts/ (W3-*), W3-MIGRATE evidence,
wave-3 capsule (PAPER's), RELEASE_v5.47.0.md, wave3.live.json, this capsule.
