{
  "id": "W2-S4",
  "name": "freeze discriminator probe + conditional bridge-lookup cache",
  "band": "BUILD",
  "source": {
    "doc": "docs/reviews/latency_authority_2026-08-13.md",
    "anchor": "W1-MTFIX spawn S4 / FRZ leading candidate: negative _find_bridge_instance lookup (~bridge_adapter.py:204, agent_health.py:52); FRZ_REPRO discriminator = panel sticky WITHOUT sending a prompt; remediation probe-first per R-FRZ-3"
  },
  "targets": [
    "run the FRZ_REPRO discriminator: does stickiness occur with zero prompts sent (isolates the timer/lookup path from the result path)",
    "IF AND ONLY IF the discriminator is positive: apply the single-site negative-result cache on _find_bridge_instance with an explicit invalidation note",
    "if the discriminator is UNKNOWN (no GUI) or negative: ship nothing; attach the ready-but-unapplied diff to the receipt as a finding"
  ],
  "acceptance": [
    {"predicate": "discriminator executed and its result recorded pass/fail/UNKNOWN with the probe path", "evidence": "gui_probe", "gui_required": true},
    {"predicate": "cache fix applied only on a positive discriminator; otherwise diff-in-receipt only", "evidence": "check"},
    {"predicate": "if applied: single-site change, invalidation semantics stated, lookup behaviour test added", "evidence": "test"},
    {"predicate": "no other freeze candidates chased - out of scope per FRZ sequencing", "evidence": "check"}
  ],
  "deps": [],
  "readonly": false,
  "touches": ["python/synapse/panel/bridge_adapter.py", "python/synapse/panel/agent_health.py", "tests/"],
  "crucible_criteria": [
    "R-FRZ-3 honoured: no fix without a positive probe - a fix shipped on an UNKNOWN discriminator is a BLOCK",
    "the discriminator's UNKNOWN, if headless, is recorded as UNKNOWN - never inferred from timing adjacent evidence",
    "cache carries invalidation reasoning (a stale negative cache that hides a late bridge is worse than the lookup cost)"
  ],
  "spawn_classes": ["probe"],
  "note": "Parallel lane - does not touch synapse_panel.py. The one-line fix is cheap; the discipline around it is the mission."
}
