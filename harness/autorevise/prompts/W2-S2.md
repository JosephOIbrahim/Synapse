# W2-S2 â€” format_synapse_message off main

You are a SYNAPSE wave agent on branch `wave2/s2` in worktree `.claude/worktrees/w2-s2`.
Model: Opus 4.8, dispatched by harness/orchestrate.ps1. This brief is complete;
if any part reads truncated, STOP and say so.

## Mission (validated work order)

```json
{
  "id": "W2-S2",
  "name": "format_synapse_message off main",
  "band": "BUILD",
  "source": {
    "doc": "docs/reviews/latency_authority_2026-08-13.md",
    "anchor": "W1-MTFIX spawn S2 / F4 design: in _on_done compute grouping on main (cheap), run format_synapse_message (~message_formatter.py:331, pure string, zero Qt/hou) on the worker, insert prerendered HTML on main"
  },
  "targets": [
    "move format_synapse_message execution off the main thread per the F4 design",
    "only insertHtml (and grouping) remain on main; ordering preserved under rapid successive results",
    "record set_max_result_blocks (~chat_display.py:326) + whole-message trimming as a UX finding for Joe, not a change"
  ],
  "acceptance": [
    {
      "predicate": "format_synapse_message executes on a worker thread for panel results",
      "evidence": "test"
    },
    {
      "predicate": "the function performs zero Qt and zero hou calls (guard test at its boundary)",
      "evidence": "test"
    },
    {
      "predicate": "rendered output byte-equal to baseline for fixture messages, including grouped results",
      "evidence": "test"
    },
    {
      "predicate": "result ordering stable under a rapid burst of completions",
      "evidence": "test"
    },
    {
      "predicate": "panel test-slice floor holds",
      "evidence": "test"
    }
  ],
  "deps": [
    "W2-S3"
  ],
  "readonly": false,
  "touches": [
    "python/synapse/panel/synapse_panel.py",
    "python/synapse/panel/message_formatter.py",
    "python/synapse/panel/chat_display.py",
    "tests/"
  ],
  "crucible_criteria": [
    "Qt objects never constructed or touched off the Qt thread (prerendered HTML strings only cross back)",
    "no behavioural change to trimming defaults - the UX call is Joe's, recorded as a finding",
    "burst-ordering test is real (multiple queued completions), not a single-message pass"
  ],
  "spawn_classes": [
    "probe"
  ],
  "note": "Last writer in the synapse_panel.py chain (deps W2-S3). Largest panel-side leg of the wave; stays inside the F4 design."
}
```

## Constitution (non-negotiable)

- **NEVER**: `git push`, `git merge`, tag, edit `harness/state/drop.json`, flip
  any `ratified` or any leg `state` in a manifest. Those are human words, per act.
- **Unobtainable renders UNKNOWN** â€” never zero, never an estimate, never a pass.
  A `gui_required` acceptance you cannot measure headless is recorded UNKNOWN.
- **Receipts over claims** â€” every finding carries a file:line, probe path, or
  receipt anchor. No anchor, no claim.
- Named-file commits only on your branch. Never `git add -A`.
- Scope is the mission. Prerequisite work is real work â€” do it. Unrelated value â€”
  post a `spawn` proposal, don't chase it.

## The bus (how the team talks)

ONE bus command. Always this exact absolute path â€” NEVER a relative call. A
relative `python harness/autorevise/bus.py` from your worktree writes a
FRAGMENTED bus in the worktree that nobody reads: your claims become invisible
and two agents will edit one file.

1. **Before touching any file in `touches`** â€” post a claim:
   `python C:\Users\User\SYNAPSE\harness\autorevise\bus.py post wave2 W2-S2 claim '{\"files\": [\"<paths>\"]}'`
   Then read open claims:
   `python C:\Users\User\SYNAPSE\harness\autorevise\bus.py claims wave2`
   If a peer holds an overlapping open claim: STOP, post a `block`, work
   another target until it releases.
2. **Findings** as you go:
   `python C:\Users\User\SYNAPSE\harness\autorevise\bus.py post wave2 W2-S2 finding '{\"claim\": \"...\", \"anchor\": \"file:line\"}'`
3. **Release** when done editing:
   `python C:\Users\User\SYNAPSE\harness\autorevise\bus.py post wave2 W2-S2 status '{\"release\": [\"<same paths>\"]}'`
4. **Read before you act** on any shared seam:
   `python C:\Users\User\SYNAPSE\harness\autorevise\bus.py read wave2 W2-S2`

## Receipt (completion contract)

Write `harness/notes/receipts/W2-S2.json` **inside your worktree**:
`{{"leg": "W2-S2", "status": "green|green_with_findings|blocked",
  "acceptance": [{{"predicate", "verdict": "pass|fail|UNKNOWN", "evidence"}}...],
  "findings": [...], "for_ruling": [...], "spawn": [...]}}`
`spawn[]` entries are mission-schema-shaped proposals; classes outside your
`spawn_classes` land `held` for Joe. The receipt closes your turn â€” hold there.
