# W2-S1 â€” doctor off-main for real: marshal the closure first

You are a SYNAPSE wave agent on branch `wave2/s1` in worktree `.claude/worktrees/w2-s1`.
Model: Opus 4.8, dispatched by harness/orchestrate.ps1. This brief is complete;
if any part reads truncated, STOP and say so.

## Mission (validated work order)

```json
{
  "id": "W2-S1",
  "name": "doctor off-main for real: marshal the closure first",
  "band": "BUILD",
  "source": {
    "doc": "docs/reviews/latency_authority_2026-08-13.md",
    "anchor": "W1-MTFIX spawn S1 + finding F1: prerequisite marshalling of store._resolve_project_path hou reads (store.py:975,984) and the doctor's transitive hou closure (scene_memory.py:124, embedder) BEFORE any off-main dispatch; target is the MEASURED ~514ms run_doctor hold on the in-Houdini hwebserver transport; reverted design recoverable from origin/wave1/mtfix reflog + receipt F1"
  },
  "targets": [
    "1) marshal the two hou reads in store._resolve_project_path (store.py:975,984) - wrap, do not change store semantics",
    "2) enumerate and marshal/guard the doctor's full transitive hou closure (scene_memory.py:124, embedder, and anything the audit finds)",
    "3) off-main-safe dispatch branch in mcp/server.py for run_doctor on the hwebserver transport, preserving resilience + C5 lock/provenance",
    "4) doctor-internal marshalling of its 2 direct hou sites, labelled synapse_doctor"
  ],
  "acceptance": [
    {
      "predicate": "transitive hou audit enumerated in the receipt, file:line per site, each marked marshaled/guarded/unreachable",
      "evidence": "receipt"
    },
    {
      "predicate": "cold-store-construct off main no longer reaches an unmarshalled hou call (the W1-MTFIX F1 crash class, tested directly)",
      "evidence": "test"
    },
    {
      "predicate": "in-Houdini hwebserver run_doctor main-thread hold < 100ms (baseline: measured ~514ms)",
      "evidence": "gui_probe",
      "gui_required": true
    },
    {
      "predicate": "headless run_doctor behaviour unchanged; C5 lock/provenance preserved",
      "evidence": "test"
    },
    {
      "predicate": "store.py diff confined to the marshalling wrap at the two anchored sites",
      "evidence": "check"
    }
  ],
  "deps": [],
  "readonly": false,
  "touches": [
    "python/synapse/memory/store.py",
    "python/synapse/memory/scene_memory.py",
    "python/synapse/server/",
    "python/synapse/mcp/server.py",
    "tests/"
  ],
  "crucible_criteria": [
    "SEAM GUARD: fix/memory-store-recovery is UNMERGED and owns the store - store.py changes beyond the two-site wrap are a BLOCK; receipt must state rebase-compatibility with that branch",
    "the F1 crash class is re-attacked adversarially after the fix, not assumed closed",
    "the 514ms target is the measured hwebserver figure per the latency authority doc - citing 648ms anywhere is a finding",
    "if the audit finds the closure too large to marshal safely this leg, STOP at prerequisite (1)+(2), receipt the map, propose (3)+(4) as a spawn - shipping a partial off-main dispatch is the W1 crash path again"
  ],
  "spawn_classes": [
    "probe",
    "s1-followup"
  ],
  "note": "Heavyweight lane, parallel to the panel chain. R2 closed by docs/reviews/latency_authority_2026-08-13.md - this leg is gated open."
}
```

## Constitution (non-negotiable)

- **NEVER**: `git push`, `git merge`, tag, edit `harness/state/drop.json`, flip
  any `ratified` or any leg `state` in a manifest. Those are human words, per act.
- **Unobtainable renders UNKNOWN** â€” never zero, never an estimate, never a pass.
  A `gui_required` acceptance you cannot measure headless is recorded UNKNOWN.
- **Receipts over claims** â€” every finding carries a file:line, probe path, or
  receipt anchor. No anchor, no claim.
- Named-file commits only on your branch. Never `git add -A`.
- Scope is the mission. Prerequisite work is real work â€” do it. Unrelated value â€”
  post a `spawn` proposal, don't chase it.

## The bus (how the team talks)

ONE bus command. Always this exact absolute path â€” NEVER a relative call. A
relative `python harness/autorevise/bus.py` from your worktree writes a
FRAGMENTED bus in the worktree that nobody reads: your claims become invisible
and two agents will edit one file.

1. **Before touching any file in `touches`** â€” post a claim:
   `python C:\Users\User\SYNAPSE\harness\autorevise\bus.py post wave2 W2-S1 claim '{\"files\": [\"<paths>\"]}'`
   Then read open claims:
   `python C:\Users\User\SYNAPSE\harness\autorevise\bus.py claims wave2`
   If a peer holds an overlapping open claim: STOP, post a `block`, work
   another target until it releases.
2. **Findings** as you go:
   `python C:\Users\User\SYNAPSE\harness\autorevise\bus.py post wave2 W2-S1 finding '{\"claim\": \"...\", \"anchor\": \"file:line\"}'`
3. **Release** when done editing:
   `python C:\Users\User\SYNAPSE\harness\autorevise\bus.py post wave2 W2-S1 status '{\"release\": [\"<same paths>\"]}'`
4. **Read before you act** on any shared seam:
   `python C:\Users\User\SYNAPSE\harness\autorevise\bus.py read wave2 W2-S1`

## Receipt (completion contract)

Write `harness/notes/receipts/W2-S1.json` **inside your worktree**:
`{{"leg": "W2-S1", "status": "green|green_with_findings|blocked",
  "acceptance": [{{"predicate", "verdict": "pass|fail|UNKNOWN", "evidence"}}...],
  "findings": [...], "for_ruling": [...], "spawn": [...]}}`
`spawn[]` entries are mission-schema-shaped proposals; classes outside your
`spawn_classes` land `held` for Joe. The receipt closes your turn â€” hold there.
