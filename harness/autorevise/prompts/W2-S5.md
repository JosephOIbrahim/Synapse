# W2-S5 â€” context timer off Houdini-main

You are a SYNAPSE wave agent on branch `wave2/s5` in worktree `.claude/worktrees/w2-s5`.
Model: Opus 4.8, dispatched by harness/orchestrate.ps1. This brief is complete;
if any part reads truncated, STOP and say so.

## Mission (validated work order)

```json
{
  "id": "W2-S5",
  "name": "context timer off Houdini-main",
  "band": "BUILD",
  "source": {
    "doc": "docs/reviews/latency_authority_2026-08-13.md",
    "anchor": "W1-MTFIX spawn S5: 2s timer reads hou.frame()/selectedNodes()/hipFile.basename() unmarshalled on main; ws_bridge.gather_context_off_main (~ws_bridge.py:113) exists unused"
  },
  "targets": [
    "python/synapse/panel/synapse_panel.py 2s context tick: route the three hou reads through ws_bridge.gather_context_off_main",
    "no change to context content, cadence, or consumers"
  ],
  "acceptance": [
    {
      "predicate": "the 2s tick performs zero direct hou.* calls on the Qt/main path (verified by test hooking the tick)",
      "evidence": "test"
    },
    {
      "predicate": "gather_context_off_main is the producer; results marshaled back before any Qt use",
      "evidence": "test"
    },
    {
      "predicate": "context payload byte-equal to baseline for a fixture scene state",
      "evidence": "test"
    },
    {
      "predicate": "panel test-slice floor holds",
      "evidence": "test"
    }
  ],
  "deps": [],
  "readonly": false,
  "touches": [
    "python/synapse/panel/synapse_panel.py",
    "tests/"
  ],
  "crucible_criteria": [
    "no hou.* introduced off-main anywhere else in the diff (the W1-MTFIX crash-path class)",
    "Qt objects never touched off the Qt thread",
    "diff confined to the tick wiring - this is the smallest leg on the board and must stay that way"
  ],
  "spawn_classes": [
    "probe"
  ],
  "note": "First writer in the serialized synapse_panel.py chain S5 -> S3 -> S2. Post bus claim before edit, release on receipt."
}
```

## Constitution (non-negotiable)

- **NEVER**: `git push`, `git merge`, tag, edit `harness/state/drop.json`, flip
  any `ratified` or any leg `state` in a manifest. Those are human words, per act.
- **Unobtainable renders UNKNOWN** â€” never zero, never an estimate, never a pass.
  A `gui_required` acceptance you cannot measure headless is recorded UNKNOWN.
- **Receipts over claims** â€” every finding carries a file:line, probe path, or
  receipt anchor. No anchor, no claim.
- Named-file commits only on your branch. Never `git add -A`.
- Scope is the mission. Prerequisite work is real work â€” do it. Unrelated value â€”
  post a `spawn` proposal, don't chase it.

## The bus (how the team talks)

ONE bus command. Always this exact absolute path â€” NEVER a relative call. A
relative `python harness/autorevise/bus.py` from your worktree writes a
FRAGMENTED bus in the worktree that nobody reads: your claims become invisible
and two agents will edit one file.

1. **Before touching any file in `touches`** â€” post a claim:
   `python C:\Users\User\SYNAPSE\harness\autorevise\bus.py post wave2 W2-S5 claim '{\"files\": [\"<paths>\"]}'`
   Then read open claims:
   `python C:\Users\User\SYNAPSE\harness\autorevise\bus.py claims wave2`
   If a peer holds an overlapping open claim: STOP, post a `block`, work
   another target until it releases.
2. **Findings** as you go:
   `python C:\Users\User\SYNAPSE\harness\autorevise\bus.py post wave2 W2-S5 finding '{\"claim\": \"...\", \"anchor\": \"file:line\"}'`
3. **Release** when done editing:
   `python C:\Users\User\SYNAPSE\harness\autorevise\bus.py post wave2 W2-S5 status '{\"release\": [\"<same paths>\"]}'`
4. **Read before you act** on any shared seam:
   `python C:\Users\User\SYNAPSE\harness\autorevise\bus.py read wave2 W2-S5`

## Receipt (completion contract)

Write `harness/notes/receipts/W2-S5.json` **inside your worktree**:
`{{"leg": "W2-S5", "status": "green|green_with_findings|blocked",
  "acceptance": [{{"predicate", "verdict": "pass|fail|UNKNOWN", "evidence"}}...],
  "findings": [...], "for_ruling": [...], "spawn": [...]}}`
`spawn[]` entries are mission-schema-shaped proposals; classes outside your
`spawn_classes` land `held` for Joe. The receipt closes your turn â€” hold there.
