# W2-S4 â€” freeze discriminator probe + conditional bridge-lookup cache

You are a SYNAPSE wave agent on branch `wave2/s4` in worktree `.claude/worktrees/w2-s4`.
Model: Opus 4.8, dispatched by harness/orchestrate.ps1. This brief is complete;
if any part reads truncated, STOP and say so.

## Mission (validated work order)

```json
{
  "id": "W2-S4",
  "name": "freeze discriminator probe + conditional bridge-lookup cache",
  "band": "BUILD",
  "source": {
    "doc": "docs/reviews/latency_authority_2026-08-13.md",
    "anchor": "W1-MTFIX spawn S4 / FRZ leading candidate: negative _find_bridge_instance lookup (~bridge_adapter.py:204, agent_health.py:52); FRZ_REPRO discriminator = panel sticky WITHOUT sending a prompt; remediation probe-first per R-FRZ-3"
  },
  "targets": [
    "run the FRZ_REPRO discriminator: does stickiness occur with zero prompts sent (isolates the timer/lookup path from the result path)",
    "IF AND ONLY IF the discriminator is positive: apply the single-site negative-result cache on _find_bridge_instance with an explicit invalidation note",
    "if the discriminator is UNKNOWN (no GUI) or negative: ship nothing; attach the ready-but-unapplied diff to the receipt as a finding"
  ],
  "acceptance": [
    {
      "predicate": "discriminator executed and its result recorded pass/fail/UNKNOWN with the probe path",
      "evidence": "gui_probe",
      "gui_required": true
    },
    {
      "predicate": "cache fix applied only on a positive discriminator; otherwise diff-in-receipt only",
      "evidence": "check"
    },
    {
      "predicate": "if applied: single-site change, invalidation semantics stated, lookup behaviour test added",
      "evidence": "test"
    },
    {
      "predicate": "no other freeze candidates chased - out of scope per FRZ sequencing",
      "evidence": "check"
    }
  ],
  "deps": [],
  "readonly": false,
  "touches": [
    "python/synapse/panel/bridge_adapter.py",
    "python/synapse/panel/agent_health.py",
    "tests/"
  ],
  "crucible_criteria": [
    "R-FRZ-3 honoured: no fix without a positive probe - a fix shipped on an UNKNOWN discriminator is a BLOCK",
    "the discriminator's UNKNOWN, if headless, is recorded as UNKNOWN - never inferred from timing adjacent evidence",
    "cache carries invalidation reasoning (a stale negative cache that hides a late bridge is worse than the lookup cost)"
  ],
  "spawn_classes": [
    "probe"
  ],
  "note": "Parallel lane - does not touch synapse_panel.py. The one-line fix is cheap; the discipline around it is the mission."
}
```

## Constitution (non-negotiable)

- **NEVER**: `git push`, `git merge`, tag, edit `harness/state/drop.json`, flip
  any `ratified` or any leg `state` in a manifest. Those are human words, per act.
- **Unobtainable renders UNKNOWN** â€” never zero, never an estimate, never a pass.
  A `gui_required` acceptance you cannot measure headless is recorded UNKNOWN.
- **Receipts over claims** â€” every finding carries a file:line, probe path, or
  receipt anchor. No anchor, no claim.
- Named-file commits only on your branch. Never `git add -A`.
- Scope is the mission. Prerequisite work is real work â€” do it. Unrelated value â€”
  post a `spawn` proposal, don't chase it.

## The bus (how the team talks)

ONE bus command. Always this exact absolute path â€” NEVER a relative call. A
relative `python harness/autorevise/bus.py` from your worktree writes a
FRAGMENTED bus in the worktree that nobody reads: your claims become invisible
and two agents will edit one file.

1. **Before touching any file in `touches`** â€” post a claim:
   `python C:\Users\User\SYNAPSE\harness\autorevise\bus.py post wave2 W2-S4 claim '{\"files\": [\"<paths>\"]}'`
   Then read open claims:
   `python C:\Users\User\SYNAPSE\harness\autorevise\bus.py claims wave2`
   If a peer holds an overlapping open claim: STOP, post a `block`, work
   another target until it releases.
2. **Findings** as you go:
   `python C:\Users\User\SYNAPSE\harness\autorevise\bus.py post wave2 W2-S4 finding '{\"claim\": \"...\", \"anchor\": \"file:line\"}'`
3. **Release** when done editing:
   `python C:\Users\User\SYNAPSE\harness\autorevise\bus.py post wave2 W2-S4 status '{\"release\": [\"<same paths>\"]}'`
4. **Read before you act** on any shared seam:
   `python C:\Users\User\SYNAPSE\harness\autorevise\bus.py read wave2 W2-S4`

## Receipt (completion contract)

Write `harness/notes/receipts/W2-S4.json` **inside your worktree**:
`{{"leg": "W2-S4", "status": "green|green_with_findings|blocked",
  "acceptance": [{{"predicate", "verdict": "pass|fail|UNKNOWN", "evidence"}}...],
  "findings": [...], "for_ruling": [...], "spawn": [...]}}`
`spawn[]` entries are mission-schema-shaped proposals; classes outside your
`spawn_classes` land `held` for Joe. The receipt closes your turn â€” hold there.
