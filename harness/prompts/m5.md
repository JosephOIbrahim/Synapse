You are ORCHESTRATOR for M5 — the BLOCKS reconciler. Read harness/AGENT_CONSTITUTION.md first; it binds you. Then read fixtures/solaris.basic.json in full, and harness/autoresearch/probes.py (the fixture build path already exists there — `_build_fixture_once` is your seed, written and verified 2026-08-05).

**WRITES CODE.** You are building the engine that makes a fixture real in a live session: read the definition, make /stage match it, no-op on re-apply, never touch the artist's nodes.

=== THE RULED CONTRACT (D1-D4, ruled by Joe 2026-08-06 — NOT open questions) ===

**D1 — Ownership = network box.** Every node the reconciler creates lives in a network box named `BLOCKS_<fixture>` (e.g. `BLOCKS_solaris_basic`). Membership in that box IS ownership. Nodes stay flat in /stage — native, directly editable. Do NOT stamp USD customData provenance; that schema zone is gated behind an RFC with Michael Gold and is out of scope.

**D2 — Collisions fail loudly.** If a fixture node name already exists in /stage OUTSIDE the box: create NOTHING, delete NOTHING, return a named-collision report listing every clash. No adopt, no auto-rename, no partial apply.

**D3 — Delete scope = box members only.** The reconciler may delete ONLY nodes inside its own box. This is structural, not behavioral: enumerate the box, never pattern-match names against the wider graph. "Your nodes never touched" must be provable from the code shape.

**D4 — Seam = Dispatcher tool.** Mount as a tool through the one dispatch spine, same port pattern as `synapse_inspect_stage`: pure function under the cognitive layer, schema registered alongside, adapter branch swapped. **Discover the live registration seam from the codebase you are standing in — do not trust any description of it from memory or from stale docs.** If the seam differs from the port pattern described in README/CLAUDE.md, follow the live code and record the difference in your receipt.

=== SEMANTICS: RECONCILE, NOT CREATE ===

`apply_fixture(name)` means "make /stage match the definition":

  clean stage            -> build (nodes, exact names, parms, wires,
                            positions, display flag, box)
  already applied        -> NO-OP. ops_count == 0. Not delete-and-rebuild.
  partially applied /    -> reconcile inside the box to match; report
  drifted box contents      what changed
  artist nodes present   -> untouched, always (D2/D3)

`remove_fixture(name)` deletes box members + the box. Nothing else.

Return a structured result every call: {applied, ops, collisions, box, per_node}. The panel verdict line renders from structure, never from prose.

=== THE ORACLE IS ALREADY BAKED ===

fixtures/solaris.basic.json carries its own verified baseline:

  sha256        8bb057619efe5cb2e3b7e6b7fb82bcb1bdd8d8a65017eb0f27aba6813b060ee7
  build         22.0.368
  canonicalizer c2 (harness/autoresearch/probes.py canonicalize_usda)

Reuse the canonicalizer as ONE source of truth — lift it into the product
module and have autoresearch import it, or import across; do not fork the
filter list. If layering forbids both, copy WITH a provenance comment and
a test asserting the two stay byte-identical.

=== CRUCIBLE INVARIANTS (headless hython, the autoresearch run pattern) ===

  F-1   apply on clean stage            -> stage hash == 8bb05761...
  F-2   apply -> remove -> apply        -> same hash
  F-3   apply on already-applied        -> ops == 0 AND hash unchanged
  F-4   artist node outside box, no name clash -> apply succeeds,
        artist node byte-untouched, box correct
  F-5   artist node named `camera` outside box -> collision report,
        zero mutations, stage hash unchanged from before the call

Plus plain-python unit tests with mocks for the reconcile diff logic
(the cognitive layer imports no hou — honor the lint boundary).
Commandment 7 stands: a failing invariant is fixed in the implementation,
never weakened in the test.

=== WHAT YOU ARE NOT DOING ===

Not M6: no phrase table, no alias routing, no "basic solaris setup" string
handling — the tool takes a fixture NAME. Not panel UI. Not USD customData.
Not new fixtures. Not touching harness/autoresearch/ except the shared
canonicalizer seam. **If hou surprises you, probe with dir() before coding
against it — this repo has been wrong by assumed API three times.**

=== ORACLE ===

  F-1 through F-5 green in headless hython on 22.0.368, evidence-file style
  plain-python suite green under the cognitive/host lint boundary
  the Dispatcher seam documented AS FOUND, with any drift from the port
    pattern recorded, not silently adapted to
  receipt M5.json: what shipped, file:line for the tool registration,
    invariant results, and the one verdict line the panel would show

=== STANDING ===

The fixture is committed and pushed (1e13629f). The definition is settled;
you are building its hands. Ship over perfect — a reconciler that passes
F-1..F-5 and reports honestly beats one with features nobody ruled on.
