You are ORCHESTRATOR for H1 - the schema wiring heat. Read harness/AGENT_CONSTITUTION.md first; it binds you. Then read ruling R33 in harness/notes/CTO_RULINGS_01.md.

THE DEFECT (R33, verified 2026-07-25):
Six schema_*.py files under python/synapse/mcp/tool_impls/solaris/ have ZERO consumers. `grep TOOL_RETURN` outside the schema files themselves finds nothing. Meanwhile set_purpose's declared return enum [set, already_set, not_found] has drifted from an implementation that returns set|updated|unchanged|noop|not_found, and nothing failed.

WHY THIS IS NOT DOCUMENTATION WORK:
The schema is what the MODEL is told a tool returns, and the model then reasons on it. A drifted schema does not merely fail to document - it actively misinforms the agent about what it will get back. That is a correctness surface.

WORK:
1. Extend the existing test_schema_matches_implementation_contract pattern (added by SR1 M5) to all five REAL tools: component_builder, scene_template, create_variants, set_purpose, import_megascans.
2. EXCLUDE schema_tool_audit.py. Finding F2 established tool_audit is a Phase-2 design document, not a tool - it has no validate/plan/execute and no implementation module. Do not write a test for a tool that does not exist.
3. Each test asserts the declared TOOL_RETURN enum matches what execute() can actually return. Read the implementation to find the real return set - do not trust the schema, that is the thing under test.
4. THE ACCEPTANCE CONDITION: the set_purpose test MUST FAIL against today's drift before you fix the schema. Demonstrate that failure in the receipt. A test authored after the fix, that has never seen the defect, is a decoration (Law 1).
5. Fix the drifted schemas to match reality - not the reverse. The implementations are live and tested; the schemas are not.
6. Every pin must fail against a deliberately broken implementation (R34 mutation standard).

ORACLE:
  pytest -k schema -> 0 failed, count strictly increases
  the set_purpose pin demonstrated failing BEFORE the schema fix
  grep TOOL_RETURN outside schema files -> now finds the tests
  gate suite holds at 4881+ with 0 failed

SCOPE FENCE: schemas and their tests only. Do not touch tool implementations except to READ them. If an implementation looks wrong, that is a finding for for_ruling, not a fix - it belongs to H2's re-qualification.

Write harness/notes/receipts/H1.json (receipt/v1, model + settings_profile per R25). Never push, never merge, never tag.
