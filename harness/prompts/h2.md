You are ORCHESTRATOR for H2 - the re-qualification heat. Read harness/AGENT_CONSTITUTION.md first; it binds you. Then read rulings R34, R42 and R43 in harness/notes/CTO_RULINGS_01.md.

YOU ARE UNBLOCKED BECAUSE RES LANDED. Read harness/notes/receipts/RES.json FIRST. If RES did not actually eliminate the fake-hou residency, STOP and say so - every re-probe below is uninterpretable against a stubbed hou, and running them anyway would manufacture exactly the defect this leg exists to remove.

WHY THIS LEG EXISTS (R34):
F4 of the eleven Solaris wiring findings was named HIGH with a specific mechanism. That mechanism is REFUTED-LIVE on 22.0.368. A fix was written and shipped with a green test - and the test COULD NOT FAIL. Adversarial mutation testing caught it. One in eleven was a phantom; the other ten share its provenance and have never been individually re-probed.

WORK:

PART A - re-qualify F1 through F11.
Read harness/notes/l2_wiring_findings.md. For EACH finding, dispatch assayer to re-probe it against live Houdini 22.0.368 and return exactly one of:
  CONFIRMED    - the defect exists as described, with a file:line anchor
  REFUTED      - the premise is false on this build; say what is actually true
  UNVERIFIABLE - cannot be probed; say precisely what is missing
Do not accept a finding because it is written down. l2_wiring_findings.md is a receipt, and R29 established that a receipt is not the tree.

PART B - mutation-test every existing regression pin.
For each pin written against F1-F11: break the implementation deliberately, confirm the pin FAILS, restore. A pin that survives its own mutation is a decoration - REPORT IT as a finding, do not quietly rewrite it. The count of surviving-mutation pins is a headline number for the receipt.

PART C - the 17 bucket-2 tests (R43).
Q2 bucket 2 holds ~17 tests that pass only because a fake hou cannot disagree with them. Same mechanism, same fix as RES. They are in scope for this leg because re-qualifying F1-F11 while leaving known decorations beside them produces a leg that is correct and useless.

ORACLE:
  every F1-F11 finding carries CONFIRMED/REFUTED/UNVERIFIABLE + anchor
  every pin has a recorded mutation result
  pytest -k solaris -> 0 failed, count strictly increases
  gate suite holds at 4881+ with 0 failed

Write harness/notes/receipts/H2b.json (receipt/v1, model + settings_profile per R25). Report the phantom count and the surviving-mutation count as integers in the receipt summary - those two numbers are what this leg is for. Never push, never merge, never tag.
