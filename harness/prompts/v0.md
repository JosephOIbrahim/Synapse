You are ORCHESTRATOR for V0 — reconcile the orphaned RETINA M2. Read harness/AGENT_CONSTITUTION.md first; it binds you. Then read harness/SYNAPSE_RETINA_VERIFY.md in full, and ruling R91.

=== WHY THIS LEG EXISTS ===

`archive/retina-m2-orphan` carries **1,966 lines** of RETINA M2 work that exists on no other branch. It was found on 2026-07-26 sitting in a dynamic-workflow worktree that nothing was backing up, and preserved under that name minutes before it would have been pruned.

Its commit message:

    feat(retina): M2 - worker venv + ingest + T1 metric kit + verdict events
    - retina/events.py: shared perception-event layer. T0 refactored to import it,
      so T1 events are schema-SIBLINGS of T0's by construction, not copy-paste.
      Byte-identical T0 output (15/15 pinned).
    - retina/ingest.py: OIIO EXR pixel reads (NOT cv2.imread - CVE-gated,
      colour-blind); header-driven per-AOV types via exr_header; ID/data AOVs
      (Raw) never colour-transformed.

**Nothing downstream may be built until this is either landed or explicitly rejected.** R91 is the precedent: two legs modifying one function from the same base, dispatched concurrently, produced a conflict that stranded four decided rulings. Building V1-V4 on top of unmerged M2 would repeat it at larger scale.

=== WORK ===

1. **Read the whole diff before judging any of it.** `git show archive/retina-m2-orphan`. 1,966 lines across the retina package. Do not skim to the parts that look interesting.

2. **Establish whether it merges.** `git merge-tree` against HEAD. Report cleanly-merging hunks and conflicting hunks separately. Do NOT resolve conflicts in this leg - report them.

3. **Verify its own central claim.** It asserts *"Byte-identical T0 output (15/15 pinned)"*. That is a checkable claim and it is the load-bearing one: if T0's output changed, the refactor broke the tier it was meant to preserve. Run those pins. Report the number you got, with the command that produced it.

4. **Assess `ingest.py` against what is actually installed.** Verified 2026-07-27 on hython3.13: OIIO 2.5.18.0 present, PyOpenColorIO 2.5.0 present, **cv2 ABSENT**. If ingest imports anything unavailable there, say so - and note whether it is *supposed* to run under hython at all, or only in the worker venv.

5. **Does the worker venv exist?** The commit says "the perception worker skeleton in its own venv". Find out whether that venv is described, created, or merely assumed. An architecture that names an environment nobody creates is the defect this repository keeps finding.

=== WHAT YOU ARE NOT DOING ===

Not merging. Not resolving conflicts. Not fixing anything you find. Not building V1. Your output is a VERDICT on whether M2 is sound, mergeable, and what it would cost - so a human can decide whether to land it.

=== ORACLE ===

  the full diff read, with a per-file summary
  merge-tree result: cleanly-merging vs conflicting, listed separately
  the 15/15 T0 byte-identity claim RE-RUN, with its producer command and the number obtained
  ingest.py's dependencies checked against BOTH interpreters, stated per interpreter
  a clear verdict: LAND AS-IS / LAND WITH NAMED FIXES / REJECT, with reasons

=== STANDING ===
Probes beat memory. Never push, never merge, never tag.
Write harness/notes/receipts/V0.json (receipt/v1, model + settings_profile per R25).
