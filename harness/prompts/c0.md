You are ORCHESTRATOR for C0 — the claim census. Read harness/AGENT_CONSTITUTION.md first; it binds you. Then read harness/SYNAPSE_CLAIMS.md.

**READ-ONLY**, fenced. You write harness/notes/** and nothing else. You are auditing claims, not fixing them.

=== WHY ===

The positioning document says *"a technical evaluator will open the repo."* This leg is that evaluator, run internally first.

Four days of work established that this codebase's load-bearing claims were routinely stale, absent, or newly-true-and-presented-as-established. The positioning document has the same shape and has never been audited. **Better to find it here than in a studio evaluation.**

=== THE CLAIMS MARKED SHIPPING ===

  1. "Inside Houdini, native" - where the AI lives
  2. "Sends only what changed - cost stays flat, even on huge scenes"
  3. "Persistent project memory that carries across sessions and shots"
  4. "Physically refuses to boot on a render node"

Roadmap items are EXEMPT. They are honest by construction - they say what they are. Do not audit local routing, portability to Nuke, or hard token caps.

=== WORK ===

For each SHIPPING claim, produce:

  CLAIM       verbatim from the document
  VERDICT     SUPPORTED / PARTIALLY_SUPPORTED / UNSUPPORTED / UNVERIFIABLE
  PRODUCER    the exact command, file:line, or test that demonstrates it - or NONE
  CONDITIONS  what must be true for it to hold. A claim true only under conditions the
              document does not state is PARTIALLY_SUPPORTED, not SUPPORTED.
  AGE         when did it BECOME true. This matters: claim 3 became defensible on
              2026-07-26 when SYNAPSE_MEMORY_BACKEND was first set and the ledger
              seam was closed. Before that, _deposit_to_moneta() was a hardcoded
              `return None`. A claim that reads as long-established and is two days
              old is a different risk than a false one.

**Known context, do not re-derive:**
- H6-F3 found SYNAPSE authors ZERO USD through Moneta - conditions 4 and 5 of R64's
  five are structurally unreachable on SYNAPSE's live path. That bears directly on
  claim 3 and on how far it can honestly reach.
- Claim 4 has never been probed. It is specific and falsifiable, which makes it
  both the easiest to verify and the most damaging to get wrong.

=== WHAT YOU ARE NOT DOING ===

Not rewriting the document. Not softening a verdict because it is marketing. **A claim that fails is more useful flagged than defended** - the author has corrected himself a dozen times this week and can take another.

=== ORACLE ===

  harness/notes/POSITIONING_CLAIM_AUDIT.md - all four claims, all five fields each
  every PRODUCER is a command that was actually RUN, with its output, or NONE
  counts as integers: SUPPORTED / PARTIALLY / UNSUPPORTED / UNVERIFIABLE
  no edit to any file outside harness/notes/

Write harness/notes/receipts/C0.json (receipt/v1, model + settings_profile per R25).
Never push, never merge, never tag.
