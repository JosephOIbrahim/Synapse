You are ORCHESTRATOR for U1 — author the union of `moneta_provenance()`. Read harness/AGENT_CONSTITUTION.md first; it binds you. Then read rulings R64, R91 and R52–R55 in harness/notes/CTO_RULINGS_01.md.

=== WHY THIS LEG EXISTS ===

R64 specified FIVE conditions for the Moneta substrate and said `moneta_provenance()` would compose them. The CTO then dispatched LEDGER and H6 as two independent legs, both `deps: []`, both rewriting that one function from the same base in ignorance of each other. Neither branch has all five fields.

  LEDGER (repair/ledger-moneta-seam @ eb25abe)   available + version + file + revision
  H6     (merged to the working line)            available + schema_registered + schema_in_use

`git merge-tree` VERIFIED-RUNTIME: LEDGER + H6 CONFLICTS on
  python/synapse/memory/moneta_runtime.py
  docs/studio/DEPLOYMENT.md

**This is AUTHORING, not merge resolution.** No automatic strategy produces a five-field function. Do not attempt `git merge` and then patch the markers — read both implementations and write the union deliberately.

=== WHAT IS AT STAKE ===

LEDGER's rulings **R52, R53, R54 and R55 are DECIDED** and implemented ONLY in the stranded branch. Four closed rulings are currently un-shipped. That is the cost of delay and it is the highest in the ruling block.

LEDGER's branch carries 2,012 insertions across 8 files — ledger.py, moneta_runtime.py, deposit.py, a 715-line test module, and a 302-line mutation harness. **It is not just the provenance function.** Read the whole diff before deciding what merges cleanly and what needs authoring.

=== WORK ===

1. Read BOTH implementations in full before writing anything.
     git show repair/ledger-moneta-seam:python/synapse/memory/moneta_runtime.py
     the current HEAD version (carries H6's rewrite)
   Note what each documents, not just what it returns. LEDGER's docstring carries real findings — importlib.metadata reports the same version for rc1, rc2 and rc2+N, so the version string cannot discriminate builds, and `revision` is the field that actually does when $MONETA_SRC points at a live worktree.

2. Author the union. All five fields, per R64:
     available            LEDGER + H6 agree
     version + file       LEDGER
     revision             LEDGER  (bounded walk - R91/LEDGER.F2: the walk was
                                   UNBOUNDED and reported the ENCLOSING repo's
                                   HEAD as Moneta's revision. Provenance was not
                                   absent, it was CONFIDENTLY WRONG.)
     schema_registered    H6
     schema_in_use        H6      (R75: NOT evidence alone - Sdf authoring is
                                   schema-blind, typeName lands on disk whether
                                   or not USD knows the type. Meaningful only
                                   PAIRED with schema_registered.)

3. Preserve every constraint both legs established:
   - No raise, anywhere. `_make_store`'s contract is that the flag can never break startup.
   - Fields degrade to None (unknown) when pxr or Moneta is absent. Distinguish "checked and false" from "could not check" — collapsing those is the defect the substrate work exists to remove.
   - `ledger.py` stays zero-hou (R54).

4. Bring across the REST of LEDGER's branch — ledger.py's seam, deposit.py, the test module, the mutation harness. Anything that merges cleanly, merge. Anything that conflicts, author, and say which in the receipt.

5. Every pin must fail against its mutation (R34) and every reader must be calibrated (R60). Both legs already wrote mutation harnesses — use them, and report whether they still catch what they caught.

=== ORACLE ===

  moneta_provenance() returns ALL FIVE fields; none raise with pxr or moneta absent
  the revision walk is BOUNDED - demonstrate it does NOT report an enclosing repo's HEAD
  R52-R55's implementations are present on the working line, not stranded
  pytest -k "moneta or ledger or provenance" -> 0 failed, count strictly increases
  gate suite holds at 4940+ with 0 failed
  the receipt states which hunks merged and which were AUTHORED

=== STANDING ===
Never push, never merge to master, never tag. Commit your product on your own branch BEFORE writing the receipt — R93: `green` with zero commits is no longer a permitted terminal state.
Write harness/notes/receipts/U1.json (receipt/v1, model + settings_profile per R25).
