You are ORCHESTRATOR for I1 — build the ingest. Read harness/AGENT_CONSTITUTION.md first; it binds you. Then harness/SYNAPSE_INGEST.md, docs/H22_FRONTIER.md, and **I0's scout report in full**.

You are gated on I0 because **the join key is its finding, not your assumption.** A corpus keyed on the wrong field does not fail loudly — it silently matches nothing.

=== WHAT I0 SETTLED — FOUR OF THESE DECIDE YOUR EXTRACTOR'S SHAPE ===

Read the full report. These four are here because missing any one produces a corpus that
looks complete and joins to nothing, which is the failure this harness exists to prevent.

**LABEL is the join key. Confirmed on the LIVE RUNTIME, four ways (I0-F3).**
```
label    100%  present   88.7-89.1%  resolves
#id      62-85% present  43.4-52.9%  resolves
```
Key on label. `#id` is evidence, not a key. R97 now stands on runtime measurement rather
than a prior leg's assertion.

**`#channels` is a SECOND internal-name key and is NOT optional (I0-F4).** 1,833 occurrences
archive-wide, and it DOMINATES cop2 — 248 `#channels` against 51 `#id`. A reader that knows
only `#id` will read the Copernicus surface as undocumented. **Copernicus is the entire point
of this leg.**

**Include resolution is REQUIRED, and this is measured not theoretical (I0-F6).**
`lop/distantlight.txt` documents ZERO parameters until its `:include:` is followed. **An
extractor that skips includes reports fully-documented nodes as ungrounded** — the same
mechanism that made H5 read a deprecated subsystem as current.

**One parser per context, not one parser (I0-F1).** Nine single-parser assumptions were
measured FALSE across the full 5,032-page census. Three of them fail SILENTLY: CRLF, BOM, and
colon-directive id capture.

**And take I0-F2 personally.** Its own controls caught THREE silent defects in its own reader —
each returning a plausible number, none raising an exception. Calibrate every reader before
trusting it (R60). A number that looks right is the failure mode here, not a crash.

=== WHAT IS NOW MECHANICAL, AND WHAT IS NOT ===

**All 161 new Copernicus nodes have a page. Present 161, absent 0 (I0-F5).** 158 of them clear
the floor AND reach ACTIONABLE — at least one parameter with an internal name (I0-F7). Three do
not; name them rather than dropping them.

So the frontier gap is closable from a file that ships with the product. **Report the three that
are not, and what would ground them.**

=== THE DEPRECATION FINDING IS A DELIVERABLE, NOT A FOOTNOTE ===

**Doc and runtime disagree on 195 node types. 51 are the dangerous direction: the runtime flags
them deprecated while the documentation reads completely clean.**

That is R72 confirmed at scale. Every one of those 51 is a node an assistant would teach as
current from documentation alone. **Carry the runtime verdict on the entry, and emit the list of
51 separately** — it is the most directly useful thing this harness produces for anyone shipping
against H22, us or SideFX.

=== WHAT YOU ARE BUILDING ===

A grounding corpus from `$HFS/houdini/help/nodes.zip`, and the extractor that produces it. Version-pinned to 22.0.368 by construction; no network.

**Design it as if SideFX owned the outcome, because they effectively do.** A confidently wrong answer about Houdini damages Houdini more than a refusal does. Three consequences, each costing coverage on purpose:

**Provenance per ENTRY, never per corpus.** Every record carries `VERIFIED-DOC` and the build. **Never summed with probe-derived grounding into one number** — docs supply what a node is FOR; only a probe supplies what it DOES.

**A stub is not knowledge.** An entry clearing I0's floor is ingested. One that does not is recorded `known-thin` and counted, **not padded to look complete**. Report the exists-vs-clears gap as an integer per context.

**Deprecation travels with the entry.** R72: it is the union of runtime `deprecationInfo()` and authored help, and they disagree. `karmarenderproperties` has 56,325 chars of documentation that never mentions it, while SYNAPSE emits it 123 times. **A corpus without that axis teaches decaying nodes as current.**

=== WORK ===

1. **Extractor**, keyed on whatever I0 measured — not on what H9 found, unless I0 confirmed it. Its reader is calibrated first: shown returning a KNOWN answer on a page you have already read by hand, before it is trusted on 5,000 you have not (R60).

2. **The 161 first.** They are the measured frontier gap — SYNAPSE grounds 3. Whatever I0 says is present, ingest that, and report the rest as needing a runtime probe rather than quietly dropping them.

3. **Then the rest of `cop/` and `lop/`**, in that order. COP grounding is 6.2% against a subsystem that barely existed in H21; LOP is 18.3%. **Copernicus is where the release went.**

4. **Report the number that matters**, not the flattering one: entries that clear the floor, per context, against the live catalogue totals — 384 Copernicus, 169 Cop2, 218 LOP.

=== EXPLICITLY OUT OF SCOPE ===

**Nothing wires into the RAG corpus or the emission path.** U.6 found 15 phantom `createNode` sites already living in the RAG corpus, outside the emission gate, re-teaching phantoms through `knowledge_lookup`. **Adding thousands of doc-derived entries to that surface without a gate is that mistake at scale.** Wiring is a separate decision with its own oracle.

Do not touch `rag/skills/houdini21-reference` — R119: it IS H21 documentation, accurately labelled, and relabelling it would make the corpus lie about its own provenance.

=== ORACLE ===

```
harness/notes/ingest/h22_node_corpus.json - every entry tiered VERIFIED-DOC
  with its build, its source path inside nodes.zip, and its floor verdict
counts per context as integers: exists / clears-floor / ingested
the 161: how many ingested, how many need a probe, named
a 20-node cross-check against the LIVE runtime - documented parm names vs
  node.parms() - with the agreement count reported
NO change to rag/, to the emission corpus, or to any product file
```

=== STANDING ===
Commit product before the receipt (R93). Read committed paths, never worktree globs (R127).
Never push, never merge, never tag.
Write harness/notes/receipts/I1.json (receipt/v1, model + settings_profile per R25). Put the clears-floor counts in the summary as integers — that is what this leg is for.
