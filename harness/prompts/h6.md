You are ORCHESTRATOR for H6 — substrate truth. Read harness/AGENT_CONSTITUTION.md first; it binds you. Then read ruling R64 in harness/notes/CTO_RULINGS_01.md.

=== FIRST PRINCIPLES: WHY THIS LEG EXISTS ===

For Moneta to be genuinely working as SYNAPSE's USD substrate, FIVE conditions must hold. They are independently falsifiable. They are currently collapsed into moneta_available() -> bool, which tests exactly the first:

  1  the module imports                     <- the only one measured today
  2  the SAME module on both interpreters   <- LEDGER.F1: they load DIFFERENT copies
  3  the schema is REGISTERED with USD      <- plugInfo on PXR_PLUGINPATH_NAME
  4  prims are AUTHORED with that type      <- the migration actually happened
  5  a memory ROUND-TRIPS typed             <- written and read back as MonetaMemory

Any link can break while the boolean still reads True, because it only ever tested link 1.

=== WHAT IS ALREADY TRUE — VERIFIED, DO NOT RE-DERIVE ===

C:\Users\User\Moneta\schema\plugInfo.json declares:
    "MonetaMemory": { "schemaKind": "concreteTyped", "bases": ["UsdTyped"] }

So Usd.SchemaRegistry().FindConcretePrimDefinition("MonetaMemory") IS the correct API. It is a typed IsA schema, not an applied API schema — the design brief weighed MonetaMemoryAPI and locked IsA. generatedSchema.usda carries six documented attributes.

BUT Moneta's DEEP_THINK_BRIEF_codeless_schema.md:15 states memory prims are authored TODAY as untyped `def` prims, with the typed migration listed as "the next surgery."

Expected live state is therefore registered=True, in_use=False. CONFIRM THAT — do not assume it. If prims ARE typed, that is a finding and the brief is stale.

=== WORK ===

1. Extend moneta_provenance() in python/synapse/memory/moneta_runtime.py. It already reports available/version/file. Add:
     schema_registered  - FindConcretePrimDefinition("MonetaMemory") is not None
     schema_in_use      - does any authored prim report typeName == "MonetaMemory"
   Both must degrade to None (unknown), never raise, when pxr is unavailable. Distinguish "checked and false" from "could not check" — collapsing those is the defect this leg exists to remove.

2. DO NOT RAISE ANYWHERE. _make_store's docstring is a contract: "falls back to JSONL with a warning if Moneta can't be imported, so setting the flag can never break startup." A memory backend must not prevent Houdini's panel from loading. In _make_store's moneta branch, log at ERROR and fall back — the shape store.py:830 already uses to distinguish not-installed from installed-but-broken.

3. Surface it in synapse_doctor. The cell that matters is registered && !in_use: it is the ONLY combination that looks like success while the substrate is doing nothing typed.

4. MEASURE ON BOTH INTERPRETERS. LEDGER.F1 established that gate (python 3.14.2, pip Moneta) and shipping (hython 3.13.10, MONETA_SRC worktree) load different Moneta copies. Report the pair. A schema registered under one says nothing about the other.

5. REUSE Moneta's isolation pattern — do not reinvent it. C:\Users\User\Moneta\tests\_schema_gate_subprocess.py runs under subprocess.run with PXR_PLUGINPATH_NAME pointed at schema/, because USD plugin registration is PROCESS-GLOBAL and cannot be tested in-process without contaminating the run. Read that file before writing any test.

6. POSITIVE CONTROL, mandatory: demonstrate the check FIRING with PXR_PLUGINPATH_NAME unset. A registry check that has never seen an unregistered schema is a decoration (Law 1). Record the demonstration in the receipt.

=== ORACLE ===

  moneta_provenance() returns all five fields; none raise with pxr absent
  the pair (gate, shipping) is reported for schema_registered
  the positive control DEMONSTRATED: check fires with PXR_PLUGINPATH_NAME unset
  no raise path added to _make_store or any import path
  pytest -k "moneta or schema or provenance" -> 0 failed, count strictly increases
  every new pin fails against its mutation (R34), and its READER is calibrated too (R60)
  gate suite holds at 4881+ with 0 failed

=== STANDING ===
Probes beat memory. Never push, never merge, never tag.
Write harness/notes/receipts/H6.json (receipt/v1, model + settings_profile per R25). Report the five conditions as an explicit table in the receipt summary, per interpreter.
