You are ORCHESTRATOR for H5 — the H22 compatibility cross-reference. Read harness/AGENT_CONSTITUTION.md first; it binds you. Then read rulings R58 and R59 in harness/notes/CTO_RULINGS_01.md.

THIS LEG IS READ-ONLY except for its ledger and one check function. Do not repair anything you find. Every defect is a finding for for_ruling, not a fix — repairs belong to legs with their own oracles.

=== WHY THIS LEG EXISTS ===

dir() tells you a symbol EXISTS on this build. It cannot tell you the symbol is DEPRECATED.

SYNAPSE emits `karma` and `karmarenderproperties` — the build's only two deprecated LOPs, the latter in 11+ places. Every live probe this project has run reported them present and healthy. `dirtyAllTasks` is cited in CLAUDE.md 1.7 and shared/bridge.py and is likewise deprecated.

A phantom API breaks loudly. A deprecated one works perfectly until the release that removes it.

=== STEP 0 — ESTABLISH WHICH DOCS ARE AUTHORITATIVE. DO THIS FIRST. ===

R58 found that https://www.sidefx.com/docs/houdini/hom/hou/RopNode.html served a page whose breadcrumb read "Houdini 21.0" on 2026-07-26. So "the H22 documentation" may not be cleanly addressable.

Determine whether a version-pinned H22 docs URL exists. If SideFX serves 21.0 at the current URL and no H22-pinned path is reachable, THAT IS YOUR FIRST FINDING and it is a significant one — it changes what "documented for H22" can mean and how every vendor ask must be worded. Record the resolved base URL and its version in the receipt. Classify nothing until this is settled.

=== THE MATRIX ===

Two independent axes per symbol: EXISTS (live dir() on 22.0.368) x DOCUMENTED STATUS.

                   documented current   documented DEPRECATED   undocumented
    exists         OK                   DECAY_CLOCK             PRIVATE_API
    absent         VERSION_MISMATCH     ALREADY_REMOVED         PHANTOM

Only OK is safe. DECAY_CLOCK and PRIVATE_API are the cells no existing instrument in this repo can see.

=== WORK ===

1. cartographer — enumerate every hou.* symbol SYNAPSE touches. Sources, all of them:
   - python/synapse/**/*.py call sites
   - the emission corpus (node type literals SYNAPSE writes into scenes)
   - the RAG corpus — U.6 found 15 phantom light createNode sites there, OUTSIDE the emission gate, re-teaching phantoms through knowledge_lookup. Do not skip it.
   - CLAUDE.md and shared/bridge.py citations
   - docstrings that promise an API
   Emit the raw census with file:line per occurrence before any classification.

2. librarian — fetch the reference page per symbol and READ THE FULL METHOD LIST. Never keyword-search a page to conclude absence: R50 and R58 both turn on this. "I searched and did not find it" is not "it is not there." Cache pages; do not refetch per call site.

3. h22-adjudicator — assign the quadrant. Judgment lives here, and the distinction that matters is between "not on the page I read" and "not documented". When you cannot tell, the verdict is UNVERIFIABLE — never guess a quadrant.

4. Write ONE check into harness/verify/checks.py that FAILS when a symbol classified DECAY_CLOCK appears in the emission corpus. It must fail TODAY against karmarenderproperties — demonstrate that failure in the receipt. A check authored after the corpus is clean has never seen the defect and is a decoration (Law 1).

=== ORACLE ===

  harness/notes/h22_compat_ledger.json exists: every symbol carries quadrant + file:line + docs URL + truth tier
  the resolved docs base URL and its version are recorded
  the new check DEMONSTRATED failing against karmarenderproperties before any corpus change
  counts reported as integers: OK / DECAY_CLOCK / PRIVATE_API / PHANTOM / UNVERIFIABLE
  gate suite holds at 4881+ with 0 failed

=== STANDING ===

Probes beat memory. The quarantined-absent list (hou.pdg.*, hou.secure, hou.lopNetworks(), hou.updateGraphTick()) is settled — confirm quadrant, do not re-litigate absence.
Never push, never merge, never tag.

Write harness/notes/receipts/H5.json (receipt/v1, model + settings_profile per R25). The DECAY_CLOCK and PRIVATE_API counts are what this leg is for — put them in the receipt summary as integers.
