You are ORCHESTRATOR for F1 — integration. This is the LAST leg. Read harness/AGENT_CONSTITUTION.md first; it binds you. Then read harness/notes/CTO_RULINGS_01.md in full — 72 rulings, and F1 is where the ones marked for integration land.

You are gated on H1, H2, H6 and H7 because integration must never merge a picture that is still moving. All four have receipts before you start.

=== WHAT YOU ARE INTEGRATING ===

Nine legs with receipts, each in its own worktree:
  Q1 unpoison · Q2 baseline · RES residency · H3a cancel-probe · LEDGER moneta seam
  H1 schemas · H2b requalify · H5 h22 compat · H6 substrate truth · H7 compat re-adjudicate

=== WORK, IN ORDER ===

1. MERGE, one at a time, suite green after EACH. Not a batch.
   Order: RES, H1, H2, LEDGER, H6, then the read-only legs (H3a, H5, H7) whose
   only product is receipts and notes.
   A red suite after any single merge STOPS the sequence. Report which merge and
   what broke; do not continue and do not "fix forward" through a merge conflict
   into product code you have not read.

2. RECEIPT CONSOLIDATION. Every leg wrote its receipt into its OWN worktree.
   Copy all of them into the main tree's harness/notes/receipts/ so the record
   survives worktree pruning. The orchestrator reads worktree-first, main-tree
   second (R-fix 2026-07-26) — after pruning, main tree is the only copy.

3. HOUSEKEEP.
   - Remove merged worktrees and their branches. KEEP q1-unpoison until last:
     the shipping-suite measurements were produced from it.
   - Delete merged remote feature branches.
   - KEEP archive/root-scratch-2026-07-25 INDEFINITELY. It is the only copy of
     eleven deleted files outside history.
   - Check for stray worktree-wf_* trees (Claude Code's dynamic-workflow scratch)
     and remove them locally. They must never reach origin — the backup filter
     excludes them but the local trees accumulate.

4. VERIFY THE INTEGRATION, do not assume it.
   - gate suite: python -m pytest -q -p no:cacheprovider --basetemp=<fresh>
   - shipping suite: harness/run_suite_shipping_python.ps1 (--basetemp is REQUIRED;
     without it pytest's temp cleanup raises PermissionError and EATS the summary
     line, which is why three runs on 2026-07-26 produced no number)
   - Report BOTH numbers with their interpreters, per R31. Neither substitutes
     for the other.

5. RULING BLOCK. Concatenate every unresolved for_ruling[] item across all ten
   receipts into harness/notes/REPAIR_HEATS_RULING.md. Rank by cost of delay, not
   by leg order. Mark which are already decided in CTO_RULINGS_01.md and which
   are genuinely open — several were ruled during the run and must not be
   re-presented as open.

=== EXPLICITLY OUT OF SCOPE ===

- NEVER push to master. NEVER merge to master. NEVER tag. Gate C is Joe's, and it
  is the whole point of this harness.
- Do NOT release H3b. It is held by ruling R48 and only Joe releases it.
- Do NOT send the SideFX ask. Drafted at docs/SIDEFX_ASK_H22_DRAFT.md, Joe sends.
- Do NOT fix shared/bridge.py. Human-authored by standing rule (S.2 boundary).
- Do NOT run git add --renormalize. R24's debt was cleared 2026-07-26; a second
  renormalization on a tree with ten merges in it would be unreviewable.

=== ORACLE ===

  every leg merged individually with a green suite recorded after each
  all ten receipts present in the MAIN tree's harness/notes/receipts/
  gate AND shipping numbers reported with interpreters
  REPAIR_HEATS_RULING.md exists, ranked, decided-vs-open marked
  git status clean but for known stragglers
  master UNTOUCHED — verify with git log --oneline -1 master and say so

Write harness/notes/receipts/F1.json (receipt/v1, model + settings_profile per R25). Then STOP. Do not ask Joe anything until the receipt is written.
