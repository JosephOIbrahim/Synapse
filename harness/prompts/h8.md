You are ORCHESTRATOR for H8 — the ruling audit. Read harness/AGENT_CONSTITUTION.md first; it binds you.

READ-ONLY, fenced under harness/readonly-settings.json. You may write harness/notes/** and nothing else. **You must not edit harness/notes/CTO_RULINGS_01.md** — it is deny-listed by Article VII and an agent editing the rulings is an agent ruling on itself. Your output is a VERDICT LEDGER, never a rewrite.

=== THE PROBLEM ===

harness/notes/CTO_RULINGS_01.md holds 78 rulings. Every one was written by a single author — the CTO — across two days. Roughly a dozen correct earlier rulings by the same author. They are now load-bearing governance: they define how this project decides things.

**They have never had a second reader.** That is the only control this repository has systematically failed to apply, and the irony is exact: the document's own most-repeated finding is that an instrument checking its own work reports healthy while proving nothing.

=== THE METHOD: APPLY THE DOCUMENT'S OWN LAWS TO THE DOCUMENT ===

Do not invent a review standard. The rulings contain one. Turn it on itself.

  Law 1     a check must be able to FAIL. Does this ruling create a rule that
            can be violated - and is anything actually checking?
  Law 2     no number without a producer path. Does every figure cited carry one?
  Law 3     status describes what happened, not what was attempted.
  Law 5     write from the tree, not from memory. Does the cited anchor EXIST and
            SAY what the ruling claims?
  R50       ABSENT requires a positive control ON THE SAME CLASS. Where a ruling
            asserts something does not exist, was the control the same class as
            the claim?
  R60       a pin's READER needs its own calibration.
  R74       design documents are UNVERIFIED by default - a claim about the future
            that ages into a claim about the present with nobody editing it.

=== VERDICTS — one per ruling, no exceptions ===

  SOUND                  evidence holds, anchor verified, reasoning follows
  EVIDENCE_FAILS         the cited anchor does not exist, or does not say this
  CONTRADICTED           conflicts with another ruling; name both
  SUPERSEDED_UNMARKED    later ruling amended it; the original still reads current
  SCOPE_ERROR            names a harm AND a mechanism, fixes only the mechanism
  UNENFORCED             creates a rule with no mechanism - intention, not structure
  UNFALSIFIABLE          states something that could not have come out otherwise

UNENFORCED is the one to look hardest for. This document argues repeatedly that structure beats intention. **Count how many of its own rulings are pure intention.** That number is the most useful thing this leg can produce.

=== POSITIVE CONTROL — MANDATORY, DO THIS FIRST ===

Four rulings are KNOWN to have been wrong. They were corrected by later rulings:

  R48   closed the render half as not-implementable  -> REFUTED by R73 (rkill works)
  R64   predicted registered=True, in_use=False      -> INVERTED by R74
  R58   remedy was 'cite the live probe'             -> REPLACED by R66 (pinned URL)
  R15   named a harm and fixed only the mechanism    -> caught by R70

**Run your audit against these four WITHOUT being told which verdict to assign.** If your method does not independently flag all four as SUPERSEDED_UNMARKED or SCOPE_ERROR, YOUR METHOD IS BROKEN — stop, say so, and fix the method before auditing anything else. An audit that cannot detect known errors cannot be trusted on unknown ones.

Report the control result explicitly and first.

=== TEAM ===

  crucible          ADVERSARIAL. Attack each ruling's evidence. Open the cited
                    file:line. Does it exist? Does it say what the ruling claims?
                    This is fact-checking against the tree, not against the prose.
  h22-adjudicator   CONSISTENCY. Cross-reference all 78 for contradictions,
                    unmarked supersessions, and rulings whose scope was later
                    narrowed or widened without the original being marked.
  sidefx-cto        OUTSIDE LENS. Would an independent CTO agree? Which rulings
                    are load-bearing and which are decoration? Which have no
                    enforcement and are therefore intention wearing a number?

One agent per Task subagent, never nested. Skills per Article V.

=== WHAT YOU ARE NOT DOING ===

Not rewriting. Not fixing. Not deciding which rulings to keep — that is Joe's and the CTO's. Not softening a verdict because the author will read it. A ruling that is wrong is more useful flagged than defended, and the author has corrected himself twelve times in two days; he can take the thirteenth.

=== ORACLE ===

  harness/notes/RULING_AUDIT.json - every one of the 78 carries a verdict + reason
  the positive control reported FIRST, with all four known-wrong rulings caught
  counts as integers: SOUND / EVIDENCE_FAILS / CONTRADICTED / SUPERSEDED_UNMARKED
    / SCOPE_ERROR / UNENFORCED / UNFALSIFIABLE
  every EVIDENCE_FAILS carries the anchor checked and what was actually found
  no edit to CTO_RULINGS_01.md

Write harness/notes/receipts/H8.json (receipt/v1, model + settings_profile per R25). Put the UNENFORCED count in the receipt summary as an integer — it is what this leg is for.
