You are ORCHESTRATOR for M6 - the phrase table. The last mile. Read
harness/AGENT_CONSTITUTION.md first; it binds you.

=== GATE + RULING SLOT (Joe fills before flipping to ready) ===
HELD until (a) CI0 is merged to master (Gate C) and (b) Joe rules the
design question below. Dispatch base is post-merge master.
RULING: [ ] exact-match only   [ ] aliases ("solaris basic" ==
"basic solaris" == "basic Solaris setup")
If this slot is unfilled, STOP - do not choose for him.

=== WHY ===
The engine is done, merged, released (apply_fixture, v5.43.0). M6 maps
typed phrases to fixture names so "basic Solaris setup" FIRES at the
keyboard, not just via a fixture name. Design already fixed by the
handoff: exact-match table first, model only on miss, ZERO tokens on the
common path.

=== THE WORK ===
1. The table: phrase -> fixture name. Normalization on lookup (case,
   whitespace) is not aliasing; alias EXPANSION happens only if the
   ruling says aliases.
2. Wire it ahead of the model call on the typed-prompt path: hit -> fire
   apply_fixture directly, no provider request. Miss -> current behavior
   exactly. The common path must be observably zero-token (evidence: no
   provider call in the hit path's trace/log).
3. Tests: hit fires the fixture once (the ari-01 lesson - exactly one);
   miss is byte-identical to pre-M6 behavior; unknown phrase never
   errors, it just misses.

=== WHAT YOU MAY NOT DO ===
No fuzzy matching, no embeddings, no model-assisted lookup on the hit
path. No new fixtures - the table maps to what exists.

=== RECEIPT harness/notes/receipts/M6.json ===
{ "table_at": "file:line", "hit_path_zero_token_evidence": "...",
  "single_fire_test": "id", "miss_path_unchanged_test": "id",
  "ruling_applied": "exact|aliases" }
