You are ORCHESTRATOR for H7 — re-adjudicate the H22 compat ledger against LOCAL references. Read harness/AGENT_CONSTITUTION.md first; it binds you. Then read ruling R72 in harness/notes/CTO_RULINGS_01.md.

THIS LEG IS READ-ONLY and fenced under harness/readonly-settings.json. You can write harness/notes/** and nothing else. Do not repair anything you find; every defect is a finding.

=== WHY THIS LEG EXISTS ===

H5 produced the compat census and returned UNVERIFIABLE: 1267 — its largest bucket by far. That number is partly a REACHABILITY artifact, not a knowledge one: the pinned /docs/houdini22.0/ tree is ROBOTS_DISALLOWED to automated fetch, and the unpinned tree serves 21.0.

Two LOCAL references exist that H5 did not have. Both were control-tested in R72.

=== THE TWO SOURCES, AND WHAT EACH IS FOR ===

1. HOM AXIS — hom.zip, AUTHORITATIVE
   C:\Program Files\Side Effects Software\Houdini 22.0.368\houdini\help\hom.zip
   967 .txt entries, one per HOM symbol, e.g. hou/RopNode.txt, hou/TopNode.txt.
   62 mention deprecation. SHIPS WITH 22.0.368, so it is version-pinned by
   construction — no robots issue, no breadcrumb ambiguity, no network.
   CONTROL PASSED: RopNode.txt has no cancel verb; TopNode.txt carries
   `dirtyAllTasks(self, remove_outputs)` and its deprecation notice verbatim.

2. NODE AXIS — the help cache, SECOND SOURCE ONLY, NEVER AUTHORITATIVE
   C:\Users\User\OneDrive\Documents\houdini22.0\config\Help\cache
   1,588 node docs, 48 LOP, 207 mention deprecation. JSON with keys
   type/attrs/body/title/summary/included.
   IT IS A BROWSING CACHE. A node absent from it means "nobody opened that page",
   NOT "undocumented". Conflating those is the exact defect this leg exists to
   remove — and it is the mistake the orchestrator made before R72 corrected it.
   hom.zip does NOT cover node types at all, which is why this source matters.

=== THE FINDING THAT SHAPES YOUR ADJUDICATION ===

nodes/lop/karmarenderproperties.json is 69,921 chars, current, substantial — and DOES NOT MENTION DEPRECATION. Same for nodes/lop/karma.json at 95,777 chars. The RUNTIME flags both; that is how H5 found them.

So the authored help is LESS informative than the runtime for node types. A doc-only oracle reports the headline DECAY_CLOCK symbol as clean.

Deprecation is the UNION of runtime deprecationInfo() and authored help. Disagreement between them is itself a finding, and it gets its own cell.

=== WORK ===

1. Load H5's ledger: harness/notes/h22_compat_ledger.json (in the h5-compat worktree — read it from there, do not assume it was merged).

2. Re-adjudicate every UNVERIFIABLE symbol against the correct local source for its kind:
   - hou.* symbol  -> hom.zip
   - node type     -> help cache (second source) + the runtime verdict H5 already has
   Report how many of the 1,267 resolve, and to which cell.

3. Add the sub-cell doc_silent_deprecation: runtime says deprecated, authored help does not. R72 rules it the most dangerous cell, because every human-facing surface says the symbol is fine. Report its count as an integer — karmarenderproperties and karma are its first two known members.

4. State the residual honestly. A symbol with no entry in either local source and no runtime verdict is STILL UNVERIFIABLE, and that is the correct answer. Do not manufacture coverage — the point of this leg is that H5's number was partly reachability, and the residual after fixing reachability is the real unknown.

5. POSITIVE CONTROL, mandatory: demonstrate your hom.zip reader returning the KNOWN answers for RopNode (no cancel verb) and TopNode (dirtyAllTasks deprecated) before adjudicating anything else. A reader that has not been shown to produce a known-correct answer is uncalibrated (R60).

=== ORACLE ===

  harness/notes/h22_compat_ledger_v2.json written, superseding neither - both kept
  every re-adjudicated symbol carries: cell + source used + local path + truth tier
  the reader control DEMONSTRATED against RopNode and TopNode
  counts reported as integers: resolved / still-UNVERIFIABLE / doc_silent_deprecation
  H5's DECAY_CLOCK floor of 19 is restated as a floor or corrected upward with evidence

=== STANDING ===
No network fetches. Both sources are local and version-pinned; a network call would reintroduce exactly the version ambiguity this leg removes.
Never push, never merge, never tag.
Write harness/notes/receipts/H7.json (receipt/v1, model + settings_profile per R25).
