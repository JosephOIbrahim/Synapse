You are ORCHESTRATOR for H9 — harvest local H22 node documentation into semantic grounding. Read harness/AGENT_CONSTITUTION.md first; it binds you.

READ-ONLY on product code, fenced under harness/readonly-settings.json. You write harness/notes/** only. You are producing a GROUNDING CORPUS and a coverage report, not editing the emission path.

=== WHY THIS LEG EXISTS ===

LOP grounding is 40/218 = 18%. COP grounding is 24/384 = 6%. Both are less than desirable and both are D2-shaped gaps - missing SEMANTIC knowledge about what a node is for, not missing knowledge that it exists.

The shipped reference has been measured (2026-07-26) and covers almost all of it:

  C:\Program Files\Side Effects Software\Houdini 22.0.368\houdini\help\nodes.zip
  6.2 MB, 5,034 pages
    lop/   198 pages, median 4,376 B
    cop/   375 pages, median 2,928 B
    cop2/  150 pages, median 1,942 B

  Coverage against the live catalogues this project already probed:
    LOP  179/198 = 90%
    COP  460/491 = 94%

It ships with the build, so it is version-pinned by construction. No network, no robots restriction, no breadcrumb ambiguity.

=== THE TIER RULE — THIS IS THE WHOLE DISCIPLINE OF THE LEG ===

Documentation supplies D2 (semantic). It CANNOT supply D3 (behavioural). Do not conflate them and do not report one number.

Every harvested entry carries truth tier **VERIFIED-DOC**, which is a NEW tier, distinct from VERIFIED-RUNTIME. A grounding figure derived from docs must be reported SEPARATELY from probe-derived grounding. Summing them would produce a number that looks like coverage and is partly hearsay.

**The known failure mode, proven today:** nodes/lop/karmarenderproperties is a 69,921-character page that NEVER MENTIONS the type is deprecated - the runtime flags it, the docs do not (R72). Doc-derived grounding inherits every place the documentation is silent or stale. Say so in the corpus, not just in the receipt.

=== WORK ===

1. Establish exact coverage. harness/notes/_doc_coverage.py is a first pass and its catalogue parse is APPROXIMATE - it found 198 LOP where the ruling says 218, and its "missing" list contains parse artifacts ('1.0', '2.0') and network-manager nodes (chopnet, dopnet, matnet) that are not really missing. Redo the comparison against the live catalogues properly:
     harness/notes/h22_lop_catalog_live_22.0.368.json
     harness/notes/h22_cop_catalog_live_22.0.368.json
   Report TRUE coverage, and list what is genuinely absent.

2. Harvest each page into a structured entry: type name, context, summary, parameter names with their descriptions, and the source path inside nodes.zip. A page is not grounding if you store it as a blob - extract structure.

3. **Quality gate, mandatory.** A page that exists is not automatically grounding. Define a floor (a summary AND at least one documented parameter is a reasonable start, but justify whatever you choose) and report how many pages CLEAR it versus merely exist. The gap between "has a page" and "is grounded" is the number that matters, and it is the one a coverage percentage hides.

4. Cross-check a sample against the runtime. Take 10 LOP and 10 COP at random, and for each compare the documented parameter names against the live node's actual parms. Report agreement. **If the docs disagree with the runtime, the docs are wrong and that is a finding** - it is the same class as R72's silent deprecation.

5. Do NOT wire the corpus into the emission path or the RAG corpus in this leg. Produce it, measure it, report it. Wiring is a separate decision with its own oracle - U.6 found 15 phantom createNode sites already living in the RAG corpus outside the emission gate, and adding 600 doc-derived entries to that surface without a gate would be the same mistake at scale.

=== ORACLE ===

  harness/notes/h22_doc_grounding_corpus.json - every entry tiered VERIFIED-DOC
  true LOP and COP coverage reported as integers AND percentages, against the live catalogues
  the "has a page" vs "clears the quality floor" gap reported explicitly
  the 20-node runtime cross-check reported with an agreement count
  NO change to the emission corpus, the RAG corpus, or any product file

Write harness/notes/receipts/H9.json (receipt/v1, model + settings_profile per R25). Put the projected D2 grounding for LOP and COP in the receipt summary as integers and percentages - that is what this leg is for, and it must be labelled PROJECTED-DOC, never merged into the existing grounding figure.
