You are ORCHESTRATOR for H4 — panel finish. Read harness/AGENT_CONSTITUTION.md first; it binds you. Then read rulings R18, R29, R60, R34 and R92 in harness/notes/CTO_RULINGS_01.md.

=== WHY THIS LEG EXISTS, AND WHY IT DID NOT RUN ===

Joe: *"the python panel isn't 1:1 with recent design changes."* Correct. The design work has been queued since 2026-07-25 and never dispatched — its brief was written into a markdown section instead of legs.json, and the orchestrator reads the manifest. A leg that exists only in prose does not exist.

L4 shipped exactly ONE thing: the wordmark at weight 400 with ~4px BRAND tracking, reasoning recorded in the code at `synapse_panel.py:398-400` — *"hierarchy comes from tracking and position, never from weight."* Keep it.

**Everything else is blocked on a token collision, and L4 was RIGHT to block it.** Measured on HEAD 2026-07-27:

```
t.SIGNAL    "#00D4FF"  cyan   11 sites   panel/tokens.py:39
_ds.SIGNAL  "#8FB3D9"  blue   21 sites   panel/designsystem/tokens.py:29
CONIFEROUS                     0 refs    never landed
```

**The panel renders TWO DIFFERENT ACCENT BLUES** depending on which import a given line reached for. Mile 7's de-cyan pass converted 20 of 31 sites AT THE CALL SITES — its own comment says *"token sources stay untouched (local fix)"* — so both modules still declare `SIGNAL`. Painting a palette over that produces a third state, not a fix.

=== WHAT CHANGED THAT MAKES THIS RUNNABLE NOW ===

When this was queued it was gated on the panel having a test surface. **That gate is now satisfied.** RES eliminated the fake-hou/PySide residency and R30 established `tests/panel/` runs under hython3.13 — 27 passed. Panel changes are verifiable for the first time in this project's history.

=== WORK ===

1. **Resolve the collision AT THE SOURCE.** Convert the remaining 11 `t.SIGNAL` sites, then make `panel/tokens.py` RE-EXPORT from `panel/designsystem/tokens.py` rather than redeclare. One authority. **Fixing call sites again is what produced 20-of-31 and stopped** — do not repeat it.

2. Resolve the `SIZE_HERO` branch: `tokens.py:59` sets 44 and `tokens.py:83` sets it to 19. Two live assignments in one module. Determine which is live, document it, remove the other.

3. Land `CONIFEROUS = "#6E8F72"` and migrate the `OK_SOFT` call sites. Two accents per view remains the ceiling; the render is the only chromatic event.

4. Re-run L4's ungraded 40%. Its receipt records that portion of its gate as never graded — the hython-offscreen permission was granted after that leg had started. It is granted now.

5. Screenshot-diff every view before and after into `design/repair_h4/`.

=== EXPLICITLY OUT OF SCOPE ===

- **The v3 design study** — halt-is-the-mark, two surfaces, the Voronoi cook grid. R29 established a working, honest Stop already exists in the live rail and `_on_stop` refuses to claim idle. Do NOT replace it, do NOT make Stop always-visible — state-gating is correct, and a Stop shown when nothing is running is the same lie as a consent gate that does not gate (R18).
- **Removing ORPHAN affordances.** 17 of them, its own leg, its own evidence.
- **Anything in `python/synapse/memory/`.** U1 owns that surface (R92).

=== ORACLE ===

```
grep -c 't\.SIGNAL' python/synapse/panel/*.py        ->  0
panel/tokens.py re-exports and declares no colour of its own
every token name present before is present after
  (assert against harness/notes/panel_token_inventory_before.json - R20)
pytest -k panel      ->  0 failed, count strictly increases
no import of routing/ or server/ added to panel/
```

Every pin fails against its mutation (R34). **Any reader you write to inspect tokens is itself calibrated (R60)** — H1 proved an uncalibrated AST reader makes five pins pass vacuously, and a token reader that cannot see a redeclaration would pass this leg while the collision survived.

=== STANDING ===
Never push, never merge, never tag. **Commit your product on your own branch BEFORE writing the receipt** — R93: `green` with zero commits is not a permitted terminal state.
Write harness/notes/receipts/H4.json (receipt/v1, model + settings_profile per R25).
