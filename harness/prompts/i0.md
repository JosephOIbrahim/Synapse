You are ORCHESTRATOR for I0 — scout the shipped node reference. Read harness/AGENT_CONSTITUTION.md first; it binds you. Then read harness/SYNAPSE_INGEST.md and docs/H22_FRONTIER.md.

**READ-ONLY**, fenced under harness/readonly-settings.json. You write harness/notes/** and nothing else. **You are not building the extractor. You are measuring the archive so that I1 can build one that joins.**

=== WHY A SCOUT AND NOT JUST AN INGEST ===

`$HFS/houdini/help/nodes.zip` holds 5,034 pages, version-pinned to 22.0.368. **That is a PAGE count, not a KNOWLEDGE count**, and an extractor written against an assumed structure produces a corpus that looks complete and joins to nothing.

Three measured findings say this risk is real, not theoretical:

  H9-F1  385 documented parameter IDs are WRONG as names, while their LABELS
         resolve correctly. A corpus keyed on #id would carry 385 entries that
         match no live parm - and would not fail loudly, it would just miss.
  H9-F3  Cop2 pages carry an explicit #id on 7.7% of parameter records. Only
         13 of 139 pages have even ONE.
  H7-F4  An `:include:` banner made an ENTIRE deprecated subsystem read as
         current, because H5's reader did not follow the banner.

=== THE QUESTIONS ===

**Q1 - STRUCTURE.** Is the page format consistent across `cop/`, `lop/`, `sop/`, `out/`, `top/`? Sample generously - at least 20 per context - and report where a single parser would break. **One parser per context is a fine answer; discovering that after writing one is not.**

**Q2 - THE JOIN KEY.** For parameters: is it `#id`, the label, or something else? Test against the LIVE runtime - take 15 nodes, pull their documented parameters, and compare to `node.parms()` on 22.0.368. **Report the match rate for each candidate key.** H9 says label wins; confirm or refute it rather than inheriting it.

**Q3 - COVERAGE OF THE 161.** `news/22/copernicus.json` names 161 new Copernicus nodes by node path. How many have a `cop/<name>.txt` in the archive? **If it is 161, the ingest is mechanical. If it is 90, the remainder need runtime probing and that is a different leg.** Name the absent ones.

**Q4 - THE QUALITY FLOOR.** Define one and justify it — a summary AND at least one documented parameter is a reasonable start. Then report **how many pages CLEAR it versus merely EXIST**, per context. That gap is the number this whole harness turns on.

**Q5 - RELATIONSHIPS AND BANNERS.** Do pages carry "See also", example references, or `:include:` directives? The banners matter most: H5 read a deprecated subsystem as current by ignoring one. **Report every directive shape you find, whether or not you think it matters.**

**Q6 - THE DEPRECATION AXIS.** R72: deprecation is the union of runtime `deprecationInfo()` and authored help, and they DISAGREE. How many pages state a deprecation? Cross-check a sample against the runtime and report the disagreement count in both directions — doc-says-runtime-does-not, and runtime-says-doc-does-not.

=== WHAT YOU ARE NOT DOING ===

Not writing the extractor. Not building a corpus. Not wiring anything into RAG. **If a question cannot be answered from the archive, say so and name what would answer it** — a scout that guesses is worse than one that reports a gap.

=== ORACLE ===

```
harness/notes/ingest/I0_SCOUT.md
Q1-Q6 answered, each with the sample size it rests on
Q2's join-key match rate measured AGAINST THE LIVE RUNTIME, per candidate key
Q3 as integers: present / absent, with the absent ones named
Q4 as integers per context: exists / clears-the-floor
every reader you write is calibrated - shown returning a KNOWN answer before
  it is trusted on unknown ones (R60)
```

=== STANDING ===
Probes beat memory. Never push, never merge, never tag.
Write harness/notes/receipts/I0.json (receipt/v1, model + settings_profile per R25).
