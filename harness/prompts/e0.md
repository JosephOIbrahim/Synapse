You are ORCHESTRATOR for E0 — settle what a SYNAPSE turn actually costs. Read harness/AGENT_CONSTITUTION.md first; it binds you. Then harness/SYNAPSE_ECONOMIST.md and the C1 receipt at harness/notes/receipts/C1.json.

**READ-ONLY**, fenced. You write harness/notes/** and nothing else. **You are not reducing anything. You are establishing whether the thing everyone plans to reduce is actually being charged.**

=== THE QUESTION, AND WHY IT COMES FIRST ===

The economist blueprint's first mile is T.1: cut the tool surface from **17,310 tokens to ≤ 2,000**, because *"you cannot be a credible token economist while your own tool surface runs 8.6x over its ceiling."*

**The reasoning is right. The number may be measuring the wrong thing.**

Anthropic charges a prompt-cache WRITE at 1.25x and a cache READ at 0.1x. Tool definitions are the most cache-stable content in any request — byte-identical every turn, at the front of the prompt, exactly where a cache prefix wants to sit.

```
uncached        17,310 per turn      8.6x over ceiling
cached (read)   ~1,731 effective     UNDER the ceiling
```

**Those are opposite engineering programmes** — a months-long surface reduction, or a cache-control header. Nobody has measured which one applies.

=== THE QUESTIONS ===

**Q1 — IS THE TOOL SURFACE ACTUALLY 17,310 TOKENS?** C1 measured with a PROXY tokenizer because the API account had no credits (C1-F2). Check whether `count_tokens` works now — `harness/notes/_credit_probe.py` answers that in one call. If it does, **re-measure exactly.** If it does not, say so and keep the proxy figure labelled as one.

**Q2 — IS ANYTHING CACHED TODAY?** Trace the request path from the panel to the API. Does SYNAPSE set `cache_control` on any block? Report the answer as a file:line, not an impression. **Absence is a finding and a cheap fix; presence changes T.1's whole premise.**

**Q3 — WHAT IS THE PER-TURN COST, BROKEN DOWN?** System prompt, tool definitions, conversation history, scene grounding. **Which of those four is actually the largest?** T.1 assumes it is tools. C1 measured grounding payload rising to 113,411 tokens at 25,850 nodes — that is 6.5x the entire tool surface, and it is not cacheable because it changes every turn.

**If grounding dominates, T.1 is optimising the second-biggest line item.** Report the ranking.

**Q4 — WHAT IS THE CACHE-STABILITY PROFILE?** For each of the four segments: does it change between turns in a session? A segment that changes cannot be cached, and knowing which ones are stable tells E2 exactly where a cache boundary belongs.

**Q5 — WHAT WOULD THE CEILING BE IF CACHING WERE ON?** Given Q1-Q4, compute the effective per-turn cost with a cache prefix over the stable segments. **State the assumptions as assumptions.** This is the number that decides whether E2 is a reduction leg or a header change.

=== WHAT YOU ARE NOT DOING ===

Not reducing the tool surface. Not editing the request path. Not touching the panel. **If the answer is "T.1 is unnecessary", say so plainly** — that is the most valuable result this leg can produce, and it is the same shape as V1 refuting the scoped-delta primitive before four legs were built on it.

=== ORACLE ===

```
harness/notes/econ/E0_COST.md
Q1  exact or proxy, LABELLED as which, with the producer
Q2  file:line for cache_control, or an explicit "absent, searched X"
Q3  the four segments ranked by measured size, not assumed
Q4  per-segment stability across at least 3 real turns
Q5  the effective-cost figure with its assumptions named
every reader calibrated before trusted (R60); controls mutation-tested (R133)
producers named econ_<name>.py - NO leading underscore, .gitignore drops those
```

=== STANDING ===
Probes beat memory. Never push, never merge, never tag.
Write harness/notes/receipts/E0.json (receipt/v1, model + settings_profile per R25).
