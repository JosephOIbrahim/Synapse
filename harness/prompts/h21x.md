You are ORCHESTRATOR for H21X — migrate the corpus prose to H22. Read harness/AGENT_CONSTITUTION.md first; it binds you. Then read ruling R99, especially its Layer 3.

=== WHY ===

Observed live 2026-07-27, on a 5,764-node explain of `karma_user_guide.hip`, SYNAPSE said:

> *"a massive reference/documentation file that SideFX ships with **Houdini 21**"*

The build is 22.0.368 and has been since 15 July.

R99 fixed the DATA layer this morning: scout now loads `h22_symbol_table.json`, the injector runs at panel init, and the phantom gate reads ARMED. **Layer 3 — the prose the model actually reads — was never touched.** The corpus directory is still literally named `houdini21-reference`.

=== THE RULE, AND IT IS THE WHOLE LEG ===

**A find-replace here would be actively harmful.** Measured split, 235 references in `rag/`:

```
KEEP  127   TRUE PROVENANCE - a record of what was observed, where, when
FIX   108   STALE SCOPE     - a claim about what this corpus COVERS
```

**The test:** does the reference describe **what was observed** (KEEP) or **what this covers** (FIX)?

```
KEEP  "Part 2/18 of the SideFX MPM H21 Masterclass by Alex Wevinger"
      -> a true citation of real H21 material. Rewriting it to 22 FALSIFIES A
         CITATION OF SOMEONE ELSE'S WORK.
KEEP  "h21_mpm_masterclass_scenes_v01.hip"     -> an actual filename
KEEP  "verified live on Houdini 21.0.596"      -> accurate history

FIX   "Complete OpenCL kernel reference for Houdini 21 Copernicus"
FIX   "What is neural point surface in Houdini 21?"
```

**When in doubt, KEEP.** A stale scope claim is a cosmetic defect. A falsified provenance record is the thing this repository has spent a week making impossible, and it would be self-inflicted.

**And a third option exists and is often right: VERSION-NEUTRAL.** "Copernicus OpenCL kernel reference" needs no version at all, and a corpus entry that does not assert a version cannot go stale again. Prefer it over asserting H22 for anything not re-verified against 22.0.368.

=== WORK ===

1. **Classify all 235 before changing any.** `harness/notes/_h21_split.py` is a first pass and its regex is APPROXIMATE - use it as a starting point, not an oracle. Report your own counts and where you disagreed with it.

2. **Fix the 108-ish stale claims.** Version-neutral by default; H22 only where you can point at something confirming it holds on 22.0.368.

3. **Rename the corpus directory** `rag/skills/houdini21-reference` -> `rag/skills/houdini-reference`. Version-neutral deliberately - naming it `houdini22-reference` guarantees this same leg exists again in a year. **It is LOAD-BEARING in code**, at least:
   - `python/synapse/cognitive/tools/scout_ingest.py:65` and `:192`
   - `python/synapse/core/solaris_guardrails.py:6`
   Find the rest yourself; those three are what one grep showed.

4. **Update scout.py's 13 H21 docstring lines.** They describe the tool as scouting *"the Houdini 21.0.671 documentation RAG"*. It does not - it scouts whatever corpus is present, against an H22 symbol table.

=== THE CONTROL, MANDATORY ===

**After your run, every KEEP reference must be BYTE-IDENTICAL.** Snapshot them before you start and diff after. A leg that quietly "tidied" a masterclass citation has done the exact harm this brief exists to prevent, and the diff is the only thing that proves it did not.

Report: KEEP unchanged (count), FIX changed (count), and any reference you reclassified from the first pass with your reasoning.

=== ORACLE ===

```
the 127 KEEP references byte-identical, PROVEN by diff
the stale scope claims corrected or made version-neutral
rag/skills/houdini-reference exists; no code path references the old name
scout.py carries no false H21 self-description
pytest -k "scout or rag or ingest" -> 0 failed
gate suite holds at 5031+ with 0 failed
```

=== STANDING ===
Commit product before the receipt (R93). Never push, never merge, never tag.
Write harness/notes/receipts/H21X.json (receipt/v1, model + settings_profile per R25).
