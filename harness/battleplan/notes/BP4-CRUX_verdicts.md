# BP4-CRUX — Verdicts for wave BP4

**Referee:** BP4-CRUX (adversarial crucible, branch `bp4/crux`, tier referee = claude-fable-5-1 per `harness/battleplan/runs/2026-09-03/preflight_bp4.json`) · 2026-09-03
**Method:** every builder receipt's acceptance array independently re-run in a **fresh `git clone --shared -c core.longpaths=true` scratch clone** of the leg branch (never the builder's worktree, never the main tree): `scratchpad/{i,r,b,s,p,u}` for the six leg branches plus `scratchpad/m` (master) and a `git archive 2574de6b docs/intake` for the INTAKE harvest target. Six lanes ran in parallel as an agent team (INTAKE, RULINGS on sonnet; B7FIX, SPATIAL, PANELFONT, USDKNOW on opus), each writing a report JSON the referee read in full; the referee additionally re-derived every headline number itself from git objects and full-package archives before ruling (marked **[ref]** below). Probes and tests re-run on **pinned hython 22.0.400** (`SYNAPSE_HYTHON` set; 22.0.429 also installed and never used) with `HOUDINI_USER_PREF_DIR=C:/Users/User/OneDrive/Documents/houdini22.0`. **37 self-authored mutations** (ledger: `BP4-CRUX_mutations.json`), every one run against a crux-green baseline, restored with `git show HEAD:<path> > <path>`, and the baseline re-confirmed green. Every verdict row carries the crucible's own anchor, never the builder's. gui_required acceptance is UNKNOWN — unobtainable headless, said plainly.
**Crucible artifacts (durable, text only):** `harness/battleplan/notes/bp4_crux/{intake,rulings,b7fix,spatial,panelfont,usdknow}/` — 100+ files: crux stdout/probe_results, EXR stats from three readers, pytest logs, capture JSONs, per-row verbatim tables, mutation logs. Binaries (EXR/USDC) stayed in the scratch clones (public-autopush policy).
**Audit targets:** bp4/intake @ 0d738db9 (product 03a4e43d) **and** master @ 2574de6b (the R135 harvest of the same leg); bp4/rulings @ a62267f9 (product ff3d6f73); bp4/b7fix @ 88ab1184 (product b3e24d14); bp4/spatial @ 83cca627 (product c48e26fa); bp4/panelfont @ 4b3b3967 (product 81f3fb08); bp4/usdknow @ 73905537 (product a0cc33b1). Common base 28a0e183. Master moved to 3a27d1ff (tests/test_rails.py fix + receipts_bp4.py) at 18:46 during the audit; it touches none of the audited surfaces (**[ref]** `git diff --stat 28a0e183 master -- <probe script, review doc, python/synapse/>` empty).

## Summary

| Leg | Verdict | chain_broken_at | Why not SOUND |
|---|---|---|---|
| BP4-INTAKE | **SOUND-WITH-NITS** | none | leg bus value `dossier_in_repo: true` overclaims (both .docx missing; the harvest post says `partial`); two receipts for one leg, the master one falsely says no leg branch exists; MANIFEST pointers :18/:377 off by one |
| BP4-RULINGS | **BROKEN** | **T1 extraction — `harness/notes/receipts/BP3-CRUX.json:for_ruling[0]` never read** (the leg searched `BP3-CRUX_verdicts.md` instead); table 21 rows vs crucible recount **22**; acceptance row 1 fails on the crucible's recount | — |
| BP4-B7FIX | **SOUND-WITH-NITS** | none | verdicts (D2.4 PASS / R-1 CLEAR) reproduce on crux evidence, but the review's per-component causal story is refuted by crux controls: dropping the camera bind does **not** go black (the brief's expected signature), dropping the light does not either; only the pre-fix function goes black |
| BP4-SPATIAL | **SOUND-WITH-NITS** | none | every number exact, but the recorded timing is the wrong measurement (tool `seconds` starts after the triangle gather; a 6 s stall survives every timing assert), the registration pin has two evasions, and classes stop partitioning above 45° |
| BP4-PANELFONT | **SOUND-WITH-NITS** | none | 1 gui_required acceptance UNKNOWN (constitutional ceiling); acceptance 2 holds only as the builder scoped it (design-system authority) — 166 hardcoded typography literals remain across 22 panel modules, disclosed and sent for ruling; the `-k panel` acceptance command deselects the design-conformance guard |
| BP4-USDKNOW | **SOUND-WITH-NITS** | none | probe identical line-for-line and 15/15 anchors hold on the crux's own run, but the checker matches by substring (three false-assertion mutations survive), the probe self-finds the fixture through a hardcoded absolute path, and the fixture nests component inside component |

**One leg is BROKEN: BP4-RULINGS does not ride as-is.** The break is a single missing row plus its count line — a mechanical fix (spelled out under the RULINGS section), not a re-do. **The other five ride.** A green CRUX receipt is a PRECONDITION for Joe's merge words, never a substitute — these verdicts are for reading before merge words fire.

---

## BP4-INTAKE — SOUND-WITH-NITS

Scratch: clone of `bp4/intake` @ 0d738db9 (product 03a4e43d) **and** archive of master @ 2574de6b (the harvest). All 3 acceptance rows re-ran **pass** on **both** targets with crux anchors.

| Acceptance | Builder | Crux | Crux anchor |
|---|---|---|---|
| MANIFEST row per present file (bytes + sha256 + role) + 'missing' row per absent one | pass | **pass** (both targets) | **[ref]** python hashlib straight from git objects: `bp4/intake:docs/intake/src/synapse_worldlabs_blueprint.md` 7155 B `9c08a168…c5edf9`, `…coffee_shop_talk.md` 6384 B `903fb2f4…f36cd98`; identical bytes+hash at `2574de6b:`; both rows match MANIFEST.md:7-8 on both refs; both .docx absent on disk with `missing` rows (MANIFEST.md:9-10). Lane re-derivation on the scratch trees agrees (`bp4_crux/intake/T-A_hash_verification.txt`). MANIFEST.md does not self-list (2160 B on the branch, 2678 B on master with the harvest note) — by design |
| blueprint header gained exactly one Sources line; diff vs master confined to the header block | pass | **pass** (both targets) | **[ref]** `git diff 28a0e183 bp4/intake -- docs/intake/blueprint-h22-worldlabs-intent.md` = one hunk `@@ -8,6 +8,7 @@`, +1 line at new-file line 11 (`**Sources (intake):** …`); first `---` separator at line 13 → 11 < 13. `git diff 2d26231f 2574de6b` on master: byte-identical hunk (`diff` of the two diffs = IDENTICAL). `git diff --stat` per target: branch = MANIFEST.md + blueprint + receipt only; master 2d26231f..2574de6b = MANIFEST.md + blueprint (`T-B_diff_analysis.txt`) |
| bus finding posted with dossier_in_repo value and MANIFEST anchor | pass | **pass** (both) — value honesty is a nit | **[ref]** `harness/battleplan/bus/bp4/bus.jsonl` n=18d1ef1fca958b70 18:10:46 `dossier_in_repo: true` anchor `docs/intake/src/MANIFEST.md` (the leg); n=18d1f0b17fcd6908 18:39:32 `dossier_in_repo: partial - … both .docx missing` (the harvester). Posted-with-value-and-anchor holds for both; only the second value is accurate |

**Crucible's own cross-check of the MANIFEST pointer table:** **[ref]** `grep -n -i "dossier|coffee"` over the branch blueprint puts the cited content at lines 8, **19** and **378**; MANIFEST.md cites :8, :18, :377. Lines 18 and 377 are blank on the branch. Cause is visible in the diff: the pointers were computed on the base file, then the Sources line was inserted at 11 and shifted everything below it by one; line 8 sits above the insertion and stays right. Section-level claims ('Found' / 'Partial match') are honest.

**Mutations (3/3 REDDENED, ledger INTAKE-M1..M3):** hash-cell flip → hash recompute reddens; line inserted outside the header (line 16) → the range-aware header check reddens, **but a naive `grep -c '^@@'` hunk count SURVIVES it** because git's 3-line context folds the line-11 and line-16 edits into one hunk (`-U0` separates them) — the predicate's own wording ("the crucible reads the hunk's line range") is load-bearing; bytes-cell change → reddens.

**NITS (verdict-neutral, recorded for ruling):**
1. The leg's bus value `true` contradicts its own receipt (`findings[0].cross_checks.missing_docx_files: 2`, BP4-INTAKE.json:33 on the branch). The accurate value is `partial`, which the harvester posted 29 minutes later.
2. Two receipts for one leg. The master harvest receipt (`harness/notes/receipts/BP4-INTAKE.json` @ 9a90e9af, field `branch`) says *"master - no worktree, no leg branch"* — false: **[ref]** `git log --format='%h %ci' bp4/intake` → 0d738db9 2026-09-03 18:12:25 -0400 and 03a4e43d 18:12:01, i.e. 27 minutes **before** the 18:39:31 harvest commit 2574de6b. The same receipt's third finding correctly describes a second writer, so this reads as a drafting slip, but the two receipts disagree on status (green vs green_with_findings) and head (03a4e43d vs 2574de6b). Which one is canonical is Joe's/CTO's word.
3. The harvest note's *"byte counts were wrong (7168 / 6451)"* is unverifiable from git: both committed MANIFEST.md states (03a4e43d, 2574de6b) already carry 7155/6384 (`git log --all -- docs/intake/src/MANIFEST.md` = exactly those two commits). Not false — an uncommitted intermediate — but it is a claim with no repo anchor.
4. Pointer rows :18 / :377 off by one (above).

---

## BP4-RULINGS — BROKEN (chain_broken_at: T1 extraction — BP3-CRUX.json never read)

Scratch: clone of `bp4/rulings` @ a62267f9 (product ff3d6f73). Acceptance row 1 **fails** on the crucible's recount; rows 2 and 3 pass; the builder's extra row (capsule mapping) passes.

| Acceptance | Builder | Crux | Crux anchor |
|---|---|---|---|
| table row count equals the for_ruling total the crucible recounts; count line present | pass | **FAIL** | **[ref]** python over `harness/notes/receipts/BP3-*.json` on master and in the scratch: CORPUS 7, **CRUX 1**, PANEL 1, PROBE 5, RECON 3, STUBS 2, TIDY 3 = **22**; no `banked`/`open`/`for_joe` keys anywhere. `git ls-tree 28a0e183 -- harness/notes/receipts/BP3-CRUX.json` → blob 8d2c48d6 — the file existed at the leg's base (landed on master 2d1d22c0, 16:56). Product `CTO_RULINGS_BP3.md`: 21 summary rows, 21 `### Row` sections, count line *"rows: 21 (expected 22; … CRUX 0 …)"*. Lane recount by two methods agrees (`bp4_crux/rulings/RULINGS_crux_evidence.md`) |
| every row has claim + anchor + recommendation cells; every ruling cell is PENDING | pass | **pass** (for the 21 rows that exist) | **[ref]** verbatim membership of each details blockquote in its anchored `for_ruling[i]`: 20/21 exact string members; row 4 (PANEL) is the `item` field of a dict entry `{"item": …, "context": …}` — exact to that field (lane confirms 21/21 under that reading). Summary-table `\| PENDING \|` cells 21/21; details `**CTO Ruling:** PENDING` 21/21. Anchors `BP3-<LEG>.json:for_ruling[i]` all resolve. Truncated summary cells are prefixes of the verbatim claims (lane, 21/21) |
| bus finding posted with the row count and the file anchor | pass | **pass** | **[ref]** bus.jsonl n=18d1ef4f2f56f2b8 18:14:10 `rulings table compiled: 21 rows (expected 22; CRUX yielded 0 distinct items)` anchor `harness/notes/CTO_RULINGS_BP3.md` — present; it propagates the wrong root cause to the wave-visible bus |
| (builder-added) capsule recommendations mapped to rows | pass | **pass** | lane: M-1→1, M-2→2, D-DEP-03→3, PANEL-SCOPE→4, TIDY-R1→19, ids match |

**Where the chain broke.** The brief's sources are *"harness/notes/receipts/BP3-*.json for_ruling arrays … ; BP3-CRUX_verdicts.md; BP3_TIDY.md"*. The leg read six of the seven JSON receipts — its own finding anchors `BP3-{RECON,PANEL,PROBE,CORPUS,STUBS,TIDY}.json` — and looked for the CRUX contribution in the verdicts markdown, which carries verdict prose (its L97 "Confirmed for-ruling" paragraph restates the STUBS item), not a `for_ruling` array. It then reported *"CRUX expected 1, found 0"* as a finding. Reporting the delta was the right reflex; the count is still wrong on the facts, and the first acceptance predicate is written against **the crucible's** recount.

**The missing row, verbatim** (`harness/notes/receipts/BP3-CRUX.json:for_ruling[0]`): *"Confirmed and re-anchored the BP3-STUBS ruling item: the lane diff's verbatim §3.2 contract path 'schemas/world_manifest.schema.json' (BP3_lane_spatial.diff L19) points at a directory with 0 tracked files; actual schema_home is docs/intake/ (blueprint L9 Sidecars line already agrees). The verbatim lane text must be reconciled BEFORE the diff is ever applied. The diff itself remains unapplied and apply-check-clean."* It is a distinct entry (a confirmation with its own anchor), even though it concerns the same tension as row 17.

**Fix path (mechanical, one re-run of the leg, no ruling required to execute):** add row 22 (leg CRUX, an id such as `CRUX-1`, the claim above verbatim, anchor `BP3-CRUX.json:for_ruling[0]`, recommendation `none stated`, CTO ruling PENDING); update the count line to `rows: 22 (expected 22; … CRUX 1 …)` and drop the delta note; correct receipt finding #3; and while there, the nits below.

**Mutations (3/3 REDDENED, ledger RULINGS-M1..M3):** one word changed in row 6's claim → verbatim check reddens for exactly that row; row 9 ruling cell PENDING→ACCEPT → all-PENDING count reddens (details count stays 21/21, proving the two counters are independent); one summary row deleted → table-vs-details consistency reddens.

**NITS (fold into the fix):**
1. The summary table has **8 columns, no `severity` column**; the brief's T1 header has 9 (`… | item id | severity | claim (verbatim) | …`). The claim column is labelled `claim (truncated)`; verbatim text exists only in the details section.
2. Row 17's item_id is a claim fragment (`D3.1 lane diff is a proposal (ratified`) — a first-colon extraction caught the colon in `ratified:false`.
3. The file is **not valid UTF-8**: byte 0x97 at offset 14 (cp1252 em dash in the title) and 0xb7 at offset 74 (cp1252 middle dot), while `§` elsewhere is proper UTF-8 `C2 A7`; `open(path, encoding='utf-8')` raises. **[ref]** confirmed by byte scan.
4. Receipt finding #3 claims *"M-1, M-2, D-DEP-03, D3.1, D3.2 marked yes"*; rows 17 and 18 (D3.1 / D3.2) read **no** in the committed table.
5. Judgment, not a predicate: rows 10/11/12 (CORPUS tier promotions) and 20 (TIDY-R2, a settings-fence edit) match the brief's ratification=yes rule verbatim yet carry `no`, inconsistent with how rows 1–3 were classified.

---

## BP4-B7FIX — SOUND-WITH-NITS

Scratch: clone of `bp4/b7fix` @ 88ab1184 (product b3e24d14). All 4 acceptance rows + the append-only crucible criterion re-ran **pass** on crux evidence. **Crux D2.4 = PASS, crux R-1 = CLEAR**, on the crux's own EXR read by three independent tools.

| Acceptance | Builder | Crux | Crux anchor |
|---|---|---|---|
| probe-script diff limited to the B-7 block + --only plumbing | pass | **pass** | **[ref]** hunk→def map over the NEW file: hunk 1 (366–403) opens at `@probe("B-7")`/`def b7`; hunk 2 (404–411) inside `b7` (context ends at `def b8`); hunk 3 (493–534) opens on `s3`'s final `return` as context, its added lines (PROBE_DEPS, `_resolve_run_set`, `main`) sit after the `# --- main` banner at module level. Lane `-U0` map: 10 hunks, 4 in b7, 6 in the main region, zero in any other `@probe` (`bp4_crux/b7fix/hunk_audit.txt`). `git diff --stat 28a0e183 HEAD` = 9 files, none under python/synapse/. `--only` plumbing is behaviour-neutral for the default `P,B,S` case (all 22 ids resolve; the NOT_RUN branch never fires) |
| bp4_b7fix/stdout.txt exists with the 22.0.400 build line and the B-7 block | pass | **pass** | builder stdout:5 `Houdini: 22.0.400`; :68 B-7 header; :70 authored light; :71 render camera bound; :76 EXR written 530347 B; :96 `B-7 RAN 5.84` (crux read, 102 lines) |
| EXR stats recorded with the command; D2.4 verdict pass\|fail\|UNKNOWN with anchor | pass | **pass** | review §13.3 states the command verbatim; §13.5 row 1 `D2.4 … PASS` anchored to exr_stats.txt + exr_info.txt + stdout:68-76 (**[ref]** grep of the appended section). Crux reproduced non-zero RGB independently (below) |
| R-1 status stated (triggered\|clear\|UNKNOWN) with anchor in the appended section | pass | **pass** | review §13.5 row 2 `R-1 … TRIGGERED → CLEAR` with anchors (**[ref]** grep) |
| crucible criterion: review doc append-only, BP3 rows byte-identical | (claimed) | **pass** | lane python byte compare: base 19461 B, HEAD 25406 B, `head.startswith(base)` True, appended section begins at byte 19461, single hunk `@@ -195,3 +195,66 @@` (`append_only_check.txt`); **[ref]** master has not touched the doc since base |

**Crux re-run (own out dir `scratchpad/b/crux_b7_out`, pinned hython, pref dir set):** exit 0, 9 s process wall, probe total 6.3 s, B-7 5.46 s (builder 5.84). **[ref]** fixture hashes recomputed: PLY 34000416 B `be734a30…beb65`, GLB 959216 B `08e75e0a…eb00c9` — match. Status diff vs builder `probe_results.json`: **identical, 22 ids, 6 RAN (P-0, P-3, B-1, B-3, B-6, B-7) / 16 NOT_RUN.** Crux EXR (oiiotool 3.0.6.1 = hoiiotool 22.0.400 byte-identical = hython OpenImageIO+numpy): 1280×720 RGBA half, RGB max **0.455811**, avg **0.013786**, stddev 0.064177, alpha avg 0.035126, 69802 px with non-zero RGB, 0 NaN/Inf; husk: render_camera `/cameras/wl_cam`, environment lights 1 ("Total Lights: 1"), points 500000, polygons 0, Converged 100 %, **ttfp 0.65083 s**, shader_calls.surface 0 (`bp4_crux/b7fix/exr_stats_*.txt`, `exr_render_stats_summary.txt`). Builder vs crux: every categorical field identical; RGB max 0.452881 vs 0.455811, ttfp 0.662 vs 0.651, EXR 530347 vs 530296 B — Karma XPU run-to-run noise, verdict-neutral.

**Mutations (ledger B7FIX-M1, M1b, M1c, M2, M3 — 3 REDDENED, 2 SURVIVED and the survivors are the finding):**
- **M1 (brief-specified: drop the camera bind) — SURVIVED.** With `kc.set(cam_path)` neutralised and the light kept, the EXR is still non-black (max 0.457031, avg 0.013787); husk printed 5× "No render camera defined", `render_settings.rendercamera` = `/cameras/camera1` (the BP3 mismatch state), yet `render_camera` resolved to `/cameras/wl_cam` and the world_to_camera / world_to_screen matrices are byte-identical to the bound run. **The bind silences six error lines and changes no pixels on this fixture** (the authored camera sits at the default transform). The brief's expected signature "must go black again" does not hold on 22.0.400.
- **M1b (crux control: remove the light, keep the bind) — SURVIVED.** Probe printed "render will be unlit"; husk reported `light_types.distant = 1`, "Total Lights: 1": Karma XPU 22.0.400 substitutes a default distant light in this topology. Max 0.618652, non-black.
- **M1c (crux control: pre-fix BP3 `b7` spliced back verbatim, --only plumbing kept) — REDDENED.** Flat black: max/avg/stddev 0.000000, alpha avg 0.035138 (same geometry, unlit), husk "Total Lights: 0", 5 errors, 209447 B. **The BP3 black reproduces on 22.0.400 and the fix as a whole flips it.** The builder did not run this control; it is the strongest single piece of evidence in the leg.
- **M2** (a `pass` inside `b3()`) → hunk audit reddens (attempt 1 reddened for the wrong reason — a base..HEAD diff cannot see a worktree edit and re-mapped the b7 decorator hunk onto b6 by line shift; recorded, checker given a worktree mode, attempt 2 reddened for the right reason). **M3** (one char in a BP3 row) → byte-identity reddens at byte 4026.

**Findings for ruling (verdict-neutral to D2.4 / R-1, not neutral to the review's prose):**
1. **The review's causal story is wrong in both named parts while its verdicts are right.** §13.1/§13.5 attribute the flip to "once a camera + light are present". Across baseline / M1 / M1b / M1c the only variable that co-varies with black is **husk Total Lights == 0**, which arises only under the BP3 topology — `usdrender_rop` fed from the camera LOP downstream of the KRS. The builder's diff did change that topology (`rop.setInput(0, cam)` → `rop.setInput(0, krs)`) but the review names it nowhere as causal. Recommended amendment (D-1's word, one sentence in §13.1/§13.5): the composite rewiring restores lighting; the camera bind removes the husk errors without changing pixels.
2. The leg's D2.4 check cannot discriminate the fix from its absence at the per-component level (M1, M1b survive; only M1c reddens). The composite verdict stands; the per-component narrative does not.
3. Nondeterministic numbers quoted at full precision in §13.3 (a byte count, a 6-decimal max) — reproducible only categorically.
4. Committed `exr_stats.txt` ends `Monochrome: No`, which reads as contradicting the monochrome nit; it is an alpha artefact (`--ch R,G,B` → `Monochrome: Yes`). The nit itself is **understated**: R, G, B are pixelwise identical on all 921600 px (max |R−G| = 0.0) — solid support for the held BP4-B7SPLAT-COLOR spawn.
5. `exr_info.txt` was produced by the OIIO `iinfo` on PATH, not Houdini's `iinfo.exe` (different metadata key format); §13.4 does not name the tool.

---

## BP4-SPATIAL — SOUND-WITH-NITS

Scratch: clone of `bp4/spatial` @ 83cca627 (product c48e26fa). All 4 acceptance rows **pass**; **14 passed / 0 failed / 0 skipped** in 3.19 s on hython 22.0.400 (so no row falls to UNKNOWN); every published number reproduced **exactly**, not merely within tolerance.

Binding proof (the trap is stronger than briefed): unbound, `hython -c "import synapse.spatial"` → ModuleNotFoundError (master has no spatial package); **PYTHONPATH alone does not bind under hython** because the Houdini package inserts the main-tree paths at `sys.path[6]`/`[7]`, ahead of PYTHONPATH at `[9]` — helpers need `sys.path.insert(0, …)`; pytest binds from the scratch root via `pyproject.toml pythonpath=["python"]`. A green fixture-anchored run is therefore only possible against the scratch tree (`bp4_crux/spatial/binding_*.log`).

| Acceptance | Builder | Crux | Crux anchor |
|---|---|---|---|
| three tools return correct answers on the fixture per T2 tolerances | pass | **pass** | crux `crux_capture.py` (own triangle gather with `Gf.Matrix4d.Transform` per point, own fan triangulation, own leftHanded flip): describe bounds == B-3 to **0.0** on all six components (tolerance 1e-3; a second BBoxCache-free bbox agrees to 0.0); classify at 20/35/45 == published S-2 **on all twelve counts, delta 0** (4919/26900/350/14824 · 6414/34415/963/5201 · 7733/37441/1819/0 on 46,993 tris); frustum inside **20146** == crux tan-formula oracle 20146 (tolerance 2 % = 939.86); floor z-span 99.149 % of the B-3 z-extent (bar 80 %); walls 22,664 at x<0 / 11,751 at x>0; dominant floor y 0.60076 == crux re-derivation, |Δ| 0.0 (bin width 0.1129). **[ref]** anchors re-read on master: stdout.txt:284 bbox; supplementary.txt v2 L91 bbox, L94-96 counts (`capture_out.json`) |
| each call < 5 s on the fixture collider (46,993 tris), recorded | pass | **pass** — on the crux wall clock; the recorded number is the wrong measurement | crux wall clock around the whole call: describe 0.00567 s, classify@45 **0.04274 s**, frustum **0.04704 s** → worst margin ~106× under budget. The builder recorded the tool `seconds` field (0.0051 / 0.0011 / 0.0009) which is initialised **after** `_collect_triangles` returns (queries.py:269-273, :343-347) and so excludes the dominant cost (34× understated for classify, 61× for frustum); the receipt's "~980× under" should read ~106× |
| tools run on a second stage without code change (D3.4) | pass | **pass** | crux ran all three tools on **three** stages of its own with the package untouched (`second_stage_out.json`): LOP `sphere` (native gprim, no Mesh) → describe SUCCESS / classify UNAVAILABLE / frustum UNAVAILABLE; sopimport of a box+grid merge (real Mesh) → SUCCESS ×3 with real numbers; `fixtures/solaris.basic.json` as the D3.4 test builds it → UNAVAILABLE ×3 (empty geometry, matching the builder). Read-only re-verified live: prim list byte-identical before/after |
| no default-on registration in mcp_server / tool registries | pass | **pass** — verified by the crux grep, not by the test | **[ref]** `git grep` on bp4/spatial: tool names appear only in docs/prose (blueprint :292-434, intake src :61-64), never in code. Lane grep wider: mcp_server.py, all six root `mcp_tools_*.py`, `python/synapse/server/`, `python/synapse/mcp/_tool_registry.py`, panel/providers/registry.py, science/registry.py — zero hits; `SYNAPSE_SPATIAL_LANE` read by nothing (`registry_grep.log`) |

**Mutations (ledger SPATIAL-M1, M2a, M2b, M2c, M3, M4, M5 — 3 REDDENED, 4 SURVIVED):** M1 floor threshold +1° → 3 classify tests fail, floor counts +141/+113/+153, and at 45° the counts stop summing to 46,993 (double counting, see nit 3) — REDDENED; M2a default-on import in `mcp_server.py` → test + crux grep both redden; **M2b the same line in root `mcp_tools_scene.py` → test SURVIVES** (it scans `python/**` + `mcp_server.py` only; crux grep catches it); **M2c aliased `from synapse import spatial as _sp; _REG = _sp.synapse_spatial_classify` inside the scanned root → test SURVIVES** (its name list omits `synapse_spatial_classify` and matches the literal `synapse.spatial`; crux grep catches it); M3 tolerance 1e-3 → 1e-9 SURVIVES — information: describe is byte-exact; M4 +0.01 on the max bound → both describe tiers redden; **M5 `time.sleep(6.0)` inside `_collect_triangles` → all eight timing asserts SURVIVE while individual tests take 6.04–13.06 s.**

**NITS / findings for ruling:**
1. **Timing pin is blind to the gather (M5).** Move `t0` above `_collect_triangles` (or add a `gather_seconds` and assert on the sum). Today's wall clock is ~106× under budget, so nothing is broken now.
2. **Registration pin has two evasions (M2b, M2c).** Scan the repo-root `*.py` too; match the bare `synapse_spatial_` token and `from synapse import spatial`. The review doc grep at L155/L157 shares the two-root scope.
3. **Classes stop partitioning above `max_angle_deg` 45** (floor `a < thr` and wall `90−thr < a < 90+thr` overlap once thr > 45; the default is exactly 45; a face at a == thr falls to slope). No test covers thr > 45.
4. The timing producer named in the review (`C:/Users/User/bp4_scratch/capture_out.json`) exists on this machine with numbers matching the crux, but lives outside the repository — a third uncommitted producer alongside the two fixture binaries the builder already flagged (F-3).
5. Info: native USD gprims (Sphere/Cube) are not Meshes, so classify/frustum honestly return UNAVAILABLE on a `sphere` LOP; describe still returns bounds. Documented behaviour, worth a docstring line.
6. Pre-existing, out of scope: `tests/conftest.py` labels `-k` deselections as "deselected by -m not needs_houdini".

Builder's F-1 (the .usdc proxy is a 4-face auto-proxy; the collider lives in the GLB) and F-3 (uncommitted fixture binaries) confirmed and re-anchored by the crux; both stay for ruling.

---

## BP4-PANELFONT — SOUND-WITH-NITS

Scratch: clone of `bp4/panelfont` @ 4b3b3967 (product 81f3fb08). 4 of 5 acceptance rows **pass** (one of them only as scoped — referee call below); 1 **UNKNOWN** (gui_required — constitutional ceiling).

Binding proof: unbound `python -c` resolves tokens.py to the master tree and raises `AttributeError: … no attribute 'FONT_FLOOR_PX'`; bound (`PYTHONPATH=<scratch>/python;<scratch>`) prints the scratch path and `FONT_FLOOR_PX: 10`; pytest runs printed rootdir = the scratch clone. (**[ref]** the referee first hit a subtler form of the trap: a `git archive` of only `python/synapse/panel` has no `synapse/__init__.py`, so the namespace-package candidate loses to master's regular package even at `sys.path[1]`; re-archived the whole package and got the branch tree — recorded under Cross-cutting.)

| Acceptance | Builder | Crux | Crux anchor |
|---|---|---|---|
| audit has typography rows per file:line and the floor's provenance | pass | **pass** | audit §3 = 32-row per-file table + file:line citations in §3a–3d; crux opened 10 cited lines, 10/10 accurate (e.g. context_bar.py:364 `font-size: 9pt; font-family: '{mono}'…`, hda_views.py:72, qss.py:52/107/216, synapse_panel.py:1837, gate_widget.py:182); §4 floor = UNKNOWN with the cache-search ladder. Nit: §9 describes the §3 table as carrying file:line — it is per-file with counts; the lines live in the prose beneath |
| token module defines family + scale + weights + line-heights; test_panel_typography finds no typography literal outside it | pass (scoped) | **pass as scoped / fail as written — scope ruling requested by the builder (BP4-P-1)** | tokens half **passes**: FONT_SANS "Space Grotesk" / FONT_MONO "Space Mono", 5 distinct role sizes 10/11/12/15/19, WEIGHT 400/500/600, `CHAT_LEADING_PT` 0.75 (a pt leading — Qt's QTextDocument HTML subset does not implement CSS line-height, chat_display.py:42 says exactly that). Test half: the shipped test opens exactly ONE file (designsystem/qss.py). Crux applied the test's own regex to all 92 panel modules: **174 hits in 23 files, 166 truly hardcoded** (styles.py 44, hda_views.py 33, context_bar.py 14, gate_widget.py 13, render_preflight.py 9, +17 more), 8 are `%`-interpolated token feeds; all 8 `designsystem/*.py` modules score 0 (`bp4_crux/panelfont/literal_split.txt`). **[ref]** broad `git grep` on the branch: 251 typography property lines outside tokens.py; qss.py literal count under the test regex = 0. Audit inventory 167/88/33/13 reproduces to the digit under the audit's stated method |
| no size token below the floor constant | pass | **pass** | branch-bound dump: min SIZE_* = 10 (SIZE_MICRO, alias SIZE_LABEL) == FONT_FLOOR_PX 10; TYPE_ROLES sizes ≥ 10; **[ref]** FONT_FLOOR_PX absent on base. Nit: the floor is DEFINED as the smallest shipped size, so this invariant could not have failed on the day it was written — a real ratchet (M2 proves it bites, and it is the only test in `-k panel` that does), not yet the measurement Joe's law asks for |
| panel tests green before and after; diff limited to designsystem/manifests/qss/layout/scripts + the new test | pass | **pass** | crux, fresh clones: master 3a27d1ff **346 passed / 85 skipped / 0 failed**; branch **352 / 85 / 0** (+6 = the new file); `tests/test_panel_typography.py` 6/6. **[ref]** `git diff --stat 28a0e183 bp4/panelfont` = 6 paths (audit, receipt, designsystem/qss.py, designsystem/tokens.py, scripts/probe_ui_font.py, tests/test_panel_typography.py); `synapse_panel.py` diff = 0 bytes; added lines carry no import/def/class/QTimer/connect(/QWidget/Signal/Slot. **[ref]** 5-scale stylesheet sha256 branch == base == builder: 1779b114 / 7a1a99e7 / e7d4298e / be8d2aaa / c99024cd, 18076 B each — and M1/M2 each move all five, so the identity is a measurement. Nit: `-k panel` deselects `test_rope_design_conformance.py::test_no_hardcoded_hex_or_px_outside_designsystem` (see M3) |
| probe_ui_font.py output pasted from the H22.0.400 GUI + before/after screenshots | UNKNOWN | **UNKNOWN** | gui_required, unobtainable headless. Static read of the 112-line probe: read-only (no setParm/.set(/createNode/hipFile), prints family/pointSize/pixelSize + QFontInfo + `hou.ui.scaledSize(1)` behind a callable guard, explicit UNAVAILABLE paths that say "Skip is NOT a pass" |

**DOC-STATED floor re-checked wider than the builder:** crux searched the whole H22.0.400 help cache (2280 files); the only file matching UI-font phrasings is the third-party MOPs Plus Typography SOP's own "Font Size" parameter doc. UNKNOWN provenance is honest.

**Mutations (ledger PANELFONT-M1, M2, M3, M4, M4b — 3 REDDENED, 2 SURVIVED):** M1 `font-size: 13px` in qss.py → `test_qss_stylesheet_source_has_no_literal_typography` reddens (reports L42 for source line 51 — line numbers are computed on the comment-stripped body, cosmetic); M2 `SIZE_MICRO = 9` → `test_no_size_token_below_floor` reddens, the only red in 352; **M3 (brief-specified: QWidget subclass with `#ff0066` + `13px` appended to synapse_panel.py) → SURVIVES the acceptance command `pytest tests -k panel` (352 green)** — the BP3 guard `test_no_hardcoded_hex_or_px_outside_designsystem` reddens the instant it is run by path (`synapse_panel.py:2757: ['#ff0066', '13px']`), but neither its module name nor its test name contains "panel", so `-k panel` never selects it; the crux diff-scope check reddens (727 bytes); M4 bare `400` in TYPE_ROLES → SURVIVES (value-based guard; 400 == WEIGHT_REGULAR); control M4b `450` → REDDENS.

**Referee call on acceptance 2 (BP4-P-1, for Joe/CTO):** the predicate as written ("no typography literal outside it") and the same brief's T4 diff-surface constraint ("designsystem/manifests/qss/layout/scripts files only") are in tension with each other, not with the builder, who disclosed the narrowing in the test docstring, audit §3/§9, and for_ruling[0], and carried the 166 literals forward as four held spawns (BP4-INLINE-TYPO, BP4-PT-HOTSPOT, BP4-STYLES-TYPO, BP4-FONTLOAD-1SRC). If the ruling is "panel-wide", the leg is incomplete and the spawns are the path; if "authority-scoped", it is complete. Same shape as the BP3-PANEL scope ruling (CTO_RULINGS_BP3 row 4).

**NITS:** receipt names one pre-existing red in tests/test_rails.py; the branch has two (`test_resolve_referee_prints_fable5`, `test_cli_resolve_positional_referee`), both green on master 3a27d1ff (a rebase clears it). Audit §9's crucible map should say a non-token *value* in TYPE_ROLES reddens (M4 vs M4b). The test reads only qss.py although all 8 designsystem modules are clean — widen the read to pin what is already true.

---

## BP4-USDKNOW — SOUND-WITH-NITS

Scratch: clone of `bp4/usdknow` @ 73905537 (product a0cc33b1). All 4 acceptance rows **pass**; 11/14 crux mutations REDDENED; the 3 survivors are unclaimed checker holes.

| Acceptance | Builder | Crux | Crux anchor |
|---|---|---|---|
| decision record covers every sec.2/sec.4 choice with arc + LIVRPS reason + tier + anchor | pass | **pass** | lane `decision_record_check2.txt`: 8/8 choices (§2.1 payload, §2.2 purpose split, §2.3 splatTier variant, §2.4 physics variant, §2.5 kind, §2.6 customData, §2.7 instanceable, §2.8 transform placement) carry arc + failure-prevented + tier; 7/8 enumerate rejected neighbours (§2.4 delegates to §2.3); §3 covers sec.4; all 23 distinct stdout citations resolve on the crux's own run with identical text |
| seed parses; every VERIFIED-RUNTIME / FIXTURE-VERIFIED anchor greps; checker exits 0 on the committed seed | pass | **pass** | **[ref]** checker on the referee's own archive of the branch files: exit 0, "15 promotable rows anchor clean"; **[ref]** own python: 15/15 anchored lines carry `arc=<arc>` + verify token, `ratified` is JSON `false` (bool). Lane: checker exit 0 **against the crux's independently produced stdout at the same line numbers** — the anchors bind to the probe's output, not to one saved file; 22 rows, tiers 8/7/5/2; 6/7 doc anchors resolve to a real blueprint heading, the 7th (`instanceable-false`) declares itself anchorless |
| probe stdout has the per-prim arc listing, the payload round trip counts, and the LIVRPS winner table | pass | **pass** | crux re-run (own out dir `scratchpad/u/crux_usdknow_out`, pinned hython, 2.59 s, exit 0, 85 lines): per-prim table :18-28, payload loaded=10 / unloaded=1 / reloaded=10 :41-43, reference non-unloadable :45-46, LIVRPS winner table :53-68. **Diff vs builder after normalising only the anon layer id, the mkdtemp name and the absolute usdc path: IDENTICAL, 86/86 elements, zero hunks, same line numbers** (`diff_arcs_winners.txt`). Two crux runs differ only in those tokens |
| no edit under python/synapse/; ratified:false present in the seed | pass | **pass** | **[ref]** `git diff 28a0e183 bp4/usdknow -- python/synapse/` = 0 bytes; vs master 0 bytes; 7 files, all additions under harness/ or docs/; `ratified: false` (bool) |

**Mutations (ledger USDKNOW-M1…M9 — 11 REDDENED, 3 SURVIVED):** rows chosen by the crux, not the builder: strip anchor (kind-component) → exit 1; promote PROPOSED → VERIFIED-RUNTIME (instanceable-false) and → FIXTURE-VERIFIED (variantset-splattier) → exit 1 each; promote DOC-STATED (frame-conversion-order) → exit 1; arc change on a VERIFIED row (livrps-reference-over-payload) and on a FIXTURE row (variantsets-absent) → exit 1 each; ratified true → exit 1; blank-line anchor, past-EOF anchor, real-but-wrong line → exit 1 each; synthetic BLOCKED section → 6 FAILs (the guard fires). **Survivors: arc `specialize` → `spec` (prefix substring), verify `reference_unloadable=False` → `unloadable=True` (a semantic inversion that substring-matches `payload_unloadable=True`), verify `dropped=9` → `loaded=1` (prefix of `loaded=10`) — all exit 0.**

**NITS / findings for ruling:**
1. **Checker matching is substring, not token** (`arc_needle in line`, `verify in line`). Fix: token-boundary matching. This is the one place a future editor could quietly wrong a promoted row without the gate noticing; every class the leg claims is caught.
2. **The probe self-finds the fixture through a hardcoded absolute path** (`C:/Users/User/SYNAPSE/.claude/worktrees/bp3-probe/…` as candidate 3): with `BP4_WL_USDC` unset and argv pointing at a nonexistent file it still ran and produced identical output. So the BLOCKED degradation path is unreachable on this host and has never been exercised by a real run (the crux tested the checker's BLOCKED guard synthetically); "ran in a fresh checkout" is not evidence of self-containment.
3. **The fixture nests kind=component inside kind=component** (`/wl_import` and `/wl_import/WL_fixture`, stdout:20-21); USD's model hierarchy drops the inner one (crux probe: `IsModel()=False` on WL_fixture, `Traverse(PrimIsModel)` yields exactly 1 prim). The record's "one queryable unit" behaviour holds by accident of a malformed hierarchy, not by design.
4. Nothing gates the decision record's own 23 stdout citations (the checker reads only the seed); all resolve today.
5. §2.4 is the thinnest section (delegates neighbour rejection to §2.3). Receipt F-3 slightly overstates novelty: both deep-dive lines (138, 201) were already flagged by the CTO note at the head of that skill file; the probe's contribution is turning "asserted wrong" into "refuted on 22.0.400 (measured)" and "unverified" into "confirmed" — the decision record says exactly that.
6. Receipt evidence cites `git diff master..HEAD`; a `--branch` shared clone has no local `master` — substance holds against `origin/master` and the base.

---

## Cross-cutting (environment truths demonstrated this run)

1. **Import-binding trap, three forms.** (a) Under hython, PYTHONPATH does not win: the Houdini package inserts `C:/Users/User/SYNAPSE` and `…/python` ahead of it — use `sys.path.insert(0, …)` in helpers, or pytest's rootdir binding. (b) Under stock python, the editable install points at master; PYTHONPATH wins **only** for a complete package tree — a `git archive` of a sub-package has no `synapse/__init__.py`, so master's regular package beats the scratch namespace candidate even at `sys.path[1]`. (c) The negative proof pattern works: a symbol that exists only on the branch (`synapse.spatial`, `FONT_FLOOR_PX`) must fail unbound; a green run then proves binding. Both leg tests (spatial, typography) pass that bar by construction.
2. **`pytest tests -k panel` is a weaker gate than it reads:** it deselects `test_rope_design_conformance.py::test_no_hardcoded_hex_or_px_outside_designsystem` (no "panel" in the module or test name). Select by path. Every panel leg that cites `-k panel` as its gate inherits the hole.
3. **Hunk counting needs `-U0` or line ranges:** two edits five lines apart fold into one 3-line-context hunk (INTAKE-M2).
4. **Karma XPU 22.0.400 substitutes a default distant light** when a stage carries none in the fixed topology (B7FIX-M1b) — "no light → black" is not a safe expectation on this build.
5. **Master moved during the audit** (3a27d1ff, 18:46); leg bases were pinned at 28a0e183 so nothing shifted under the lanes, but a receipt that says "diff vs master" without a sha is a moving target.
6. The referee's timing lane (SPATIAL) and the probe lane (USDKNOW) both found **honest pins that cannot observe the regression they exist to catch** — the `seconds` field that starts after the gather, the substring gate. Neither breaks a claim today; both are one-line fixes worth a NITS leg.

## For ruling (compiled)

- **BP4-R-1 (RULINGS):** re-run the leg with the fix path above; the leg does not ride until row 22 and the count line exist.
- **BP4-I-1 (INTAKE):** which INTAKE receipt is canonical (branch 03a4e43d/green vs master 2574de6b/green_with_findings); the harvest receipt's "no leg branch exists" sentence is false and the harvest note's 7168/6451 claim has no repo anchor. The two-writers rule proposed in the harvest receipt stands for ruling.
- **BP4-B7-1 (B7FIX):** amend review §13.1/§13.5 causal sentence (composite rewiring restores lighting; the camera bind removes husk errors without changing pixels). D2.4 → PASS and R-1 → CLEAR promotions are supported by crux evidence.
- **BP4-S-1 (SPATIAL):** timing pin (`t0` before the gather), registration pin (root `*.py` + bare token), class partition above 45°, commit or regen the fixture binaries + the timing producer (F-3).
- **BP4-P-1 (PANELFONT):** scope of acceptance 2 (authority-scoped vs panel-wide); the `-k panel` gate; floor stays UNKNOWN until Joe's probe paste.
- **BP4-U-1 (USDKNOW):** token-boundary matching in the checker; remove the absolute fixture fallback or make it announce itself; the nested-component fixture; D-1 promotions remain a human + CTO act.
