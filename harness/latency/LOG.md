# LATENCY HARNESS — LOG

Append-only. Newest row last. The board reads the last row.

| date | tag | entry |
|---|---|---|
| 2026-08-02 | SCAFFOLD | Harness created. SPEC ladder L0-L5 + 8 predicates. verify.py honest at 0 PASS / 0 FAIL / 8 PENDING — nothing claimed, nothing broken. |
| 2026-08-02 | WHY | Opened on a contradiction, not a hunch: the 07-27 report's ledger says Houdini-side work is 1-70 ms and therefore "the 5%"; commit `98b556f` measured 6.9-7.7 s of Houdini-side stage serialization at 100k prims. Both survive only if the ledger is scale-scoped and says so nowhere. |
| 2026-08-02 | SELF-CATCH | P5's producer path was wrong on first run — it looked for `scripts/_benchmark_latency.py`; the benchmarks live at the repo root. Found by running the verifier, fixed in the verifier. The design doc names them without a directory, which is how the mistake was inherited. |
| 2026-08-02 | SCOUT | 20-agent read-only fan-out dispatched (run `wf_b9c32632-1fc`): 5 ground mappers, 9 hypothesis prospectors, 4 crucibles, 2 instrument designers. No worktrees, no writes — a second fan-out was live in `.claude/worktrees/wf_9aef52f3-c03-*` at dispatch time. |
| 2026-08-02 | HARVEST | Fan-out complete: 20/20 agents, 0 errors, 3.13M subagent tokens. LEDGER.md written; registry 9 -> 11 hypotheses (H10 gate-axis bypass + H11 unconsumed honesty fields), all adjudicated by 4 crucibles. Producer: wf_b9c32632-1fc journal.jsonl. |
| 2026-08-02 | HEADLINE | Contradiction RESOLVED: regime real+per-op, axis WRONG (authored array volume, not prims — 16,677x measured gate miss). The 07-27 report's #1 lever (extend declarative coverage) REFUTED by its own prospector, kill confirmed 3/4 crucibles: it would ADD a round-trip vs synapse_batch. |
| 2026-08-02 | DESIGNS | I1 scale-bench + I2 perf-ratchet designed, unbuilt (journal). I2's ruling: counted proxy, not wall-clock — CI has no pxr, a timer cannot gate this repo. Building them is the next wave. |
| 2026-08-02 | BUILD | Wave wf_006556ba-f9c: Lane A (cache breakpoint + usage producer, crucible SOUND-WITH-NITS) + Lane B (volume-gate re-key, crucible SOUND) both MERGED per R301. H10 -> L4 (measured ~55,000x on the repro, both machines). H4 fix landed, still unpriced -- the producer exists, the pricing awaits a real session. |
| 2026-08-02 | FOUND | Lane A's crucible surfaced a PRE-EXISTING panel defect while attacking: claude_worker's else-branch never appends the final end_turn assistant text to _messages -- multi-turn API history silently loses the assistant's last words. Not lane A's; queued to the panel session (R309/R204). |
| 2026-08-02 | PROBE-H1 | Lane I (R302 rank 7): the R1 topo-hash's per-node read on a DIRTY 1M-pt grid — cookCount() is a pure read (0.005 ms, no cook); node.geometry() FORCES the cook (12.908 ms, cookCount 1->2); intrinsics on the handle 0.231 ms; repeat read on the clean node 0.238 ms. Mechanism confirmed: the hash pays a forced cook via geometry(), never via cookCount; magnitude on this repro 2x ~13 ms/op, under the 70 ms T4 ceiling. Whether that cook is removable vs re-timed is context: headless/live-envelope nothing else demanded it (hash-added work); a displayed node would have cooked for the viewport anyway. Producer: scripts/probe_h1_topo_hash_cook.py, hython 22.0.397 (NOTE: 22.0.368 is the doc target; .397 is what this host runs today, per GROUND). |
| 2026-08-02 | PROBE-H2 | Lane I (R302 rank 7): R7 dependents() forward-trace cost — container.dependents() on 500 children: 0.086 ms wired-chain / 0.071 ms flat (~0.17 us/node, linear 100->500); the bridge-shaped depth-3 trace visits 4 nodes on a chain (depth cap holds) at 0.039-0.044 ms; head dependents+outputs 0.011 ms. C4's explicit 1-10 us/descendant bracket REFUTED low by 1-2 orders on trivial nodes; linear extrapolation puts the 70 ms T4 ceiling at ~400k+ children. Caveats: null nodes (no parm-reference fan-out); wired links surface in outputs() not dependents() (S3 consistent, 0 deps on the chain). Producer: scripts/probe_h2_dependents_trace.py, hython 22.0.397. |
| 2026-08-02 | WAVE-2 | wf_ab4a9489-6a9 merged per R301 (both SOUND-WITH-NITS, followups landed BEFORE the push): Lane I R302 instrument + Lane R R304 ratchet. Suite 5361 -> see commit. |
| 2026-08-02 | KILLED | H1 and H2 die by MEASUREMENT, not argument. H1: `cookCount()` never cooks (0.005 ms, cooked=False) — `node.geometry()` is the cook (12.908 ms vs 0.238 ms clean); attribution shift, not the claimed cost. H2: depth-3 trace 0.039 ms at N=500 (~0.17 us/node) — C4's 1-10 us/descendant bracket wrong LOW by 1-2 orders; 70 ms ceiling sits at ~400k children. Producers: `scripts/probe_h1_topo_hash_cook.py`, `scripts/probe_h2_dependents_trace.py` (hython 22.0.397). |
| 2026-08-02 | HONESTY | `composition_valid` had ZERO assignment sites — a constant True the Scene Integrity anchor could never falsify. Now carries the real verdict, so a failed composition reads fidelity 0.0. Crucible K4 followup closed the other half: ops failing BEFORE validation (consent-denied, fn raises) now record `composition_applicable=False` by default-deny instead of inheriting a default that read "ran and passed". |
| 2026-08-02 | CROSS-CHECK | The perf ratchet independently confirmed Lane I's cache claim on its first run: `stage_traversals 5 -> 4`, `prim_visits 17002 -> 16001`, reported as "improved" under the direction rule. Two lanes built in parallel from the same base, one measuring the other. Floor refreshed to the better bar (legitimate while PROPOSED/unarmed). |
| 2026-08-02 | ANCHOR | R304 crucible landed a severity-3 attack: `read_floor` looked up only a LOCAL `master`, so on a GitHub PR checkout (no local master) it silently degraded to HEAD-committed and a branch could gate against its own doctored floor. CLOSED — resolves `origin/master`; proven on a constructed PR-shaped clone (`anchor=merge-base(origin/master)`); pinned by `test_anchor_resolves_via_origin_master_when_no_local_master`. |
| 2026-08-02 | SELF-CORRECT | The R306 lane flagged my own LEDGER §1.5 wording and it was WRONG, not imprecise: `session_integrity.py:66` does NOT filter the blind-spot case out of mutation accounting. Mutations count unconditionally at `:53`; `:66` guards `_node_paths` against the sentinel strings in `scene_hash_before/after` — correct as written. I inherited that claim from a fan-out result and published it without driving the code. Corrected in LEDGER §1.5 and REGISTRY H11; the underlying gap (no reduced-op count) is real and is what R306 closes. |
| 2026-08-02 | SCALE-BENCH | R305 lane I1: the missing denominator is now a swept CURVE, not two endpoints. `scripts/bench_scale.py` + `--tier offline` on both root benches (extended, never rebuilt — the legacy no-arg sequence is pinned by a frozen-literal call-list test on both). P5 PENDING -> PASS; the latency board is 8 PASS / 0 FAIL / 0 PENDING. Perf ratchet unchanged at 8 PASS / 0 FAIL / 3 PENDING; `perf_baseline.json` untouched. |
| 2026-08-02 | AXIS-SHOWN | The I1 design's prim-count parameterization was applied as CORRECTED, not as written: the volume sweep holds prims at **4** (2,500x below the 10k prim gate) while authored elements grow 100k -> 4M, so the prim term cannot fire and any response is the volume term or nothing. Measured: `flatten_exports` 2 -> 0 between 500k and 1M elements at four prims. The H10 bypass class is now a curve, not a single repro. Producer: `python scripts/bench_scale.py --json-out -`. |
| 2026-08-02 | PRICE-OF-THE-WIN | First counted statement of what the 4x gate COSTS: crossing 10,000 -> 10,001 prims removes 2 Flatten calls AND takes `prim_state_reads` from **2.0/prim to 8.0/prim** (r2 1.0 both sides), `prim_visits` 24,097 -> 40,004. A trade, not a free lunch. Also visible as a curve for the first time: `attrs_examined` saturates at 4,096 = `_STAGE_HASH_VOLUME_ATTR_BUDGET` (r2 0.982 is the flag that its linear slope is not a growth rate). Producer: same command; LEDGER §7.2. |
| 2026-08-02 | UNEXERCISED | Stated, not buried: the LIVE tier (`_benchmark_latency.py --tier live`) was WRITTEN AND NEVER RUN — no bridge was reachable in the lane. It emits nothing and returns 2 with the socket absent. No live number from it exists in this repo. The in-process L0b arm is not built either, so the stage term stays unpriced at scale — it is ABSENT BY CONSTRUCTION on the live WS path (`python/synapse/server/integrity_envelope.py:219`, `include_stage=False`). |
| 2026-08-02 | BOARD-CLOSED | I1 merged (crucible SOUND-WITH-NITS, three SEV-2s closed BEFORE the push per R301): the widened wall-clock denylist (16 escapes incl. `wall_time`/`runtime`/`took`), `assert_record_honest` wired into the live emit (it had ZERO call sites there), and degenerate fits now report `r2: None` instead of a fabricated 1.0. Latency board **8 PASS / 0 FAIL / 0 PENDING** — P5 closed by the thing it was written last to answer. H10 -> **L5**: pinned by the armed ratchet AND swept as a curve. |
| 2026-08-02 | AUDIT | Closing audit (wf_ed634082-d3c, adversarial pass over the ORCHESTRATOR's claim surface — every lane got attacked today; he hadn't been) returned 2 hits among 12 claims checked. (1) DISCREPANT: the ratchet was armed WITHOUT the deny-list line its own governance texts prescribe — `Edit(harness/verify/perf_baseline.json)` was absent from harness/agent-settings.json while the allow list grants `Edit(harness/verify/**)`. Fixed in this commit. (2) UNCHECKABLE-as-written: commit 1d7a119's `Suite: 5540 passed / 0 failed` carried no producer. The producer: the orchestrator's own full run on the MAIN checkout immediately pre-push (`python -m pytest tests/ -q -rf` -> `5540 passed, 137 skipped ... in 164.45s`). Reconciliation vs the lane's max 5486: 5457 (master post-H+O merge) + 83 bench tests = 5540, measured where the 4-test statusline linked-worktree class does not exist. Number sound; message lacked its producer — the same Law-2 standard applied to everyone else all day. |
| 2026-08-02 | FIRST-LIVE | The live tier ran against a real bridge for the first time (v5.42.0 seat, pid 69380, Houdini 22.0.397 — doc pin 22.0.368, drift stated). Two blockers found ON CONTACT and fixed client-side: the arm dialed the bare root (handler mounts at /synapse — HTTP 400), and the live handler STRINGIFIES execute_python results (handlers.py:1294 `str(result)`) so the rung read-back got a repr string, parsed now via ast.literal_eval. RESULT: live envelope cost FLAT across 100k->4M authored elements (ping ~0.4ms, set_parm ~3.7ms, 50/50 samples per rung) — live-verifying that the WS path's stage term is absent by construction (integrity_envelope.py:219) and never paid the volume cost the /mcp path needed H10's gate for. Rows: .claude/live_rows_v5420.json. Producer: `python _benchmark_latency.py --tier live --json-out .claude/live_rows_v5420.json`. |
