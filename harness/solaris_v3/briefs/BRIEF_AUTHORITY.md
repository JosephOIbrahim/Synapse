# BRIEF — AUTHORITY stream (C3 dispatch fence + run_recipe)

You are the AUTHORITY worker in a six-agent swarm implementing `docs/SOLARIS_RECIPES_H22_BLUEPRINT_V3.md`. Read, in order: `docs/solaris_v3/SWARM_CONTRACT.md`, `python/synapse/recipes/contracts.py`, then blueprint pages 06, 03, 13 (S1, S2). Your worktree is the current working directory; branch `bp5/solaris-authority`. Work only inside your exclusive write set.

## What the blueprint established
The reviewed panel path is `SynapsePanel._start_worker` → `ClaudeWorker(enforce_worker_policy=True)` → `_execute_tool_block` → `worker_policy.is_tool_allowed_for_worker`. Hiding a tool description is not authorization; the fence must hold at **final dispatch**. Standard mode today permits generic Solaris composite builders (`_WORKER_BUILDER_ALLOWLIST`).

## Deliverables

1. **`python/synapse/panel/worker_policy.py`** — add a `demo` mode (`SYNAPSE_WORKER_TOOL_MODE=demo`). In demo: advertise registered read tools + `contracts.RUN_RECIPE_TOOL_NAME`; deny at dispatch every generic builder, assemble-all, arbitrary setter (`houdini_set_parm`, `houdini_set_usd_attribute`, …), `execute_python`/`execute_vex`, and every `synapse_group_*` composite. Unknown tool → deny (fail closed). Invalid/unknown mode string or profile conflict → resolve to **strict**, never to `unrestricted`; no env-var fallthrough to a looser mode. Keep the existing three modes byte-for-byte for their existing tests.
2. **`python/synapse/recipes/authority.py`** — `validate_request(req: RunRecipeRequest, spec: RecipeSpec) -> RunRecipeRequest | Refusal`: every slot key/type/finite numeric/bounds/enum checked against `SlotSchema`; typed bindings only, no string interpolation into Python or VEX. `effective_permission(action: ActionSpec, wrapped_gate: PermissionCategory) -> PermissionCategory` via `contracts.max_permission`. `bind_approval(...) -> ApprovalBinding` and `recheck_approval(binding, live_scope) -> bool` (instance revision, engine, resolution, samples, output destination must match exactly, rechecked immediately before start). Terminal action = hard stop: expose `is_terminal(action)` and a per-turn `MutationBudget` that refuses any second mutating call after a terminal action.
3. **`python/synapse/recipes/phrases.py`** — deterministic exact-phrase matcher over `contracts.DEMO_PHRASES`: the match must consume the **entire** request (after whitespace/case normalization only); any trailing or leading unsupported clause → `Refusal(TRAILING_CLAUSE, …, supported_alternative=<nearest phrase>)`; ambiguity → `AMBIGUOUS`. Slot extraction is typed (`{exposure}` → float, `{color}` → enum of named colors mapped to color3 constants defined in the spec presentation). Off-list requests return a Refusal, never a partial plan.
4. **`python/synapse/server/handlers_recipe.py`** — a `RecipeHandlerMixin` with `_handle_run_recipe(payload) -> dict` that: parses `RunRecipeRequest`; loads the spec via `blocks.fixtures.load_recipe_spec` if present on your branch, else via a `SpecLoader` Protocol you inject; validates; computes effective permission; for APPROVE-level actions returns `{"status": "awaiting_approval", "binding": …}` without mutating; for others hands a prepared operation to an injected `Executor` Protocol (LIFECYCLE builds the real one — do not import it). Dedup by `request_id` (a repeat returns the prior outcome, never re-executes). Do NOT edit `handlers.py`/`mcp_server.py`; write the exact mixin/tool registration lines into `docs/solaris_v3/HOOKUP_AUTHORITY.md`.
5. **Tests** — `tests/test_worker_policy_demo*.py`: T5 (direct generic tool call denied at dispatch even when the advertisement filter is bypassed — call `is_tool_allowed_for_worker` directly for every mutating tool name in the registry), mode fallthrough negative control. `tests/test_recipe_authority*.py`: T4 (`"build the solaris spine and add fog"` refused before any dispatch; the trailing clause appears in the refusal reason), T6 (render without approval → `APPROVAL_REQUIRED`; wrapper cannot self-approve), slot bounds/enum/NaN/inf negative controls, approval mismatch on each bound field, request_id dedup, terminal hard stop.

## Notes
- Consent posture is pinned by `tests/test_phase0b_consent_posture.py` and `synapse-worker-consent-on` (2026-08-18 DECIDE): the interactive worker may not self-authorize gated ops. Your work strengthens that; never weakens it.
- Status lines to `harness/solaris_v3/STATUS_AUTHORITY.md`; final report `docs/solaris_v3/REPORT_AUTHORITY.md`.
