# BRIEF — SPEC stream (C1 schema half)

You are the SPEC worker in a six-agent swarm implementing `docs/SOLARIS_RECIPES_H22_BLUEPRINT_V3.md`. Read, in order: `docs/solaris_v3/SWARM_CONTRACT.md` (laws + ownership), `python/synapse/recipes/contracts.py` (frozen seam), then the blueprint pages 04, 05 (canonicalization), 03. Your worktree is the current working directory; its branch is `bp5/solaris-spec`. Work only inside your exclusive write set from the contract.

## Deliverables

1. **`fixtures/solaris.spine.json`** — schema v2, contract-only. The one owned baseline topology from blueprint p03: hero sphere, ground plane (`plane`, not `grid` — p13 S5), two real MaterialX materials with valid bindings (nested VOP shader nodes recorded as nodes with `parent_id`), dome light + key light, camera, Karma render settings, render branch, display output. Node records: id, parent_id, category, exact resolved type, typed parms, flags, position. Connections: src_id/src_output → dst_id/dst_input. VOP port identities must be captured from the H22.0.400 catalog (`rag/catalog/h22.0.400/Lop.json`, and the VOP catalog if present under `rag/catalog/`), never guessed — where a port cannot be resolved from a catalog, the spec **must** refuse publication (see 3). `golden_reference` is REQUIRED and currently **absent** (Joe has not built the golden HIP yet): populate it with `{"status": "PENDING_HUMAN", "hip": null, "reference_render": null, "dependency_record": null}` and make the loader treat that as `Availability.BLOCKED`, not an error. Actions: the four `ActionId`s with slot schemas per `contracts.SlotSchema` (LIGHT: exposure float with bounds; MATERIAL: hero base_color color3 bounded 0..1; RENDER: resolution/samples/engine enum pinned to one engine; BUILD: no slots), `required_checks` from `contracts.REQUIRED_CHECKS`, `effect_scope`, `permission` (RENDER = APPROVE), `phrases` from `contracts.DEMO_PHRASES`.
2. **`python/synapse/blocks/fixtures.py`** — backward-compatible: every existing v1 call (`load_fixture`, `validate_fixture`, `declared_nodes`, `declared_wires`, `box_name_for`) keeps working byte-for-byte on `solaris.basic.json` (existing tests `tests/test_blocks_*.py` must stay green). Add `load_recipe_spec(name) -> RecipeSpec` and `validate_recipe_spec(spec)`.
3. **`python/synapse/recipes/spec.py`** — the validator: rejects missing subgraphs, unresolved type aliases, empty required material definitions, unresolved VOP ports, wrong `supported_build`, missing `golden_reference` key (absent key = reject; `PENDING_HUMAN` = BLOCKED). Produces `RecipeSpec` from JSON. Adapters `spec_to_fixture_v1(spec)` so the BLOCKS reconciler (`blocks/runtime.apply_fixture`) can consume the outer graph unchanged.
4. **`python/synapse/recipes/canon.py`** — canonicalization per p05: `semantic_digest(spec_or_observed)` (types, parents, ports, parms, expressions, flags) and `layout_digest` (positions) computed separately; stage-canonicalization record (version, frame/time, load rules, layers, resolver identity); normalize only known volatile identifiers and approved path tokens — reuse the existing c3 canonicalizer rules if they live in `harness/blocks/` (find them; do not copy-paste, import or extract). Exact equality for authored state after documented normalization.
5. **Tests** `tests/test_recipe_spec*.py`: valid spine loads; each rejection class has a negative control that fails for the stated reason; v1 fixtures untouched; semantic vs layout digest independence (move a node → layout changes, semantic does not); `catalog_digest` pins to the actual catalog file digest.

## Notes
- `python/synapse/loop/recipe.py` is a different, older contract. Ignore it.
- The v1 fixture `fixtures/solaris.basic.json` shows the house style for provenance/epistemics blocks; keep that honesty (which values are catalog-verified vs design choices).
- Status lines to `harness/solaris_v3/STATUS_SPEC.md`; final report `docs/solaris_v3/REPORT_SPEC.md`.
