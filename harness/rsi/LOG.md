# LOG — the RSI closure harness

*Run log. One row per attempt. Reads actual state; never narrates. `?` where a check could not be made.*

| date | phase | what happened |
|---|---|---|
| 2026-08-01 | FRAME | Harness framed. SPEC + PLAN + REGISTRY + verify.py + CHAMPION + LEDGER + DEADENDS written. **SPEC AWAITING RATIFICATION.** |
| 2026-08-01 | VERIFY | `harness/rsi/verify.py` first run: **7 PASS / 2 FAIL**. P1 failed naming 3 unregistered surfaces; P4 failed on a parser defect in this file (the `def` line at `router.py:917` was counted as a call site because `"    def ".strip().startswith("def ")` is False). |
| 2026-08-01 | FIX | P4 parser corrected to `^\s*(async\s+)?def\s+$`. Registry gained `shared/conductor_advisor.py` (O), `shared/constants.py` (F), `shared/evolution.py` (O). Re-run: **9 PASS / 0 FAIL / 0 PENDING**. |
| 2026-08-01 | CROSS-CHECK | P4's live grep independently re-derived the eight `_record_metric` call sites named by `harness/notes/RSI_SURFACE_AUDIT.md` — `router.py` :285, :448, :515, :554, :584, :706, :742, :819 — with no shared source. Audit Q4 finding **VERIFIED-AT-HEAD** at `f427320`. |
| 2026-08-01 | BOARD | `harness/progress.py` built (discovers `harness/*/verify.py`). Verified against CLEAR: **5/8 clear, 3 open**, matching CLEAR's own logged run. This harness auto-appeared with no edit to the tool. |

| 2026-08-01 | RATIFY | **SPEC RATIFIED** (human sanction, CTO authority granted). Recorded explicitly that ratification opens the lines but promotes no rung — permission cannot manufacture proof. |
| 2026-08-01 | GATE-FIX | Symbol table regenerated against the live build: was stamped `22.0.368`, running `22.0.397`. New table `version=22.0.397 symbols=35908 blake2b=2b6d06030628`; sanity checks green (`hou.LopNode`, `hou.SopNode`, `pdg.EventType`, `pxr.Usd`, `pxr.Sdf`). **The phantom-API gate was DOWN and is now up.** |
| 2026-08-01 | RL-1 | RECONCILE run for all six June lines. `R` → L0+L1 (blocked L2). `O` → L0 (blocked L1). `S` → L0 (blocked L1; operative L2). `E` → L0 (blocked L1, confirmed exact lines). `F`/`C` unchanged. verify.py re-run: **9 PASS / 0 FAIL**, 10 proven rungs all carrying evidence. |
| 2026-08-01 | RL-1 CATCH | `tests/rsi/eval_line_r_closure.py` collects **ZERO** tests under pytest — its `eval_` prefix matches neither `test_*.py` nor `*_test.py`. Run directly (`python tests/rsi/eval_line_r_closure.py`) it returns **OVERALL: PASS**. A pytest-based existence check would have read live evidence as absent. |

| 2026-08-01 | RL-2 `F` CONFIRM | Claimed defect **CONFIRMED at HEAD** `f427320` before any edit. `shared/router.py` `MOERouter.route(self, features)` takes no outcome argument; the promotion block gated on `self._fingerprint_counts[fp] >= FAST_PATH_PROMOTION_THRESHOLD` alone. Live-path cross-check: `grep -c "_session_fast_paths" python/synapse/routing/router.py` → `0`; `grep -n "promot" …` → exit `1`. **TieredRouter has no promotion at all** — the fix is panel-side only, as believed. Second finding not in the brief: `RoutingLog.apply_learned_fast_paths` (`panel/routing_log.py:100-117`) writes the same table via `learn_fast_path()` off its own **frequency** list — the identical defect through a side door. |
| 2026-08-01 | RL-2 `F` FIX | R18 landed. `record_outcome(fingerprint, success)` + failure veto on **both** writers; `learn_fast_path()` returns `bool`; entries carry `outcome_confirmed` at index 3; a recorded failure also **evicts** an already-promoted entry. `_session_fast_paths` left in-memory — L4 not attempted, per the loop's own hazard note. **Negative control:** `git checkout HEAD -- shared/router.py` then `pytest tests/test_router_internals.py::TestOutcomeVetoedPromotion` → **`8 failed`**; restored → **`30 passed`** for the whole file. Suite (three runs, because the first two disagreed): baseline at HEAD **4 failed / 5341 passed**; after **5 failed / 5348 passed** (×2, reproducible). The extra failure is `test_m3_logs_doctor.py::test_dump_failure_never_blocks_escalation` — a `time.sleep(0.6)` watchdog race that **passes isolated** (`18 passed`) and, decisively, **also fails at HEAD with this lane's work `git stash`ed: 5 failed / 5340 passed**. Pre-existing full-suite timing flake, not this change; `5340 + 8 new tests = 5348` accounts for every passing test. Floor held: identical failure set. Consumers: `-k "router or routing or tool_filter or conductor or advisor or pass7 or pass8 or panel"` → **619 passed, 69 skipped**. |
| 2026-08-01 | RL-2 `F` VERIFY | Registry updated in the same commit: `F` `rungs_proven` `[] → ["L0","L1"]`, `blocked_at` `L1 → L2`, `verification` `unverified → verified-at-head`, `panel/routing_log.py` added as a surface. `python harness/rsi/verify.py` → **9 PASS / 0 FAIL / 0 PENDING**, 12 proven rungs. **P4 line (unchanged, and correctly so — P4 greps A1's `routing/router.py`, which this lane did not touch):** `PASS — structural rule holds; live signal still constant and registry agrees — all 8 call sites pass only (tier, latency) — success defaults True (lines 285, 448, 515, 554, 584, 706, 742, 819)`. |
| 2026-08-01 | RL-2 `F` CAVEAT | The honest limit of this rung: **nothing calls `record_outcome()`.** Every promotion written today is stamped `outcome_confirmed=False`. Further, the only live caller of `MOERouter.route()` is `panel/tool_filter.py:245`, and **no production code calls `filter_tools()`** — only `classify_tool` is imported (`command_palette.py:69`, `tool_palette.py:28`). So loop F's promotion path is currently **dormant**, not merely unfed. That is the L2 work, and it is why `blocked_at` is `L2` and not a closure claim. |
| 2026-08-01 | RL-2c `F` CONFIRM | Crucible attacked `RL-2`'s own fix; **both findings REPRODUCED at lane HEAD `eca11ef` before any edit.** (1) `record_outcome('x\|y\|z\|w', 'FAIL')` → `outcome_counts()` `{'x\|y\|z\|w': (1, 0)}`, `_promotion_allowed` `True`, `_outcome_confirmed` `True` — a reported **failure** recorded as a **success**, because `shared/router.py:224` was a bare `if success:` and every non-empty string is truthy. Not a missing veto: **manufactured positive evidence.** (2) `execution\|moderate\|usd\|normal` promoted to `(HANDS, None, c2d5389a5924, False)`, then two `record_outcome(fp, True)` calls → tally `(2, 0)` but index 3 **still `False`** — `outcome_confirmed` could never go `False → True`, since promotion fires on the frequency-crossing call, necessarily before any outcome exists. |
| 2026-08-01 | RL-2c `F` FIX | R19 landed. **(a)** `success` checked by identity against `True`/`False`; anything else raises `TypeError` and records nothing. **Raise, not log-and-ignore** — silently dropping a malformed outcome is safe for the veto but reproduces `A1`'s defect one level up (a producer that believes it is reporting and is not); a tri-state producer resolves "never observed" by *not calling*. **(b)** A recorded success relabels an existing entry `False → True`; rewrite is **indexed, not unpacked**, so a hand-injected 3-tuple normalises to 4 instead of `IndexError`. Neither change can create a promotion. **Negative control:** 26 new tests vs `git checkout HEAD -- shared/router.py` → **`23 failed, 3 passed`**; restored → **`56 passed`** for the file (was 30). The 3 passing at HEAD are the invariants R19 must not break, not evidence of the defect. |
| 2026-08-01 | RL-2c `F` DORMANT | Registry correction. Re-verified by tree-wide grep: `MOERouter` constructed in **two** non-test places (`tool_filter.py:48`, `shared/router.py:445`); `MOERouter.route()` has **one** non-test call site (`tool_filter.py:245`) — the other `.route(` hits are different routers (`handlers.py:1626` `TieredRouter`, `planner.py:277` injected cascade); `filter_tools` has **zero references anywhere in the repo**, prod or test, only `classify_tool`/`PALETTE_VERBS`/`PALETTE_CONTEXTS` are imported from that module; `route_task()` has no caller outside the `shared/__init__.py` re-export. (`agent/synapse_agent.py:86 _filter_tools` is an unrelated profile filter.) So the path executes **zero times in production** — **DORMANT, not unfed.** The distinction is the point: *unfed* frames the work as wiring a producer and presumes the loop should exist; *dormant* makes it **wire-or-delete**, the same call `A2` faces. Independent corroboration: `docs/RFC_agent_usd_ledger.md:307` ("the dead `panel/tool_filter.filter_tools` (no caller)"), `docs/SCIENCE_HARNESS_LEDGER.md:256` ("DORMANT"). |
| 2026-08-01 | RL-2c SCOPE | Third frequency-only promotion door found: `ConductorAdvisor._analyze_routing_promotions` (`shared/conductor_advisor.py:296-324`), confidence *scaling* with frequency at `:320`. **Ruled `O`, not `F` — recorded, not fixed**, per the no-silent-lane-expansion rule. Four grounds: the file is already an `O` surface and `O`'s own note calls it "the read side of this loop"; it writes no routing table (INFO `Recommendation` → human edits `constants.FAST_PATHS`); `analyze()` takes `routing_fingerprints: dict[str,int]` (`:107-111`) and `advise_from_bridge` calls only `fingerprint_counts()` (`:480`), so a veto needs **`O`'s** input contract widened; and it is the concrete instance of `O`'s pre-existing blocker sentence about a failure-blind source. Filed at `REGISTRY.json` → `O` → `_third_door_note`. `F` exposed `outcome_counts()` so `O` has a source when it gets there. |
| 2026-08-01 | RL-2c `F` VERIFY | Suite **in this worktree**, baseline vs after: **5 failed / 5348 passed** → **4 failed / 5375 passed**. `5348 + 26 new + 1 = 5375`; the `+1` is `test_m3_logs_doctor.py::test_escalate_dumps_evidence`, which **failed in the baseline run and passed after** — the known `time.sleep(0.6)` watchdog race in that file (isolated: **18 passed**). Floor held; failure set shrank to the 4 known `test_statusline.py` worktree-identity failures. Consumers: `-k "router or routing or tool_filter or conductor or advisor or pass7 or pass8 or panel or observability"` → **655 passed, 69 skipped**. `python harness/rsi/verify.py` → **9 PASS / 0 FAIL / 0 PENDING**, 12 proven rungs. **P4 verbatim (unchanged, correctly — P4 greps `A1`'s `routing/router.py`, untouched by this lane):** `PASS — structural rule holds; live signal still constant and registry agrees — all 8 call sites pass only (tier, latency) — success defaults True (lines 285, 448, 515, 554, 584, 706, 742, 819)`. |
| 2026-08-01 | RL-2 A1 | SIGNAL fix landed for `A1`. All three defects re-confirmed at HEAD `c574d49` before touching anything, then fixed together: (1) `success` made a REQUIRED parameter of `_record_metric` (`router.py:937-962`) and all nine call sites pass a real outcome; (2) `_try_tier0` computes `all(r.success for r in responses)` (`router.py:557`) instead of hardcoding `success=True` — `_try_tier2` carried the identical defect (`router.py:713`) and was fixed in the same commit; (3) the `no_tier_matched` fallback records a failure under the reserved `NO_TIER_KEY="no_tier"` (`router.py:386`) where it previously recorded nothing. Safety fact re-verified BEFORE landing: `TierThresholds.get()` has zero non-test callers, so honest recording changes no routing decision today. Pinned by `tests/test_routing.py::TestRewardSignalHonesty` (10 tests reading `_epoch._current_epoch.outcomes` — what the adapter was TOLD, not what `RoutingResult` claimed). Mutation-verified: 3 mutants, each killed (tier-0 hardcode -> 4 failed; fallback recording deleted -> 2 failed; default restored + one call site silenced -> 6 failed). `verify.py` **9 PASS / 0 FAIL**, P4 flipped to "live signal now carries an outcome and registry agrees — 9/9 call sites". `A1` L0 -> **L1**, blocked_at L1 -> **L2**. |
| 2026-08-01 | RL-2 CATCH | One existing test *asserted the defect*: `tests/test_routing.py::TestTieredRouter::test_tier0_command_fn_error` asserted `result.success` on the reasoning "routing succeeded even if execution failed". That comment is exactly how the tier-0 reward signal became a constant. Rewritten to the new truth. A green suite was pinning the bug — the suite passing is not the same as the suite being right. |
| 2026-08-01 | RL-2 A1 L2 | New `A1` blocker is NOT a code defect. `synapse_live_metrics` against a live bridge (`synapse_ping` -> `pong=true`, protocol 4.0.0) returned `routing.total_requests=0`, `tier_counts=[]`. The router has never routed a production request. An honest signal nothing has exercised is honest and unread; L2 needs traffic, not another patch. |
| 2026-08-01 | RL-2b A1 | CORRECTIVE pass on `RL-2`. The crucible caught the closure overclaiming its reach: passing `result.success` at all nine call sites is not honesty when `RoutingResult.success` is ITSELF a literal at four sites. Adjudicated one by one. **CONFIRMED and FIXED (1 of 4):** `_try_tier3` recorded DEEP at LAUNCH with a hardcoded `success=True`, before the background worker had any outcome — the worker's real verdict, stored into `_async_results` later, never entered the sample, so every async DEEP route scored 1.0 however it ended. Design call: **option (c)**, do not record at launch. `_try_tier3` (`router.py:752-806`) now records nothing; `_tier3_worker` (`:808-864`) records what it observes — False on raise (`:863`), False on `_tier3_sync`→None (`:850`), success left to `_tier3_sync`'s own record (`:921`) so one route yields exactly one sample. Both failure paths now also STORE a result, so a dead handle resolves instead of hanging a poller forever. Option (a) defer-to-`get_async_result` rejected: it POPs, so an unpolled handle is never sampled. Option (b) record-then-amend rejected: `EpochAdapter.record` appends into a rotating fixed-size deque with no amend API — a provisional True can be aggregated into a threshold before its correction lands. **REFUTED (3 of 4):** `router.py:246` conversational short-circuit returns before any `_record_metric`/`_cache_result` — outside the sample entirely, cannot pin anything. `:589` (`_try_tier1`) and `:828` (`_tier3_sync`) never touch `command_fn` and never populate `responses`, so `all(r.success for r in responses)` is `all([])` — deriving would be a literal in a costume. Truth contracts documented inline at all three; no behaviour changed. New tests: `TestAsyncDeepSignalHonesty` (5) + `TestRefutedConstantSites` (3), every failure assertion paired with a success control. Mutation-verified: mutant A (restore launch record) → 1 failed; mutant B (silence both worker records) → 3 failed. `verify.py` **9 PASS / 0 FAIL**, P4 now **10/10 call sites**. Suite 5351→5359 passed, same 4 known-flaky `test_statusline` failures. |
| 2026-08-01 | RL-2b CATCH | **The `RL-2` L2 blocker text was FALSE and is retracted.** It read *"L2 needs live routed traffic through TieredRouter -- not another code change."* `handlers.py:1625` builds `TieredRouter(config=config)` — the only non-test construction in the tree — with **no `command_fn`** (defaults to None). Tier 0 and tier 2 both gate execution on `self._command_fn`, so in production they cannot execute a command at all and can never observe one fail. The corrected tier-0/tier-2 signal is UNREACHABLE from the live path, not merely un-exercised; traffic alone would not have moved it. L2 needs the wiring AND the traffic. A blocker that misnames the obstacle sends the next pass to the wrong place — the same failure class as the signal it was describing. |
| 2026-08-01 | RL-2b SCOPE | Honesty claim scoped rather than restated. `INSTANT`/`STANDARD`/`RECIPE`/`DEEP`/`CACHE`/`no_tier` can carry failure. **`FAST` cannot and was deliberately not changed:** `_try_tier1` executes nothing and reaches `_record_metric` only on its success path; its not-found/low-confidence exit is `return None`, a CASCADE decision — the request continues to tier 2 and may succeed there, so scoring it a FAST failure would count one request twice in two series. `FAST`'s 1.0 is definitional, not evidence. Recorded in `danger_if_closed_now` as the live residual risk. Incidental finds, both pinned by tests: repeats are served by the TIER PIN (step 0) not the cache (step 0.5), and `cache.py`'s `_DEFAULT_TTLS` gives `recipe`/`instant` TTL 0 so tier-0 results are never cached at all. |
| 2026-08-01 | RL-3 `A2` RETIRED | **The harness's first SUBTRACTION.** Human-ratified wire-or-delete answered **delete**. Deleted: `python/synapse/agent/learning.py` in full (194 lines — `OutcomeTracker` + `record`/`get_relevant`/`get_rejections`/`success_rate`); its executor wiring (`from .learning import OutcomeTracker`, `self._tracker = OutcomeTracker(memory) if memory else None` at the old `executor.py:60`, the tracker-driven `prepare()` context block at the old `:89-111`, `record_outcome()` at the old `:297-309`, and its two `execute()` call sites at the old `:286`/`:294`); and the exports (`agent/__init__.py` import + `__all__`; `synapse/__init__.py` `_agent_names` + lazy import + `_map` + top-level `__all__`). Dormancy re-verified at HEAD before cutting, not carried from the audit: `grep -rn "AgentExecutor(" --include=*.py python/ shared/ scripts/ host/ \| grep -v _vendor` returns **exactly one line**, `python/synapse/agent/__init__.py:12`, **inside the module docstring** (spans 1-21). Every real construction is in `tests/test_agent.py`. So the `if memory else None` guard was never the reason — there was no live executor to reach it. Registry entry **RETAINED** per `P9` with `disposition: RETIRED-2026-08-01`, `rungs_proven: []`, `blocked_at: L0`, dormancy evidence kept, and a blocker rewritten to state what was deleted, why, and the revival precondition. |
| 2026-08-01 | RL-3 SCOPE | **Cut narrowed twice on purpose.** (1) `AgentExecutor` **NOT deleted** although the same grep condemns it — that is a larger subtraction than the one ratified, escalated to the human instead of taken on agent authority. (2) Its `memory` constructor parameter **kept**, now assigned and never read: it is `AgentExecutor`'s signature, not `OutcomeTracker`'s wiring, and under-deleting is the sanctioned error direction. Both residues are documented in code rather than left silent. **Finding that widened while scoping:** a symbol grep for `SparseToolIndexer`/`ReasoningContextManager`/`get_specialist`/`TaskSynthesizer`/`build_enhanced_prompt` over every `*.py` returns **10 files — 4 defining modules, their 4 test files, 2 `__init__` re-exports. Zero production consumers.** `agent/protocol.py` is the same outside the package (`tests/test_set_usd_primvar.py` only). The candidate is plausibly the **whole `python/synapse/agent/` package**, held live by nothing but its own re-exports and its own tests — the same self-referential trap `A2` sat in one level down. Filed as an open recommendation in `CHAMPION.md`; **not acted on.** Caveat recorded with it: static name grep would miss dynamic `getattr`/registry access. |
| 2026-08-01 | RL-3 PIN | A deletion needs a pin or it grows back. `tests/test_agent.py::TestExecutorMemoryIsInert` (7 tests) replaces the 12 that died: no `_tracker` attribute, no `record_outcome` method, `import synapse.agent.learning` raises `ImportError` (as do both re-export paths), `prepare()` no longer seeds `relevant_memories`/`constraints`/`context_summary` from prior FEEDBACK memories, and **neither the success nor the failure path** of `execute()` writes a FEEDBACK memory. The failure path is pinned separately on purpose — it was a separate `record_outcome` call site, so one test covering only the success path would have let half the mechanism return unnoticed. |
| 2026-08-01 | RL-3 VERIFY | Suite **in this worktree**, baseline vs after: **4 failed / 5419 passed / 137 skipped** → **4 failed / 5414 passed / 137 skipped**. Failure SET identical both sides — the 4 known-flaky `test_statusline.py` worktree-identity tests; floor held. The −5 reconciles exactly: **12 tests deleted** (9 `TestOutcomeTracker` + 3 `TestExecutorWithMemory`) **− 7 added** (`TestExecutorMemoryIsInert`) **= −5**. Consumers: `tests/test_agent.py` **57 passed**; `test_agent + test_sparse_router + test_reasoning_context + test_specialist_modes + test_task_synthesizer + test_set_usd_primvar + test_vendored_deps` → **208 passed, 2 skipped**. Root package import re-checked live: `import synapse` OK, `synapse.AgentExecutor` resolves, `hasattr(synapse,"OutcomeTracker")` is **False** (the lazy `__getattr__` must not resurrect it). `python harness/rsi/verify.py` → **9 PASS / 0 FAIL / 0 PENDING**, proven rungs **14 → 13** (P2's count drops by exactly `A2`'s vacated L0 — the expected signature of a retirement, and the reason to read P2's number rather than only its status). |
| 2026-08-01 | RELAY | Execution arm built: `.claude/workflows/rsi-closure.js` (3 phases, ~7/5/4 agents, args-as-string parsed defensively) + `rsi-closure-orchestrator` agent + `RELAY.md` + operator card. No new predicates, no new bar (R140). File-overlap check for parallel SIGNAL worktrees: A1→routing/router.py, F→shared/router.py only, E→forge/engine/ — disjoint, verified by grep. |

| 2026-08-01 | RL-2 · E | SIGNAL fix for loop `E` (FORGE build counters). Defect re-confirmed at HEAD `c574d49`: `orchestrator.py:177` `fixes_applied += 1  # Optimistic`, `:214` `fixes_validated=0`, `reporter.py:65` rendering it. **First question answered: NO verification phase exists** — grep over `forge/` found no apply/verify/re-run function and no runnable entry point; `FORGE.md` Phase 5 is prose. The `:214` comment's promised phase was a phantom, so took the **honest-reporting** branch, not a fabricated validator. `fixes_validated` → `int \| None` with `None` = *unvalidated*; `reporter.validated_cell()` renders the word; `fixes_applied`/`fixes_failed`/`fixes_validated` all derive from new `FixOutcome` evidence. Suite **5341→5356 passed** (+15 new tests, same 4 pre-existing `test_statusline.py` worktree failures both sides). Mutation-checked: re-introducing the optimism fails 4 tests. `E` → **L0+L1, blocked L2**. verify.py: **9 PASS / 0 FAIL**. |
| 2026-08-01 | RL-2 · E CATCH | Two first-draft alignment tests asserted every report box row shares a width. They failed — the FORGE report boxes were **already ragged upstream of this change** (rows measure 61/58/55/52/63/62 chars). The tests were wrong, not the code; rewritten to the invariant that actually belongs to this fix: swapping the sentinel for a number must not change *that row's* width. A test that pins a defect you did not introduce is a false floor. |
| 2026-08-01 | RL-2 · E CRUCIBLE | **The fix's own tests did not pin the fix.** Crucible flipped `metrics.py:44` back to `fixes_validated: int \| None = 0` and **all 15 tests passed** — reproduced here exactly (4 red under the mutant, none of them from the original 15). The gap was one layer deep: the tests covered the `CycleMetrics` dataclass default (`schemas.py:420`) and `ForgeOrchestrator.process_results` above it, but never `MetricsTracker.compute_cycle_metrics` — the single function *between* them where every cycle's metrics are actually built. Closed with 4 tests (behavioural sentinel check, follow-through to `cycles.json` + the rendered box, `inspect.signature` pin, source pin), **run in both states**: default `0` → 4 red, default `None` → 26 green. |
| 2026-08-01 | RL-2 · E STALE DATA | The repo's own `forge/metrics/cycles.json` carried `fixes_validated: 0` on all three cycles, making `MetricsTracker.total_fixes_validated` return a **measured 0** for a project that has never run a validator (reproduced on the real file: `0` before, `None` after). Rewritten to `null`. Attribution correction: those rows are **hand-authored**, not fabricator output — they carry `friction_scenarios` / `scenario_results`, fields `CycleMetrics` has no equivalent of. Same fabrication, different author. Pinned by `test_committed_cycles_json_claims_no_validation_that_never_happened`. |
| 2026-08-01 | RL-2 · E PARTIAL | Validation was per-cycle and all-or-nothing: one `FixOutcome` carrying a re-run verdict flipped the entire cycle from *unvalidated* to a number, with the un-re-run remainder silently folded in. Added the denominator — `CycleMetrics.fixes_revalidated`, `validation_is_partial`, and a `! PARTIAL: N of M applied fixes re-run (K unvalidated)` report row. Mutating `validation_is_partial` to always-False fails 2 tests. |
| 2026-08-01 | RL-2 · E DEFERRED | `process_results` still accepts any `FixOutcome` list and cross-checks none of it. **Deferred on purpose:** there is nothing in `forge/engine` to cross-check against — no apply stage, no re-run stage, no execution surface — so any check would be a self-assertion in a different costume (the caller writes `scenario_id`, `applied` *and* `validated`). The claim this rung actually makes is narrower: the engine no longer manufactures counts on its **own** authority. Stated in the `FixOutcome` docstring so the type is not read as a verification boundary. Closing it needs the real verifier that also blocks L2. |
| 2026-08-01 | RL-2 · E SUITE | Clean stash-controlled measurement in the lane worktree: **5356 → 5367 passed** (+11, exactly the new tests), 137 skipped both sides, same **4** `test_statusline.py` worktree-identity failures on both sides. Floor not lowered. Verdict unchanged: `E` is **HONEST, NOT IMPROVING** — L0+L1, blocked L2. verify.py: **9 PASS / 0 FAIL**. |

---

## Standing state at frame

- **Registry honesty:** 9 PASS / 0 FAIL.
- **Closure:** **9 registered, 8 live** — `A2` RETIRED 2026-08-01 (`RL-3`), entry kept as a `P9` tombstone.
  0 of 8 live loops beneficial. 0 of 8 reach L3 CONSUMED. Highest rung anywhere is `A3` at **L2**.
- **`A2` is answered, by subtraction.** `OutcomeTracker` deleted, not wired: it never recorded an outcome and
  structurally could not, because `AgentExecutor` has zero non-test constructions. **A subtraction is not a
  closure** — the scoreboard did not move; the denominator did, and the registry now describes less code.
- **L1 failures: 3 at frame → 0 open.** All three closed on 2026-08-01 by `RL-2`, and every one of them
  *corrected the same day* after its crucible attacked the remedy itself — `A1` (`RL-2b`, the async DEEP
  verdict recorded at launch), `F` (`RL-2c`, `record_outcome` manufacturing a success from a truthy
  string), `E` (`RL-2c`, the mutant that survived E's own fix). Three for three, the first remedy was
  incomplete and the attack caught it. Scoped honestly: `FAST` is still structurally 1.0 by construction
  (documented, not fixed), and `A1`'s L2 obstacle is `handlers.py:1625` building the router with no
  `command_fn` **plus** zero traffic — not traffic alone, as `RL-2` first claimed and later retracted.
- **`F` is dormant, not unfed.** Nothing calls `record_outcome()` *and* nothing calls `filter_tools()`. L2 is
  a **wire-or-delete** decision before it is a wiring task.
- **Open finding filed under `O`:** a third frequency-only promotion door at
  `shared/conductor_advisor.py:296-324` (`REGISTRY.json` → `O` → `_third_door_note`). Recorded, not fixed.
- **None of it moved a closure number.** Three L1 rungs earned and one loop retired, and the scoreboard still
  reads 0 beneficial, 0 at L3. That is the harness working as specified, not a disappointing result: L1 was
  always a precondition for closure, never closure itself — and deleting dead machinery is progress of a
  different kind than closing a loop. Do not let one be reported as the other.
- **New open recommendation (`RL-3`, NOT acted on):** the whole `python/synapse/agent/` package has no
  production consumer by symbol grep — only its own `__init__` re-exports and its own tests. Bigger than the
  ratified cut; needs its own scoping pass, its own ratification, and a dynamic-access check. See
  `CHAMPION.md` → "`A2` RETIRED".
- **Next action:** finish `RL-3 DECIDE` — `A2` is **done (deleted)**; remaining are `F` wire-or-delete (same
  dormancy shape, same question) and `C` the substrate keystone. `RL-1 RECONCILE` is done.
- **Open human gates:** SPEC ratification · any L3→L4 advance · signal-semantics change (`RL-2`) ·
  `F` wire-or-delete · `C` substrate call · **`python/synapse/agent/` package-level cut (`RL-3`, new)** ·
  push/merge. *(`A2` wire-or-delete: CLOSED 2026-08-01 — ratified delete, executed by `RL-3`.)*
