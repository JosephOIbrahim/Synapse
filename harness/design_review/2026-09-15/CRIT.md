# CRIT — SYNAPSE panel against Pentagram practice · 2026-09-15

Design crit room, four lens partners (TYPE · SPACE · RESTRAINT · USE), three rounds, chair-mediated. Material: `harness/design_review/2026-09-15/MAP.json` (tokens · anatomy · review · lens). Every claim below cites a token name + value or a file:line; anything not verified against source is labelled UNVERIFIED. Read-only over the repo; this file is the only artifact written. A parallel measurement leg (`harness/design_review/2026-09-15/measure/numbers.json`, hython offscreen, M1–M7) was launched by the parent during round 3 and had not landed at compose time — every HOLD that rests on it is marked *pending measurement Mn*.

Principles under review: (1) typography carries hierarchy · (2) one type system, few sizes · (3) structural whitespace · (4) restraint — monochrome + one accent, no gradients/decorative borders/shadows/radii · (5) a grid the eye can feel · (6) copy as design · (7) state in form, never a second hue · (8) nothing that does not earn its place.

---

## The headline

The room agreed the panel is a typography-led, near-monochrome instrument whose marks are already Pentagram-grade — the MarkDot that shows state by arc length in one colour (`components.py:271, :285-303`), verbs set as flat type (`QPushButton#DsVerb` transparent / border none / padding 2px 0 / 11px / TEXT_SECONDARY #A0A0A0, `qss.py:182-186`), the underline tab (`DsPill` radius 0, 2px rule, `qss.py:119-128`), a contrast-solved grey ladder (TEXT_PRIMARY #C5C5C5 7.0:1 · SECONDARY #A0A0A0 4.6 · TERTIARY #868686 3.3 · BRIGHT #DEDEDE 9.0, `tokens.py:118-121, :200-205`) — with a second, unearned system glued underneath it: a 10/11/12 size smear (SIZE_MICRO 10 `tokens.py:314`, FONT_FLOOR_PX 10 provenance UNKNOWN `:345-352`), a written hue-as-emphasis policy (`qss.py:303` "colour emphasis only, never size, font, or layout"), eleven chromatic tokens on a face whose words already carry every state, four rhythm roles sharing one gap (shell/group/card/tag = SPACE_GRID[3] = 16, `rhythm.py:29-33`), two owners of the transcript turn beat, and enum words shipped where authored phrases exist (`gate_widget.py:218` `_level.upper()` vs GATE_LEVELS "Heads up / Quick review / Approve? / Confirm" `tokens.py:624-627`, zero references).

What it most needs is subtraction at zero vertical cost, not a redesign: delete SIZE_MICRO so the ramp is 11/12/15/19; retire the hidden fourth hue (SEND ink TEXT_ON_ACCENT #0F1F2B, chroma 28, `tokens.py:206`) and HOT_SOFT #D08A57 / NO_SOFT #D96975 so SIGNAL #8FB3D9 is the one action family; give the turn beat one owner (`message_formatter.py:68-69`); put the gate header on the GATE_LEVELS phrase with hierarchy in weight; delete the dead rows and hidden widgets — all priced BC-5 0 / D1 0 by SPACE.

What it must not lose: the ruled J3 speaker marks (YOU = SIGNAL, SYNAPSE = CONIFEROUS #6E8F72, `message_formatter.py:46`; pinned `tests/panel/test_j3_speakers.py:157-160`), the WARM #FF7759 MarkDot (J1), GUTTER 30 (`tokens.py:637`, Joe's call), the state sentence that never elides (`synapse_panel.py:1005-1011`), TYPE_ROLES as the single type authority (`tokens.py:371-379`), the 24/8 transcript rhythm (`message_formatter.py:68-69`), and zero box-shadow (`qss.py`, grep).

---

## Positions

### TYPE — typography carries the hierarchy (P1, P2, P7)

**Position.** The ramp is two systems glued together: a real scale on top (12 → 15 → 19, ratios 1.25/1.27, `tokens.py:317-319`) and a 1px smear underneath (10/11/12) where roles are told apart by weight, family, case and grey, never by size. P1 mostly holds for roles — size + weight live in TYPE_ROLES (`tokens.py:371-379`) and QSS adds only grey (`qss.py:290`); the MAP lens overstates "coloured not sized". It breaks where hue-as-emphasis is policy (`qss.py:302-304`; verb tones `:188-199`; badge kinds `:258-259`).

**10px is not defensible on the repo's own evidence.** `audit_panel.py:388` READABLE_FLOOR = 11 ("chrome must clear this") is checked only on rail-header widgets (`:402-403`) while ~15 live 10px sites ship: `qss.py:221` DsChip, `:256` DsBadge 10/600, `:665` gate header, `:675/:681/:686` fidelity, `:801`; `face_work.py:216/:222`; `integrity_readout.py:101/:112`; `face_review.py:450/:490/:506`; timestamp `max(11-1, 8)` = 10 `message_formatter.py:377-379`. FONT_FLOOR_PX = 10 is pinned to "the smallest size shipped" (`tokens.py:332-352`) — circular.

**Also.** Transcript `_MONO` = Consolas (`message_formatter.py:75`) is off-token vs TYPE_ROLES["code"] = FONT_MONO_CSS (`tokens.py:376`). Status 11/500 and tag 12/500 mono ask for a weight Space Mono does not ship (Regular 400 + Bold 700 only, `fonts/README.md`; fontload maps ≥600 → setBold `fontload.py:158-190`) — rendered weight UNVERIFIED, *pending measurement M7*. Self-correction in round 2: the speaker label sits inside the ruled body cell (`message_formatter.py:417-418`), label and body share x.

**First change.** P2 · delete SIZE_MICRO (`tokens.py:314`); SIZE_LABEL → SIZE_SMALL (`:322`); FONT_FLOOR_PX 10 → 11 (`:345`, = audit `:388`); replace the literal 10s; drop the `-1` at `message_formatter.py:379` → ramp 11/12/15/19 · M · new.

**Strengths named.** TYPE_ROLES is a real role system (family + size + weight + tracking per role, mono/sans rule written as reasoning `tokens.py:364-370`); 12 → 15 → 19 is deliberate; WORDMARK 0.16em vs BRAND 0.286em at weight 600 is typographer's logic (`tokens.py:395-405`); state-in-form exists where it matters (DsVerb, DsPill).

### SPACE — structural whitespace and the grid (P3, P5)

**Position.** The big rhythm is designed, the leaves are arithmetic. Four nested roles share one gap (shell/group/card/tag = SPACE_GRID[3] = 16, `rhythm.py:29-33`; `tokens.py:491`) so nesting has no spatial signature; roles use only {0, 4, 12, 16} of a 7-rung ladder. The transcript turn beat has two owners in two files — `message_formatter.py:68-69` (_GROUP_MARGIN_Y 24 / _MSG_MARGIN_Y 8 as table margin, not density-scaled) and `chat_display.py:190-191` (block topMargin = gap(ROLE_GAPS group 16 | row 12, density)) — source sum standard 40/20, airy 48/26, none on the ladder; rendered collapse UNVERIFIED, *pending M4*. Five left edges on a 280px interior: 30 chrome (GUTTER `tokens.py:637`); 46 composer text (1px BORDER + 15 pad, `qss.py:271-272`) = consent tag (margin-left 16, `qss.py:422`); 50 transcript body (Qt document margin 4 — no setDocumentMargin call — + rule 2 + spacer 14, `message_formatter.py:404-405`); 55 wordmark (30 + 16 + 4 + 5, `synapse_panel.py:938, :986`); khint and footer links centred (`synapse_panel.py:2510, :2541-2549`; `qss.py:206`). Tag leaf (10, 6) and _BUBBLE_MARGIN_Y 2 are divisions, not rungs (`rhythm.py:39-41`; `chat_display.py:69`).

**Disagreements with the MAP lens.** Composer "16px 15px" is 16 border-box (1 + 15) → keep. Card header 39 + 1px border = 40 = footer 40 (`qss.py:434, :446`) → parity, keep. GUTTER 30 → 32 is the wrong sign for D2 (the only standing remedy is narrower: 24 = SPACE_LG, body 264 → 276, helps C4) but sits inside `tokens.py:637` "26–36 is safe" → Joe's call, not a partner's.

**First change.** P3 · transcript · delete `chat_display.py:190-192` bf.setTopMargin so `message_formatter.py:68-69` solely owns the turn beat; keep the group/row property tagging · BC-5 0 / D1 0 · new.

**Strengths named.** One vocabulary (`rhythm.py:15`; roles index SPACE_GRID, never literals); GUTTER has one consumer so it cannot lie (`rhythm.py:42`); J5's top-edge air is scoped to the owner via rhythm_edge (`rhythm.py:44-51`) — structural; the reading column is designed (90-char measure clamped 460..1100, `chat_display.py:283-285`); gaps scale, paddings fixed is a real rule (`tokens.py:499-512`).

### RESTRAINT — monochrome + one accent, state in form (P4, P7)

**Position.** The resting CHAT face is nearer monochrome than its token file. At boot it paints the SIGNAL family (underline `qss.py:127` + SEND fill SIGNAL_DEEP #627A93 `:283`), the ruled WARM mark, Doctor yellow, and one hidden fourth hue: the SEND ink TEXT_ON_ACCENT #0F1F2B (`tokens.py:206`; chroma 28, hue ~206° = bucket 13 — the bucket the 2026-09-14 review blamed on a SIGNAL straddle; SIGNAL_DEEP ~211° is bucket 14 with SIGNAL). Zero headroom on the ≤3 pin (`test_bc_wave.py:329`) is the ink, not the accent. Eleven chromatic tokens serve a face whose words already carry every state.

**Evidence.** Three ceilings, three rules: `tokens.py:225` two-accent; `qss.py:302` "never a third"; `test_bc_wave.py:295` ONE accent for actions + state marks. The emphasis rule *is* hue (`qss.py:303`; hero = WARM `:195`; tone ok/hot/accent/doctor = four hues on one verb class `:188-199`) — this rule generates the drift. MarkDot is already form (`components.py:271` one colour; `:285-303` open 300° arc at rest / filling arc working / ring + check done); the STATUS hue slot (`tokens.py:613-619`) has no visible consumer — only the hidden `_foot_dot` (`synapse_panel.py:1063-1101`). Gradient `qss.py:29` ATMOSPHERE_DELTA 4 over 760px = sub-perceptual decoration. HOT_SOFT #D08A57 (bucket 1) beside WARM (bucket 0): DsStop rests HOT_SOFT and hovers WARM (`qss.py:103-111`) — changes warm family on hover. NO_SOFT #D96975 has no consumer in MAP (UNVERIFIED dead). F4's rebuilt card at 0.19% chromatic / 2 buckets (`DECISION-BRIEF.md:22-27`) proves the gate card works without GATE_LEVELS hues or tone borders.

**Hue ledger (boot, resting CHAT).** SIGNAL #8FB3D9 THE accent · SIGNAL_DEEP #627A93 accent fill · TEXT_ON_ACCENT #0F1F2B hidden 4th hue · WARM #FF7759 second accent, RULED (MarkDot; hero verbs open) · HOUDINI_TAB_YELLOW #B98620 third accent = prominence not state (B1) · CONIFEROUS #6E8F72 state mark, RULED (none at boot) · HOT_SOFT decoration · NO_SOFT dead · FIRE/GROW/WARN/ERROR legacy semantic set, redundant with the words · SLATE #555555 achromatic · MUSHROOM #92867A ~24 chroma UNVERIFIED.

**First change.** P4 · SEND · TEXT_ON_ACCENT #0F1F2B → neutral near-black; re-solve the 5.60:1 claim at `qss.py:73-83` · S · new. Boot rest becomes {0, 14} + Doctor's bucket 2 = 3, so B1 is decided on principle, not under a red pin.

**B1 read (principle, not ruling).** P4: the face already spends its accent (SIGNAL) and its ruled mark (WARM); yellow is a third family. P7: yellow is not state (nothing on Doctor changes) — it is prominence, which the sheet's ladder (hero WARM / quiet TERTIARY, `qss.py:195-197`) plus "never a third" (`:302`) already forbids; `tokens.py:254` "keep diagnostic emphasis separate from warning status" argues for distinction by form, not a new hue. Option (1) retargets the instrument — the Cohere finding says bucket count is the rule doing the work (`REFINEMENT-COHERE.md:33-41`), so (1) weakens the measure; (2)/(3) are principle-consistent; yellow is principled only in the host-native direction where it is *the* accent.

**Strengths named.** MarkDot — one pen, one colour, state by arc (the P7 exemplar); the palette polices itself (OK_SOFT aliased, `tokens.py:273-278` "two live greens is how a colour system grows a second authority"; zero box-shadow; contrast-solved ladder); consent card 0.19% / 2 buckets.

### USE — copy as design, what earns its place (P6, P8)

**Position.** The copy is Pentagram-grade where authored as outcome phrases (STATUS, GATE_LEVELS, khint) and fails where the code ships an enum or subsystem name instead of the phrase it already owns. The rail's lead verb "Connect" (`synapse_panel.py:1035`) starts the bridge, not the model the artist needs — `:3129` "Connect a model before sending. Use Connect models below the prompt." proves it. At boot the face makes two contradictory claims: "Ready. What are we building?" (`:1342`; "Ready" = STATUS connected `tokens.py:614`) one row under "Not connected" (`tokens.py:619`; `:1005`). One surface, three names: placeholder "Ask SYNAPSE… · / commands" `:280` + "Commands" `:2513` + overflow "Palette {key}" `:2711`. The gate header ships the enum `_level.upper()` (`gate_widget.py:218` → "APPROVE" / "CRITICAL") while GATE_LEVELS has zero references, and the footer verb "APPROVE" (`:265`) repeats the header word with a different meaning. "Fidelity UNKNOWN" (`:128`) / "{n} violations" (`:499/:735`) are pipeline words on a row born UNKNOWN (`:474`). "PLAN · routed %s" (`face_work.py:336`) names the router; three resting phrases on the Work face ("Standing by" `:196`, "waiting for work" `:215`, "PLAN" `:221`). "Events" (`:2519`) names-system; "Recipes" is not Houdini vocabulary; "Session · Revoke" (`:2526`) hidden + overflow duplicate; "Ground the corpus" names RAG; "TOKEN" pill (`:1318`) → "USAGE" per J2; "signed {model}" (`message_formatter.py:441`) redundant with the rail token unless the model changed.

**First change (as it stands after round 3).** P6 · rail row 2 · "Connect" → "Bridge" (6 mono cells, one fewer than "Connect"; tooltip unchanged); overflow gains "Start bridge" if row 2 binds at 380 post-F1 (*pending M2*); "Connect models" keeps the artist's verb · S · new. Disagrees with the lens's "Connect models → Models" (a noun naming the system).

**Empty state.** "What are we building?" — delete "Ready." (booked by STATUS connected; contradicts "Not connected" one row above; no undo promise belongs here — undoability is per turn, C2a).

**Delete list.** `_session_permission_btn` (`:2526-2537`) · "waiting for work" · "· routed %s" · "/ commands" · the integrity row (`gate_widget.py:128/:499`) until a non-UNKNOWN value is reachable · "signed {model}" when unchanged · "TOKEN" pill per J2 (and then the lone "CHAT" pill `:1308` + ribbon) · the six hidden rail owners (`:1063-1101`).

**C2a / D3 / D4 (principle, not ruling).** The receipt shows the verdict word alone ("Can undo" beats "Undoable" at 10px; "mcp · 4/4" → tooltip/Health); D3: two "← REVERT" controls must name scope ("Undo turn" vs "Undo this proposal"); D4: the honest word per `gate_widget.py:382` is "Undo requested" and it needs a dwell longer than DUR_SLOW 320ms (`tokens.py:586`).

**Strengths named.** STATUS `tokens.py:614-619` — six states, zero nouns; khint `:2507-2513` — the label is the manual; "Cancel cook — {node}", "Larger text / Default text" — verb + object in Houdini's words.

---

## Critiques and concessions

12 critiques in round 2, 12 answers in round 3. **4 CONCEDE · 7 CONCEDE-IN-PART · 1 HOLD.**

| # | from → to | the critique (with evidence) | answer | what changed |
|---|---|---|---|---|
| 1 | TYPE → RESTRAINT | "tone=hot / BLOCKED → weight 600": `designsystem/fonts/` ships SpaceMono-Regular + SpaceMono-Bold only (glob); weight tokens are 400/500/600 (`tokens.py:358-360`), no 700; fontload maps ≥600 → setBold (`fontload.py:158-190`) but a QSS `font-weight: 600` row requests a DemiBold the font lacks (Qt substitute UNVERIFIED). Same trap live: status 11/500 mono (`tokens.py:378`). | **CONCEDE** | No QSS font-weight row anywhere. tone=hot → TEXT_PRIMARY at the verb's own weight, no hue, no bold of its own (REJECT needs no mark: APPROVE is the one accented thing, `gate_widget.py:31, :181`). HOT_SOFT retires; DsStop rest → WARM. WEIGHT_BOLD = 700 is TYPE's token to file. (BLOCKED → 600 had already been self-withdrawn in round 2 → TEXT_BRIGHT at the tag's weight.) |
| 2 | TYPE → SPACE | ALSO(1) deleting `#DsHeader` margin-bottom (`qss.py:344-346`) buys BC-5 +8 std / +12 airy: the selector is a bare `QWidget` (`synapse_panel.py:926`); Qt honours QSS margin only on box-model widgets, so the rule paints nothing, the seam is 25 not 33, "airy −13 → −1" is phantom. DsVerb margin 8/8 (`qss.py:339-341`) sits on a QPushButton and *is* real. | **CONCEDE** | `#DsHeader` is a bare `QtWidgets.QWidget()` with WA_StyledBackground (`synapse_panel.py:703-706`, renamed `:926`); no test measures it. Withdrawn: "+8/+12" and "airy −13 → −1"; seam by layout is 25 (verb 8 + shell 8 + HAIR 1 + ribbon 8). `:344-346` stays on the delete list as a P8 no-op. Re-filed: DsVerb margin 8/8 → margin 0 + min-height 26 (audit floor): row 2 ≈ 31 → 26 std (BC-5 +27 → +32), ≈ 39 → 26 airy (−13 → 0, touches the bar). Derived, UNVERIFIED — *pending M1*; A2's −13 stands until measured. |
| 3 | TYPE → USE | ALSO(1) "gate header → GATE_LEVELS phrase": `gate_widget.py:218` builds it with `_tag(...)`; the tag role forces mono + AllUppercase + 0.06em (`rhythm.py:62-78`) so "Approve?" ships as "APPROVE?" and the repeated-word complaint survives. | **CONCEDE** | Delete the enum tag at `:218`; header band = GATE_LEVELS phrase (`tokens.py:624-627`) as a sans label-role title, 12/500, sentence case; footer verbs `:262/:265` → "Reject" / "Approve", sentence-case mono. Deleting the tag also removes the BLOCKED HOT_SOFT paint (`:149, :154-155`); CRITICAL is carried by "Confirm" + the body line "Arbitrary code execution" (`:240`). Moots #11(b). |
| 4 | SPACE → USE | "Connect" → "Start bridge" costs width, not px height: Space Mono 11px at LABEL 0.15em (`synapse_panel.py:1112-1115`; `tokens.py:397-402`), 7 → 12 cells ≈ +41px (derived, UNVERIFIED). Row 2 has zero slack at the docking floor (sentence never elides `:1009-1011` + gaps + Connect + 12 spacer `:1049` + Doctor + overflow 32); R3-01 airy 393 vs 400 = 7px headroom; D2's remedy (GUTTER 24, −12) cannot cover +41. | **CONCEDE-IN-PART** | "Start bridge" withdrawn; new label "Bridge" (6 cells), tooltip unchanged — resolves the collision with "Connect models", not the noun-vs-outcome fault. HOLD on the slack evidence: R3-01's 393 was measured with the F1 verb rail ("verb rail 237 + four group gaps"), retired (`README.md:18-21`); airy is off the UI (J4) — "zero slack at row 2" derives from a retired layout, UNVERIFIED post-F1, *pending M2*. If row 2 binds at 380, the verb moves to the overflow as "Start bridge". |
| 5 | SPACE → TYPE | ALSO(4) caption 11 → 12: height is not the cost (row 2 is verb-set ≈ 31, `qss.py:182-186, :339`; BC-5 0; SIZE_MICRO → 11 costs 0 on CHAT, +2–3 on Work). Width *is*: the sentence floor (`:1010-1011`) widens ≈ +7px = exactly R3-01's 7px airy headroom. | **CONCEDE-IN-PART** | Height pricing withdrawn; width cost accepted. ALSO(4) withdrawn whole — caption at 12 on Work and 11 on the rail is two sizes for one role, worse under P2. Caption stays 11 sans; the ramp is 11/12/15/19. GUTTER 24 is Joe's call. |
| 6 | SPACE → RESTRAINT | ALSO(4) deleting tone borders `qss.py:249-251`: BORDER #363636 == SURFACE #363636 (`tokens.py:139`; C1 unruled) so the WARN/FIRE/ERROR rows are the only painting edge on APPROVE/CRITICAL cards; with radius → 0 the consent card becomes an edgeless SURFACE patch. Sequence after C1 or pair with air (direct-face gap 16 → 24 around the consent slot only). Also: (8,4) leaf does not collide with bold — mono advance unchanged. | **HOLD** (on sequencing) | The rows are dead: no producer sets `tone` on DsCard (`gate_widget.py:142` sets tone on verbs only, `:155/:346` status on tags; docstring `:172` "A DsCard (no tone - the level is never a border hue)"); F4's 0.19% / 2 buckets proves they never paint. The card is *already* the edgeless patch — C1's defect, not this deletion's. Delete as P8 dead code now, 0px. Backs SPACE's air pairing as the C1 remedy in form, filed under C1. Conceded from its own round 2: the tag-width claim was wrong; SPACE's (8,4) leaf stands. |
| 7 | RESTRAINT → TYPE | "one hue only on the single primary verb per view": the hue is unnamed and the count is wrong — hero verbs take WARM (`qss.py:195`), a second action accent against `test_bc_wave.py:295`; a view can hold two primaries when the gate is lent into the consent slot (BC-6b, `synapse_panel.py:1442-1448`): SEND (SIGNAL_DEEP) and APPROVE (tone=accent → TEXT_ACCENT = SIGNAL, `gate_widget.py:265`; `qss.py:190`). J3's speaker hues are ruled and pinned (`test_j3_speakers.py:157-160`) — "type never got a turn" is a supersession request, not a crit change. | **CONCEDE-IN-PART** | Hue named: the SIGNAL family (SIGNAL `tokens.py:29` / TEXT_ACCENT `:204` / SIGNAL_DEEP `:239`); clause becomes "actions: one family, SIGNAL, any count". Lent-card yield blocked by pin `test_bc_wave.py:651-652` ("APPROVE the one accented thing on the card, REJECT hot") — until that pin moves, SEND + APPROVE both SIGNAL = one family, in rule. Speaker point dropped from the crit; a J3 supersession if anyone files it. |
| 8 | RESTRAINT → USE | "Doctor → Check · B1-adjacent": the sweep-A pin protects the QSS rule string, not the label (`test_panel_sweep_a.py:177-178`, count==1 `:192`, guards `:226/:229`); paint comes from `setProperty("tone","doctor")` (`synapse_panel.py:1042`) — "Check" ships yellow unchanged and makes B1 worse: hue on the no-consequence verb (tooltip `:1044` "no model request or scene changes") beside a grey gating Connect whose `variant="primary"` no longer matches after the DsVerb rename (`:1112-1115` vs `qss.py:55-59`); and "Check" deletes the rationale `tokens.py:254` gives the yellow. | **CONCEDE-IN-PART** | Pricing/bundling conceded: B1-dependent, ships only with B1 option (2); removed from ALSO. HOLD on "makes B1 worse": the inversion exists today; `tokens.py:254` is a hue-vs-WARN rationale, not a defence of the word. |
| 9 | RESTRAINT → SPACE | Tag leaf (10,6) → (8,4) is priced on a 12/500 mono tag (`qss.py:414-425`), but Space Mono ships 400 + 700 only and ≥600 → setBold; TYPE moves the tag to 400/700, so a 700 uppercase glyph lands in the smaller leaf; rendered width UNVERIFIED. | **CONCEDE-IN-PART** | HOLD the leaf: (8,4) is weight-independent — monospaced, Bold keeps Regular's pitch and line box (*pending M3* for the rendered width). CONCEDE sequencing: `rhythm.py:40-41` + `qss.py:422` land in the same commit as TYPE's tag-weight naming. |
| 10 | USE → TYPE | Verbs tone → 700 + TEXT_PRIMARY: gate footer literals are "REJECT" `gate_widget.py:262` / "APPROVE" `:265` — caps in code, not QFont AllUppercase; both at 700 flatten the only hierarchy the gate has (timeout → rejection, CLAUDE.md §1.2.1: APPROVE consequential, REJECT default). F4 proved the card at 0.19% / 2 buckets; "one hue on the primary verb" reintroduces hue where zero worked. | **CONCEDE** | ALSO(2) rewritten: APPROVE 700 TEXT_PRIMARY, REJECT 400 TEXT_SECONDARY; literals → "Reject" / "Approve" in the same change; header "Approve?" (sans) + verb "Approve" (mono) = call/response. One-hue clause withdrawn for the card. Caveat: `test_bc_wave.py:650-654` pins level-as-tag, APPROVE accented, REJECT hot — the change must carry or supersede that pin or it lands red. |
| 11 | USE → RESTRAINT | (4) "retire FIRE/GROW/WARN" priced M: the STATUS hue consumer is not UNVERIFIED — `_foot_dot` is built in no layout and hidden (`synapse_panel.py:1063-1101`); the rail reads slot [2] only (`:1005`) → STATUS retirement is zero-pixel; the M is bullets (`message_formatter.py:99-103`) + badge kinds (`qss.py:258-259`). (3) "600" renders as 700 = the weight TYPE gives every verb, so BLOCKED loses its mark; use the 2px left rule DsChip uses (`qss.py:219`). | **CONCEDE-IN-PART** | (a) conceded: slot [0] has no visible consumer (`synapse_panel.py:1004/:1079/:3719` read [2]; `:3720` drives MarkDot, WARM only; `:3800/:3805` feed the hidden `_foot_dot`) → split: STATUS / GATE_LEVELS slot [0] → one grey, tuple shape kept · S · 0px; bullets + badge kinds as their own S–M. (b) held: the tag is RADIUS_ROUND 999 (`qss.py:417`); DsChip's 2px edge works only because DsChip is radius 0 (`:220`) — a border-left on a 999-radius pill is a crescent (rendering UNVERIFIED). BLOCKED stays TEXT_BRIGHT at the tag's weight; if the tag squares under C3's lineage it takes USE's rule. Mooted anyway if #3 deletes the tag. |
| 12 | USE → SPACE | FIRST (drop `chat_display.py:190-192` setTopMargin, "BC-5 0, D1 0 · S") is the `row` KeyError site MAP names as the prerequisite for SUBTRACT/SYSTEM (conflict (c)), with two pinning tests (`test_panel_rhythm_owner.py:233`, `test_panel_camera_rhythm_qt.py:201`) and a ratified order "SUBTRACT then SYSTEM" (SCAFFOLD §7 ruling 3); S omits both. Separately, SPACE's D1 objection to USE's deletions does not hold: the ribbon is region order 2, outside the faces stack whose page 0 owns D1. | **CONCEDE-IN-PART** | Effort S → M (`test_panel_camera_rhythm_qt.py:201` may pin the block margin itself; `:233` unread). HOLD sequencing, inverted: `chat_display.py:191` is the *only* ROLE_GAPS index in chat_display — deleting it removes conflict (c)'s KeyError prerequisite, so this lands BEFORE SUBTRACT, not after. HOLD D1/BC-5: USE's deletions are on the Work face (faces page 1), inside the stack, not the ribbon; BC-5's predicate is `p._chat.height()/H` on page 0 (`test_bc_wave.py:626-627`) → Work-face deletions are BC-5 0, D1-neutral iff page 0 drives. |

**Withdrawals across the rounds (6):** TYPE ALSO(4) caption → 12 (whole); TYPE's speaker-rule point (dropped, J3 supersession if filed); SPACE's "+8 std / +12 airy" and "airy −13 → −1" pricing; USE's "Start bridge"; USE's "Doctor → Check" from ALSO (now B1-dependent); RESTRAINT's "BLOCKED → weight 600" (round 2, self-withdrawn) and its tag-width claim (round 3).

---

## What is already Pentagram-grade

An honest crit names strengths. Evidence per item.

- **State by form, one colour — MarkDot.** `components.py:271` one colour; `:285-303` open 300° arc at rest / filling arc while working / ring + check when done. The P7 exemplar; the lens's "STATUS colour table worn by the MarkDot" was wrong (RESTRAINT, confirmed by USE's consumer trace `synapse_panel.py:3720` WARM only).
- **Verbs set as type, not buttons.** `QPushButton#DsVerb`: transparent, border none, padding 2px 0, 11px, TEXT_SECONDARY #A0A0A0 (`qss.py:182-186`). The word is the control.
- **Tabs on a baseline track.** `DsPill`: radius 0, no fill, 2px rule under the active word, TEXT_TERTIARY #868686 → TEXT_BRIGHT #DEDEDE (`qss.py:119-128`). The filled pill was retired on purpose and the comment says why (`qss.py:116-118`).
- **The chip as a cell, not a box.** `DsChip` radius 0, border-left STROKE_PX 1.5 → 2px, asymmetric 3/8/3/7 padding (`qss.py:217-227`).
- **One type authority.** TYPE_ROLES carries family + size + weight + tracking per role (`tokens.py:371-379`); QSS carries only grey (`qss.py:290`); the mono/sans rule is written as reasoning (`tokens.py:364-370`). The top of the ramp is a real scale: 12 → 15 → 19 (SIZE_BODY 12 `:317`, SIZE_TITLE 15 `:318`, SIZE_HERO 19 `:319`). WORDMARK 0.16em vs BRAND 0.286em at weight 600 is a typographer's decision (`tokens.py:395-405`).
- **Monochrome done as engineering.** TEXT_PRIMARY #C5C5C5 solved to 7.0:1, SECONDARY #A0A0A0 4.6, TERTIARY #868686 3.3, BRIGHT #DEDEDE 9.0, DISABLED #636363 2.0 against the worst landing surface (`tokens.py:118-121, :200-205`); seeded from the host pane and re-solved on any grey (`tokens.py:123-158`; sweep `audit_panel.py:111-153`).
- **Zero box-shadow** anywhere in the stylesheet (`qss.py`, grep returns nothing). Depth is elevation greys + hairlines only.
- **One spacing vocabulary.** SPACE_GRID = (4, 8, 12, 16, 24, 32, 48) (`tokens.py:491`); roles index the ladder, never literals (`rhythm.py:15, :26-36`); GUTTER 30 has exactly one consumer (`rhythm.py:42`); gaps scale with density, paddings stay fixed (`tokens.py:499-512`); J5's edge air is scoped to its owner through rhythm_edge (`rhythm.py:44-51`).
- **The reading column is designed.** ~90-char measure clamped 460..1100px with symmetric viewport margins (`chat_display.py:283-285, :339-347`).
- **Transcript rhythm bought where the mechanism is real.** 24 between speakers, 8 within (`message_formatter.py:68-69`), with the honest note that CSS line-height is inert in QTextDocument (`:57-67`); the +1.0px absolute leading is applied where it actually works (`tokens.py:522-533`; `chat_display.py:477-497`).
- **Copy that names the outcome.** STATUS "Ready / Working on it / Standing by / Worth a look / We hit a snag / Not connected" — six states, zero nouns (`tokens.py:613-619`); the state sentence is floored to its widest phrase in its own font and never elides (`synapse_panel.py:1005-1011`); khint "Enter sends · Shift+Enter adds a line" (`:2507-2513`) — the label is the manual; "Cancel cook — {node}", "Larger text / Default text" — verb + object in Houdini's words; GATE_LEVELS "Heads up / Quick review / Approve? / Confirm" already authored (`tokens.py:624-627`) — the phrases exist, they are just not wired.
- **The palette polices itself.** OK_SOFT aliased to CONIFEROUS with the reason written down (`tokens.py:273-278`); the consent card was rebuilt to 0.19% chromatic / 2 buckets (`DECISION-BRIEF.md:22-27`); the restraint floor 0.82–0.84% / 3 buckets stands (`README.md:3-6`).
- **The stylesheet argues with itself in writing.** `qss.py:303-315` is a design rationale, not a comment — which is also where the one policy the room rejects (hue as emphasis) is written, so it can be rewritten in the same place.

---

## The changes, ranked

Ranked by: conceded by the room · zero vertical cost · unblocked by pins or measurement. Each line: principle · region · exact change · effort · prior ruling id or "new". *Top five are the deliverable.*

1. **P2 · type scale · delete SIZE_MICRO.** `tokens.py:314` SIZE_MICRO = 10 deleted; `:322` SIZE_LABEL → SIZE_SMALL 11; `:345` FONT_FLOOR_PX 10 → 11 (= `audit_panel.py:388` READABLE_FLOOR); replace the literal 10s at `qss.py:221, :256, :665, :675, :681, :686, :801`, `face_work.py:216, :222`, `integrity_readout.py:101, :112`, `face_review.py:450, :490, :506`; drop the `-1` at `message_formatter.py:379`. Ramp becomes 11 / 12 / 15 / 19. · **M** · new · CHAT 0px; Work +2–3px (D1-neutral iff page 0 drives). Backed TYPE, priced SPACE, no HOLD. BP4-PANELFONT probe still owed.
2. **P6 + P1 · gate card · phrase header, weighted verbs.** Delete the enum tag `_level.upper()` at `gate_widget.py:218`; header band = GATE_LEVELS phrase (`tokens.py:624-627`) as sans label-role 12/500 sentence case; footer literals `:262` "REJECT" → "Reject" at 400 TEXT_SECONDARY, `:265` "APPROVE" → "Approve" at 700 TEXT_PRIMARY (via QFont ≥600 → SpaceMono-Bold; no QSS font-weight row); tag deletion also removes the BLOCKED HOT_SOFT paint (`:149, :154-155`). · **S** · new · 0px on the 39px band. **Must carry or supersede `tests/panel/test_bc_wave.py:650-654`** (level-as-tag, APPROVE accented, REJECT hot) or it lands red. Backed USE + TYPE + RESTRAINT.
3. **P3 · transcript · one owner for the turn beat.** Delete `chat_display.py:190-192` bf.setTopMargin; `message_formatter.py:68-69` (_GROUP_MARGIN_Y 24 / _MSG_MARGIN_Y 8) is the sole author; keep the group/row property tagging. · **M** · conflict (c) — lands **before** SUBTRACT/SYSTEM as its KeyError unblocker (`chat_display.py:191` is the only ROLE_GAPS index in that file) · BC-5 0 / D1 0. `test_panel_camera_rhythm_qt.py:201` may pin the block margin (UNVERIFIED); rendered collapse *pending M4*.
4. **P4 · SEND · retire the hidden fourth hue.** `tokens.py:206` TEXT_ON_ACCENT #0F1F2B → neutral near-black (chroma 0); re-solve the 5.60:1 claim at `qss.py:73-83` for SIGNAL_DEEP #627A93 / SIGNAL #8FB3D9 / SIGNAL_PRESS #7398BE fills. · **S** · new · 0px. Removes bucket 13 from the boot face; rest becomes {0, 14} + Doctor's 2 = 3 against `test_bc_wave.py:329`, so B1 is argued on principle rather than under a red pin. *Pending M6* (bucket count with the ink swapped).
5. **P4 + P8 · palette · retire HOT_SOFT and NO_SOFT; SIGNAL = actions, one family, any count.** `tokens.py:271` HOT_SOFT #D08A57: DsStop rest → WARM #FF7759 (`qss.py:103-111`), tone=hot → TEXT_PRIMARY at the verb's weight (`qss.py:189`), BLOCKED tag → TEXT_BRIGHT at the tag's weight (`qss.py:425`; `gate_widget.py:155`) — mooted by #2 if the tag goes; `tokens.py:270` NO_SOFT #D96975 deleted if consumer-less (UNVERIFIED). Write into `qss.py:302-306`: "actions: one family, SIGNAL, any count; WARM = the ruled mark + Stop; CONIFEROUS ruled (J1/J3)". · **S** · new · 0px.
6. **P8 · gate card · delete the dead tone-border rows.** `qss.py:249-251` (warn WARN / approve FIRE / critical ERROR) — no producer sets `tone` on DsCard (`gate_widget.py:142, :155, :346`; docstring `:172`). · **S** · C1-adjacent · 0px. The edgeless SURFACE patch (BORDER #363636 == SURFACE #363636, `tokens.py:139`) is C1's defect; the air pairing (direct-face gap 16 → SPACE_LG 24 above/below the consent slot only while a card is shown; −16px BC-5 while shown, 0 at rest) is filed under C1.
7. **P7 · ribbon · underline in value, not hue.** `qss.py:127` `DsPill[active=true]` border-bottom 2px SIGNAL #8FB3D9 → TEXT_BRIGHT #DEDEDE. · **S** · new · 0px. SIGNAL then paints actions only (SEND `:283`, `:focus` `:275`, verb `:hover` `:187`, YOU rule per J3).
8. **P4 + P8 · root · flat ground.** `qss.py:29` `QWidget#DsRoot` qlineargradient #2E2E2E → #262626 → `{t.PANEL}`; delete `atmosphere()` / ATMOSPHERE_DELTA 4 (`tokens.py:565-577`). · **S** · new · 0px.
9. **P7 · state slots and marks.** STATUS (`tokens.py:613-619`) and GATE_LEVELS (`:623-628`) slot [0] → one grey, tuple shape kept (consumers read [2]: `synapse_panel.py:1004, :1079, :3719`; `:3720` drives MarkDot WARM only; `:3800, :3805` feed the hidden `_foot_dot`) · **S** · 0px · new. Transcript status bullets `message_formatter.py:99-103` → one glyph set (● / ○ / ✕) in TEXT_TERTIARY; DsBadge kinds `qss.py:258-259` deleted; then FIRE/GROW/WARN/ERROR slots retire. · **S–M** · F4 (closed) is the precedent.
10. **P5 · one content edge at 46.** `ChatDisplay.document().setDocumentMargin(0)` (body 50 → 46 = composer text 46 = consent tag 46 = GUTTER 30 + 16); khint `synapse_panel.py:2510` AlignCenter → AlignLeft; `_ShortcutLayout` left-packed (`:2541-2549`); `qss.py:206` DsFooterLink text-align center → left; the wordmark stays at 55 as the one named lockup exception; body column = exactly 264. · **S** · C4, C6 (settles the 264/256 contest) · 0px · *pending M5*.
11. **P3 · rail row 2 · the rung belongs on the row.** `qss.py:339-341` DsVerb margin-top/bottom 8 → margin 0, min-height 26 (the audit's interactive-target floor); `qss.py:344-346` `#DsHeader` margin-bottom deleted as a P8 no-op (bare QWidget, `synapse_panel.py:703-706`). · **S** · B2, A2 · derived: row 2 ≈ 31 → 26 std (BC-5 +27 → +32), ≈ 39 → 26 airy (−13 → 0) — UNVERIFIED, *pending M1*; seam becomes 16 + 1 on-ladder.
12. **P3 · rhythm roles · break the F12 collision downward.** `rhythm.py:29-33`: shell 16, group 16 stay; card → SPACE_12; tag → SPACE_SM 8; tag leaf `rhythm.py:39-41` (10, 6) → (8, 4); `qss.py:422` follows. Nesting reads 16 / 12 / 8. · **S** · new · 0px at rest · lands in the same commit as #19 (tag weight named); *pending M3* for the 700-weight width.
13. **P2 · transcript mono · one mono.** `message_formatter.py:75` `_MONO = "'Consolas', 'Courier New', monospace"` → `t.FONT_MONO_CSS` (Space Mono chain, `tokens.py:302`). · **S** · new · 0px. (Host-native direction keeps Consolas instead — see Directions.)
14. **P6 · rail · "Connect" → "Bridge".** `synapse_panel.py:1035` label → "Bridge" (6 mono cells; tooltip unchanged); overflow gains "Start bridge" if row 2 binds at 380 post-F1 (*pending M2*); drop `variant="primary"` on the same line — dead after the DsVerb rename (`:1112-1115` vs `qss.py:55-59`). · **S** · new · 0px.
15. **P6 · empty state.** `synapse_panel.py:1341-1343` "Ready. What are we building?" → "What are we building?" ("Ready" is STATUS connected `tokens.py:614` and contradicts the rail's "Not connected" `:619`, `:1005` one row above). · **S** · new · 0px.
16. **P6 + P8 · one name for the palette.** `synapse_panel.py:280` placeholder "Ask SYNAPSE… · / commands" → "Ask SYNAPSE…"; `:2711` overflow "Palette {key}" → "Commands {key}"; keep "Commands" `:2513`. · **S** · new · 0px.
17. **P8 · Work face resting phrases.** Delete "waiting for work" (`face_work.py:215`); hide "PLAN" until steps exist (`:221`); drop "· routed %s" (`:336`). · **S** · new · faces page 1 only — BC-5 0; D1-neutral iff page 0 drives.
18. **P8 · dead weight.** Delete the six hidden rail owners `synapse_panel.py:1063-1101` (`_meter_lbl`, `_palette_hint`, `_foot_dot`, `_foot_label`, `_help_btn`, `_corpus_btn`) and `_session_permission_btn` (`:2526-2537`) — the overflow already owns those actions; delete `line-height:150%` (`message_formatter.py:406`) and `format_timestamp_divider` (`:457-461`); delete the duplicate DsCard block `qss.py:245-248` (keep `:429-432`). · **S–M** · new · 0px. The legacy sheet `qss.py:562-768` (CARBON/GRAPHITE/SLATE/BONE/VOID palette, computed sizes 18/14/9pt, weight 700 arithmetic) → retoken or quarantine. · **L** · new.
19. **P2 · weights that exist.** Add WEIGHT_BOLD = 700 beside `tokens.py:358-360` (or document that SEMIBOLD 600 resolves to SpaceMono-Bold on mono); status role 11/500 (`tokens.py:378`) and tag 12/500 (`qss.py:422`) → 400 | 700, named in TYPE_ROLES — Space Mono ships Regular 400 + Bold 700 only (`fonts/README.md`). · **S** · new · 0px · *pending M7* (what weight 500 renders as today).

Not ranked because not decidable here: Doctor → Check (B1 option (2) bundle only); GUTTER 30 → 24 (D2, Joe's); the receipt line (C2a/C2b/C4/C5); a J3 supersession. See *Not decided here*.

---

## Directions

Four partner directions and the lens's three were on the table; they collapse to two the room converged on plus one it did not.

### Direction A — Subtractive Column
*Keep the comp, delete the second system; one edge, ladder air, hierarchy by weight and case at today's sizes.*

**Thesis.** The marks are already Pentagram-grade (MarkDot, DsVerb, DsPill, the grey ladder) and every P2/P4/P7 miss is an *addition* — a tenth pixel size, a hidden hue, a second warm, a fourth rhythm role at the same gap, a gradient, a tone row nobody sets. Restraint is therefore a deletion list, not a redirect; composure comes from one content edge and ladder air at 0px vertical cost, never from a larger ramp (there is no height for 13/17/22: D1 +38, BC-5 +27).

**Changes (ranked list numbers).** #1 SIZE_MICRO out, ramp 11/12/15/19 · #4 + #5 TEXT_ON_ACCENT neutral, HOT_SOFT/NO_SOFT retired, SIGNAL the one action family · #8 flat PANEL root · #7 underline TEXT_BRIGHT · #10 one content edge at 46 (setDocumentMargin 0, khint/footer left) · #3 one owner for the turn beat, then #12 F12 broken downward 16/12/8 · #11 DsVerb margins → row min-height 26 · #2 gate header phrase + weighted verbs, #6 dead rows and #18 hidden widgets deleted.

**Tradeoff.** Leaves Doctor's yellow standing until B1 is ruled (this direction cannot settle it structurally); nothing gets bigger, so the "editorial" air is only what the rung fixes buy; keeps the bundled Space Grotesk / Space Mono and the blue/warm identity, which means the ≤3 bucket pin (`test_bc_wave.py:329`) and the gate pin (`:650-654`) must be carried by hand. Several px claims are derived (M1, M3, M4, M5) — the direction is safe to start from the 0px items while measurement lands.

**Backed by.** RESTRAINT ("Subtractive"), SPACE ("Column"), TYPE ("Editorial at today's sizes — Editorial's air comes from SPACE's rung fixes"). USE's copy changes (#14–#17) are direction-independent and ride along.

### Direction B — Host-native
*Recede into Houdini: host font, host greys, the host's own tab marker as the one accent; the only brand on screen is the wordmark.*

**Thesis.** The only direction in which copy and chrome both speak Houdini, and the only one that settles B1 structurally: HOUDINI_TAB_YELLOW (`hou.qt.getColor('PaneTabMarker')`, `tokens.py:255-256`) becomes *the* accent, spent on SEND, the focus ring and the active underline — and never on Doctor.

**Changes.** (1) Drop the bundled families; the `build_mismatch` branch of fontload (`fontload.py:106-115`) becomes the only branch, mono = QFontDatabase FixedFont. (2) Accent = HOUDINI_TAB_YELLOW on `qss.py:127` (underline), `:275` (:focus), `:283` (SEND fill); SIGNAL / SIGNAL_DEEP / WARM retired from chrome. (3) CONIFEROUS and the J3 speaker pair need a **J1/J3 supersession** — ruled items, pinned `test_j3_speakers.py:157-160`. (4) Radii = Houdini's 2px (`qss.py:357-359` already uses it on DsCookBar); no gradient (#8). (5) Transcript keeps `_MONO` = Consolas (`message_formatter.py:75`) as the deliberate choice it already claims to be — the inverse of #13. (6) All copy changes #14–#17, plus "Doctor → Check" becomes shippable because the yellow leaves Doctor. (7) PANEL/GROUND are already host-seeded (`tokens.py:123-158`) — nothing else moves.

**Tradeoff.** Loses the v9 identity (Space Grotesk wordmark, the blue/warm pair) — the panel is indistinguishable from a native pane until it speaks. Yellow on warm grey is lower-contrast than SIGNAL (TEXT_ACCENT 6.57:1 on PANEL, `tokens.py:204`; the tab yellow's ratio is UNVERIFIED), so the accent gets used less. Requires superseding two ruled items (J1, J3) and re-pinning the restraint floor. Every G3 number (`audit_panel.py`) re-baselines on the host font.

**Backed by.** USE. (Lens direction 3.)

### Not converged — Instrument
*Denser, mono, tabular, receipt-first* (lens direction 2). No partner backed it. Retained for the record because it is the only direction that ships the receipt (C2a + C4 + C5 together, scroll-content placement per C2b's 0px option) as a design move rather than a ruling; the room's objection stands as the lens wrote it — it reads as a terminal, and 11px mono on a 280px pane is at the edge of legibility (TYPE's 10px evidence applies one size up on a 4K host, UNVERIFIED).

---

## Not decided here

Everything below awaits a ruling id from `harness/design_review/2026-09-14/RULINGS-OPEN.md`, a measurement, or a human.

**B1 — Doctor yellow vs monochrome + one accent.** The room's read, not a ruling: the yellow is prominence, not state (nothing on Doctor changes); it is a third hue family on a face that already spends SIGNAL and the ruled WARM; option (1) "count tokens not buckets" retargets the instrument the Cohere finding says is doing the work; options (2)/(3) are principle-consistent; the yellow is principled only in Direction B where it is *the* accent. "Doctor → Check" (`synapse_panel.py:1041`; accessibleName "Check SYNAPSE" `:1043`) ships **only** bundled with option (2) — the sweep-A pin (`test_panel_sweep_a.py:177-178, :192, :229`) protects the paint, so the word alone ships yellow. After #4 the boot face sits at 3 buckets with Doctor included, so B1 can be argued without a red pin.

**A1 / A2 / A3 / B2 — the airy chain.** #11 (DsVerb margins → min-height 26) touches B2 and A2: derived airy −13 → 0, standard +27 → +32 — *pending M1*. A2's −13 stands until measured. Nothing here moves A1's "airy is specification" or A3's bar on D1.

**C1 — BORDER == SURFACE.** The consent card is already an edgeless SURFACE patch (#6 evidence); the room files SPACE's air pairing (16 → 24 around the consent slot while shown) as the remedy in form, under C1.

**C2a / C2b / C4 / C5 / C6 — the receipt.** USE's principle read: verdict word alone ("Can undo" over "Undoable" at 10px — and 10px goes with #1); "mcp · 4/4" belongs in a tooltip or Health; the ordinal rides the timestamp (C4 fallback); #10 settles C4's body column at exactly 264. Instrument is the only direction that ships it.

**C3 — the squared corner.** RESTRAINT's hold on the 2px left rule (a border-left on RADIUS_ROUND 999 is a crescent, UNVERIFIED) resolves if the tag squares under C3's lineage; mooted for the gate card if #2 deletes the tag.

**D1 — faces-stack 400px red.** Unassigned. #1 and #17 are D1-neutral *iff* page 0 drives the minimumSizeHint (RULINGS-OPEN.md:497-504 says it does; not re-measured).

**D2 — the gutter.** GUTTER 30 → 24 (SPACE_LG) is the only standing remedy and the wrong sign from the lens's 32; inside `tokens.py:637` "26–36 is safe" it is Joe's call. USE's "Start bridge" moves to the overflow if row 2 binds at 380 post-F1 (*pending M2*).

**D3 / D4 — two REVERTs, UNDO SENT.** Principle only: scope words ("Undo turn" vs "Undo this proposal"); "Undo requested" with a dwell longer than DUR_SLOW 320ms.

**Pins that must move with the changes** (ranked-list dependencies, not rulings): `tests/panel/test_bc_wave.py:650-654` (gate card: level-as-tag, APPROVE accented, REJECT hot) for #2 and #10's concession; `test_bc_wave.py:329` (≤3 buckets) re-baselined after #4; `test_panel_sweep_a.py:177-178, :192, :229` (Doctor rule string) only under B1 option (2); `test_panel_camera_rhythm_qt.py:201` / `test_panel_rhythm_owner.py:233` (ROLE_GAPS KeyError) for #3; `test_j3_speakers.py:157-160` only under Direction B.

**Filed, not ruled.** A J3 supersession ("type never got a turn — both speaker names are identical mono 11/1.2px, `message_formatter.py:384`") — TYPE dropped it from the crit; anyone may file. WEIGHT_BOLD = 700 — TYPE's token to file (#19). The Instrument direction — unbacked.

**UNVERIFIED at compose time.** M1 rail height with/without `qss.py:344-346` and the DsVerb line box · M2 "Bridge"/"Start bridge" width vs row-2 slack post-F1 · M3 tag width at weight 700 in the (8,4) leaf · M4 rendered collapse of the 24+16 / 8+12 turn-beat sum · M5 transcript body x with document margin 0 · M6 bucket count with TEXT_ON_ACCENT neutral · M7 what mono weight 500 renders as — all in `measure/numbers.json`, pending. Also: Qt's substitute for a QSS `font-weight: 600` on a 400/700 family; NO_SOFT consumer count; MUSHROOM chroma vs the >24 threshold; border-left rendering on a 999-radius pill; R3-01's 393 post-F1; the wordmark's live 14 vs 15px (`synapse_panel.py:967` per MAP vs `tokens.py:386-402` comments); G3 strict at HEAD not run (no hython in this room); HiDPI everywhere.

---

## Room record

- **Rounds run:** 3 — positions · critique by name · answer and concede. Compose followed.
- **Partners:** TYPE, SPACE, RESTRAINT, USE — all four replied in all three rounds. No "no reply" in any round.
- **Spawn:** the first spawn (named teammates, subagent_type crucible) was refused by the runtime ("Teammates cannot spawn other teammates — the team roster is flat"); respawned as unnamed crucible subagents (ids a8d5375f…, a88f1409…, aa65d79b…, a1a0364e…), each signing "FROM: <lens>".
- **Hand-back failures: 16 of 16.** The crucible definition carries no SendMessage tool, so the "reply to design-chair by SendMessage" instruction was unexecutable; all four initial runs "ended without delivering a report", and every subsequent reply (4 × round 1, 4 × round 2, 4 × round 3) went to the parent session as a final-text hand-back and was relayed to the chair by the team lead. Zero partner replies reached the chair directly. Lesson for the next room: with runtime-fenced partners, route every round through the parent by design and give the parent a relay budget.
- **Chair messages:** 4 pre-round ("deliver as final text") + 4 round 2 + 4 round 3 = 12 sent; **8 of the 12-message cap for rounds 2–3** used.
- **Chair tool calls (excluding Agent / SendMessage):** ToolSearch 1 · Read 3 (MAP.json in three pages) · Write 1 (this file) = **5 of 12**.
- **Partner budgets (calls used / cap):** round 1 — TYPE 10/10, SPACE 10/10, RESTRAINT 9/10, USE 10/10; round 2 — TYPE 4/4, SPACE 2/4, RESTRAINT 2/4, USE 4/4; round 3 — TYPE 2/4, SPACE 4/4, RESTRAINT 3/4, USE 4/4. All read-only; no partner wrote a file.
- **Critiques:** 12 (three per partner, each to a named peer). **Concessions:** 4 CONCEDE · 7 CONCEDE-IN-PART · 1 HOLD (RESTRAINT to SPACE, on sequencing — re-characterised the disputed rows as dead code with evidence). **Withdrawals:** 6 (listed under the table). **Self-corrections:** TYPE (speaker label inside the ruled cell); RESTRAINT (BLOCKED → 600, tag width); SPACE (DsHeader margin no-op).
- **Tooling artifact:** in round 2 USE read a train worktree (`rebase-84`) that lacked `tests/`; its UNVERIFIED on two pin tests was tooling, not evidence — the main tree has both files, and SPACE had cited them by line in round 1. Noted so the UNVERIFIED label is not over-read.
- **Parallel leg:** the parent launched a hython-offscreen measurement leg during round 3 (`harness/design_review/2026-09-15/measure/numbers.json`, M1–M7). Not landed at compose; every dependent claim above is marked *pending Mn*.
- **Written:** this file only. No tracked file edited, no commit, no push.
