# QUARANTINED: model.md

Failed the phantom gate on 1 unresolved backticked token(s). Not read at retrieval time; a place to fix from, not a filter. Promotion to `rag/corpus/guides/` is a human act (JEV-HARVEST triages, Joe decides).

## Failing tokens

- `foo`  ->  nearest: floor, for, fog
  - in: group names: `@foo>5` in a Group field selects every component whose `foo`

origin: `fxhoudinimcp@29b6695b:prompts/markdown/workflows/model.md` · licence: mit

