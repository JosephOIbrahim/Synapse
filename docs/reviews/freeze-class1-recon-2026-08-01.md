# Freeze class 1 — chunkability recon synthesis. Verdict: chunking is the wrong approach on the path that matters, and the evidence base the brief rests on is instrument-corrupted. Verified against HEAD 38aba509c7738ee340150dd36c494bd80a86b2e7.

## Recommendation

DO NOT START CHUNKING WORK. Two verified structural facts kill most of it before ranking, and a third makes ranking impossible.

=== LEAD: THE COST IS NOT IN THE PATH, AND IT IS NOT IN THE PAYLOAD EITHER — THE HEADLINE NUMBER IS A TIMEOUT CONSTANT ===

The brief's premise ("synapse_inspect_scene is 1.98ms via /mcp and 10,005ms main-thread-blocking via the panel") does not describe two paths doing the same work. I verified this three ways:

(a) 10005 = _DEFAULT_TIMEOUT 10.0s (python/synapse/server/main_thread.py:20) + ~5ms overhead. Two DIFFERENT tools produce the identical value 105 seconds apart: C:/Users/User/.synapse/logs/synapse.log:18506 `Inline tool 'houdini_create_node' ran 10005ms` and :18536 `Inline tool 'synapse_inspect_scene' ran 10005ms`. Each is preceded within 5ms by `Main thread timeout (N consecutive, 10s limit)` (:18505, :18535). Same millisecond value on two unrelated tools is a constant, not a measurement.

(b) The C4 abandoned flag is checked BEFORE fn() runs — main_thread.py:297-299 `with state_lock: if abandoned[0]: return` — and it is set at :312-313 when done.wait(timeout) expires at :311. inspect_scene never executed. It waited 10s for a main thread held by something else and was refused entry.

(c) The shared `_consecutive_timeouts` counter reads "16 consecutive" on that line, having been walked up by fifteen 1s-limit timeouts from 08:44:31 onward. A fast-path-2 inline call cannot produce a timeout at all (main_thread.py:240-243 returns fn() with no bound), so that run_on_main was demonstrably the DEFERRED path — caller off-main.

Worse for the brief: the log string "on the main thread (Qt loop stalled this long)" was UNCONDITIONAL on 2026-07-31. The discriminating on_main/off-main version landed in 13bd541 (2026-08-01), one day later, and its own comment at python/synapse/panel/tool_executor.py:557-563 says verbatim: "The previous single string claimed 'on the main thread ... Qt loop stalled this long' for BOTH paths, so every off-main dispatch read as a freeze contribution. FREEZE_FORENSICS_20260731 §2 names this as the follow-on defect that corrupted that run's forensics." Every 07-31 tool-attribution number in the brief is from the corrupted instrument. The FREEZE DETECTED / recovered lines are real (heartbeat-based); the attribution of those freezes to named tools is not.

Independent corroboration: the 46724ms houdini_set_parm record (synapse.log:18721) CONTAINS three separate `Main thread recovered` events at 12:51:03,142 / 12:51:26,294 / 12:51:49,441 (:18711, :18715, :18719). The main thread came back to life three times inside one tool's measured span. That number is not a hold.

Also verified: the panel's PRIMARY tool path IS /mcp. claude_worker.py:278 calls try_mcp_tool_call first; tool_executor.py:210 does `conn.request("POST", "/mcp", body=payload, headers=all_headers)` to localhost hwebserver. Same endpoint, same handler code, same marshalling as the external MCP client. The tool_executor._dispatch path that wrote the 10005ms record is only reached when that POST returns None. There is no payload-level panel-vs-/mcp duality to exploit.

=== KILLER #1: ON /mcp, INNER CHUNKING IS A NO-OP FOR EVERY MUTATING TOOL ===

mcp/server.py:623-626 wraps every non-fast-path tool in one outer `run_on_main(lambda: dispatch_tool(handler, tool_name, arguments), timeout=timeout_for(tool_name))`. That callback sets `_tls.on_main = True` (main_thread.py:300). Every inner run_on_main in every handler then hits Fast path 1 (main_thread.py:230-231, `if getattr(_tls, "on_main", False): return fn()`) and executes INLINE with no queue re-entry and no Qt iteration. Splitting a handler into N run_on_main hops yields N back-to-back inline calls and pumps nothing.

I probed the escape set live (`synapse.mcp.server.is_transport_fast_path`): TRUE for synapse_inspect_scene, houdini_capture_viewport, cops_analyze_render. FALSE for houdini_create_material, houdini_create_node, houdini_set_parm, synapse_query_prims, houdini_reference_usd, synapse_solaris_build_graph, houdini_execute_vex, cops_batch_cook, cops_temporal_analysis. So chunking is structurally dead on the panel's primary path for every site the three lanes proposed chunking.

=== KILLER #2: THE UNDO GROUP DISQUALIFIES THE REST BY YOUR OWN RULE 3 ===

hou.undos.group's docstring in the installed build (C:/Program Files/Side Effects Software/Houdini 22.0.397/houdini/python3.13libs/hou.py:106531-106534) reads "within which ALL changes to Houdini are recorded as a single action on the undo stack" — not "all changes made by this code". The entire purpose of pumping the Qt loop between chunks is to give the artist a responsive UI, which is precisely the window in which their manual edits land inside SYNAPSE's single undo entry. Every divisible body on these surfaces is inside such a group: handlers_material.py:232 and :503 (verified — `with hou.undos.group("SYNAPSE: create_material")` at :232, `matlib.cook(force=True)` at :246, `matlib.setInput(0, node)` at :235), handlers_usd.py:428/:809/:1298/:1374, handlers_solaris_graph.py:622-853, solaris_compose_tools.py:216/:321/:346. Per your rule 3, all disqualified regardless of hold.

=== KILLER #3 (the one that outranks everything): THERE IS NO OCCUPANCY INSTRUMENT ON THE DEFERRED PATH ===

This is why all three lanes returned "realistic_hold: UNKNOWN" for nearly every site. It is not lane laziness — it is genuinely unmeasurable with what ships.

Verified by grep across python/synapse/: `_record_main_thread_direct` has exactly ONE call site, main_thread.py:248, inside fast path 2. `note_main_thread_inline_overrun` is called from main_thread.py:265 (fast path 2), handlers.py:318, handlers_render.py:551, host/main_thread_executor.py:254. handlers.py:326-329 states the contract verbatim: "Off-main callers get run_on_main verbatim... Only the inline main-thread path is instrumented." And `_on_main` (main_thread.py:290-307) records the enqueue-to-start WAIT at :295-296, then calls fn() at :302 with no timing whatsoever.

Live proof from the currently-running session (C:/Users/User/.synapse/logs/telemetry.json, pid 63280): `dispatch_waits count 13947` alongside `main_thread_direct {count: 0, max_ms: 0.0}`. Nearly fourteen thousand marshals, zero of them timed for occupancy. `tool_durations` contains only get_live_metrics and ping.

And the h7 fix moved ALL tool work onto exactly that unmeasured path. tool_executor.py:484-488 says so: "In production today this should never fire: the worker dispatches off-main (claude_worker.py:348-374) and h9's tool_requested wire has zero emitters." Post-h7 the guard is a dead-man's-switch, and every payload runs deferred, where nothing measures it.

=== RANKED PLAN ===

TIER 0 — do first, everything else is unrankable without it
0.1 INSTRUMENT THE DEFERRED PATH. Time fn() inside `_on_main` (main_thread.py:300-307) and feed the existing `_record_main_thread_direct` histogram (already built at main_thread.py:108, already serialized into telemetry.json and every freeze dump) under a distinct key, plus a per-tool label so `tool_durations` finally has content. ~10 lines, no behaviour change, no undo/cook/consent surface touched. Without this, every "realistic hold" in this recon and the brief is a guess.
0.2 RETIRE the 2026-07-31 tool-attribution numbers from FREEZE_FORENSICS_20260731.md as evidence. Keep the heartbeat freeze durations (real); drop the tool names attached to them (instrument-corrupted, per the fix's own commit message).

TIER 1 — the ONE verified long main-thread hold in the entire corpus
1.1 houdini_capture_viewport. 18 slow records across synapse.log/.1/.3, bimodal: a 1448-1648ms cluster and a 20324-24219ms cluster, max 24219ms. Unlike the 10005ms artifacts these are heterogeneous AND corroborated 1:1 by a second instrument at a different layer — marshal_guard fast_path_2 records of 22.26s / 20.32s / 22.54s / 24.22s at the same milliseconds as the 22259 / 20324 / 22540 / 24219ms tool records. fast_path_2 only fires when the caller IS main, so those are genuine Qt-loop grips.
  NOT CHUNKABLE: handlers_render.py:292 `sv.flipbook(viewport=vp, settings=settings, open_dialog=False)` is one opaque C++ call already reduced to a single frame (`settings.frameRange((cur, cur))` at :288).
  ACTIONABLE ANYWAY, no chunking involved: it is ABSENT from `_KNOWN_SLOW_TOOLS` (verified — bridge_adapter.py:241-248 lists 13 tools; capture_viewport is not one), so the shipped pre-flight would never flag the largest verified grip in the log. Adding it is a one-line membership change to an already-shipped mechanism.
  CAVEAT I will not hide: all 18 records are 2026-07-27..07-29, caller-on-main era. Post-h7 the caller is off-main and on /mcp capture_viewport takes the transport fast path (probed True), so the flipbook now marshals rather than running inline. The ~20s of main-thread occupancy is unchanged; only the freeze SHAPE changed. Confirm with 0.1 before spending on it.

TIER 2 — real defects, cheap, none of them chunking
2.1 query_prims unbounded traversal. handlers_usd.py:957 guards `if depth > max_depth or len(results) >= limit: return` — bounds RESULTS, not VISITS. I read all three filter-reject branches (:963-967 prim_type, :970-977 purpose, :980-990 name_pattern): each recurses into every child then returns WITHOUT appending. A filtered query matching nothing walks the whole stage to depth 10 with zero backpressure. One-line visit counter. This is the only genuinely unbounded Python-level hold found across all three surfaces.
2.2 inspect_scene budget mismatch. handlers.py:1486 `run_on_main(lambda: inspect_scene(...))` with no timeout arg → 10.0s, while clients budget 30s (core/timeouts.py). The tool self-reports failure 20s before its caller would have — and that mismatch is what wrote the brief's phantom 10,005ms.
2.3 reference_usd 10s default on unbounded input. All 14 run_on_main calls in handlers_usd.py take the default; reference_usd (cook at :875) opens a caller-supplied USD layer of arbitrary size. Its Solaris siblings were deliberately raised to 30s with the stated reason "so a timeout doesn't ghost-callback into the duplicate-node spiral" (handlers_solaris_assemble.py:707-709). reference_usd never got it. On a >10s open the mutation completes, the caller is told it failed, and a retry adds a second reference LOP.
2.4 live_metrics poisons the stall detector. live_metrics.py:255 `run_on_main(_gather, timeout=_SCENE_COLLECT_TIMEOUT)` with `_SCENE_COLLECT_TIMEOUT = 1.0` (:98) on a 2.0s aggregator interval (:90). I verified it is the ONLY 1.0s marshal in the codebase that leaves record_stall at default True (integrity_envelope.py:221 and ws_bridge.py:143 both pass record_stall=False). Its timeouts increment the same `_consecutive_timeouts` that `is_main_thread_stalled()` uses to fast-fail REAL commands. Arithmetic confirms the source: log timestamps 08:44:31,027 / 08:44:34,028 / 08:44:37,029 are 3.001s apart = 2s interval + 1s timeout, exactly.
2.5 Composition validation is crashing and failing closed. 14 occurrences in synapse.log: `Composition validation FAILED-CLOSED on ...: AttributeError: 'References' object has no attribute 'GetAddedOrExplicitItems'` — 10 on 2026-07-29, 2 on 07-31, 2 on 08-01. Phantom pxr API inside _verify_composition; raises RuntimeError at shared/bridge.py:1285 and drives _guarded_rollback on operations that had already succeeded. Still live.
2.6 read_only divergence doc drift. mcp/server.py:122 names three tools; I ran `synapse.mcp.server.read_only_set_divergence()` and got FOUR: cops_temporal_analysis, synapse_propose_graph, synapse_render_processes, synapse_validate_frame. Transport set 39, bridge set 35. `synapse_render_processes` is undocumented drift on the render surface.

TIER 3 — NOISE. Drop from the plan, do not pad.
- cops_analyze_render (handlers_cops.py:929), cops_temporal_analysis (:1987), cops_batch_cook (:2127): ZERO invocations across ~14MB of production logs (synapse.log + .log.1 + .log.3). Never fired. Chunking them optimizes a dead surface.
- handlers_node.py:79: verified `new_node.cook(force=True)` gated at :77 on `node_type in ("materiallibrary",)` — a LOP, not a SOP — and the handler contains no setInput anywhere, so the node is unwired and composes only its own childless layer.
- handlers_material.py:90 is not a third site: _query_material_usd_path's only two callers (:274, :618) both invoke it from inside the enclosing _on_main and inside the already-open undo group. Two holds on that surface, not three.
- handlers_usd.py:1646-1756 display-chain walk: no cook, no stage(), depth-50 bound plus a visited set.
- handlers_render.py:2014: AOV-count-scaled pythonscript code-validation cook, not a pixel cook, and deliberately inside the group (:2001-2005 documents that moving it corrupts the undo stack).
- matlib_bind, solaris_shotsetup_karma_xpu: registered as WS commands only, absent from mcp/_tool_registry.py, therefore not reachable from panel chat except nested inside shot_render_ready with verify=False.
- The current freeze dumps are false alarms: all five (freeze_dump_20260802_*.json) report `reason: sustained_freeze` with `total_heartbeats: 1` and 0-5 dispatch waits — startup/scene-load freezes, not tool freezes. And the entire 2026-08-01 log tail is pytest temp-dir noise (the known "pytest pollutes the production log" class).

=== THE ONE CHUNK-SHAPED THING THAT SURVIVES BOTH FILTERS ===
handlers_solaris_assemble.py:679-700 — hoist the _handle_configure_render_passes call out of assemble_chain's _on_main up to handler-thread level (the shot_render_ready pattern at handlers_usd.py:2003/:2036). It sits AFTER assemble_chain's undo group has closed and it opens its own group (handlers_render.py:2007), so the undo shape is unchanged and rule 3 is not violated. But it is defeated on /mcp by the outer marshal, so it only helps the WS path. Marginal. Do not spend Tier-0/1 budget on it.

=== HONEST BOTTOM LINE ===
None of the twenty-plus sites the three lanes examined is worth chunking. Not one clears all three filters (reachable + measured-long + chunkable-without-breaking-single-Ctrl+Z). The alternative that IS worth doing, in order: measure the deferred path (0.1), fix the four bound/budget defects (2.1-2.4), correct the pre-flight membership so the mechanism that already ships points at the tools that actually hold, and accept marshal_guard's own conclusion for the genuinely long ones — "it is the residual freeze that only an out-of-process execution path can remove."

## Blocking facts

- The 10,005ms figure is _DEFAULT_TIMEOUT, not a hold. main_thread.py:20 sets _DEFAULT_TIMEOUT = 10.0; :311 raises after done.wait(10.0); :312-313 sets abandoned[0]=True; :297-299 checks that flag BEFORE fn() runs. Two different tools produce the identical 10005ms value 105 seconds apart (synapse.log:18506 houdini_create_node, :18536 synapse_inspect_scene), each preceded within 5ms by a 'Main thread timeout (N consecutive, 10s limit)' line. inspect_scene never executed.

- Every 2026-07-31 tool-attribution number is from a corrupted instrument. The log string 'on the main thread (Qt loop stalled this long)' was emitted UNCONDITIONALLY for both on-main and off-main dispatches until commit 13bd541 (2026-08-01). tool_executor.py:557-563's own comment: 'The previous single string claimed ... for BOTH paths, so every off-main dispatch read as a freeze contribution. FREEZE_FORENSICS_20260731 §2 names this as the follow-on defect that corrupted that run's forensics.' The heartbeat freeze durations are real; the tool names attached to them are not.

- The 46724ms houdini_set_parm record contains three main-thread recoveries. synapse.log:18711/:18715/:18719 log 'Main thread recovered' at 12:51:03,142 / 12:51:26,294 / 12:51:49,441, all inside the 12:51:02.98-12:51:49.70 span of the record at :18721. A tool-executor wall clock is not main-thread hold time.

- There is NO occupancy instrument on the deferred path. `_record_main_thread_direct` has exactly one call site (main_thread.py:248, fast path 2). handlers.py:326-329 states 'Off-main callers get run_on_main verbatim... Only the inline main-thread path is instrumented.' _on_main (main_thread.py:290-307) records the enqueue-to-start wait at :295-296 and then calls fn() at :302 untimed. Live proof from the running session (telemetry.json, pid 63280): dispatch_waits count 13947, main_thread_direct count 0, tool_durations = {get_live_metrics, ping} only. The h7 fix moved all tool work onto this unmeasured path (tool_executor.py:484-488: 'In production today this should never fire: the worker dispatches off-main').

- Inner chunking is a no-op on /mcp for every mutating tool. mcp/server.py:623-626 wraps non-fast-path dispatch in one outer run_on_main; main_thread.py:300 sets _tls.on_main=True; every inner run_on_main then hits Fast path 1 (main_thread.py:230-231) and runs inline. Probed live via synapse.mcp.server.is_transport_fast_path: FALSE for create_material, create_node, set_parm, query_prims, reference_usd, solaris_build_graph, execute_vex, cops_batch_cook, cops_temporal_analysis; TRUE only for inspect_scene, capture_viewport, cops_analyze_render.

- The panel's primary tool path IS /mcp. claude_worker.py:278 calls try_mcp_tool_call first; tool_executor.py:210 does conn.request('POST', '/mcp', ...) to the localhost hwebserver port. Same endpoint and handler code as the external MCP client. tool_executor._dispatch is only reached when that POST returns None. There is no payload-level panel-vs-/mcp duality.

- hou.undos.group captures ALL changes, not just yours. Installed build C:/Program Files/Side Effects Software/Houdini 22.0.397/houdini/python3.13libs/hou.py:106531-106534: 'Returns a context manager, within which ALL changes to Houdini are recorded as a single action on the undo stack.' Pumping the Qt loop between chunks is exactly the window in which artist edits get swallowed into SYNAPSE's undo entry — a single-Ctrl+Z break at handlers_material.py:232/:503, handlers_usd.py:428/:809/:1298/:1374, handlers_solaris_graph.py:622-853, solaris_compose_tools.py:216/:321/:346.

- hou.Node.cook has no chunking surface. Installed build hou.py:20052-20067: cook(self, force=False, frame_range=()) returning void. No budget, no callback, no yield point. hou.InterruptableOperation exists on H22 but only lets a Python loop you drive honour Escape; long_operation.py:88-89 runs it with open_interrupt_dialog=False and its own comment records that the modal caused a WORSE freeze. It has exactly one caller in the tree (introspection.py:431).

- The three named COPs sites have never fired. Zero occurrences of cops_analyze_render, cops_temporal_analysis or cops_batch_cook across synapse.log + synapse.log.1 + synapse.log.3 (~14MB).

- The only corroborated long hold is houdini_capture_viewport. 18 slow records, bimodal 1448-1648ms and 20324-24219ms, max 24219ms — heterogeneous values, and matched 1:1 by marshal_guard fast_path_2 records (22.26/20.32/22.54/24.22s) at the same milliseconds. fast_path_2 fires only when the caller IS main. It is absent from _KNOWN_SLOW_TOOLS (bridge_adapter.py:241-248). All 18 records are 2026-07-27..07-29, pre-h7.

- query_prims bounds results, not visits. handlers_usd.py:957 `if depth > max_depth or len(results) >= limit: return`; the three filter-reject branches at :963-967, :970-977 and :980-990 each recurse into every child and return without appending. A filtered query matching nothing walks the whole stage to depth 10. Verified by reading the function.

- live_metrics is the sole producer of the '1s limit' stall lines and it feeds the real-command stall gate. live_metrics.py:98 _SCENE_COLLECT_TIMEOUT = 1.0, :255 run_on_main(_gather, timeout=1.0) with record_stall at default True, on a 2.0s interval (:90). Grep confirms integrity_envelope.py:221 and ws_bridge.py:143 are the only other short marshals and both pass record_stall=False. Log timestamps 08:44:31,027 / 34,028 / 37,029 are 3.001s apart = 2s + 1s exactly.

- The named 'SOP cook' sites are LOP cooks. handlers_node.py:77 gates on node_type in ('materiallibrary',); handlers_material.py:234 and :505 both createNode('materiallibrary'). The only cook(force=True) on a SOP reachable from panel chat is handlers.py:1392 (attribwrangle in execute_vex), which the brief does not name and which never appears in any slow-tool record.

- Composition validation is crashing on a phantom pxr API and failing closed. 14 occurrences of "AttributeError: 'References' object has no attribute 'GetAddedOrExplicitItems'" in synapse.log (10 on 2026-07-29, 2 on 07-31, 2 on 08-01), raising RuntimeError at shared/bridge.py:1285 and driving _guarded_rollback at :1297 on already-succeeded material ops.

- Current freeze dumps are startup artifacts, not tool freezes. All five freeze_dump_20260802_*.json report reason=sustained_freeze with total_heartbeats=1 and 0-5 dispatch waits. The 2026-08-01 log tail is entirely pytest temp-dir noise.

- read_only divergence doc drift: mcp/server.py:122 names three tools; live read_only_set_divergence() returns four — cops_temporal_analysis, synapse_propose_graph, synapse_render_processes, synapse_validate_frame (transport set 39, bridge set 35).

- _HEAVY_TOOLS from docs/L8_FREEZE_FIX_DESIGN.md was specified but never implemented: grep -rn '_HEAVY_TOOLS' python/ returns nothing.

- UNKNOWN AND HONESTLY SO: not one cook site on any of the three surfaces has a measured main-thread hold time. That is a consequence of the instrumentation gap above, not of insufficient recon. No live cook was run this session and no hold number should be invented before item 0.1 ships.

- HUMAN DECISIONS REQUIRED BEFORE ANY WORK STARTS — D1: ship the deferred-path occupancy timer and re-rank before spending on relief, or proceed on structure alone? D2: is a ~20s viewport capture acceptable with progress + cancel, or must capture move out-of-process (noting husk is already refuted on this Indie licence)? D3: live_metrics — set record_stall=False, raise the interval, or make scene collection on-demand? Each trades dashboard freshness against stall-counter honesty. D4: SYNAPSE_STAGE_HASH_PRIM_THRESHOLD stays at 1<<62 (Flatten always, proven but unmeasured at scale per shared/bridge.py:339-341/:399-404) or opts in to the structural path the repo itself calls unproven and possibly slower? Needs real-stage scene_hash telemetry first — the only sample on this host is a 9-node scene (max 268ms over 54 samples). D5: formally retire the 2026-07-31 tool-attribution numbers from FREEZE_FORENSICS_20260731.md?

## Options

- A. MEASURE FIRST (recommended) — ship the deferred-path occupancy timer (main_thread.py:300-307 feeding the existing _record_main_thread_direct histogram at :108, plus per-tool labels into tool_durations), run one normal artist session, then re-rank. FOR: every hold number in this recon and the brief is currently unmeasured or instrument-corrupted; the histogram, the telemetry serializer and the freeze-dump plumbing all already exist, so this is wiring not building; it converts 'UNKNOWN' into data for ~20 sites at once. AGAINST: it ships no freeze relief on its own; one more session of artist pain before any fix lands. EFFORT: ~10 lines + a test. RISK: very low — no behaviour change, telemetry-only, no cook/undo/consent surface touched.

- B. FIX THE FOUR BOUNDS AND BUDGETS — query_prims visit counter (handlers_usd.py:957), inspect_scene 10s→30s (handlers.py:1486), reference_usd 10s→30s (handlers_usd.py:905), live_metrics record_stall=False or on-demand collection (live_metrics.py:255). FOR: all four are verified defects, none is a chunking change, none touches an undo group, and 2.4 directly stops the metrics daemon from poisoning the stall counter that gates real commands. AGAINST: none of them is proven to be a cause of the artist's 44.4s freeze — they are correctness fixes riding a freeze investigation; raising two timeouts makes a stuck tool hold its caller longer. EFFORT: small, four independent one-to-few-line changes. RISK: low, but 2.3/2.2 shift failure timing so anything asserting the 10s behaviour needs re-checking.

- C. CORRECT THE PRE-FLIGHT MEMBERSHIP — add houdini_capture_viewport and the USD/Solaris builders to _KNOWN_SLOW_TOOLS (bridge_adapter.py:241-248), which currently contains zero USD/Solaris tools and not capture_viewport; the L8 design's _HEAVY_TOOLS list was specified but never implemented (grep -rn '_HEAVY_TOOLS' python/ returns nothing). FOR: the mechanism already ships and is already wired to the h7 refusal; this is a membership edit, not new machinery; capture_viewport is the only site with a corroborated 20-24s occupancy. AGAINST: post-h7 the guard's own comment says it 'should never fire' because the worker dispatches off-main — so this arms a path that is currently unreachable, and buys an advisory string rather than relief. EFFORT: trivial. RISK: low; the only real cost is believing it fixed something.

- D. OUT-OF-PROCESS FOR THE LONG ONES — move viewport capture and render off the Houdini main thread entirely (separate process / husk-class execution). FOR: it is the only mechanism that actually removes a 20-24s grip, and marshal_guard's own warning text already names it as the sole remedy; hou.Node.cook (hou.py:20052-20067, signature cook(self, force=False, frame_range=())) has no budget, callback or yield point, and hou.InterruptableOperation only lets a Python loop you drive honour Escape. AGAINST: large, crosses a process boundary, and husk is already REFUTED on this Indie licence (known: silently no-ops, no errors, no output); viewport flipbook has no obvious out-of-process equivalent at all. EFFORT: large, multi-sprint. RISK: high — new failure surface, licence-dependent, and the flipbook half may be impossible.

- E. CHUNK THE HANDLERS AS BRIEFED — split cook-heavy closures into multiple run_on_main hops. FOR: it is the brief's hypothesis and the mechanism nominally exists (each top-level run_on_main is its own hdefereval.executeDeferred at main_thread.py:309). AGAINST: refuted on three independent grounds — (1) the /mcp outer marshal at mcp/server.py:623-626 sets _tls.on_main=True so every inner hop collapses inline via Fast path 1 (main_thread.py:230-231), making it a no-op on the panel's primary path for every mutating tool; (2) every divisible body sits inside a hou.undos.group whose installed docstring (hou.py:106531-106534) says it captures ALL changes to Houdini, so pumping the loop between chunks swallows the artist's own edits into SYNAPSE's undo entry — a single-Ctrl+Z break, disqualified by rule 3; (3) the cooks themselves are atomic C++ with no sub-unit. EFFORT: high. RISK: high and unrewarded. RECOMMEND AGAINST.

- F. FIX THE LIVE pxr PHANTOM — _verify_composition calls a non-existent 'References.GetAddedOrExplicitItems', failing closed 14 times in the log (10 on 07-29, 2 on 07-31, 2 on 08-01) and driving _guarded_rollback (shared/bridge.py:1285/:1297) on material ops that had already succeeded. FOR: it is a live, dated, reproducing defect on the material path that silently rolls back successful artist work — arguably more damaging than a freeze. AGAINST: unrelated to freeze class 1; it is scope creep on this run's mandate. EFFORT: small once the correct H22 pxr API is scouted. RISK: low to fix, but it must be phantom-guarded (synapse_scout) rather than guessed, per rulebook discipline.

## Proposed flywheel entry (paste-ready, file is fenced)


