# H22 NOW-Probe Synthesis — 2026-07-16

## Provenance

| Field | Value |
|---|---|
| Date | 2026-07-16 |
| Build | **H22.0.368** — every probe recorded `hou.applicationVersionString()` → `22.0.368` verbatim (husk additionally: `Build version: 22.0.368`) |
| Path | **hython-headless FALLBACK** — `C:\Program Files\Side Effects Software\Houdini 22.0.368\bin\hython.exe` (HYTHON env var); husk items ran the target CLI binary directly |
| Bridge | **DOWN** (port 9999 connect timeout) — every PASS below is **PROVISIONAL-headless** until reconfirmed on the live bridge. husk CLI verdicts are CLI-native (the CLI *is* the target), but hython-authored stage defaults feeding them should still be re-checked live |
| Deferred by design | **N-1, N-4, and `hou.secure` are explicitly deferred to the live-bridge session** — not run, not attempted headless (see DEFERRED list at the bottom) |
| Repo writes | None by any probe. All artifacts under the session scratchpad (`...\Temp\claude\C--Users-User-SYNAPSE\d102983f-b6e0-47c7-ab56-cc4aa51ccc52\scratchpad\`) |

Probe count across the seven sections: **N-5** 7 scripts (all PASS-or-resolved, 1 upstream-note refutation) · **N-3** 4/4 PASS · **husk session** 6 VERIFIED-PROVISIONAL, 1 legacy claim refuted-as-obsolete · **N-6** 4 VERIFIED + 1 QUARANTINE · **N-8** 2/2 pins HOLD · **N-9** 8 VERIFIED + 2 QUARANTINE + 1 absent-as-probed · **N-10** 3 VERIFIED + 1 variant-divergence flag + 1 QUARANTINE.

---

## N-5 — Layout LOP successor (SUCCESSOR EXISTS: rename, not removal)

**VERDICT — SUCCESSOR EXISTS ON THE DEFAULT SURFACE (rename, not removal).**

### Ranked successor list

**1. `Lop/paintinstances` ("Paint Instances") — VERIFIED RENAME of `Lop/layout`. Confidence: definitive.**

- Official, verbatim (`$HFS/houdini/help/news.zip` → `22/solaris.txt` L137):

  > "The Layout LOP is now named [Paint Instances LOP|Node:lop/paintinstances]. The node has undergone a complete review with bug fixing and UI improvements."

- Alias shipped, verbatim (`$HFS/houdini/OPcustomize`): `opalias Lop paintinstances layout`
- Parm-surface diff: H21 layout (42 parms, from `harness/notes/verified_nodetype_catalog_21.0.671.json`) vs H22 paintinstances (45): **41/42 shared = 98%**; sole drop = `method`.
- Live: `stage.createNode('layout')` → SUCCEEDED: `/stage/paintinstances1 (type=Lop/paintinstances)`.
- Lineage residue: help card `#since: 19.0`, embeds `/videos/layout_place.webm`; default viewer state `sidefx_lop_paintinstances`; the 11 `layoutbrush*` SOPs survive (now `ophide`-hidden in OPcustomize) and `python3.13libs/layout/` (brush.py, assetgallery.py, brushitemmodel.py) still ships. Tab submenu: "Instancing".

**2. `Lop/copytopoints` ("Copy to Points") — VERIFIED RENAME of `Lop/instancer` (the sibling removal in the same adjudication).**

- what's-new L143: *"The Instancer LOP was renamed to [Copy to Points LOP|Node:lop/copytopoints]..."*; `opalias Lop copytopoints instancer` + `oprename Lop instancer "Copy to Points"`.
- Parm overlap vs H21 instancer: **39/41 = 95%** (dropped: `allowmissingprototypes`, `protooptionsgroup`). `createNode('instancer')` → `/stage/copytopoints1`.

**3. New H22 complements (not renames):** `scatterinstances` (`#since: 22.0`, render-time Hydra generative procedural, 167 parms, OPlibLop.hda), `pointinstancer` (new, create+edit, what's-new L141), `retimeinstances` (aliased from `instanceretime`). Pre-existing retained: `modifypointinstances`, `extractinstances`, `split/mergepointinstancers`.

**4. Ruled out:** `stagemanager` (reference/set-assembly parm surface, no brush semantics — not the successor). Labs biome scatter tools (SideFXLabs22.0 installed on this machine — NOT default surface).

**5. Missing-HDA/package hypothesis REFUTED:** 0 layout-named files among all 900 otl/hda in `$HFS`; not in the 36 `hou.hda.loadedFiles()`; OPlibLop.hda's 115 defs contain no layout def; `$HFS/packages` = apex/kinefx/sculpt/shotbuilder/texturepaint only.

### Correction for the human gate (gate 2)

`C:\Users\User\SYNAPSE\harness\notes\verified_connectivity_H22.json` (diff.removed[2].note) claims *"createNode('layout') in a LOP net will fail on H22"* — **REFUTED**. The alias makes it silently succeed as `Lop/paintinstances`. Real breakage moves to:

- (a) `hou.nodeType(lopCat,'layout')` → `None` (alias applies at creation, not type lookup);
- (b) any `node.type().name()=='layout'` check;
- (c) emit paths setting the dropped `method` parm;
- (d) instancer emitters relying on `allowmissingprototypes`.

Recommend emitting canonical new spellings, not leaning on the SideFX alias table.

Counts: 7 probe scripts, all PASS-or-resolved, 0 QUARANTINE, 0 PENDING, 1 upstream-note refutation. Artifacts (probes + verbatim outputs + full 218-type LOP catalog dump `n5_lop_catalog_22.0.368.json` + `n5_all_hda_files.txt`) in the session scratchpad. No repo writes.

---

## N-3 — Karma relationships (KAR-08 identification)

**COMPLETE** (4 probes, 0 quarantined, assignment landed on attempt 3 of 3). All results PROVISIONAL-headless.

Probe setup (verbatim chain, all in-memory /stage): `light::2.0` ("keylight") → `karmablockerlightfilter` ("blocker") → `karmarendersettings` ("krs"), assignment via light parm `xn__lightfilters_lva` = `/blocker`, `krs.cook(force=True)`, walked `krs.stage()`. Assignment verified live: `light:filters IsValid: True | targets: ['/blocker']`.

### The migration set — karma/husk-graph properties that are RELATIONSHIPS on 22.0.368

| Prim (type) | Relationships (verbatim) |
|---|---|
| `/Render/krs` (RenderSettings) | `camera`, `husk:orderedImageFilters`, `products` |
| `/Render/krs/Products/renderproduct` (RenderProduct) | `camera`, `husk:orderedImageFilters`, `orderedVars` |
| `/lights/keylight` (SphereLight) | `light:filters`, `collection:lightLink:includes`, `collection:lightLink:excludes`, `collection:shadowLink:includes`, `collection:shadowLink:excludes`, `proxyPrim` |
| `/blocker` (KarmaBlockerLightFilter) | `collection:filterLink:includes`, `collection:filterLink:excludes`, `proxyPrim` |
| `/Render/krs/Vars/beauty` (RenderVar) | (none) |

Counter-finding that scopes KAR-08 tightly: **zero `karma:*` properties are relationships.** All 113 `karma:global:*` / `karma:object:*` plus `husk:default_delegate`, `husk:scene_lights`, `husk:scene_materials` are plain ATTRIBUTES on the RenderSettings prim. The only husk-namespaced relationship is `husk:orderedImageFilters` (present on BOTH RenderSettings and RenderProduct). The rest of the relationship surface is stock UsdRender/UsdLux wiring (`camera`, `products`, `orderedVars`, `light:filters`, `collection:*:includes/excludes`).

Seam test, run per relationship name: `prim.GetAttribute(<rel>).IsValid()` → **False for all 10 distinct names**, including `light:filters` WITH a populated target (`GetAttribute('light:filters').IsValid(): False` while `GetRelationship` targets `['/blocker']`).

### Code seam verdict — `C:\Users\User\SYNAPSE\python\synapse\server\handlers_usd.py`

`grep GetRelationship handlers_usd.py` → zero hits. Two failure modes, and only one is actually "returns None":

1. **Read path — `_handle_get_usd_attribute` (line 340, `prim.GetAttribute(attr_name)` at line 365):** for every name in the migration set, `attr.IsValid()` is False → **raises ValueError at line 369** ("That attribute name didn't match"), it does not return None. Worse: the "Available attributes" hint (line 368) is built from `prim.GetAttributes()` only, so it can never list the relationship the caller asked for — the error actively implies the property doesn't exist. Affected names: `camera`, `products`, `orderedVars`, `husk:orderedImageFilters`, `light:filters`, all four `collection:lightLink/shadowLink` includes/excludes, `collection:filterLink:*`, `proxyPrim`.

2. **Write path — `_handle_set_usd_attribute` (line 386; generated pythonscript at lines 408–416):** `attr = prim.GetAttribute(...)` then `if attr:` — invalid attr is falsy → **silent no-op**: cook succeeds, nothing authored, handler reports the node created. This is the true silent-None seam for the same 10 names (e.g. any attempt to set a light's filter binding or a rendersettings camera through SYNAPSE dies silently).

Fix shape for W.5-H22-karmarels: fall back to `prim.GetRelationship(name)` when `GetAttribute` is invalid (read: return `GetTargets()` as string list; write: `SetTargets`), and include relationship names in the line-368 hint list.

**Caveat:** no H21 baseline exists (H21 uninstalled, perception window closed per project memory), so "migration vs H21" is unverifiable — the table above is the ground-truth H22.0.368 shape, not a diff. `camera`/`products`/`light:filters` have been USD relationships upstream for multiple releases; the KAR-08 story is "handlers_usd.py never handled relationships at all," not "H22 moved karma:* attrs to rels."

Probe scripts (scratchpad only): `n3_recon.py`, `n3_main.py` (full per-prim attr/rel dump + seam test), `n3_assign.py`, `n3_assign2.py`.

---

## N-2 + N-7 + C-flags — husk CLI session (KAR-14, KAR-01, C-8/C-9)

Build derived live, verbatim: `hou.applicationVersionString()` → `22.0.368` (hython); husk header: `Build version: 22.0.368`. husk results are CLI-native (husk is the target binary itself).

### N-2 (KAR-14) — husk-on-Indie license shape: PASS (H21 trap REFUTED on H22)

The H21 trap (silent no-op, exit 0, nothing written) is **GONE**. 1-frame render on this Indie box:

- Probe: `husk.exe -f 1 -n 1 -p 8 -V a1 --make-output-path -o .../render1f/probe_$F4.exr stage_1f.usda`
- Verbatim: `EXIT=0`, stderr EMPTY, `>>> Render .../render1f/probe_0001.exr (1 of 1) - 1280x720 ... frame: 1`, output file APPEARED: `probe_0001.exr` 318,914 bytes (real image, ~1.8s wall).
- License evidence (verbatim from `--list-license-checks`): `License Request:` cascade includes `["Houdini Engine Indie","Houdini Indie"]` and `["Houdini Core Apprentice","Houdini Apprentice","Karma Renderer (Indie/Apprentice)"]` — Indie is a first-class husk license tier in H22.
- Mode-forcing flags PRESENT in `--help`: `--indie` ("Force an indie session."), `--core`, `--apprentice`, `--skip-license-modes`, `--check-license-modes`, `--check-licenses`, `--skip-licenses`, `--list-license-checks`, `--license-release-delay`, `--license-max-seat-hold`, `--allow-license-retries`.

### N-7 (KAR-01) — frame-range default from stage timeCodes: PASS (live-confirmed)

- Stage authored via hython (sphere + camera + karmarendersettings LOP, flattened export, then `SetStartTimeCode(1.0)`/`SetEndTimeCode(5.0)` via pxr; verified on reopen: `startTimeCode: 1.0 endTimeCode: 5.0`).
- Probe: `husk.exe -p 8 -V a1 --make-output-path -o .../render5f/probe_$F4.exr stage_5f.usda` — NO frame flags.
- Verbatim: `EXIT=0`, render lines `(1 of 5)` ... `(5 of 5)` frames 1–5, exactly 5 files on disk (probe_0001–probe_0005.exr, ~318KB each).
- Matching `--help` text (verbatim): `-f [ --frame ] arg (=1) ... If the startTimeCode metadata exists on the stage, that value will be used as the default.` / `-n [ --frame-count ] arg (=1) ... If the startTimeCode and endTimeCode metadata exist on the stage, this value will be computed from those values.`

### C-8/C-9 flag existence — 4/4 PASS (verbatim `--help` lines)

- `--skip-existing-frames    Skip frames which have existing render products` (Frame Range section)
- `--karma-percent-of-samples arg (=100)  Percentage of samples to render before completing render`
- `--error-summary arg (=log)  Print a summary of warnings/errors at the end of renders: off | log | force`
- `--pass arg  Render using a pass defined by the given node. You can specify a path relative to /Render. ... Examples: '/Render/pass1', 'pass1', 'pass1,pass2', or 'pass*'`

Counts: 6 VERIFIED-PROVISIONAL (N-2, N-7, `--skip-existing-frames`, `--karma-percent-of-samples`, `--error-summary`, `--pass`) / 0 REFUTED / 0 DOC-CLAIM / 0 QUARANTINE. One legacy claim REFUTED-as-obsolete: H21 Indie husk no-op does not reproduce on 22.0.368.

### H22 traps caught incidentally

1. karmarendersettings/camera LOPs now author prims named after the NODE (`/Render/probe_krs`, `/cameras/probe_cam`) — hard-coded `/Render/rendersettings` + `/cameras/camera1` defaults from H21-era code miss; the LOP's own `camera` parm default (`/cameras/camera1`) dangled and needed correction.
2. Default `productName` timeSamples point at `$HIP/render/untitled.<node>.NNNN.exr` — repo-dir writes if not overridden with `-o`.
3. `husk --help` exits 1 (help text complete); `--list-license-checks` requires no USD file and exits 0.

Artifacts (scratchpad, full verbatim captures): `assayer_husk\husk_help.txt` (468 lines), `husk_1f_stdout.txt` / `husk_1f_stderr.txt` / `husk_5f_stdout.txt` / `husk_5f_stderr.txt`, `stage_1f.usda`, `stage_5f.usda`, `render1f\`, `render5f\`, `author_stages.py`. Reconfirm on the live bridge is NOT required for husk CLI items, but the hython-authored stage defaults (trap 1) should be re-checked live before any panel/handler code relies on prim paths.

---

## N-6 — MaterialX VOP surface (KAR-06)

Emitted-spelling confirmation: `handlers_material.py` emits via constants from `C:\Users\User\SYNAPSE\python\synapse\core\mtlx_types.py:19-34` — literal spellings are exactly the 4 named, plus a 5th repo-emitted type `mtlxstandard_volume` (in `MTLX_TYPES`, pre-flagged elevated-risk). Probed all 5.

### The 4 SYNAPSE-emitted names — ALL PASS

- `'mtlxstandard_surface' in hou.vopNodeTypeCategory().nodeTypes()` → `True` — VERIFIED (provisional-headless)
- `'mtlximage' in hou.vopNodeTypeCategory().nodeTypes()` → `True` — VERIFIED (provisional-headless)
- `'mtlxgeompropvalue' in hou.vopNodeTypeCategory().nodeTypes()` → `True` — VERIFIED (provisional-headless)
- `'mtlxnormalmap' in hou.vopNodeTypeCategory().nodeTypes()` → `True` — VERIFIED (provisional-headless). Note: a versioned `mtlxnormalmap::2.0` also exists on 22.0.368; bare-name createNode will resolve per Houdini version preference — behavior note, not a failure.

### 5th repo-emitted name — GONE

- `'mtlxstandard_volume' in hou.vopNodeTypeCategory().nodeTypes()` → `False` — **QUARANTINE** on 22.0.368 (absent as spelled; nearest survivors in the set: `mtlxvolume`, `mtlxvolumematerial`, `mtlxabsorption_vdf`, `mtlxanisotropic_vdf`). Blast radius: 1 call site — `python/synapse/routing/recipes/render_recipes.py:701` emits `createNode('mtlxstandard_volume', 'dust_shader')` inside generated recipe text → that recipe breaks at runtime on H22.

### Surface metrics

- H22 MTLX VOP set: `len([n for n in hou.vopNodeTypeCategory().nodeTypes() if n.startswith('mtlx')])` → `239`. No H21 baseline exists in harness/notes (`verified_nodetype_catalog_21.0.671.json` has 0 mtlx entries — APEX/createNode-literal scope), so no runtime diff; full 239-name JSON is in the probe output (scratch script: `kar06_mtlx_probe.py`).
- Runtime MaterialX version: `MaterialX.getVersionString()` → `'1.39.5'` (verbatim, same hython).
- New-looking types (docs-knowledge hints only — NOT runtime-diffed, no H21 baseline): OpenPBR family (`mtlxopen_pbr_surface`, `mtlxopen_pbr_anisotropy`, `mtlxopen_pbr_surface_to_standard_surface`, `mtlxstandard_surface_to_open_pbr_surface`), hair BSDFs (`mtlxchiang_hair_bsdf`, `mtlxchiang_hair_roughness`, `mtlxchiang_hair_absorption_from_color`, `mtlxdeon_hair_absorption_from_melanin`), 1.39 pattern set (`mtlxcircle`, `mtlxcloverleaf`, `mtlxhexagon`, `mtlxtiledcircles`, `mtlxtiledcloverleafs`, `mtlxtiledhexagons`, `mtlxcrosshatch`, `mtlxgrid`, `mtlxline`), `mtlxunifiednoise2d/3d`, `mtlxcreatematrix/3`, `mtlxhextiledimage`, `mtlxhextilednormalmap`, `mtlxUsdUVTexture23`, versioned `mtlxnormalmap::2.0`/`mtlxatan2::2.0`. Also present-despite-1.39-core-deprecation: `mtlxswizzle`.

**Summary:** 5 probed → 4 VERIFIED (provisional-headless), 1 QUARANTINE (`mtlxstandard_volume` absent on 22.0.368; single call site render_recipes.py:701). Material surface for wave usd-2 is CLEAR for the 4 handler-emitted types.

---

## N-8 — Output naming defaults (KAR-03)

Probe script: scratchpad `n8_kar03_probe.py` (repo untouched).

### Verbatim defaults (fresh nodes in /stage)

```
karmarendersettings.picture:
  template.defaultValue()      = ('$HIP/render/$HIPNAME.$OS.$F4.exr',)
  unexpandedString()           = '$HIP/render/$HIPNAME.$OS.$F4.exr'
  eval() [expanded]            = 'C:/Users/User/SYNAPSE/render/untitled.KRS_PROBE.0001.exr'
karmarendersettings.productName:
  template.defaultValue()      = ('$HIP/render/$HIPNAME.$OS.$F4.checkpoint',)
  unexpandedString()           = '$HIP/render/$HIPNAME.$OS.$F4.checkpoint'
  eval() [expanded]            = 'C:/Users/User/SYNAPSE/render/untitled.KRS_PROBE.0001.checkpoint'
usdrender_rop.outputimage:
  template.defaultValue()      = ('',)   # empty
usdrender_rop.picture: PARM ABSENT
usdrender_rop.renderer:
  template.defaultValue()      = ('default_delegate',)
```

**Delegate-name question: NO** — no delegate-name component appears in any default output path on 22.0.368. `$OS` expands to the node name, not the delegate. The only delegate string found is the ROP's `renderer` parm value `'default_delegate'` (note: a generic token, not `BRAY_HdKarma` — H22 delta from the H21 recon, but it lives in the renderer parm, never in an output path).

### Pin verdicts

**PIN A — `handlers_render.py:~431` synthesized-default path: PIN HOLDS (PROVISIONAL-headless).** `usdrender_rop.outputimage` default is empty on 22.0.368, so the `if not (artist_output and artist_output.strip())` branch fires exactly as designed and the SYNAPSE-synthesized `{base}.$F{pad}.exr` path remains necessary and delegate-free. One behavioral note, not a flip: the upstream `picture` walk (handlers_render.py:334-338) will still pick up karmarendersettings' non-empty factory default `$HIP/render/$HIPNAME.$OS.$F4.exr` as "artist output" — same as H21, unchanged.

**PIN B — `solaris_compose_tools.py:55` productName-does-not-author-the-prim: PIN HOLDS (PROVISIONAL-headless).** Sentinel probe: set `productName` parm to `$HIP/PIN_B_SENTINEL.$F4.exr`, cooked, read the stage — verbatim output:

```
/Render/KRS_PROBE/Products/renderproduct.productName: HasAuthoredValue=True Get=''
SENTINEL reached prim productName attr: False
```

The prim attr is authored to empty string and the parm value never reaches it — BL-007 behavior confirmed on 22.0.368; the direct-schema authoring in `_PRODUCT_NAME_CODE` remains required. New H22 wrinkle worth logging: the `productName` parm's factory default is now a `.checkpoint` path.

**Summary:** 2/2 pins VERIFIED-HOLD (PROVISIONAL-headless, build 22.0.368), 0 flipped, 0 quarantined. **Nothing feeds W.7 — non-trigger.** Reconfirm on live bridge when it comes up.

---

## N-9 — Color pipeline (KAR-13)

### (1) OCIO config verdict — VERIFIED (provisional-headless)

Probe (verbatim): `"$HYTHON" -c 'import os; print("OCIO env =", repr(os.environ.get("OCIO"))); import PyOpenColorIO as ocio; print("PyOpenColorIO version =", ocio.GetVersion()); cfg = ocio.GetCurrentConfig(); print("config name =", repr(cfg.getName())); ...'`

Output (verbatim):

```
OCIO env = 'C:/PROGRA~1/SIDEEF~1/HOUDIN~1.368/packages/ocio/houdini-config-v3.0.0_aces-v2.0_ocio-v2.5.ocio'
PyOpenColorIO version = 2.5.0
config name = 'cg-config-v3.0.0_aces-v2.0_ocio-v2.5'
config description = 'Academy Color Encoding System - CG Config [COLORSPACES v3.0.0] [ACES v2.0] [OCIO v2.5]\n...'
```

- H22.0.368 auto-sets `OCIO` to the **bundled** `$HFS/packages/ocio/houdini-config-v3.0.0_aces-v2.0_ocio-v2.5.ocio` — ACES **2.0** era confirmed (expectation "ACES 3.0-era" refers to CG-config **v3.0.0**; the ACES system version is 2.0).
- 24 colorspaces. `ACEScg` FOUND, `ACES2065-1` FOUND, `Raw` FOUND, plain `sRGB` **MISSING** (canonical: `'sRGB Encoded Rec.709 (sRGB)'`; aliases `'sRGB - Texture'`, `'srgb_tx'` resolve via `getColorSpace`). Default display `'sRGB - Display'`, default view `'Un-tone-mapped'`.
- **QUARANTINE:** `Config.getNumColorSpaces()` — `AttributeError: 'PyOpenColorIO.PyOpenColorIO.Config' object has no attribute 'getNumColorSpaces'. Did you mean: 'getColorSpaces'?` (verbatim). Phantom on OCIO 2.5.0; `hasattr = False`. The corpus file uses it at line 40.

### (2) imaketx flag verdicts — VERIFIED (provisional-headless)

Probe: `"C:/Program Files/Side Effects Software/Houdini 22.0.368/bin/imaketx.exe" --help` piped to grep.

- `--opaque-detect` **PRESENT**, verbatim: `--opaque-detect    Remove alpha channel that is fully opaque (always 1.0), saving only an RGB texture.`
- **16-bit-float mentions: ABSENT.** `grep -i -E "half|float"` over the full help → zero matches (grep exit 1). Only bit-depth text in the entire help is under `-l/--linearize`: "Three channel 8-bit images are assumed to be in sRGB space." Only format control: `-F [ --format ] arg   Specify the texture format (OpenEXR, RAT, TIFF)` — no explicit depth/precision flag exposed in `--help`.

### (3) Correction spec — W.6 corpus reseed (spec only; file NOT edited)

Target: `C:/Users/User/SYNAPSE/rag/skills/houdini21-reference/aces_color_management.md` (lines 1–60)

```
CORRECTION SPEC — aces_color_management.md — probed on 22.0.368 (hython, PROVISIONAL-headless)

[C1] Line 1
OLD: # ACES Color Management in Houdini 21
NEW: # ACES Color Management in Houdini 22
     (scope note: verified on 22.0.368)

[C2] Line 9
OLD: Houdini 21 integrates ACES via OpenColorIO. This reference covers setup, texture handling,
NEW: Houdini 22 ships OpenColorIO 2.5.0 with a bundled ACES 2.0 CG config as the out-of-the-box
     default. This reference covers setup, texture handling,

[C3] Lines 22–26 (the "OCIO not set" branch + fix instructions)
OLD:         print("OCIO environment variable not set")
             print("Fix: Set OCIO to your ACES config path")
             print('  Windows: setx OCIO "D:\\ACES\\cg-config-v2.0.0_aces-v1.3_ocio-v2.3.ocio"')
             print('  houdini.env: OCIO = "D:/ACES/cg-config-v2.0.0.ocio"')
NEW:         print("OCIO not set -- unexpected: H22 auto-sets OCIO to its bundled config")
             print('  Bundled default: $HFS/packages/ocio/houdini-config-v3.0.0_aces-v2.0_ocio-v2.5.ocio')
             print('  Override only if pinning a studio config, e.g. cg-config-v3.0.0_aces-v2.0_ocio-v2.5.ocio')
TRUTH: on 22.0.368, os.environ["OCIO"] is pre-set by Houdini to
       .../packages/ocio/houdini-config-v3.0.0_aces-v2.0_ocio-v2.5.ocio;
       the ACES 1.3 / cg-config-v2.0.0 / ocio-v2.3 era guidance is stale.

[C4] Line 40
OLD:         print(f"Color spaces: {config.getNumColorSpaces()}")
NEW:         print(f"Color spaces: {len(list(config.getColorSpaces()))}")
TRUTH: Config.getNumColorSpaces() is PHANTOM on PyOpenColorIO 2.5.0 (AttributeError,
       hasattr=False). getColorSpaces() iterable is the live API (yields 24 on default config).

[C5] Line 43
OLD:         for cs_name in ["ACEScg", "ACES2065-1", "sRGB", "Raw"]:
NEW:         for cs_name in ["ACEScg", "ACES2065-1", "sRGB - Texture", "Raw"]:
TRUTH: plain "sRGB" does not resolve in the H22 default config (getColorSpace -> None).
       Canonical name is 'sRGB Encoded Rec.709 (sRGB)'; aliases 'sRGB - Texture' and
       'srgb_tx' resolve. ACEScg / ACES2065-1 / Raw unchanged.

[C6] ADDITIVE (context section) — default display/view on H22 default config:
       default display = 'sRGB - Display', default view = 'Un-tone-mapped'.
       Any corpus text implying an ACES 1.x tone-mapped default view is stale.
```

**Summary:** VERIFIED 8 (build string, OCIO env auto-set, PyOpenColorIO 2.5.0, config identity `cg-config-v3.0.0_aces-v2.0_ocio-v2.5`, `getColorSpaces`, `getColorSpace` alias resolution, imaketx `--opaque-detect`, imaketx `-F/--format`) — all PROVISIONAL-headless. QUARANTINE 2: `Config.getNumColorSpaces()` (AttributeError on OCIO 2.5.0 binding); plain `"sRGB"` colorspace name (unresolvable in default H22 config). ABSENT-as-probed 1: 16-bit-float/half behavior mentions in `imaketx --help` (zero grep hits). Correction spec: 5 line-level corrections + 1 additive, file untouched.

---

## N-10 — Pixel filter parms (KAR-02)

Method: `nodeType.parmTemplateGroup()` scan AND live instance `createNode()` ptg scan (both agreed) on all 3 render-settings LOP variants found: `karmarenderproperties`, `karmarendersettings`, `rendersettings`. Probe scripts (scratchpad): `n10_kar02_probe.py`, `n10_kar02_probe2.py`.

### Per-parm verdicts (verbatim internal names)

**`karmarendersettings` (330 parm templates):**

- `pixelfilter` — PRESENT, label 'Pixel Filter', String, default `('gauss',)`, hidden=False, folder 'AOVs (Render Vars)'
- `pixelfilterscale` — PRESENT, label 'Pixel Filter Scale', Float, default `(1.0,)`, hidden=False — the live visible control
- `pixelfiltersize` — PRESENT but **hidden=True**, label 'Pixel Filter Size', Float, default `(2.0,)` — UI-absent, API-present (legacy kept for compat; setting it won't error)
- `karma_pixelfilterclamp` — ABSENT (type + instance) → **QUARANTINE: phantom on 22.0.368**. Only clamp-named parm on the node: `causticsroughnessclamp` ('Caustics Roughness Clamp') — unrelated, not a successor.

**`karmarenderproperties` (second variant, ALSO labeled "Karma Render Settings", 293 templates):**

- `pixelfiltersize` — PRESENT, hidden=False (still the live parm here)
- `pixelfilterscale` — ABSENT
- `pixelfilter` — PRESENT, default `('gauss',)`
- `karma_pixelfilterclamp` — ABSENT

**`rendersettings` (generic USD; karma spare parms materialize on instance: 48→378 templates):**

- 'Pixel Filter Scale' — PRESENT as punycode spare `xn__karmaglobalpixelfilterscale_0kbg` (+ `..._control_1xbg`)
- 'Pixel Filter Size' — ABSENT (no equivalent)
- 'Pixel Filter' — PRESENT as `xn__karmaglobalpixelfilter_vcbg`
- `karma_pixelfilterclamp` — ABSENT

**Verdict vs KAR-02 hypothesis: PARTIALLY REFUTED as stated.** "Size ABSENT / Scale PRESENT" is only UI-true on `karmarendersettings` — `pixelfiltersize` survives hidden there, and on `karmarenderproperties` the situation inverts (Size visible, Scale ABSENT). `karma_pixelfilterclamp` verdict: DOES NOT SURVIVE — phantom on all 3 variants.

### W.6 corpus seeds should say

1. H22.0.368 `karmarendersettings`: author `pixelfilterscale` (float, default 1.0) — the visible pixel-filter width control. `pixelfiltersize` still exists but is HIDDEN (default 2.0); legacy scripts setting it won't error, but new code should use `pixelfilterscale`. Filter kernel is `pixelfilter` (string, default 'gauss').
2. `karmarenderproperties` (same UI label "Karma Render Settings") is NOT interchangeable: it has `pixelfiltersize` (visible) and NO `pixelfilterscale` — branch on node type name, not label.
3. Generic `rendersettings` LOP: the karma spare parm is punycode `xn__karmaglobalpixelfilterscale_0kbg` ('Pixel Filter Scale'); no Size equivalent. Probe-generate punycode names, never guess (matches existing UsdLux punycode rule).
4. `common_errors.md` / `render_farm.md`: DELETE every `karma_pixelfilterclamp` recipe — parm ABSENT on all render-settings LOPs in 22.0.368, no direct clamp successor on these nodes (`causticsroughnessclamp` is caustics-only, unrelated).

**Summary:** 5 probed → 3 VERIFIED-provisional-headless (`pixelfilter`, `pixelfilterscale`, `pixelfiltersize`-hidden on karmarendersettings), 1 variant-divergence flag (karmarenderproperties inversion), 1 QUARANTINE (`karma_pixelfilterclamp`, absent everywhere). Bridge/hython build-mismatch check: N/A (bridge down); reconfirm on live bridge when it returns.

---

## GATE-FEED — which verdict feeds which gate or cycle

| Probe | Verdict (compressed) | Feeds | What the consumer does with it |
|---|---|---|---|
| **N-5** | Layout LOP = RENAME to `Lop/paintinstances` (alias shipped, 41/42 parms survive, `method` dropped); `instancer` → `copytopoints` sibling rename; `verified_connectivity_H22.json` removed[2].note REFUTED | **Human gate 2 + W.3** | Gate 2 must correct the connectivity-note ("createNode('layout') will fail" is false — it aliases silently); W.3 emits canonical new spellings (`paintinstances`, `copytopoints`), drops `method` / `allowmissingprototypes` usage, and never type-checks for the old names |
| **N-3** | 10 relationship names across RenderSettings/RenderProduct/Light/LightFilter; zero `karma:*` rels; handlers_usd.py read path RAISES, set path silently no-ops on all 10 | **W.5 scope** | W.5-H22-karmarels is scoped tight: add `GetRelationship` fallback (read: `GetTargets()`; write: `SetTargets`) + relationship names in the line-368 hint. NOT a karma-attr migration — no `karma:*` rewrites needed |
| **N-2** | husk renders on Indie (exit 0, real EXR on disk); Indie is a first-class license tier; `--indie`/`--core`/`--apprentice` mode flags exist; H21 silent-no-op trap REFUTED-as-obsolete | **KAR-14 wiring** | KAR-14 can wire husk into the render path on this box; the H21 Indie-no-op guard/workaround (flipbook-only verification) can be retired for H22 — pending live-bridge reconfirm of the hython-authored stage defaults |
| **N-7 + C-flags** | Frame range defaults from stage timeCodes (5 frames rendered with no `-f`/`-n`); all 4 flags (`--skip-existing-frames`, `--karma-percent-of-samples`, `--error-summary`, `--pass`) exist verbatim | **KAR-14 wiring** (with N-2) | Sequence-render wiring may rely on stage timeCode defaults and the 4 C-flags as real husk surface on 22.0.368 |
| **N-6** | 4/4 handler-emitted MTLX types VERIFIED; `mtlxstandard_volume` QUARANTINE (1 call site: `render_recipes.py:701`); MaterialX 1.39.5; 239 mtlx VOPs | **usd-2 clearance** | Wave usd-2 is CLEAR on the material surface for the 4 handler-emitted types; the single quarantined recipe emitter (`render_recipes.py:701`) is the one fix usd-2 must carry |
| **N-8** | Both pins HOLD (usdrender_rop.outputimage default empty → synthesized path still needed; productName parm still doesn't author the prim); no delegate name in any default path | **W.7 non-trigger** | **W.7 does NOT trigger** — nothing flipped; existing handlers_render.py / solaris_compose_tools.py behavior remains correct. Log the `.checkpoint` productName default + `'default_delegate'` renderer token as notes only |
| **N-9** | Bundled OCIO auto-set (ACES 2.0 CG-config v3.0.0, OCIO 2.5.0); `getNumColorSpaces()` phantom; plain `"sRGB"` unresolvable; imaketx `--opaque-detect` present, no half/float flag | **W.6 seeds** | W.6 reseeds `aces_color_management.md` per the 5+1 correction spec (C1–C6, spec verbatim above; file deliberately untouched by the probe) |
| **N-10** | `karmarendersettings`: scale visible / size hidden; `karmarenderproperties` INVERTED (size visible / no scale); generic `rendersettings` punycode spare; `karma_pixelfilterclamp` phantom on all 3 | **W.6 seeds** | W.6 seeds the 4 corpus statements above (scale-vs-size per node type, branch on type name not label, punycode probe-don't-guess, delete every `karma_pixelfilterclamp` recipe in `common_errors.md`/`render_farm.md`) |

---

## DEFERRED — deliberately not run this session

Honest list. None of these were attempted headless; no partial results exist to be over-trusted.

| Item | Why deferred | Where it runs |
|---|---|---|
| **N-1** | Requires the live WS bridge (bridge DOWN all session) | Live-bridge session |
| **N-4** | Requires the live WS bridge | Live-bridge session |
| **`hou.secure`** | Live-runtime posture check — headless answer would not be authoritative for the served process | Live-bridge session |
| **N-11 / N-12 (vcc pair)** | Deliberately not run — held back with the vcc pair as a unit | Future session (scheduled with vcc work) |

Additionally, every PASS in this document carries the **PROVISIONAL-headless** stamp: reconfirm on the live bridge when it returns (husk CLI items exempt — the CLI is the target — except the hython-authored stage-default trap noted in the husk section).
