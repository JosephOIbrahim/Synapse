# H22 Doc-Intel — Prioritized Report

> **Provenance:** fetch-date `2026-07-15` · symbol-table stamp **22.0.368** · doc source **sidefx.com/docs/houdini22.0**
> Generated by the `h22-doc-scout` workflow (28 agents · 21 pages · 42 findings). `HOM-02` independently CTO-confirmed: `CopNode.planes` absent from the H22 table; SYNAPSE calls `node.planes()` at `handlers_cops.py:446,684`.
> Cross-verified from 42 findings against the introspected H22 `dir()` symbol table. Every candidate below is **DOC-CLAIM** until its runtime probe runs under H22 hython. This report never mutates code.

---

## Executive Summary

42 doc-derived candidates, cross-checked against the committed 22.0.368 symbol table and the live SYNAPSE code/corpus surface.

**By bucket:** NEW_MCP_TOOL 10 · RECIPE_CHANGE 11 · CAPABILITY_GAP 10 · API_MIGRATION 2 · CORPUS_SEED 9
**By tier:** VERIFIED 19 · DOC-CLAIM 13 · DOC-ONLY 10
**By domain:** Solaris 17 · COPs/Copernicus 13 · MCP/HOM 12
**Escalations (breaking / version-bump smell):** 3 → `HOM-02`, `COP-01`, `COP-12`

**The headline signal:** H22 is a real Copernicus rewrite. The single most urgent item is a **live break** — `hou.CopNode.planes()` is gone from H22's 301-method `CopNode` table, and three shipping tools (`cops_read_layer_info`, `cops_composite_aovs`, `cops_analyze_render`) still read through it. Alongside that, an entire **neural COP capability class** (SAM2 masks, MoGe-2 depth) and a **GPU COP-terrain** workflow arrive with zero SYNAPSE coverage. On the HOM side, two clean wins land fully symbol-verified: `hou.CopVerb` (headless COP cooking) and `hou.data` (native recipe/preset substrate), both with all symbols PRESENT and safe to build against after a confirming probe.

**Tier discipline:** VERIFIED here means the *dotted Python API symbols* resolve in the 22.0.368 table — it does **not** clear node-type-name strings (LOP/COP type names are never table-verifiable and must be runtime-probed before any `createNode`). DOC-ONLY items are dominated by the `hou.lop` submodule, which has **0 children introspected** in the headless table — that is a coverage blind-spot, **not** a phantom verdict (see PHANTOM WATCH).

### TOP 10 Highest-Leverage

| # | Domain | Bucket | Item | Tier | Gap |
|---|---|---|---|---|---|
| 1 | MCP/HOM | RECIPE_CHANGE | **`HOM-02`** CopNode read model — migrate `node.planes()` → `cable()`/`layer()` | VERIFIED | **LIVE BREAK**: `.planes` absent in H22; 3 shipping tools read through it |
| 2 | COPs | NEW_MCP_TOOL | **`COP-01`** Neural AI COP nodes (SAM2 mask, MoGe-2 depth) | VERIFIED | Whole new class; 0 tools, 0 corpus |
| 3 | MCP/HOM | NEW_MCP_TOOL | **`HOM-01`** `hou.CopVerb` headless COP cooking | VERIFIED | All 8 symbols PRESENT; deterministic path for ~20 `cops_*` tools |
| 4 | MCP/HOM | NEW_MCP_TOOL | **`HOM-03`** `hou.data` native recipe/preset substrate | VERIFIED | All 10 symbols PRESENT; unreferenced repo-wide |
| 5 | COPs | API_MIGRATION | **`COP-12`** Copernicus category vs legacy COP2 | VERIFIED | Silent phantom-type breakage if targeting `Cop2`/`CopNet` |
| 6 | Solaris | NEW_MCP_TOOL | **`SOL-14`** Component Builder 4-node chain wrapper | VERIFIED | Core set-dressing; no single `build_component` tool |
| 7 | COPs | NEW_MCP_TOOL | **`COP-09`** GPU COP heightfields / terrain | VERIFIED | Whole GPU-COP-terrain workflow uncovered |
| 8 | MCP/HOM | API_MIGRATION | **`HOM-04`** `hou.lop.makeValidPrimName/Path` | DOC-ONLY | Resolves open RFC D-3 (blind-spot — probe, don't dismiss) |
| 9 | Solaris | CAPABILITY_GAP | **`SOL-04`** Python Script LOP reversible pxr-authoring | VERIFIED | Bridge-reversible alt to *ungated* `execute_python` |
| 10 | COPs | RECIPE_CHANGE | **`COP-03`** Bake Geometry Textures versioned type (`::2.0`) | VERIFIED | Existing tool is an empty scaffold (vopcop2gen placeholders) |

---

## Solaris

### NEW_MCP_TOOL

**`SOL-02` — Prune LOP** · `DOC-CLAIM` · [about_lops.html](https://www.sidefx.com/docs/houdini22.0/solaris/about_lops.html)
Reversible-by-construction prim deactivation as an alternative to hard USD prim deletion.
Probe: `hou.nodeType(hou.lopNodeTypeCategory(), 'prune') is not None`
Gap: **PARTIAL** — `prune` known to `handlers_solaris_assemble.py` ordering (key 290) + 5 corpus files, but no dedicated prune MCP tool.

**`SOL-06` — Stage Manager LOP (bulk set-dressing)** · `DOC-CLAIM` · [about_lops.html](https://www.sidefx.com/docs/houdini22.0/solaris/about_lops.html)
Spreadsheet-style bulk referencer/layout; one reversible op to assemble many references + xforms.
Probe: `hou.nodeType(hou.lopNodeTypeCategory(), 'stagemanager') is not None`
Gap: **PARTIAL** — `stagemanager` known to assemble ordering (key 5) only; no bulk tool, **zero** corpus coverage.

**`SOL-08` — Inline USD LOP (raw .usda injection)** · `DOC-CLAIM` · [usd.html](https://www.sidefx.com/docs/houdini22.0/solaris/usd.html)
An `inlineusd`-backed tool would inject arbitrary `.usda` text as a first-class, undoable op.
Probe: `hython -c "print(hou.nodeType(hou.lopNodeTypeCategory(),'inlineusd'))"` → non-None confirms the type; then inspect `parmTemplateGroup()` for the USDA text parm.
Gap: **GAP** — `inlineusd` in **zero** code files and **zero** corpus files. Genuine unexposed capability.

**`SOL-10` — Stage Manager batch-reference** · `DOC-CLAIM` · [usd.html](https://www.sidefx.com/docs/houdini22.0/solaris/usd.html)
Batch-reference N USD files in one node; matches "assemble a shot from N assets" at constant node count.
Probe: `hython -c "print(hou.nodeType(hou.lopNodeTypeCategory(),'stagemanager'))"` → enumerate its multiparm for per-asset rows.
Gap: **PARTIAL** — duplicate angle of `SOL-06`; `houdini_reference_usd` covers single-asset only.

**`SOL-14` — Component Builder 4-node chain wrapper** · `VERIFIED` · [component_builder.html](https://www.sidefx.com/docs/houdini22.0/solaris/component_builder.html)
`houdini_build_component` could orchestrate `componentgeometry → materiallibrary → componentmaterial → componentoutput`.
Probe: `cat=hou.lopNodeTypeCategory(); [print(n, hou.nodeType(cat,n) is not None) for n in ['componentgeometry','materiallibrary','componentmaterial','componentoutput','componentgeometryvariants']]`
Gap: **PARTIAL** — `hou.lopNodeTypeCategory`+`hou.nodeType` PRESENT; node-type strings need runtime probe. Assemble has a `component_builder` chain template + `scene_recipes.py` a partial recipe, but **no** single `build_component` tool.

### RECIPE_CHANGE

**`SOL-03` — Three instancing LOPs (Copy to Points / PointInstancer / Scatter Instances)** · `DOC-CLAIM` · [about_lops.html](https://www.sidefx.com/docs/houdini22.0/solaris/about_lops.html)
`houdini_create_point_instancer` must target the correct H22 LOP internal type name.
Probe: `cat=hou.lopNodeTypeCategory(); [n for n in ('instancer','copytopoints','scatterinstances','pointinstancer') if hou.nodeType(cat, n) is not None]`
Gap: **PARTIAL** — current tool authors a raw `UsdGeom.PointInstancer` via pythonscript LOP, not a LOP type. `scene_recipes.py` records NO native Solaris scatter LOP on H21 → **`scatterinstances` may be phantom** in H22 (probe guards it).

**`SOL-11` — Configure Stage LOP (payload / asset resolution)** · `DOC-CLAIM` · [usd.html](https://www.sidefx.com/docs/houdini22.0/solaris/usd.html)
`set_payload_loadstate` should route through `configurestage`; verify H22 payload/load-mask parm names haven't drifted.
Probe: `nt=hou.nodeType(hou.lopNodeTypeCategory(),'configurestage'); print(nt is not None)` then list `[p.name() for p in nt.parmTemplateGroup().entries()]`.
Gap: **PARTIAL** — payload COVERED by `houdini_set_payload_loadstate`, but `configurestage` = 0 references (tool doesn't route through it). Listed in `solaris_nodes.md` header. Verify parm names before wiring.

**`SOL-15` — Component Output write / Save-to-Disk params** · `DOC-ONLY` · [component_builder.html](https://www.sidefx.com/docs/houdini22.0/solaris/component_builder.html)
A component-write recipe must set Root Prim / Name / Filename / Location + Export Options and trigger Save to Disk.
Probe: `n=hou.node('/stage').createNode('componentoutput'); print(sorted(p.name() for p in n.parms())); n.destroy()`
Gap: **GAP** — `componentoutput` is a node-type-name (not table-verifiable → DOC-ONLY). Instantiated as bare `createNode` but **no** code drives its write parms. Introspect first.

### API_MIGRATION

*(none in Solaris)*

### CAPABILITY_GAP

**`SOL-04` — Python Script LOP reversible pxr-authoring surface** · `VERIFIED` · [about_lops.html](https://www.sidefx.com/docs/houdini22.0/solaris/about_lops.html)
Bridge-reversible, undo-wrapped alternative to *ungated* `houdini_execute_python` for direct `pxr.Usd` authoring inside a LOP layer.
Probe: `hou.nodeType(hou.lopNodeTypeCategory(), 'pythonscript') is not None` then inside cook: `type(node.editableStage()).__module__.startswith('pxr')`
Gap: **PARTIAL** — symbols present; already used *internally* across `handlers_usd.py`/`handlers_render.py` but not exposed as a general-purpose reversible pxr-authoring tool distinct from `execute_python`.

**`SOL-05` — Layer-management node set (Layer Break / Configure Layer / Load Layer / Layer Replace / Edit Target Layer)** · `DOC-CLAIM` · [about_lops.html](https://www.sidefx.com/docs/houdini22.0/solaris/about_lops.html)
Unexposed layer-management capabilities for composition-safe authoring.
Probe: `{n: hou.nodeType(hou.lopNodeTypeCategory(), n) is not None for n in ('sublayer','reference','configurelayer','layerbreak','loadlayer','layerreplace','edittargetlayer')}`
Gap: **PARTIAL** — sublayer/reference/payload COVERED; `layerbreak` referenced; `configurelayer`/`loadlayer`/`layerreplace`/`edittargetlayer` = 0 references → unexposed.

**`SOL-09` — Python Script LOP is the valid pxr-authoring context** · `VERIFIED` · [usd.html](https://www.sidefx.com/docs/houdini22.0/solaris/usd.html)
Addresses `editableStage()==None`-outside-a-cook: pxr authoring must run inside a pythonscript LOP cook, not a bare `execute_python` on a read-only stage.
Probe: create a lop `pythonscript`, cook it, and inside its snippet call `node.editableStage()` — writable `Usd.Stage` (non-None) only during the cook.
Gap: **COVERED** — idiom already live (`stage = hou.pwd().editableStage()`), documented in `usd_stage_composition.md:143`. Confirm-still-true corpus item.

**`SOL-12` — Create LOD / Auto Select LOD LOPs** · `DOC-CLAIM` · [usd.html](https://www.sidefx.com/docs/houdini22.0/solaris/usd.html)
Auto-LOD generation + distance-selection SYNAPSE doesn't expose. LOP names are summarizer-derived (**phantom risk**).
Probe: `c=hou.lopNodeTypeCategory(); print(hou.nodeType(c,'createlod'), hou.nodeType(c,'autoselectlod'))` — any None ⇒ phantom as-spelled; search `c.nodeTypes()` for `'lod'`.
Gap: **GAP** — `createlod`/`autoselectlod` = 0 references code + corpus. Treat as unverified until probe resolves real names.

**`SOL-16` — Component-native variant authoring (`componentgeometryvariants` set `geo`, `componentmaterial` set `mtl`)** · `DOC-ONLY` · [component_builder.html](https://www.sidefx.com/docs/houdini22.0/solaris/component_builder.html)
Component Builder authors geo/material variant sets through dedicated nodes — distinct from generic variants.
Probe: `c=hou.lopNodeTypeCategory(); [print(n, hou.nodeType(c,n) is not None) for n in ['componentgeometryvariants','componentmaterial']]` then confirm variant-set-name parm defaults (`geo`/`mtl`).
Gap: **PARTIAL** — generic variants COVERED by `houdini_manage_variant_set`; component-native path recognized in ordering + `solaris_variants.md`, not wired as a tool.

### CORPUS_SEED

**`SOL-01` — Edit Target unsupported outside a cook (author node-based, read via `stage()`)** · `VERIFIED` · [about_lops.html](https://www.sidefx.com/docs/houdini22.0/solaris/about_lops.html)
Grounds the `editableStage()`-is-None-outside-a-cook recon. **Re-seed the nuance, not the overclaim**: edit-targets DO work *inside* a pythonscript cook.
Probe: `hasattr(hou.LopNode, 'editableStage') and hou.node('/stage/<a_lop>').editableStage() is None`
Gap: **COVERED** — both symbols present; live authoring backbone. Needs H22 re-seed of nuance only.

**`SOL-07` — Composed-stage read via `hou.LopNode.stage()`** · `VERIFIED` · [usd.html](https://www.sidefx.com/docs/houdini22.0/solaris/usd.html)
Canonical H22 read idiom that `inspect_stage`/`query_prims`/`get_usd_attribute` all depend on.
Probe: `hython -c "print(hasattr(hou.LopNode,'stage'), hasattr(hou.LopNode,'editableStage'))"` — both True.
Gap: **COVERED** — read-path signature confirmed unchanged for H22.

**`SOL-13` — LIVRPS composition-strength ordering** · `DOC-CLAIM` · [usd.html](https://www.sidefx.com/docs/houdini22.0/solaris/usd.html)
Local > Inherits > Variants > References > Payloads > Specializes — grounds composition-validation reasoning.
Probe: prose (no runtime symbol); sanity-check by authoring an `over` on a referenced prim and confirming the local opinion wins after cook.
Gap: **COVERED** — documented in 5 corpus files (full strength table in `usd_stage_composition.md`). Needs H22 re-seed; content not a gap.

**`SOL-17` — Component asset layer/composition structure (payload/geo/mtl/extra split)** · `DOC-CLAIM` · [component_builder.html](https://www.sidefx.com/docs/houdini22.0/solaris/component_builder.html)
Prose for the houdini22-reference RAG skill. *(Finding truncated in input; reconstructed.)*
Probe: prose (no runtime symbol); cross-check against `solaris_network_blueprint.md` + `solaris_variants.md`.
Gap: **COVERED** — structure documented in 2 corpus files; H22 re-seed genuine, concept not a gap.

---

## COPs / Copernicus

### NEW_MCP_TOOL

**`COP-01` — Neural AI COP nodes (SAM2 mask, MoGe-2 depth, Cellular Automata, Denoise AI)** · `VERIFIED` · **ESCALATE** · [nodes/cop/index.html](https://www.sidefx.com/docs/houdini22.0/nodes/cop/index.html)
Whole new capability class: SAM2 prompt-driven masks + MoGe-2 monocular depth as COP layers. Candidate `cops_segment_mask` / `cops_estimate_depth`.
Probe: `[t.name() for cat in hou.nodeTypeCategories().values() for t in cat.nodeTypes() if t.name() in ('neural_layertomask_sam2','neural_layertodepth_moge2','neural_cellularautomatacore','neural_cellularautomatadecode','denoiseai')]`
Gap: **GAP** — no neural tool anywhere; `copernicus_compositing.md` covers only cryptomatte+OIDN. Node-type strings absent from table **by design** (not Python API symbols) — runtime-probe on 22.0.

**`COP-09` — GPU COP heightfields / terrain** · `VERIFIED` · [heightfields_cop/index.html](https://www.sidefx.com/docs/houdini22.0/heightfields_cop/index.html)
Full GPU terrain workflow (creation, noise, erosion, strata, boulders, masking, colorizing) inside COP networks. Candidate `cops_heightfield` / `cops_terrain`.
Probe: `cat=hou.nodeTypeCategories().get('Cop'); print(sorted(nt.name() for nt in cat.nodeTypes() if any(s in nt.name().lower() for s in ('height','terrain','noise','erode','erosion'))))`
Gap: **GAP** — no terrain tool; `terrain_heightfields.md` is SOP-voxel-only. (`hou.node.createNode` as-written is a notation artifact; real `hou.Node.createNode` PRESENT.)

### RECIPE_CHANGE

**`COP-03` — Bake Geometry Textures is a versioned type (`::2.0`)** · `VERIFIED` · [nodes/cop/index.html](https://www.sidefx.com/docs/houdini22.0/nodes/cop/index.html)
HOM namespaced types use `::` (`bakegeometrytextures::2.0`), **not** the URL `-2.0`; an unversioned/old string raises on `createNode`.
Probe: `[t.name() for cat in hou.nodeTypeCategories().values() for t in cat.nodeTypes() if 'bakegeometrytextures' in t.name()]`
Gap: **PARTIAL** — `cops_bake_textures` exists (registry ~1323) but is a scaffold making `vopcop2gen` placeholders (no bake, no files). Needs the runtime-resolved versioned name.

**`COP-04` — Reaction-Diffusion is a Block Begin/End pair** · `VERIFIED` · [nodes/cop/index.html](https://www.sidefx.com/docs/houdini22.0/nodes/cop/index.html)
Must author `reactiondiffusion_block_begin → … → reactiondiffusion_block_end`; a single-node recipe won't cook.
Probe: `[t.name() for cat in hou.nodeTypeCategories().values() for t in cat.nodeTypes() if 'reactiondiffusion' in t.name()]`
Gap: **PARTIAL** — `cops_reaction_diffusion` builds GENERIC `block_begin/end` + an opencl kernel (~1118-1160), not the native RD block pair.

**`COP-05` — Slap Comp Import (`slapcompimport`) bridges Solaris viewport → COPs** · `VERIFIED` · [nodes/cop/index.html](https://www.sidefx.com/docs/houdini22.0/nodes/cop/index.html)
Confirm the type name and that it pulls live layers from the Solaris viewport.
Probe: `next((t.name() for cat in hou.nodeTypeCategories().values() for t in cat.nodeTypes() if t.name()=='slapcompimport'), None)`
Gap: **PARTIAL** — `cops_slap_comp` (~714) configures a viewport OVERLAY on an existing `cop_path`; doesn't instantiate `slapcompimport`. Distinct mechanism.

**`COP-06` — USD Material COP (`usdmaterial`)** · `VERIFIED` · [nodes/cop/index.html](https://www.sidefx.com/docs/houdini22.0/nodes/cop/index.html)
The concrete COP→USD/MaterialX bridge; drive texture/material output back into a USD material.
Probe: `next((t.name() for cat in hou.nodeTypeCategories().values() for t in cat.nodeTypes() if t.name()=='usdmaterial'), None)`
Gap: **PARTIAL** — `cops_to_materialx` (~469) wires an `op:` path into a MaterialX shader input; doesn't use the `usdmaterial` COP node. Distinct capability.

**`COP-07` — AOV/matte set: Cryptomatte (+Decode/Encode) + Z Composite (`zcomp`)** · `VERIFIED` · [nodes/cop/index.html](https://www.sidefx.com/docs/houdini22.0/nodes/cop/index.html)
Build the AOV recipe on native `cryptomatte`/`cryptomattedecode`/`cryptomatteencode`/`zcomp` rather than manual channel math.
Probe: `[t.name() for cat in hou.nodeTypeCategories().values() for t in cat.nodeTypes() if t.name() in ('cryptomatte','cryptomattedecode','cryptomatteencode','zcomp')]`
Gap: **PARTIAL** — `cops_composite_aovs` merges via file + `composite`/`over` (~576-600), not native crypto/zcomp; corpus covers cryptomatte extraction conceptually only.

**`COP-08` — COP render output `rop_image`; OpenCL kernel node `opencl`** · `VERIFIED` · [nodes/cop/index.html](https://www.sidefx.com/docs/houdini22.0/nodes/cop/index.html)
`cops_analyze_render`/`cops_batch_cook` drive output through `rop_image`; `cops_set_opencl` targets `opencl`.
Probe: `[t.name() for cat in hou.nodeTypeCategories().values() for t in cat.nodeTypes() if t.name() in ('rop_image','opencl')]`
Gap: **PARTIAL** — `cops_set_opencl` already targets `opencl` (confirmed live ~1135/1239); `rop_image` as output type is UNCONFIRMED in code — verify at runtime.

**`COP-10` — New COP terrain operators: Strata, Boulders, Erosion, Slump, Terracing** · `DOC-CLAIM` · [heightfields_cop/index.html](https://www.sidefx.com/docs/houdini22.0/heightfields_cop/index.html)
Docs give **display labels only** — recipes must resolve real type names at runtime (#1 phantom-API risk).
Probe: `cat=hou.nodeTypeCategories().get('Cop'); print(sorted(nt.name() for nt in cat.nodeTypes() if any(s in nt.name().lower() for s in ('strata','boulder','erod','erosion','slump','terrac','height','terrain'))))`
Gap: **GAP** — no tool or prose; treat absent labels as phantom pending name resolution.

### API_MIGRATION

**`COP-12` — Copernicus category ≠ legacy COP2** · `VERIFIED` · **ESCALATE** · [heightfields_cop/index.html](https://www.sidefx.com/docs/houdini22.0/heightfields_cop/index.html)
If `cops_create_node`/`create_copnet` target old `Cop2`/`CopNet`, they cannot reach H22 terrain nodes — silent phantom-type breakage.
Probe: `print(sorted(hou.nodeTypeCategories().keys()))` then `print(hasattr(hou,'copNodeTypeCategory'))`
Gap: **COVERED** — `handlers_cops.py` already distinguishes legacy vs modern (`_LEGACY_COP_TYPES=('cop2net','vopcop2gen','copnet')`; `create_copnet` targets modern `copnet`). Both `hou.copNodeTypeCategory` + `hou.cop2NodeTypeCategory` present. Confirm live which key is Copernicus.

### CAPABILITY_GAP

**`COP-11` — COP heightfield → SOP → USD/Solaris export path** · `VERIFIED` · [heightfields_cop/index.html](https://www.sidefx.com/docs/houdini22.0/heightfields_cop/index.html)
`Convert HeightField SOP → SOP Import LOP → USD stage` SYNAPSE already renders with Karma. Cross-domain COP→Solaris bridge.
Probe: `sc=hou.sopNodeTypeCategory(); print('convertheightfield' in [n for n in sc.nodeTypes()]); lc=hou.lopNodeTypeCategory(); print('sopimport' in [n for n in lc.nodeTypes()])`
Gap: **PARTIAL** — SOP-HF→USD path COVERED; the COP-HF origin leg (Convert HeightField fed from a COP import) is not wired.

### CORPUS_SEED

**`COP-02` — Neural COP nodes need a manual model download + GPU execution-provider** · `DOC-ONLY` · [nodes/cop/index.html](https://www.sidefx.com/docs/houdini22.0/nodes/cop/index.html)
Nodes no-op until "Download Models" runs post-install (`$SHFS`); cook needs a GPU provider (CUDA/DirectML/CoreML). A recorded op can silently produce an empty mask on a fresh/CPU-only host.
Probe: after creating a SAM2 node, read `[p.name() for p in node.parms() if any(k in p.name().lower() for k in ('model','provider','execution'))]`; model presence is a `$SHFS` filesystem check.
Gap: **GAP** — no corpus doc encodes the model-download / GPU precondition. Needs a houdini22-reference seed.

**`COP-13` — COP heightfields are image-based 2D maps, NOT SOP voxel grids (not yet at parity)** · `DOC-CLAIM` · [heightfields_cop/index.html](https://www.sidefx.com/docs/houdini22.0/heightfields_cop/index.html)
Framing so SYNAPSE never assumes COP-HF == SOP-HF: 2D maps, GPU-accelerated, parity only a mid-term goal.
Probe: `cat=hou.nodeTypeCategories().get('Cop'); print([nt.name() for nt in cat.nodeTypes() if 'height' in nt.name().lower()])`
Gap: **GAP** — no corpus seed frames the 2D-map-vs-voxel distinction. Needs houdini22-reference framing.

---

## MCP / HOM

### NEW_MCP_TOOL

**`HOM-01` — `hou.CopVerb` headless COP cooking** · `VERIFIED` · [hom/hou/index.html](https://www.sidefx.com/docs/houdini22.0/hom/hou/index.html)
Deterministic verb path for the ~20 `cops_*` recipe tools — cook COP image ops standalone/undo-free without a live node graph (like `SopVerb`).
Probe: `'CopVerb' in dir(hou) and hasattr(hou.CopNode,'verb') and hasattr(hou.CopNode,'hasVerb')` then `type(n.verb()).__name__ == 'CopVerb'`
Gap: **GAP** — all 8 symbols PRESENT. ~20 `cops_*` tools exist (node-graph based) but no verb/headless path. **Safe to implement** after a confirming probe.

**`HOM-03` — `hou.data` native recipe/preset substrate** · `VERIFIED` · [hom/hou/index.html](https://www.sidefx.com/docs/houdini22.0/hom/hou/index.html)
Could back `synapse_list_recipes`/`propose_graph`/`instantiate_graph` — save/apply node-preset graphs + capture selected items as portable data.
Probe: `d=dir(hou.data); assert all(f in d for f in ('saveNodePresetRecipe','applyNodePresetRecipe','itemsAsData','createItemsFromData','dataFromRecipe','dataFromParms'))`
Gap: **GAP** — all 10 symbols PRESENT (`hou.data` has 20 children fully introspected). Repo uses hand-built `NodeManifest`; `hou.data.*` unreferenced.

**`HOM-05` — `hou.lop.availableRendererNames/Labels/Info`** · `DOC-ONLY` · [hom/hou/index.html](https://www.sidefx.com/docs/houdini22.0/hom/hou/index.html)
Replaces hardcoded Karma/BRAY delegate ids with live-queried delegate metadata for `houdini_render_settings`.
Probe: `assert hasattr(hou.lop,'availableRendererNames'); hou.lop.availableRendererNames()`
Gap: **GAP** — no live delegate enumeration in repo. ABSENT here is the `hou.lop` 0-child coverage blind-spot, **not** a phantom; probe live before wiring.

### RECIPE_CHANGE

**`HOM-02` — CopNode read model: `cable()`/`layer()`, not `node.planes()`** · `VERIFIED` · **ESCALATE** · [hom/hou/index.html](https://www.sidefx.com/docs/houdini22.0/hom/hou/index.html)
How SYNAPSE must read COP output in H22. **`hou.CopNode.planes` is GONE** from H22's 301-method table.
Probe: `assert hasattr(hou.CopNode,'layer') and hasattr(hou.CopNode,'cable') and hasattr(hou.CopNode,'inputCableStructure'); assert all(s in dir(hou) for s in ('CopCable','CopCableStructure','ImageLayer')); assert not hasattr(hou.CopNode,'planes')`
Gap: **PARTIAL / LIVE BREAK** — all 13 new symbols PRESENT. `cops_read_layer_info`/`composite_aovs`/`analyze_render` read via `node.planes()` (`handlers_cops.py:446,684`) → live break under H22; migrate to `cable()`/`layer()`. Old verbs `saveImage`/`allImagePlanes`/`cookImage`/`imageLayers` also absent (SYNAPSE never called them — good).

### API_MIGRATION

**`HOM-04` — `hou.lop.makeValidPrimName` / `makeValidPrimPath` (official USD sanitizer)** · `DOC-ONLY` · [hom/hou/index.html](https://www.sidefx.com/docs/houdini22.0/hom/hou/index.html)
Resolves open RFC **D-3**: SYNAPSE hand-rolls `_safe_prim_name` (`agent_state.py:41`) + `evolution.py` uses `Tf.MakeValidIdentifier`. Sanctioned single sanitizer.
Probe: `assert hasattr(hou.lop,'makeValidPrimName') and hasattr(hou.lop,'makeValidPrimPath'); hou.lop.makeValidPrimName('my prim!23')`
Gap: **GAP** — **NOT phantom**: `hou.lop` 0-child coverage blind-spot (same class as `hou.ui`/`hou.qt`/`hou.hipFile`). Do NOT dismiss; runtime probe required before swap, verify output agreement with the hand-rolled sanitizer.

### CAPABILITY_GAP

**`HOM-06` — `hou.lop.translateShader` family (VOP→UsdShade)** · `DOC-ONLY` · [hom/hou/index.html](https://www.sidefx.com/docs/houdini22.0/hom/hou/index.html)
Native VOP-to-UsdShade authoring for `create_material`/`create_textured_material`/`cops_to_materialx`.
Probe: `assert all(hasattr(hou.lop,f) for f in ('translateShader','shaderTranslatorID','shaderRenderContextName','shaderNodeType'))`
Gap: **GAP** — material handlers author shaders without `translateShader`. NOT phantom: `hou.lop` blind-spot; probe live.

**`HOM-07` — `hou.lop.reloadLayer` / `forceReloadAllFilesFromDisk` / `addLockedGeometry`** · `DOC-ONLY` · [hom/hou/index.html](https://www.sidefx.com/docs/houdini22.0/hom/hou/index.html)
Cleaner alternative to R10's hand-rolled LopNetwork force-cook (`reloadLayer`); `addLockedGeometry` stages in-memory `hou.Geometry` as a USD layer without disk round-trips.
Probe: `assert all(hasattr(hou.lop,f) for f in ('reloadLayer','forceReloadAllFilesFromDisk','addLockedGeometry','removeLockedGeometry','mutingIdentifier'))`
Gap: **GAP** — R10 hand-rolls force-cook (`evolution.py`). NOT phantom: `hou.lop` blind-spot; probe live.

**`HOM-08` — `hou.lop.createParmsForProperty` / `setParmTupleFromProperty` (Edit Properties)** · `DOC-ONLY` · [hom/hou/index.html](https://www.sidefx.com/docs/houdini22.0/hom/hou/index.html)
Automates the Edit Properties workflow for `modify_usd_prim`/`set_usd_attribute` — build ParmTemplateGroups from a USD property and push values back.
Probe: `assert hasattr(hou.lop,'createParmsForProperty') and hasattr(hou.lop,'setParmTupleFromProperty') and hasattr(hou.lop,'createConnectionParmsForProperty')`
Gap: **GAP** — handlers don't build parms from USD properties. NOT phantom: `hou.lop` blind-spot; probe live.

**`HOM-11` — Embedded-browser `window.Python` control surface (via HDA help pages)** · `DOC-ONLY` · [hom/browserpython.html](https://www.sidefx.com/docs/houdini22.0/hom/browserpython.html)
`houdini_hda_set_help` authors pages that render in the embedded browser; they could expose live controls triggering Python via `window.Python.runStatements`. **SECURITY:** any such call bypasses the bridge/gate audit layer — raw statements, no consent gating.
Probe: JS-side `typeof window.Python !== 'undefined'`; hou-side `hasattr(hou, 'qt')` + open an HDA help page to see if the browser injects the bridge.
Gap: **GAP** — `houdini_hda_set_help` EXISTS (registry:979, `handlers_hda.py:246`); the `window.Python` surface is NOT wired. `hou.hipFile.save` ABSENT = `hou.hipFile` 0-child coverage gap (real API), NOT phantom; `window.Python.*` is JS, not a hou symbol.

### CORPUS_SEED

**`HOM-09` — Solaris stage/viewport classes: `LopLockedStage`, `LopViewportOverrides`, `LopPostLayer`, `LopViewportLoadMasks`** · `VERIFIED` · [hom/hou/index.html](https://www.sidefx.com/docs/houdini22.0/hom/hou/index.html)
Cached/locked-stage + viewport-override handles relevant to stage-read pain and viewport-capture set-dressing.
Probe: `[s for s in ('LopLockedStage','LopViewportOverrides','LopPostLayer','LopViewportLoadMasks','LopViewportOverridesLayer') if s in dir(hou)]`
Gap: **GAP** — 4/5 concrete classes PRESENT; none wired. **5th `hou.LopViewportOverridesLayer` (capital L) is ABSENT — a case-typo**; the real symbol is lowercase `hou.lopViewportOverridesLayer` (enum, PRESENT). Concept verified.

**`HOM-10` — `window.Python` JS-to-Python bridge (QWebChannel)** · `DOC-ONLY` · [hom/browserpython.html](https://www.sidefx.com/docs/houdini22.0/hom/browserpython.html)
Reference prose for the embedded-browser control surface. Hard caveats: `hou` NOT auto-imported; NO error handling (exceptions silently return `""`/`0`); depends on `qwebchannel.js`.
Probe: `print(os.path.isfile(os.path.join(hou.expandString('$HHP'), 'bookish', 'static', 'js', 'qwebchannel.js')))` — `window.Python` inspectable only from the browser JS console.
Gap: **GAP** — corpus has **zero** `window.Python`/`QWebChannel`/`browserpython` coverage. JS/browser-side, not hou symbols. Prose/corpus-seed finding.

**`HOM-12` — Doc references Qt5 + Python-2 print — both stale vs H22 (Qt6/PySide6 + Python 3.14)** · `DOC-CLAIM` · [hom/browserpython.html](https://www.sidefx.com/docs/houdini22.0/hom/browserpython.html)
Two doc-staleness landmines: (1) the `runStatements("print 'Hello World.'")` example is Python-2 → a hard SyntaxError under 3.14, silently swallowed; (2) prose calls the async model a "Qt5" change — H22 ships Qt6/PySide6.
Probe: `import PySide6; print(PySide6.__version__)` — do NOT trust the doc's Qt5/Python-2 claims.
Gap: **GAP** — doc-hygiene flag. Any future seed must use Py3.14 syntax + Qt6/PySide6, never the doc's Python-2 print / Qt5 assertions.

---

## PHANTOM WATCH

> Every doc-cited symbol that is **absent from the 22.0.368 symbol table**. Rule: **DO NOT implement any name in Tier A or B without a passing runtime probe first.** Tier C is the critical inverse trap — names that *look* phantom (ABSENT) but are actually real APIs hidden by a coverage blind-spot; do not dismiss them.

### Tier A — Confirmed absent-as-spelled dotted symbols (do NOT emit as written)

| Symbol | Verdict | Action | Source |
|---|---|---|---|
| `hou.CopNode.planes` | **REMOVED in H22** — absent from the full 301-method `CopNode` table. SYNAPSE **currently calls it** → live break. | Migrate `cops_read_layer_info`/`composite_aovs`/`analyze_render` to `CopNode.cable()`/`layer()`. | `HOM-02` |
| `hou.LopViewportOverridesLayer` (capital **L**) | **Case-typo** — absent as spelled. | Use lowercase `hou.lopViewportOverridesLayer` (enum, PRESENT). | `HOM-09` |
| `hou.node.createNode` (lowercase `node`) | **Notation artifact** — absent as spelled. | Real method is `hou.Node.createNode` (PRESENT) — safe once corrected. | `COP-09` |
| `hou.CopNode.saveImage` / `allImagePlanes` / `cookImage` / `imageLayers` | Absent (legacy COP2 verbs). SYNAPSE never called them. | Do not introduce; use the new `cable()`/`layer()` read model. | `HOM-02` |

### Tier B — Node-type-name strings at phantom risk (never table-verifiable — probe before any `createNode`)

Node-type names (LOP/COP internal type strings) are **not** Python API symbols and can never be cleared by the symbol table. Every one below must resolve via `hou.nodeType(cat, name) is not None` at runtime before emission.

| Node-type string | Risk note | Source |
|---|---|---|
| `scatterinstances` | Likely phantom — `scene_recipes.py` records NO native Solaris scatter LOP on H21. | `SOL-03` |
| `createlod`, `autoselectlod` | Summarizer-derived; 0 references code + corpus. | `SOL-12` |
| `neural_layertomask_sam2`, `neural_layertodepth_moge2`, `neural_cellularautomatacore`, `neural_cellularautomatadecode`, `denoiseai` | Absent by design (not API symbols); node existence must be runtime-probed. | `COP-01` |
| Strata / Boulders / Erosion / Slump / Terracing COP terrain ops | Docs give **display labels only** — real type strings unknown. | `COP-10` |
| `inlineusd` | 0 references code + corpus. | `SOL-08` |
| `stagemanager`, `configurelayer`, `loadlayer`, `layerreplace`, `edittargetlayer` | Unexposed / 0 code references. | `SOL-05`, `SOL-06`, `SOL-10` |
| `configurestage`, `componentoutput`, `bakegeometrytextures::2.0`, `reactiondiffusion_block_begin/end`, `slapcompimport`, `usdmaterial`, `cryptomatte(decode/encode)`, `zcomp`, `rop_image` | Recipe-targeted type strings — confirm exact spelling/version at runtime. | COP-03/04/05/06/07/08, SOL-11/15 |

### Tier C — NOT phantom: coverage blind-spots (ABSENT in table, but REAL APIs — probe to confirm, do NOT dismiss)

> The headless symbol table omits whole submodules whose children introspect to **0**. Anything under these namespaces showing ABSENT is a table gap, not a phantom verdict. This is the exact trap the phantom guardrails must allowlist.

| Namespace / symbol | Why it reads ABSENT | Findings |
|---|---|---|
| `hou.lop.*` (0-child submodule) — `makeValidPrimName`, `makeValidPrimPath`, `availableRendererNames/Labels/Info`, `translateShader` family, `reloadLayer`/`addLockedGeometry`, `createParmsForProperty`/`setParmTupleFromProperty` | Submodule has 0 children introspected in the headless table (same class as `hou.ui`, `hou.qt`). | `HOM-04`–`HOM-08` |
| `hou.hipFile.save` | `hou.hipFile` 0-child coverage gap — real API. | `HOM-10`, `HOM-11` |
| `window.Python.*`, `QWebChannel`, `qt.webChannelTransport`, `PySide6`/`PySide2` | JS / Qt binding symbols — not `hou`/`pxr`/`pdg` at all, so never in the table by definition. | `HOM-10`, `HOM-11`, `HOM-12` |

---

## ESCALATE

> Breaking changes and version-bump smells. These are the items that can silently break shipping tools on the H22 upgrade — prioritize their probes on drop day.

1. **`HOM-02` — `hou.CopNode.planes` removed (LIVE BREAK).** Three shipping tools (`cops_read_layer_info`, `cops_composite_aovs`, `cops_analyze_render`) read COP output via `node.planes()` at `handlers_cops.py:446,684`. The method is absent from H22's 301-method `CopNode` table. **These tools will raise under H22.** Migration target confirmed present: `CopNode.cable()`/`layer()` + `CopCable`/`CopCableStructure`/`ImageLayer` (all 13 new symbols PRESENT). First probe to run on drop.

2. **`COP-01` — Neural Copernicus is genuinely new in H22.0.** SAM2 / MoGe-2 / Cellular Automata / Denoise AI COP nodes are a version-gated capability class with zero SYNAPSE coverage. Escalated because it signals the scope of the Copernicus rewrite — the same rewrite that removed `planes()`. Node existence is runtime-probe-only (`hou.nodeTypeCategories()`).

3. **`COP-12` — Copernicus category vs legacy COP2 split.** New terrain/neural nodes live in a different category/network than legacy COP2. Currently COVERED (`handlers_cops.py` already distinguishes `_LEGACY_COP_TYPES` from modern `copnet`; both `hou.copNodeTypeCategory` + `hou.cop2NodeTypeCategory` present), but the live category **key** ("Cop" vs "Cop2"/"CopNet") must be re-confirmed on 22.0 — a wrong key silently reaches phantom types.

**Related non-escalated version smell:** `HOM-12` (docs still cite Qt5 + Python-2 print; H22 is Qt6/PySide6 + Python 3.14) and `COP-03` (versioned type `bakegeometrytextures::2.0`). Neither breaks a shipping tool, but both are drift markers to encode before seeding corpus.

---

## How to use this

1. **Everything here is DOC-CLAIM until proven.** No candidate is a fact about the H22 runtime — each is a doc-derived hypothesis. It becomes real only when its **runtime probe runs green under H22 hython**. VERIFIED tier means the *dotted Python symbols* resolve in the 22.0.368 table; it never clears a node-type-name string.

2. **Feed to the flywheel as `ratified:false`.** The companion `harness/notes/h22_doc_candidates.json` is shaped to seed flywheel entries directly. Each candidate stays unratified until a human (or a gated probe pass under hython) flips it — mirroring the D-track / U-track `ratified:false` intake discipline.

3. **Probe order:** run the ESCALATE probes first (they gate shipping-tool survival on the H22 upgrade), then the VERIFIED NEW_MCP_TOOL wins (`HOM-01`, `HOM-03` — symbols already present, safe to build), then the DOC-ONLY `hou.lop` blind-spot cluster (`HOM-04`–`HOM-08` — one hython session resolves all of them at once).

4. **Honor PHANTOM WATCH Tier C.** Do not let a phantom guardrail auto-reject a `hou.lop.*` / `hou.hipFile.*` symbol as phantom — allowlist the 0-child submodules exactly as the headless-table guardrails already do for `hou.ui`/`hou.qt`.

5. **This report never mutates code.** It is intelligence, not a patch. All wiring, migration, and tool creation happen downstream, behind the bridge gate, after the probes ratify.

---

## Probe Verdicts (Phase 3 — 2026-07-15)

> Ran every candidate's probe under H22.0.368 hython (`scripts/h22_probe_candidates.py`) and adjudicated the raw facts. Candidates stay `ratified:false` — this adds runtime evidence for the human's ratification.

**Summary:** 42 probes adjudicated → **VERIFIED 22 · REFUTED 0 · NEEDS_LIVE_NODE 13 · NOT_RUNNABLE 5 · INCONCLUSIVE 2**. Zero doc claims were denied by the runtime. Both ESCALATE items that were runnable came back VERIFIED — including the `HOM-02` live break. The 13 NEEDS_LIVE_NODE are dominated by the COPs cluster, where node-type-name strings are absent from the symbol table by design and 8 probes carried a trailing English clause that broke the parse (probe-authoring bug, **not** a runtime denial). Re-run those live.

### Promoted work queue (VERIFIED — ratified-ready)

> Runtime-backed dev queue. Escalations first. Every row's dotted symbols (or, for `SOL-14/15/16`, node-type names) resolved live under 22.0.368.

| id | domain | bucket | item | gap | evidence |
|---|---|---|---|---|---|
| **`HOM-02`** ⚠ ESCALATE / LIVE BREAK | MCP/HOM | RECIPE_CHANGE | Migrate CopNode read model `node.planes()` → `cable()`/`layer()` | 3 shipping tools (`cops_read_layer_info`/`composite_aovs`/`analyze_render`) read via `node.planes()` at `handlers_cops.py:446,684` — gone in H22 | `exec` VERIFIED: `layer`/`cable`/`inputCableStructure` + `CopCable`/`CopCableStructure`/`ImageLayer` present; `saveImage`/`allImagePlanes`/`planes` absent. Migration is real and required. |
| **`COP-12`** ⚠ ESCALATE | COPs | API_MIGRATION | Target modern `Cop` category, not legacy `Cop2`/`CopNet` | `create_copnet`/`create_node` reaching a wrong category key silently hits phantom types | `exec` stdout `['Chop','ChopNet','Cop','Cop2','CopNet',...]` — `Cop` present and distinct from legacy `Cop2`/`CopNet`; both `hou.copNodeTypeCategory` + `hou.cop2NodeTypeCategory` present. `Cop` is the live Copernicus key. |
| **`HOM-01`** | MCP/HOM | NEW_MCP_TOOL | `hou.CopVerb` headless COP cooking | ~20 `cops_*` tools are node-graph based; no verb/headless path | Table has `hou.CopVerb` (+`execute`/`parms`/`setParms`/…), `hou.CopNode.verb`, `hou.CopNode.hasVerb` — all 8 symbols present. API surface confirmed; the null-node behavioral tail needs a live node but is not blocking. |
| **`HOM-03`** | MCP/HOM | NEW_MCP_TOOL | `hou.data` native recipe/preset substrate | Repo uses hand-built `NodeManifest`; `hou.data.*` unreferenced | `exec` VERIFIED: `'data' in dir(hou)` and `hou.data` exposes `saveNodePresetRecipe`/`applyNodePresetRecipe`/`itemsAsData`/`createItemsFromData`/`dataFromRecipe`/`dataFromParms`. Safe to back `synapse_list_recipes`/`propose_graph`/`instantiate_graph`. |
| **`SOL-14`** | Solaris | NEW_MCP_TOOL | `houdini_build_component` 4-node chain wrapper | No single `build_component` tool | `exec` stdout: `componentgeometry`/`materiallibrary`/`componentmaterial`/`componentoutput`/`componentgeometryvariants` all `True`. The orchestration chain targets real types. |
| **`SOL-04`** | Solaris | CAPABILITY_GAP | Python Script LOP reversible `pxr`-authoring surface | Used internally but not exposed as a bridge-reversible alt to *ungated* `execute_python` | `eval` `True`: `hou.nodeType(hou.lopNodeTypeCategory(),'pythonscript') is not None`. With SOL-07/SOL-09, the reversible-authoring path is real. |
| **`HOM-04`** | MCP/HOM | API_MIGRATION | `hou.lop.makeValidPrimName`/`makeValidPrimPath` (resolves RFC D-3) | Repo hand-rolls `_safe_prim_name` + `Tf.MakeValidIdentifier` | `exec` VERIFIED: both attrs present and the two calls raised nothing. `hou.lop` was a Tier-C table blind-spot; the live probe RAN and passed → runtime is the authority. |
| **`HOM-05`** | MCP/HOM | NEW_MCP_TOOL | `hou.lop.availableRendererNames`/`Labels`/`Info` | No live delegate enumeration in repo | `exec` VERIFIED: `hasattr` passed and all three calls raised nothing. `hou.lop` blind-spot resolved live → present. Enables live delegate enumeration for `houdini_render_settings`. |
| **`HOM-06`** | MCP/HOM | CAPABILITY_GAP | `hou.lop.translateShader` family (VOP→UsdShade) | Material handlers author shaders without `translateShader` | `exec` VERIFIED: `translateShader`/`shaderTranslatorID`/`shaderRenderContextName`/`shaderNodeType` all present. Native VOP→UsdShade authoring confirmed. |
| **`HOM-07`** | MCP/HOM | CAPABILITY_GAP | `hou.lop.reloadLayer`/`forceReloadAllFilesFromDisk`/`addLockedGeometry` | R10 hand-rolls LopNetwork force-cook | `exec` VERIFIED: `reloadLayer`/`forceReloadAllFilesFromDisk`/`addLockedGeometry`/`removeLockedGeometry`/`mutingIdentifier` all present. Cleaner replacement for R10. |
| **`HOM-08`** | MCP/HOM | CAPABILITY_GAP | `hou.lop.createParmsForProperty`/`setParmTupleFromProperty` (Edit Properties) | Handlers don't build parms from USD properties | `exec` VERIFIED: `createParmsForProperty`/`setParmTupleFromProperty`/`createConnectionParmsForProperty` all present. Automates Edit Properties for `modify_usd_prim`/`set_usd_attribute`. |
| **`HOM-09`** | MCP/HOM | CORPUS_SEED | Solaris stage/viewport classes wiring | 4/5 classes present, none wired; 5th is a case-typo | Table has `LopLockedStage`/`LopViewportOverrides`/`LopPostLayer`/`LopViewportLoadMasks` + lowercase `lopViewportOverridesLayer` enum; capital-L `LopViewportOverridesLayer` absent. Wire the correctly-cased names. |
| **`SOL-02`** | Solaris | NEW_MCP_TOOL | Prune LOP (reversible prim deactivation) | Known to assemble ordering + corpus; no dedicated tool | `eval` `True`: `hou.nodeType(hou.lopNodeTypeCategory(),'prune') is not None`. Node is real; tool gap stands. |
| **`SOL-06`** | Solaris | NEW_MCP_TOOL | Stage Manager LOP (bulk set-dressing) | Known to assemble ordering only; zero corpus | `eval` `True`: `stagemanager` type resolves. Bulk-referencer real; tool + corpus gap stands. |
| **`SOL-08`** | Solaris | NEW_MCP_TOOL | Inline USD LOP (raw `.usda` injection) | Zero code + zero corpus refs | `exec` stdout `<hou.OpNodeType for Lop inlineusd>` — real OpNodeType. Genuinely unexposed; safe to build the type-name path. |
| **`SOL-10`** | Solaris | NEW_MCP_TOOL | Stage Manager batch-reference | `houdini_reference_usd` is single-asset only | `exec` stdout `<hou.OpNodeType for Lop stagemanager>` (print form of SOL-06). Type real; per-asset multiparm enumeration still needs a live node. |
| **`SOL-12`** | Solaris | CAPABILITY_GAP | Create LOD / Auto Select LOD LOPs | Flagged summarizer-derived / phantom-risk | `exec` stdout `<hou.OpNodeType for Lop createlod> <hou.OpNodeType for Lop autoselectlod>` — both real. Phantom concern refuted; capability gap stands. |
| **`SOL-15`** | Solaris | RECIPE_CHANGE | Component Output write / Save-to-Disk params | `componentoutput` instantiated bare; no code drives its write parms | `exec`: `hou.node('/stage').createNode('componentoutput')` succeeded, printed real parm names (`filename`, `execute`, `executebackground`, `loadfromdisk`, `assetname`, `assetversion`, `kind`, `geolayer`, `extralayer`, …). Wire against these, not UI labels. |
| **`SOL-16`** | Solaris | CAPABILITY_GAP | Component-native variant authoring | Generic variants covered; component-native path unwired | `exec` stdout `componentgeometryvariants True` / `componentmaterial True`. Node existence verified; `'geo'`/`'mtl'` default-set-name check was prose (needs a live node). |
| **`SOL-07`** | Solaris | CORPUS_SEED | Composed-stage read via `hou.LopNode.stage()` | Backbone of `inspect_stage`/`query_prims`/`get_usd_attribute` | `exec` stdout `True True` for `hasattr(hou.LopNode,'stage')`, `hasattr(hou.LopNode,'editableStage')`. Read-path signature unchanged in H22. |
| **`COP-11`** | COPs | CAPABILITY_GAP | COP-HF → Convert HeightField SOP → SOP Import LOP → USD bridge | COP-HF origin leg not wired in SYNAPSE | `exec` stdout `True`/`True` for `'convertheightfield' in sopNodeTypeCategory` and `'sopimport' in lopNodeTypeCategory`; both categories present in table. Bridge node types exist; repo capability gap (not a refutation). |
| **`HOM-12`** | MCP/HOM | CORPUS_SEED | Doc-hygiene: H22 is Qt6/PySide6 + Python 3.14 (not Qt5/Py2) | Doc examples cite stale Qt5 + Python-2 `print` | `exec` stdout `6.8.3` from `import PySide6; print(PySide6.__version__)`. Confirms Qt6/PySide6; any future corpus seed must use Py3.14 + Qt6. |

### Refuted (doc claim denied by the runtime — do NOT build)

_None._ Zero of the 42 probes produced a runtime denial. Every "empty stdout" / SyntaxError result was a probe-authoring artifact (bare comprehension with no `print`, or a trailing English clause fed to the interpreter), **not** an empty-list or absence result — so no candidate was refuted. The claims that could not be settled offline are in the table below.

### Needs a live node / manual

> `NEEDS_LIVE_NODE` + `NOT_RUNNABLE` + `INCONCLUSIVE`. None is refuted — each needs an interactive H22 session (live node, filesystem/JS surface, or a corrected re-probe).

| id | verdict | why not settled offline | probe to run by hand |
|---|---|---|---|
| `SOL-01` | NEEDS_LIVE_NODE | Placeholder `/stage/<a_lop>` resolved to `None`; `editableStage` IS in the table so the method is real. The None-outside-cook claim needs a real LOP. | In a live pythonscript LOP cook call `node.editableStage()` (expect non-None); outside a cook confirm it's `None`. |
| `SOL-09` | NEEDS_LIVE_NODE | Probe was prose ("create a lop pythonscript node, cook it…") → SyntaxError. `editableStage` exists (table + SOL-07). | Create a lop `pythonscript`, cook it, and inside its snippet call `node.editableStage()` — writable `Usd.Stage` only during the cook. |
| `SOL-11` | NEEDS_LIVE_NODE | Prose mixed with code → SyntaxError. `configurestage` existence + payload/load-mask parm names need instantiation. | `nt=hou.nodeType(hou.lopNodeTypeCategory(),'configurestage'); print(nt is not None)` then create it and `[p.name() for p in n.parms()]`. |
| `COP-01` | NEEDS_LIVE_NODE | Trailing `-- ...` English clause broke the list-comp parse (no output). Neural node-type strings absent from the table by design. `escalate=true`. | Live scan: `cat=hou.nodeTypeCategories()['Cop']; [nt.name() for nt in cat.nodeTypes() if nt.name() in ('neural_layertomask_sam2','neural_layertodepth_moge2','denoiseai')]`. |
| `COP-03` | NEEDS_LIVE_NODE | Trailing English clause broke the parse. `bakegeometrytextures[::2.0]` is a versioned type name, not a table symbol. | Live scan the `Cop` category for names containing `bakegeometrytextures` to resolve the exact `::` spelling before wiring `cops_bake_textures`. |
| `COP-04` | NEEDS_LIVE_NODE | Trailing English clause broke the parse. `reactiondiffusion_block_begin/end` are node-type names. | Live-enumerate the `Cop` category for `reactiondiffusion` → expect a `_block_begin` / `_block_end` pair. |
| `COP-05` | NEEDS_LIVE_NODE | Trailing English clause broke the `next(...)` parse. Selecting the live Solaris-viewport layer needs a real node. | `createNode('slapcompimport')` then read `inputNames()` / `parms()` to see how the viewport layer is selected. |
| `COP-06` | NEEDS_LIVE_NODE | `if present,` prose made it invalid Python. Disk-reference/material-path parm needs a real node. | `createNode('usdmaterial')` then `[p.name() for p in n.parms()]` to find the disk-reference / material-path parm. |
| `COP-07` | NEEDS_LIVE_NODE | Trailing English clause broke the parse. `cryptomatte*`/`zcomp` are node-type names, absent by design. | Live-enumerate the `Cop` category for `cryptomatte`/`cryptomattedecode`/`cryptomatteencode`/`zcomp` before building the AOV recipe. |
| `COP-08` | NEEDS_LIVE_NODE | Trailing English clause broke the parse. `rop_image`/`opencl` are node-type names + wants a render-path parm read. | Live-enumerate for `rop_image`/`opencl` on `Cop`, then `n.parms()` for a `render()`/`execute` path. (Note: `cops_set_opencl` already targets `opencl` live; `rop_image` is the unconfirmed half.) |
| `COP-09` | NEEDS_LIVE_NODE | First `print` ran (`Cop` category confirmed present) but the terrain-node enumeration produced no output → terrain-node presence unconfirmed. | Re-run the second `sorted(nt.name() ...)` enumeration live against `Cop`, filtering for `height`/`terrain`/`noise`/`erode`. |
| `COP-10` | NEEDS_LIVE_NODE | Probe bug: `cat.nodeTypes()` yields dict string-keys, so `nt.name()` raised `AttributeError`. No verdict on Strata/Boulders/Erosion/Slump/Terracing. #1 phantom-API risk. | Corrected enumeration — iterate `.nodeTypes().values()` (or test key membership) on `Cop`, filter for `strata`/`boulder`/`erod`/`slump`/`terrac`. |
| `COP-13` | NEEDS_LIVE_NODE | Same dict-key iteration bug as COP-10 → no verdict on image-based 2D-map COP-HF nodes. | Corrected `Cop`-category enumeration filtering for `height` to settle the 2D-map-vs-voxel runtime half. |
| `SOL-13` | NOT_RUNNABLE | LIVRPS composition-strength ordering is documentation semantics; no SYNAPSE symbol/type to verify. | Optional only: author an `over` on a referenced prim and confirm the local opinion wins after cook. Not required — corpus-seed content. |
| `SOL-17` | NOT_RUNNABLE | Component-asset layer-structure prose for the RAG corpus; no runtime symbol. | None — corpus re-seed item; cross-check `solaris_network_blueprint.md` + `solaris_variants.md`. |
| `COP-02` | NOT_RUNNABLE | DOC-ONLY operational precondition (post-install "Download Models" under `$SHFS` + a GPU execution provider); the node half also needs a live SAM2 node. | Filesystem `$SHFS` model-presence check + on a live SAM2 node read `[p.name() for p in n.parms() if any(k in p.name().lower() for k in ('model','provider','execution'))]`. |
| `HOM-10` | NOT_RUNNABLE | `window.Python`/`QWebChannel`/`qt.webChannelTransport` are browser-JS, not hython symbols. (`hou.hipFile.save` reads absent only via the `hou.hipFile` 0-child blind-spot — real API.) | Inspect `typeof window.Python` from Houdini's embedded-browser JS console. |
| `HOM-11` | NOT_RUNNABLE | `window.Python.runStatements` is a JS symbol; the probe is a JS-console instruction. SECURITY note (raw statements bypass the bridge/gate audit) stands but is not runtime-adjudicable. | Load an HDA help page in the embedded browser and eval `typeof window.Python !== 'undefined'` in its JS console. |
| `SOL-03` | INCONCLUSIVE | Bare list comprehension with no `print` → empty stdout regardless of which of `instancer`/`copytopoints`/`scatterinstances`/`pointinstancer` resolved. Empty stdout ≠ empty list. | Re-probe with an explicit `print([...])` of the resolved instancing LOP type names. |
| `SOL-05` | INCONCLUSIVE | Bare dict comprehension with no `print` → empty stdout; no per-name True/False emitted for the layer-management set. | Re-probe with `print({...})` over `sublayer`/`reference`/`configurelayer`/`layerbreak`/`loadlayer`/`layerreplace`/`edittargetlayer`. |
