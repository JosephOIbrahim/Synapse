# RSI HARNESS — Session Capsule (DIGEST)

> Reuses the CONTINUATION / session-capsule format. Written at INGEST, replaced
> at each cycle boundary / session end.

```
+== RSI HARNESS CAPSULE ========================================+
| MODE:                SIMULATED TEAM (no launcher; master, in turn)   |
| WHERE WE ARE:        INGEST complete — at the SPEC ratification gate  |
| MILE MARKER:         0/6 loops closed (L2+) · all lines dormant       |
| WHAT I WAS THINKING: transcribed the audit's 6 items into the SEED    |
|                      PLAN, drafted SPEC from its verdicts, seeded the  |
|                      CHAMPION ratchet + the protected DEADENDS tier    |
|                      (12 registry records, lossless). ARCHITECT lens   |
|                      only — no fix code, no live-code claim-check.     |
|                      CRUCIBLE red-team corrected the DEADENDS          |
|                      provenance + demoted Line S to L2-closure.        |
| NEXT ACTION:         user ratifies SPEC → first DELIBERATE:            |
|                      VERIFY-THE-AUDIT on Line R + resolve the          |
|                      ordering fork on the FORUM.                       |
| BLOCKERS:            (1) SPEC ratification gate — HARD HALT here.      |
|                      (2) FYI (not a blocker): 10 nodetype dead-ends    |
|                          confirmed-absent AS SPELLED (corrected probe, |
|                          kind:nodetype); real H21 names may differ →   |
|                          seeded CONFIRMED with a scope caveat.         |
| MONETA (Joe's Q):    BUILT but default-OFF. SYNAPSE_MEMORY_BACKEND     |
|                      unset → jsonl is the live substrate; Moneta stack |
|                      is on master, flag-gated. Line S/C work = flip    |
|                      the flag + wire deposit_fn (substrate exists).    |
| ENERGY REQUIRED:     low — design/seeding done; next is focused        |
|                      verification (FORGE L0 claim-check on Line R).    |
| IDEAS PARKED:        real-APEX-name discovery for the 10 nodetype      |
|                      dead-ends — parked for DELIBERATE (Line S).       |
+===============================================================+
```

## Files seeded at INGEST (`docs/rsi/`)

| File | Logical layer | State |
|---|---|---|
| `RSI_PLAN.md` | PLAN — the six lines (definitions, ROI) | seeded |
| `SPEC.md` | SPEC — Outcome / Predicates / Scope / Falsification / Strategy | **awaiting ratification** |
| `RSI_CHAMPION.md` | CHAMPION — the loop-closure ratchet | seeded, all dormant |
| `RSI_DEADENDS.md` | DEADENDS — protected-immutable tier | seeded, 12 records |
| `FORUM.md` | FORUM — proposals / critiques | empty scaffold (by design) |
| `RSI_CAPSULE.md` | DIGEST — this capsule | seeded |

Maps onto existing artifacts (not re-created): LOG/TRACE → git history;
LEDGER → `forge/corpus/`; the registry → `.synapse/science/apex_registry.jsonl`.

**GATE: HALT here.** Do not proceed to DELIBERATE. Do not open Line R until SPEC
is ratified.
