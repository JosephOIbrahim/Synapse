You are setting up a deliberately broken fixture so the gate has something to catch.

Goal: {description}

## Phantom node section

Use the `notarealnode_zzz` node to drive it. That node does not exist in Houdini 22,
so this whole guide must land in quarantine, never in the corpus.
